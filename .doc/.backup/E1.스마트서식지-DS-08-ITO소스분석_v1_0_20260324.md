# 스마트서식지 DS-08 ITO 소스 분석 전체 내용

> **원본파일:** 스마트서식지-DS-08-ITO소스분석_v1_0_20260324.xlsx  
> **시트:** AS-IS 소스 분석  
> **기준일:** 2026-03-24  
> **총 분석항목:** 119개 (서브항목 포함 전체 행수: 1,126행)  

---

## 컬럼 구성

| 컬럼 | 설명 |
|------|------|
| ITO AS-IS 분석번호 | 분석 항목 번호 |
| JSP/JS | JSP 및 JS 파일 경로 |
| URL | 요청 URL |
| Controller | Controller 소스 코드 |
| Service | Service 소스 코드 |
| Service / Dao / SQL / API | Service, Dao, SQL, API 소스 |
| 테이블/API | 연관 테이블 또는 API |
| 처리 로직 / 변환 처리 | TO-BE 처리 로직 및 변환 처리 방향 |
| 기타 | 기타 사항 (제외 여부 등) |
| 개발팀 전달 | 전달 완료일 |

---

## 전달일별 항목 통계

| 전달일 | 항목 수 |
|--------|---------|
| 03/22 전달 | 31 |
| 03/23 전달 | 13 |
| 03/24 전달 | 75 |

## 제외 항목 요약

| 분석번호 | 제외 사유 |
|---------|-----------|
| 1003-07 | 제외 - 자급제 관련 로직 불필요 |
| 1003-18 | 제외 - 불필요 |
| 1003-19 | 제외 - 계좌+PASS 인증 |
| 셀프개통 | 제외 - 신청서 직접 전송 처리 |
| m전산 온라인신청서 전송 처리 | 제외 - 신청서 직접 전송 처리 |
| 쉐어링 | 제외 - 해당없음 |
| 쉐어링 | 제외 - 해당없음 |
| 쉐어링 | 제외 - 중복이므로 제외 |
| 쉐어링 | 제외 - 불필요 |
| 쉐어링 | 제외 - 중복이므로 제외 |
| 쉐어링 | 제외 - 불필요 |
| 쉐어링 | 제외 - 불필요 |
| 5001-02 | 제외 - 불필요 |
| 아무나솔로 | 제외 - 불필요 |
| 아무나솔로 | 제외 - 불필요 |
| 아무나솔로 | 제외 - 불필요 |
| 아무나솔로 | 제외 - 불필요 |
| 5002-05 | 제외 - 불필요 |
| 5006-04 | 제외 - 불필요 |
| 5006-05 | 제외 - 불필요 |
| 5006-06 | 제외 - 불필요 |
| 5006-09 | 제외 - 불필요 |
| 5006-10 | 제외 - 불필요 |
| 5006-11 | 제외 - 불필요 |
| 요금제변경 | 제외 - 불필요 |
| 요금제변경 | 제외 - 미사용 |
| 요금제변경 | 제외 - 미사용 |
| 요금제변경 | 제외 - 요금제 예약 변경 배치 사용하므로 불필요 |
| 부가서비스신청/해제 | 제외 - 부가서비스 로직으로 처리 |
| 부가서비스신청/해제 | 제외 - 부가서비스 로직으로 처리 |
| 부가서비스신청/해제 | 제외 - 부가서비스 로직으로 처리 |
| 부가서비스신청/해제 | 제외 - 부가서비스 로직으로 처리 |
| 부가서비스신청/해제 | 제외 - 부가서비스 로직으로 처리 |
| 부가서비스신청/해제 | 제외 - 부가서비스 로직으로 처리 |

---

## 전체 항목 목록

| 순서 | 분석번호 | 연동명 | URL | 유형 | 전달일 | 기타 |
|------|---------|--------|-----|------|--------|------|
| 1 | 1001 | PhoneMobileController.java - phoneList | /m/product/phone/phoneList.do | - | 03/22 전달 |  |
| 2 | 1002 | PhoneFrontController.java - phoneViewNe | /m/product/phone/phoneView.do | - | 03/22 전달 |  |
| 3 | 1002-01 | PhoneFrontController.java - phoneViewNe | /m/product/phone/phoneView.do | - | 03/22 전달 |  |
| 4 | 1003 | AppformController.java - marketRequest | /m/request/marketRequest.do | - | 03/22 전달 |  |
| 5 | 1003-01 | AppformController.java - marketRequest | /m/request/marketRequest.do | - | 03/22 전달 |  |
| 6 | 1003-02 | AppformController.java - selRMemberAjax | /appform/selRMemberAjax.do | - | 03/22 전달 |  |
| 7 | 1003-03 | FCommonController.java - getJehuInfo | /getJehuInfoAjax.do | - | 03/22 전달 |  |
| 8 | 1003-05 | AppformController.java - moscIntmMgmt | /msp/moscIntmMgmtAjax.do | - | 03/22 전달 |  |
| 9 | 1003-06 | AppformController.java - getSalePlcy | /msp/getSalePlcyAjax.do | - | 03/22 전달 |  |
| 10 | 1003-07 | RwdController.java - selectRwdProdList | /mypage/selectRwdProdListAjax.do | - | 03/22 전달 | 제외 - 자급제 관련 로직 불필요 |
| 11 | 1003-08 | AppformController.java - selectInsrProdList | /appform/selectInsrProdListAjax.do | - | 03/22 전달 |  |
| 12 | 1003-09 | X | X | - | 03/22 전달 |  |
| 13 | 1003-10 | AppformController.java - getMcpAdditionList | /appform/getMcpAdditionListAjax.do | - | 03/22 전달 |  |
| 14 | 1003-11 | AppformController.java - containsGoldNumbersAjax | /appform/containsGoldNumbersAjax.do | - | 03/22 전달 |  |
| 15 | 1003-12 | NiceCertifyController.java - checkNiceAccount | /nice/accountCheckAjax.do | - | 03/22 전달 |  |
| 16 | 1003-13 | AppformController.java - crdtCardAthnInfo | /crdtCardAthnInfoAjax.do | - | 03/22 전달 |  |
| 17 | 1003-15 | AppformController.java - saveAppformDb | /m/appForm/saveAppformAjax.do | - | 03/22 전달 |  |
| 18 | 1003-14 | OcrController.java - getOcrImgReadyAjax | /getOcrImgReadyAjax.do | - | 03/22 전달 |  |
| 19 | 1003-22 |  | /m/appForm/appFormDesignView.do?prdtIndCd=10 | - | 03/22 전달 |  |
| 20 | 1003-16 | DbCacheController.java - getCodeList | /getCodeListAjax.do | - | 03/22 전달 | 공통모듈 - 공통코드 조회 / 홍성민C |
| 21 | 1003-17 | DbCacheController.java - getCodeNm | /m/getCodeNmAjax.do | - | 03/22 전달 | 공통모듈 - 공통코드 조회 / 홍성민C |
| 22 | 1003-18 | NiceCertifyController.java - resetProdAuth | /auth/resetProdAuth.do | - | 03/22 전달 | 제외 - 불필요 |
| 23 | 1003-19 | NiceCertifyController.java - niceAccountOtpName | /nice/niceAccountOtpNameAjax.do | - | 03/22 전달 | 제외 - 계좌+PASS 인증 |
| 24 | 1003-20 | AppformController.java - isEnabledFT1 | /appform/isEnabledFT1.do | - | 03/22 전달 |  |
| 25 | 1003-21 | AppformController.java - appOutFormComplete | /m/appForm/appOutFormComplete.do | - | 03/22 전달 |  |
| 26 | 1010 | AppformController.java - insertAppFormTempSave | /appForm/insertAppFormTempSave.do | - | 03/22 전달 |  |
| 27 | 1011 | FCommonController.java - addPopup | /m/addPopup.do | - | 03/22 전달 |  |
| 28 | 9001 | ㅇ ITO AS-IS 소스 참고하지 않고 신규 개발 | X | - | 03/22 전달 |  |
| 29 | 9002 | ㅇ ITO AS-IS 소스 참고하지 않고 신규 개발 | [참고] /termsPop.do | - | 03/22 전달 | 미확정 - 관리자 추가할 경우 조회 기준 변경 |
| 30 | 셀프개통 |  | /appform/reqPreOpenCheckAjax.do | - | 03/22 전달 | 제외 - 신청서 직접 전송 처리 |
| 31 | m전산 온라인신청서 전송 처리 | https://scm.ktmmobile.com/repo/msp/MSP/code/source |  | - | 03/23 전달 | 제외 - 신청서 직접 전송 처리 |
| 32 | N7001 |  |  | - | 03/23 전달 |  |
| 33 | N7002 |  | 접점코드로 대리점 코드 조회 | - | 03/23 전달 |  |
| 34 | N7003 |  |  | - | 03/23 전달 |  |
| 35 | N7004 | MypageController.java - channelInfo | /mypage/channelInfo | - | 03/23 전달 |  |
| 36 | 2001-01 | MyNameChgController.java - myNameChg | /m/mypage/myNameChg.do | - | 03/23 전달 |  |
| 37 | 2001-02 | MyNameChgController.java - myNameChgRequest | /mypage/myNameChgRequest.do | - | 03/23 전달 |  |
| 38 | 2001-03 | MyNameChgController.java - grantorReqChk | /mypage/grantorReqChk.do | - | 03/23 전달 |  |
| 39 | 2001-04 | MyNameChgController.java - nameChgChkTelNo | /mypage/nameChgChkTelNo.do | - | 03/23 전달 |  |
| 40 | 2001-05 | MyNameChgController.java - userInfoChk | /mypage/userInfoChk.do | - | 03/23 전달 |  |
| 41 | 3001-01 | CancelConsultController.java - cancelConsult | /m/mypage/cancelConsult.do | - | 03/23 전달 |  |
| 42 | 3001-02 | CancelConsultController.java - cancelConsultAjax | /mypage/cancelConsultAjax.do | - | 03/23 전달 |  |
| 43 | 쉐어링 | MyShareDataController.java - doMySharingCntrInfo | /m/mySharingCntrInfo.do | - | 03/24 전달 | 제외 - 해당없음 |
| 44 | 쉐어링 | MyShareDataController.java - dataSharingStep1 | /m/content/dataSharingStep1.do | - | 03/24 전달 | 제외 - 해당없음 |
| 45 | 쉐어링 | MyShareDataController.java - doMySharingView | /m/content/mySharingView.do | - | 03/24 전달 | 제외 - 중복이므로 제외 |
| 46 | 4001-01 | MyShareDataController.java - dorReqSharingView | /m/content/reqSharingView.do | - | 03/24 전달 |  |
| 47 | 4001-05 | MyShareDataController.java - dataSharingStep2 | /m/content/dataSharingStep2.do | - | 03/24 전달 |  |
| 48 | 4001-06 | AppformController.java - isSimpleApplyObj | /appForm/isSimpleApplyAjax.do | - | 03/24 전달 |  |
| 49 | 4001-02 | MyShareDataController.java - doinsertOpenRequestAj | /m/content/insertOpenRequestAjax.do | - | 03/24 전달 |  |
| 50 | 4001-03 | AppformController.java - saveDataSharingSimple | /m/appform/saveDataSharingSimpleAjax.do | - | 03/24 전달 |  |
| 51 | 4001-04 | AppformController.java - sendScan | /appform/sendScanAjax.do | - | 03/22 전달 |  |
| 52 | 쉐어링 | MyShareDataController.java - doinsertOpenRequestAj | /m/content/reqSharingCompleteView.do | - | 03/24 전달 | 제외 - 불필요 |
| 53 | 쉐어링 | MyShareDataController.java - dataSharingStep3 | /m/content/dataSharingStep3.do | - | 03/24 전달 | 제외 - 중복이므로 제외 |
| 54 | 4001-07 | AppformController.java - saveDataSharing | /appform/saveDataSharingAjax.do | - | 03/24 전달 |  |
| 55 | 4001-08 | AppformController.java - saveDataSharingStep1 | /appform/saveDataSharingStep1Ajax.do | - | 03/24 전달 |  |
| 56 | 4001-09 | AppformController.java - conPreCheck | /appform/conPreCheckAjax.do | - | 03/24 전달 |  |
| 57 | 4001-10 | AppformController.java - saveDataSharingStep2 | /appform/saveDataSharingStep2Ajax.do | - | 03/24 전달 |  |
| 58 | 4001-11 | AppformController.java - saveDataSharingStep3 | /appform/saveDataSharingStep3Ajax.do | - | 03/24 전달 |  |
| 59 | 쉐어링 | MyShareDataController.java - dataSharingStep4 | /m/content/dataSharingStep4.do | - | 03/24 전달 | 제외 - 불필요 |
| 60 | 쉐어링 | MyShareDataController.java - domyCntrListAjax | /m/content/preOpenCheckAjax.do | - | 03/24 전달 | 제외 - 불필요 |
| 61 | 7002-01 | SmsController.java - sendCertSmsAjax | /m/sms/sendCertSmsAjax.do | - | 03/24 전달 |  |
| 62 | 7002-02 | SmsController.java - checkCertSmsAjax | /m/sms/checkCertSmsAjax.do | - | 03/24 전달 |  |
| 63 | 5001-02 | ComContentController.java - insrView | /m/content/insrView.do | - | 03/24 전달 | 제외 - 불필요 |
| 64 | 5001-03 | CsReqFormController.java - reqFormDownloadCountUpd | /cs/reqFormDownloadCountUpdateAjax.do | - | 03/24 전달 |  |
| 65 | 5001-04 | CustRequestController.java - reqInsr | /m/mypage/reqInsr.do | - | 03/24 전달 |  |
| 66 | 5001-05 | CustRequestController.java - custRequestAjax | /mypage/custRequestAjax.do | - | 03/24 전달 |  |
| 67 | 5001-01 |  |  | - | 03/23 전달 |  |
| 68 | 5002-01 | CombineController.java - combineSelf | /m/content/combineSelf.do | - | 03/24 전달 |  |
| 69 | 5002-02 |  | /content/getNoLoginRateInfoAjax.do | - | 03/24 전달 |  |
| 70 | 5002-03 | CombineController.java - checkCombineSelf | /content/checkCombineSelfAjax.do | - | 03/24 전달 |  |
| 71 | 5002-04 | CombineController.java - regCombineSelf | /content/regCombineSelfAjax.do | - | 03/24 전달 |  |
| 72 | 아무나솔로 | MypageController.java - myCombineList | /mypage/myCombineList.do | - | 03/24 전달 | 제외 - 불필요 |
| 73 | 아무나솔로 | CombineController.java - getCombiSvcList | /content/getCombiSvcListAjax.do | - | 03/24 전달 | 제외 - 불필요 |
| 74 | 아무나솔로 |  | /m/content/myShareDataList.do | - | 03/24 전달 | 제외 - 불필요 |
| 75 | 아무나솔로 | CombineController.java - getRateInfo | /content/getRateInfoAjax.do | - | 03/24 전달 | 제외 - 불필요 |
| 76 | 5002-05 | CombineController.java - combineSoloType | /content/getCombineSoloTypeAjax.do | - | 03/24 전달 | 제외 - 불필요 |
| 77 | 5003-01 | MyOllehController.java - numberView | /mypage/numberView01.do | - | 03/24 전달 |  |
| 78 | 5003-02 | MyOllehController.java - numberViewStep2 | /mypage/numberView02.do | - | 03/24 전달 |  |
| 79 | 5003-03 | MyOllehController.java - numChgeListAjax | /mypage/numChgeListAjax.do | - | 03/24 전달 |  |
| 80 | 5003-04 | MyOllehController.java - numChgeChgAjax | /mypage/numChgeChgAjax.do | - | 03/24 전달 |  |
| 81 | 5004-01 | UsimSelfChgController.java - reSpnsrPlcyDc | /m/mypage/usimSelfChg.do | - | 03/24 전달 |  |
| 82 | 5004-02 | UsimSelfChgController.java - usimSelfChgAjax | /mypage/usimSelfChgAjax.do | - | 03/24 전달 |  |
| 83 | 5004-03 | UsimSelfChgController.java - usimChgChkAjax | /mypage/usimChgChkAjax.do | - | 03/24 전달 |  |
| 84 | 5005-01 | PauseController.java - suspendCnl | /m/mypage/suspendCnl.do | - | 03/24 전달 |  |
| 85 | 5005-02 | PauseController.java - suspenCnlChgInAjax | /mypage/suspenCnlChgInAjax.do | - | 03/24 전달 |  |
| 86 | 5005-03 | PauseController.java - lostCnlView | /m/mypage/lostCnlView.do | - | 03/24 전달 |  |
| 87 | 5005-04 | PauseController.java - pcsLostCnlChgAjax | /mypage/pcsLostCnlChgAjax.do | - | 03/24 전달 |  |
| 88 | 5006-01 | FarPricePlanController.java - doFarPricePlanView | /m/mypage/farPricePlanView.do | - | 03/24 전달 |  |
| 89 | 5006-02 | FarPricePlanController.java - doMyRateResvViewAjax | /m/mypage/myRateResvViewAjax.do | - | 03/24 전달 |  |
| 90 | 5006-03 | FarPricePlanController.java - deleteMyRateResvAjax | /m/mypage/deleteMyRateResvAjax.do | - | 03/24 전달 |  |
| 91 | 5006-04 | FarPricePlanController.java - farPriceCtgTab1Ajax | /m/mypage/farPriceCtgTab1Ajax.do | - | 03/24 전달 | 제외 - 불필요 |
| 92 | 5006-05 | FarPricePlanController.java - getFarPricePlanListA | /m/mypage/farPriceCtgTab2Ajax.do | - | 03/24 전달 | 제외 - 불필요 |
| 93 | 5006-06 | FarPricePlanController.java - getFarPricePlanListA | /m/mypage/farPriceCtgTab3Ajax.do | - | 03/24 전달 | 제외 - 불필요 |
| 94 | 5006-07 | FarPricePlanController.java - possibleStateCheck | /m/mypage/possibleStateCheckAjax.do | - | 03/24 전달 |  |
| 95 | 5006-08 | FarPricePlanController.java - doRegServicePop | /m/mypage/regServicePop.do | - | 03/24 전달 |  |
| 96 | 5006-09 | FarPricePlanController.java - farPriceContent | /m/mypage/farPriceContent.do | - | 03/24 전달 | 제외 - 불필요 |
| 97 | 5006-10 | CallRegController.java - isOverUsageChargeAjax | /mypage/isOverUsageChargeAjax.do | - | 03/24 전달 | 제외 - 불필요 |
| 98 | 5006-11 | CallRegController.java - selectRealTimeUseageAjax | /mypage/selectRealTimeUseageAjax.do | - | 03/24 전달 | 제외 - 불필요 |
| 99 | 5006-12 | FarPricePlanController.java - proPriceChg | /m/mypage/farPricePlanChgAjax.do | - | 03/24 전달 |  |
| 100 | 5006-13 | FarPricePlanController.java - farPricePlanResChgAj | /m/mypage/farPricePlanResChgAjax.do | - | 03/24 전달 |  |
| 101 | 요금제변경 | RateAdsvcGdncController.java - rateLayer | /m/rate/rateLayer.do | - | 03/24 전달 | 제외 - 불필요 |
| 102 | 요금제변경 | FarPricePlanController.java - chgRateInfoViewAjax | /m/mypage/chgRateInfoViewAjax.do | - | 03/24 전달 | 제외 - 미사용 |
| 103 | 요금제변경 | FarPricePlanController.java - checkOtpAndRegAjax | /mypage/checkOtpAndReg.do | - | 03/24 전달 | 제외 - 미사용 |
| 104 | 요금제변경 | FarPricePlanController.java - resChangeAddPrd | /batch/resChangeAddPrdAjax.do | - | 03/24 전달 | 제외 - 요금제 예약 변경 배치 사용하므로 불필요 |
| 105 | 5007-01 | RegSvcController.java - regServiceView | /m/mypage/regServiceView.do | - | 03/24 전달 |  |
| 106 | 5007-02 | RegSvcController.java - addSvcListAjax | /m/mypage/addSvcListAjax.do | - | 03/24 전달 |  |
| 107 | 5007-03 | RegSvcController.java - myAddSvcListAjax | /m/mypage/myAddSvcListAjax.do | - | 03/24 전달 |  |
| 108 | 5007-05 | RegSvcController.java - moscRegSvcCanChgAjax | /m/mypage/moscRegSvcCanChgAjax.do | - | 03/24 전달 |  |
| 109 | 5007-04 | RegSvcController.java - addSvcViewPop | /m/mypage/addSvcViewPop.do | - | 03/24 전달 |  |
| 110 | 5007-06 | RegSvcController.java - regSvcChgAjax | /m/mypage/regSvcChgAjax.do | - | 03/24 전달 |  |
| 111 | 5007-07 | RegSvcController.java - addSvcViewPop | /m/mypage/addSvcViewPop.do | - | 03/24 전달 |  |
| 112 | 5007-08 | RegSvcController.java - addSvcViewPop | /m/mypage/addSvcViewPop.do | - | 03/24 전달 |  |
| 113 | 5007-08 | RegSvcController.java - addSvcViewPop | /m/mypage/addSvcViewPop.do | - | 03/24 전달 |  |
| 114 | 부가서비스신청/해제 | RegSvcController.java - roamingView | /m/mypage/roamingView.do | - | 03/24 전달 | 제외 - 부가서비스 로직으로 처리 |
| 115 | 부가서비스신청/해제 | RegSvcController.java - roamingCanChgAjax | /m/mypage/roamingCanChgAjax.do | - | 03/24 전달 | 제외 - 부가서비스 로직으로 처리 |
| 116 | 부가서비스신청/해제 | RegSvcController.java - regRoamListAjax | /m/mypage/regRoamListAjax.do | - | 03/24 전달 | 제외 - 부가서비스 로직으로 처리 |
| 117 | 부가서비스신청/해제 | RegSvcController.java - myRoamJoinListAjax | /m/mypage/myRoamJoinListAjax.do | - | 03/24 전달 | 제외 - 부가서비스 로직으로 처리 |
| 118 | 부가서비스신청/해제 | RegSvcController.java - roamingViewPop | /m/mypage/roamingViewPop.do | - | 03/24 전달 | 제외 - 부가서비스 로직으로 처리 |
| 119 | 부가서비스신청/해제 | RateAdsvcGdncController.java - roamingJoinAjax | /m/rate/roamingJoinAjax.do | - | 03/24 전달 | 제외 - 부가서비스 로직으로 처리 |

---

## 상세 분석 내용

## [1001]

**JSP/JS:** /mobile/phone/phoneList.jsp , /resources/js/mobile/phone/phoneList.js  
**URL:** /m/product/phone/phoneList.do  
**Controller:**

```
PhoneMobileController.java - phoneList
  - 초기값 세팅

model.addAttribute("mspOrgList", mspService.findMspOrgListRe("01")); // 제조사 정보 공시
model.addAttribute("listOrderEnum", listOrderEnum);
model.addAttribute("rateCd", rateCd); // 임시처리
model.addAttribute("prodType", "01");//휴대폰
```

**처리 로직 / 변환 처리:**

```
o /mobile/phone/phoneList.jsp , /resources/js/mobile/phone/phoneList.js
  - 유효성 체크 참고
  - 해당 없는 로직 제외 : 휴대폰 비교 callCompareAjaxProd, allClearCompareItem, initSelectPhoneList, initCompareItem, callPhoneCompareList, compareItem, sessionProc

-----------------------------------------

o PhoneMobileController.java - phoneList
   - 세팅값 참고
```

**개발팀 전달일:** 03/22 전달  
---

**URL:** /m/product/phone/phoneAgrListAjax.do  
**Controller:**

```
PhoneMobileController.java - phoneAgrListAjax
  - 휴대폰 목록 조회

        // ========================1.정책정보 조회 시작 ===============================
        MspSalePlcyMstDto mspSalePlcyMstDto = new MspSalePlcyMstDto();
        mspSalePlcyMstDto.setPlcyTypeCd(ONLINE_FOR_MSP); // 정책타입 (온라인직영)
        mspSalePlcyMstDto.setPlcySctnCd(PHONE_FOR_MSP); // 정책유형 (핸드폰)
        mspSalePlcyMstDto.setSprtTp(PHONE_DISCOUNT_FOR_MSP); // 할인유형 (단말할인)
        mspSalePlcyMstDto.setOrgnId(orgnId); // 기관코드
        mspSalePlcyMstDto.setAgrmTrm("24");


        List<MspSalePlcyMstDto> listMspSaleDto = this.mspService.listMspSalePlcyInfoByOnlyOrgn(mspSalePlcyMstDto);
        if (ObjectUtils.isEmpty(listMspSaleDto)) {
            throw new McpCommonException(NO_EXSIST_SALE_PLCY_CD_EXCEPTION);
        }



        // ========================2.상품리스트 조회 시작 ===============================
        commonSearchDto.setListMspSaleDto(listMspSaleDto);// 정책정보가 존재하는 상품정보를 읽어오기위해 set
        List<PhoneProdBasDto> lteList = this.phoneService.listPhoneProdBasForFrontOneQuery(commonSearchDto);

        // ========================1.상품리스트 조회 끝 ===============================
        // 할부수수료 default 값 조회
        CmnGrpCdMst cdInfo = this.mspService.findCmnGrpCdMst("CMN0051", "20"); // 할부수수료 default 값
        String onOffType = "P".equals(NmcpServiceUtils.getPlatFormCd()) ? "0" : "3"; // PC = 0 / m = 3

        // ========================3.상품별요금 조회 시작 ===============================
        lteList.forEach(list -> {
            MspSalePlcyMstDto saleMstDto = listMspSaleDto.stream()
                                                         .filter(saleDto -> list.getSalePlcyCd().equals(saleDto.getSalePlcyCd()))
                                                         .findFirst().orElse(null);
            //PROD_ID   4250   REP_RATE LTEVCFL15  AGRM_TRM_BASE 0
            //TO_DO TEST 삭제 해야 함... 테스트용 ....
            /*if ("4250".equals(list.getProdId()))  {
                list.setRepRate("LTEVCFL15");
                list.setAgrmTrmBase("0");
            }*/

            //saleMstDto.setRateCd(StringUtils.isNotEmpty(list.getRepRate()) ? list.getRepRate() : saleMstDto.getRateCd());       /// ????????
            //list.setAgrmTrmBase(StringUtils.isEmpty(list.getAgrmTrmBase()) ? mspSalePlcyMstDto.getAgrmTrm() : list.getAgrmTrmBase());// nulld일 경우       ??????????????????

            saleMstDto.setRateCd(StringUtils.isNotEmpty(list.getRepRate()) ? list.getRepRate() : "");
            list.setAgrmTrmBase(StringUtils.isEmpty(list.getAgrmTrmBase()) ? "24" : list.getAgrmTrmBase());// nulld일 경우       ??????????????????

            //mspSalePlcyMstDto.setAgrmTrm("Y".equals(commonSearchDto.getSesplsYn()) ? mspSalePlcyMstDto.getAgrmTrm() : list.getAgrmTrmBase()); //자급제일때 (0)
            //mspSalePlcyMstDto.setAgrmTrm(!"Y".equals(commonSearchDto.getSesplsYn()) && "0".equals(list.getAgrmTrmBase()) ? "24" : mspSalePlcyMstDto.getAgrmTrm());//자급제가 아닌데 0이면 24    ???????????

            String modelMonthly = "0";
            String agrmTrmBase = list.getAgrmTrmBase();

            if ("Y".equals(commonSearchDto.getSesplsYn())) {
                mspSalePlcyMstDto.setAgrmTrm(mspSalePlcyMstDto.getAgrmTrm()); //자급제일때 (0)
                modelMonthly = "24";  //무약정 , 24개달 단말 할부
            } else {

                mspSalePlcyMstDto.setAgrmTrm(agrmTrmBase);

                if ("0".equals(agrmTrmBase)) {
                    modelMonthly = "24";  //무약정 , 24개달 단말 할부
                } else {
                    modelMonthly = agrmTrmBase;
                }
            }


            saleMstDto.setPlcySctnCd(PHONE_FOR_MSP);  //단말기 설정
            MspSaleSubsdMstDto mstDto = new MspSaleSubsdMstDto();
            try {
                mstDto = this.mspService.getLowPriceChargeInfoByProdList(list.getRprsPrdtId(), saleMstDto, "N",
                        mspSalePlcyMstDto.getOrgnId(), OPER_PHONE_NUMBER_TRANS, mspSalePlcyMstDto.getAgrmTrm(), saleMstDto.getRateCd(), "Y"
                        , cdInfo, onOffType,modelMonthly);

                if (!ObjectUtils.isEmpty(mstDto)) {
                    List<MspSaleSubsdMstDto> tempList = new ArrayList<MspSaleSubsdMstDto>();
                    tempList.add(mstDto);
                    list.setMspSaleSubsdMstListForLowPrice(tempList);
                }

                // 요금제 조회 null 일 경우 추천요금제로 재 조회     ????????????
                //if (ObjectUtils.isEmpty(mstDto)) {
                    /*mstDto = this.mspService.getLowPriceChargeInfoByProdList(list.getRprsPrdtId(), saleMstDto, "N",
                            mspSalePlcyMstDto.getOrgnId(), OPER_PHONE_NUMBER_TRANS, "24", null, "Y", cdInfo, onOffType);*/

                    //lteList.remove(list);
                //}

            } catch (McpCommonException e) {
                logger.debug("정책정보가 없는 상품 입니다.====>[" + list.getProdNm() + "]");
            } finally {
                if (!ObjectUtils.isEmpty(mstDto)) {
                    list.setPayMnthAmt(mstDto.getPayMnthUnContAmt(list.getAgrmTrmBase())); // 월 단말금
                    list.setInstAmt(mstDto.getInstAmt());       // 원금 최저가
                    list.setRateNm(mstDto.getRateNm());         // 대표/일반 요금제 명
                    list.setRateCd(mstDto.getRateCd());         // 대표/일반 요금제 코드
                    list.setBaseAmt(mstDto.getTotalPayAmout()); // 월 납부 부가세 포함
                    list.setRepRateNm(mstDto.getRateNm());      // 대표 요금제명
                }
            }
        });
```

**처리 로직 / 변환 처리:**

```
o PhoneMobileController.java - phoneAgrListAjax
--> /m/product/phone/phoneListAllAjax.do 과 동일
```

**개발팀 전달일:** 03/22 전달  
---

**URL:** /m/product/phone/phoneRateListAjax.do  
**Controller:**

```
PhoneMobileController.java - phoneRateListAjax
  - 요금제 조회
```

**Service:** mspService.listRateByOrgnInfos  
**Service / Dao / SQL / API:**

```
mspDao.listRateByOrgnInfos
-> [M포탈-M전산 API] /msp/rateByOrgnInfos
```

**처리 로직 / 변환 처리:**

```
o /msp/rateByOrgnInfos
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**URL:** /m/product/phone/phoneListAllAjax.do  
**Controller:**

```
PhoneMobileController.java - phoneListAllAjax
  - 휴대폰 목록 조회

        // ========================1.정책정보 조회 시작 ===============================
        /*
         * 해당 기관정보로 정책정보를 조회 한다. 정책정보는 온라인직영,핸드폰,단말할인,기관코드를 기준으로 가져온다.
         */

        List<MspSalePlcyMstDto> listMspSaleDto = new ArrayList<MspSalePlcyMstDto>();

        MspSalePlcyMstDto mspSalePlcyMstDto = new MspSalePlcyMstDto();
        mspSalePlcyMstDto.setPlcyTypeCd(ONLINE_FOR_MSP); // 정책타입 (온라인직영)
        mspSalePlcyMstDto.setPlcySctnCd(PHONE_FOR_MSP); // 정책유형 (핸드폰)
        mspSalePlcyMstDto.setSprtTp(PHONE_DISCOUNT_FOR_MSP); // 할인유형 (단말할인)
        mspSalePlcyMstDto.setOrgnId(orgnId); // 기관코드
        List<MspSalePlcyMstDto> listMspBseSaleDto = mspService.listMspSalePlcyInfoByOnlyOrgn(mspSalePlcyMstDto);

        listMspSaleDto.addAll(listMspBseSaleDto);

        // ========================1.정책정보 조회 끝 ===============================

        if (listMspSaleDto == null || listMspSaleDto.size() == 0) {
            throw new McpCommonException(NO_EXSIST_SALE_PLCY_CD_EXCEPTION);
        }
        // ========================2.상품리스트 조회 시작 ===============================
        // commonSearchDto.setListMspSaleDto(listMspSaleDto);// 정책정보가 존재하는 상품정보를 읽어오기위해
        // set
        commonSearchDto.setListMspSaleDto(listMspBseSaleDto);// 정책정보가 존재하는 상품정보를 읽어오기위해 set
        commonSearchDto.setSesplsYn("N");
        List<PhoneProdBasDto> lteList = new ArrayList<PhoneProdBasDto>();
        List<PhoneProdBasDto> lteBaseList = phoneService.listPhoneProdBasForFrontOneQuery(commonSearchDto);
        commonSearchDto.setSesplsYn("Y");
        commonSearchDto.setListMspSaleDto(listMspSelfSaleDto);// 정책정보가 존재하는 상품정보를 읽어오기위해 set
        List<PhoneProdBasDto> lteSelfList = phoneService.listPhoneProdBasForFrontOneQuery(commonSearchDto);

        lteList.addAll(lteBaseList);
        lteList.addAll(lteSelfList);
        // ========================1.상품리슽 조회 끝 ===============================

        // 할부수수료 default 값 조회
        CmnGrpCdMst cdInfo = mspService.findCmnGrpCdMst("CMN0051", "20"); // 할부수수료 default 값

        // ========================3.상품별요금 조회 시작 ===============================
        for (PhoneProdBasDto item : lteList) {
            MspSalePlcyMstDto paramPlcy = null;

            /*
             * 동일한 LTE 단말기인 경우라도 단말기 마다. 정책이 상이 할수 있다.. 각 상품별 정책 정보를 설정 한다.
             */
            for (MspSalePlcyMstDto param : listMspSaleDto) {
                if (item.getSalePlcyCd().equals(param.getSalePlcyCd())) {
                    paramPlcy = param;
                    break;
                }
            }
            // 상품 리스트는 단말할인/24개월/번호이동이 기준값이다.
            MspSaleSubsdMstDto mspSaleSubsdMst = null;
            MspSaleSubsdMstDto mspSaleSelfSubsdMst = null;
            try {

                mspSaleSubsdMst = mspService.getLowPriceChargeInfoByProdList(item.getRprsPrdtId(), paramPlcy, "N",
                        orgnId, OPER_PHONE_NUMBER_TRANS, "24", null, "Y", cdInfo);
                if (mspSaleSubsdMst != null) {
                    item.setPayMnthAmt(mspSaleSubsdMst.getPayMnthAmt());
                    item.setInstAmt(mspSaleSubsdMst.getInstAmt());

                } else {

                    mspSaleSelfSubsdMst = mspService.getLowPriceChargeInfoByProdList(item.getRprsPrdtId(), paramPlcy,
                            "N", sesplsOrgnId, OPER_PHONE_NUMBER_TRANS, "0", null, "Y", cdInfo);
                    if (mspSaleSelfSubsdMst != null) {
                        item.setPayMnthAmt(mspSaleSelfSubsdMst.getPayMnthAmt());
                        item.setInstAmt(mspSaleSelfSubsdMst.getInstAmt());
                    }

                }

            } catch (McpCommonException mce) {
                logger.debug("정책정보가 없는 상품 입니다.====>[" + item.getProdNm() + "]");
            }
        }

        // ========================3.상품별요금 조회 끝 ===============================
```

**Service:** mspService.listMspSalePlcyInfoByOnlyOrgn  
**Service / Dao / SQL / API:**

```
mspDao.listMspSalePlcyInfoByOnlyOrgn
-> [M포탈-M전산 API] /msp/mspSalePlcyInfoByOnlyOrgn
```

**처리 로직 / 변환 처리:**

```
o PhoneMobileController.java - phoneListAllAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  - 해당 없는 로직 제외 : 자급폰 관련
  - 오프라인 도매 정보로 세팅하도록 변경
        mspSalePlcyMstDto.setPlcyTypeCd("W"); // 정책타입 (온라인(직영):D ,오프라인(도매):W , 특수:S ,제휴 :A)
        mspSalePlcyMstDto.setPlcySctnCd("01"); // 정책유형 (단말:01,유심:02)
        mspSalePlcyMstDto.setSprtTp("KD"); // 할인유형 (단말할인:KD,요금할인:PM)
        mspSalePlcyMstDto.setOrgnId(orgnId); // 기관코드(로그인 조직으로 세팅, 직영 : sale.orgnId=1100011741, sale.sesplsOrgnId=V000019481)

-----------------------------------------

o /msp/mspSalePlcyInfoByOnlyOrgn
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** phoneService.listPhoneProdBasForFrontOneQuery  
**Service / Dao / SQL / API:**

```
phoneDao.listPhoneProdBasForFrontOneQuery
-> [M포탈-M전산 API] /phone/phoneProdBasForFrontOneQuery
```

**처리 로직 / 변환 처리:**

```
o /phone/phoneProdBasForFrontOneQuery
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
mspService.getLowPriceChargeInfoByProdList

        //할부수수료 default 값
        BigDecimal defaultInstRate = new BigDecimal(cmnGrpCdMst.getEtc1());

       MspSaleSubsdMstDto mspSaleSubsdMstDto = new MspSaleSubsdMstDto();
       mspSaleSubsdMstDto.setPrdtId(prdtId);
       mspSaleSubsdMstDto.setSalePlcyCd(mspSalePlcyMstDto.getSalePlcyCd());
       mspSaleSubsdMstDto.setOldYn(oldYn);
       mspSaleSubsdMstDto.setOrgnId(orgnId);
       mspSaleSubsdMstDto.setOperType(operType);//가입유형
       mspSaleSubsdMstDto.setAgrmTrm(instNom);//입력받은 할부기간을 약정기간으로 세팅한다.
       mspSaleSubsdMstDto.setRateCd(rateCd);//요금제코드
       mspSaleSubsdMstDto.setNoArgmYn(noArgmYn);
       mspSaleSubsdMstDto.setForFrontFastYn(""); //성능 개선을 위한 최소 컬럼을 호출하기 위한 처리 SQL 호출을 위한 flag

       BigDecimal instRate = mspSalePlcyMstDto.getInstRate();       //정책정보의 할부수수료값
       MspSaleSubsdMstDto mspSaleSubsdMst = mspDao.getMspSaleSubsdMst(mspSaleSubsdMstDto);

       if (mspSaleSubsdMst != null) {
           mspSaleSubsdMst.setInstRate(instRate);
           mspSaleSubsdMst.setDefaultInstRate(defaultInstRate);
           mspSaleSubsdMst.setModelMonthly(Integer.parseInt(instNom));
       }

       return mspSaleSubsdMst;
```

**Service / Dao / SQL / API:**

```
mspDao.getMspSaleSubsdMst
-> [M포탈-M전산 API] /msp/lowPriceChargeInfoByProd
```

**처리 로직 / 변환 처리:**

```
o mspService.getLowPriceChargeInfoByProdList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

-----------------------------------------

o /msp/lowPriceChargeInfoByProd
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  

---

## [1002]

**JSP/JS:** /mobile/phone/phone.view.jsp, /resources/js/mobile/phone/phone.view.js  
**URL:** /m/product/phone/phoneView.do  
**Controller:**

```
PhoneFrontController.java - phoneViewNe
  - 휴대폰별 용량/색상 정보 조회

        // 임시 저장 정보 패치
        if (StringUtils.isBlank(prodCommendDto.getPrdtSctnCd())) {
            prodCommendDto.setPrdtSctnCd("LTE");
        }

        if (StringUtils.isNotBlank(prodCommendDto.getRateCd())) {
            MspRateMstDto rateInfo = fCommonSvc.getMspRateMst(prodCommendDto.getRateCd());
            if (rateInfo != null && rateInfo.getDataType() != null) {
                prodCommendDto.setPrdtSctnCd(rateInfo.getDataType());
            }
        }

        if ("A".equals(NmcpServiceUtils.getPlatFormCd()) || "M".equals(NmcpServiceUtils.getPlatFormCd())) {
            if ("5".equals(prodCommendDto.getOnOffType())) {
                prodCommendDto.setOnOffType("7");
            } else if ("0".equals(prodCommendDto.getOnOffType())) {
                prodCommendDto.setOnOffType("3");
            } else if ("6".equals(prodCommendDto.getOnOffType())) {
                prodCommendDto.setOnOffType("8");
            }
        } else {
            if ("7".equals(prodCommendDto.getOnOffType())) {
                prodCommendDto.setOnOffType("5");
            } else if ("3".equals(prodCommendDto.getOnOffType())) {
                prodCommendDto.setOnOffType("0");
            } else if ("8".equals(prodCommendDto.getOnOffType())) {
                prodCommendDto.setOnOffType("6");
            }
        }

        model.addAttribute("ProdCommendDto", prodCommendDto);

        String orgnId2 = orgnId;
        /*
         * ## 1.상품및 단품정보 조회 상품대표이미지,용량색상별 정보 등을 조회한다.
         */
        PhoneProdBasDto phoneProdBas = phoneService.findPhoneProdBasDtoByProdId(phoneProdBasDto);

        if (phoneProdBas == null) {
            throw new McpCommonException(ExceptionMsgConstant.NO_EXSIST_PROD_BAS_EXCEPTION);
        }
        if (phoneProdBas.doNotSale()) {
            throw new McpCommonException(ExceptionMsgConstant.F_BIND_EXCEPTION);
        }

        MspSalePlcyMstDto mspSalePlcyMstDto = new MspSalePlcyMstDto();
        mspSalePlcyMstDto.setPlcyTypeCd(ONLINE_FOR_MSP);
        if (PROD_LTE_CD.equals(phoneProdBas.getProdCtgId())) {
            mspSalePlcyMstDto.setPrdtSctnCd(LTE_FOR_MSP);
        } else if (PROD_3G_CD.equals(phoneProdBas.getProdCtgId())) {
            mspSalePlcyMstDto.setPrdtSctnCd(THREE_G_FOR_MSP);
        } else if (PROD_5G_CD.equals(phoneProdBas.getProdCtgId())) {
            mspSalePlcyMstDto.setPrdtSctnCd(FIVE_G_FOR_MSP);
        } else if (PROD_LTE5G_CD.equals(phoneProdBas.getProdCtgId())) {
            mspSalePlcyMstDto.setPrdtSctnCd(LTEFIVE_G_FOR_MSP);
        }
        mspSalePlcyMstDto.setPlcySctnCd(PHONE_FOR_MSP);
        mspSalePlcyMstDto.setOrgnId(orgnId);

        /*
         * ## 2.정책 조회 (가입유형 조회) ## 이하 아래는 정책정보를 조회 하는 부분이다. 상품별 정책은(단말할인,요금할인)에 따라 최대
         * 2개까지 나올 가능성이 있다. 해당 단품에 정책이 2개가 연결되있을경우 단말할인을 으로 선택해서 조회 한다. 정책조회 정보에 [가입유형
         * 가능여부]가 포함되있다.
         */
        //String hndsetModelIdParam = hndsetModelId;
        //if ("".equals(hndsetModelId)) { // 단말(nrds)id가 없을경우 단품을 순회하면서 대표속성을 찾아서 세팅
        if ("".equals(hndsetModelIdParam)) {
            for (PhoneSntyBasDto snty : phoneProdBas.getPhoneSntyBasDtosList()) {
                if ("Y".equals(snty.getRprsPrdtYn())) {
                    hndsetModelIdParam = snty.getHndsetModelId();
                }
            }
        }

        // 현재 단품 ID 로 해당 대표모델ID를 조회한다.
        PhoneMspDto phoneMspDto = mspService.findMspPhoneInfo(hndsetModelIdParam);
        if (phoneMspDto == null) {
            throw new McpCommonException(ExceptionMsgConstant.NO_EXSIST_PROD_BAS_EXCEPTION);
        }
        String rprsPrdtIdCurrent = phoneMspDto.getRprsPrdtId(); // 해당 단품의 대표모델ID
        phoneProdBas.setHndsetModelId(hndsetModelIdParam);// 대표상품을 현대 BasDto 에 핸드셋 모델ID로 set
        String agrmTrmBase = phoneProdBas.getAgrmTrmBase();  //요금 약정 기간 기본값 처리
        MspSaleDto mspSaleDto = mspService.getMspSale(rprsPrdtIdCurrent, mspSalePlcyMstDto);


        //String operTypeParam = operType;
        //if ("".equals(operType)) { // 가입유형이 없을경우
        if ("".equals(operTypeParam)) { // 가입유형이 없을경우
            if ("Y".equals(mspSaleDto.getMspSalePlcyMstDtoSimbol().getMnpYn())) { // 번호이동이 우선수위가 제일 높다.번호이동여부가 Y 일경우
                operTypeParam = OPER_PHONE_NUMBER_TRANS;                        // 업무유형을 번호이동코드로 set
            } else if ("Y".equals(mspSaleDto.getMspSalePlcyMstDtoSimbol().getNewYn())) {
                operTypeParam = OPER_NEW;
            } else if ("Y".equals(mspSaleDto.getMspSalePlcyMstDtoSimbol().getHdnYn())) {
                operTypeParam = OPER_TYPE_EXCHANGE;
            }
        }

        /*
         *  3.단말할부기간조회  ##
         */
        List<MspSaleAgrmMst> mspSaleAgrmMstList = null;
        mspSaleAgrmMstList = mspService.listMspSaleAgrmMst(mspSaleDto.getMspSalePlcyMstDtoSimbol().getSalePlcyCd());

        /*
         * 요금 약정 기간 기본값 처리
         */
        if ("".equals(instNomParam)) {
            if (StringUtils.isBlank(agrmTrmBase)) {
                if (mspSaleAgrmMstList.size() == 1) {
                    instNomParam = mspSaleAgrmMstList.get(0).getInstNom();
                } else {
                    instNomParam = mspSaleAgrmMstList.get(0).getInstNom();//리스트의 요금제정보의 할부개월은 모두같은것을 조회 했기때문에 임의이 1개의를 가져와 set
                    if ("0".equals(instNomParam)) {        //임시로 처리 하는중 데이터가 없음
                        instNomParam = mspSaleAgrmMstList.get(1).getInstNom(); //<=== 왜?????
                    }
                }
            } else {
                instNomParam = agrmTrmBase;
            }
        }


        phoneProdBas.setSntyDisp(StringEscapeUtils.unescapeHtml(phoneProdBas.getSntyDisp()));
        model.addAttribute("phoneProdBas", phoneProdBas);
        model.addAttribute("operType", operTypeParam);
        model.addAttribute("hndsetModelId", hndsetModelIdParam);
        model.addAttribute("mspSaleDto", mspSaleDto); //정책에 정보 색상 정보가 변경 되었도 변경 되지 않는다...
        model.addAttribute("mspSaleAgrmMstList", mspSaleAgrmMstList);   //단말기 약정
        model.addAttribute("instNom", instNomParam);
        model.addAttribute("sprtTp", sprtTpParam);
        model.addAttribute("cardList", cardList);

        //단말할인만 있을경우 phoneView.jsp 할인유형 미노출 , 요금할인 있을경우 할인유형 노출

        MspSalePlcyMstDto mspSaleMstDto = new MspSalePlcyMstDto();
        mspSaleMstDto.setPrdtId(rprsPrdtIdCurrent);
        mspSaleMstDto.setSprtTp("PM"); //요금할인
        List<MspSalePlcyMstDto> plcyMstList = mspService.findMspSalePlcyMst(mspSaleMstDto);

        if(plcyMstList != null && plcyMstList.size() > 0) {
            model.addAttribute("discountType", "Y");
        }else {
            model.addAttribute("discountType", "N");
        }
```

**처리 로직 / 변환 처리:**

```
o /mobile/phone/phone.view.jsp, /resources/js/mobile/phone/phone.view.js
  - 유효성 체크 참고
  - 해당 없는 로직 제외 : 자급폰 관련, 비교하기, 제휴카드 안내, KT인터넷 트리플할인, 리뷰, 이벤트

-----------------------------------------

o PhoneFrontController.java - phoneViewNe
  - 로직 적용
  - 해당 없는 로직 제외 : 자급폰 관련
  - 오프라인 도매 정보로 세팅하도록 변경
```

**개발팀 전달일:** 03/22 전달  

---

## [1002-01]

**JSP/JS:** /mobile/phone/phone.view.jsp, /resources/js/mobile/phone/phone.view.js  
**URL:** /m/product/phone/phoneView.do  
**Controller:** PhoneFrontController.java - phoneViewNe  
**Service:** fCommonSvc.getMspRateMst  
**Service / Dao / SQL / API:**

```
fCommonDao.getMspRateMst
-> [M포탈-M전산 API] /common/mspRateMst
```

**처리 로직 / 변환 처리:**

```
o /common/mspRateMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
phoneService.findPhoneProdBasDtoByProdId
  - 상품및 단품정보 조회 상품대표이미지,용량색상별 정보 등을 조회
```

**Service / Dao / SQL / API:**

```
phoneDao.findPhoneProd
-> [M포탈-M전산 API] /phone/nmcpProdBas
```

**처리 로직 / 변환 처리:**

```
o /phone/nmcpProdBas
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
mspService.findMspPhoneInfo
  - 현재 단품 ID 로 해당 대표모델ID를 조회
```

**Service / Dao / SQL / API:**

```
mspDao.findMspPhoneInfo
-> [M포탈-M전산 API] /msp/mspPhoneInfo
```

**처리 로직 / 변환 처리:**

```
o /msp/mspPhoneInfo
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
mspService.getMspSale

        //return 할 msp 판매정책정보및 상품정보 set
        MspSaleDto mspSaleDtoRtn = new MspSaleDto();

        mspSalePlcyMstDto.setPrdtId(prdtId);
        List<MspSalePlcyMstDto> resultSalePlcyMst = findMspSalePlcyMst(mspSalePlcyMstDto);

        if (resultSalePlcyMst == null || resultSalePlcyMst.size() == 0) {
            throw new McpCommonException(NO_EXSIST_SALE_PLCY_CD_EXCEPTION);
        }

        String salePlcyCd = ""; //정책코드
        String orgnId2 = mspSalePlcyMstDto.getOrgnId();//기관코드

        if (resultSalePlcyMst.size() > 1) { //정책코드가 2개 이상일경우에는 단말할인을 기본값으로 선택해서 정책정보를 가져온다.
            for (MspSalePlcyMstDto baket: resultSalePlcyMst) {

                if(orgnId2.contentEquals(sesplsOrgnId)) {
                    // 자급제인 경우 강제로 변환 처리
                    salePlcyCd = baket.getSalePlcyCd();
                    mspSaleDtoRtn.setMspSalePlcyMstDtoSimbol(baket); // 1개 정책 세팅
                    break;

                } else {
                    // 휴대폰/중고폰인 경우

                    if (PhoneConstant.PHONE_DISCOUNT_FOR_MSP.equals(baket.getSprtTp())) {
                        salePlcyCd = baket.getSalePlcyCd();
                        mspSaleDtoRtn.setMspSalePlcyMstDtoSimbol(baket);  //2개이상의 정책이 조회 되었기떄문에 대표정책을 할당한다.(단품할인)
                        break;
                    }
                }
            }
        } else {
            salePlcyCd = resultSalePlcyMst.get(0).getSalePlcyCd();
            mspSaleDtoRtn.setMspSalePlcyMstDtoSimbol(resultSalePlcyMst.get(0)); //대표정책 할당
        }


        //검색조건 생성 정책코드와,단품(nrds) 코드값을 set
        MspSalePrdtMstDto searchParam = new MspSalePrdtMstDto();
        searchParam.setPrdtId(prdtId);
        searchParam.setSalePlcyCd(salePlcyCd);
        searchParam.setOrgnId(orgnId2);

        //MSP 판매 상품및 수수료 정보 조회
        MspSalePrdtMstDto resultSalePrdMst = mspDao.findMspSalePrdMst(searchParam);

        //판매상품정보가 msp 정보에 존재하지 않는다.
        if (resultSalePrdMst == null) {
            throw new McpCommonException(NO_EXSIST_SALE_PRDT);
        }

        mspSaleDtoRtn.setMspSalePlcyMstDto(resultSalePlcyMst);
        mspSaleDtoRtn.setMspSalePrdMstDto(resultSalePrdMst);
```

**Service / Dao / SQL / API:**

```
mspDao.findMspSalePlcyMst
-> [M포탈-M전산 API] /msp/mspSaleOrgnMst
```

**처리 로직 / 변환 처리:**

```
o mspService.getMspSale
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

-----------------------------------------

o /msp/mspSaleOrgnMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
mspDao.findMspSalePrdMst
-> [M포탈-M전산 API] /msp/mspSalePrdMst
```

**처리 로직 / 변환 처리:**

```
o /msp/mspSalePrdMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
mspService.listMspSaleAgrmMst
  - 단말기 할부기간 조회
```

**Service / Dao / SQL / API:**

```
mspDao.listMspSaleAgrmMst
-> [M포탈-M전산 API] /msp/mspSaleAgrmMst
```

**처리 로직 / 변환 처리:**

```
o /msp/mspSaleAgrmMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
mspService.findMspSalePlcyMst
  - 단말할인만 있을경우 phoneView.jsp 할인유형 미노출 , 요금할인 있을경우 할인유형 노출
```

**Service / Dao / SQL / API:**

```
mspDao.findMspSalePlcyMst
-> [M포탈-M전산 API] /msp/mspSaleOrgnMst
```

**처리 로직 / 변환 처리:**

```
o /msp/mspSaleOrgnMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**URL:** /m/product/phone/phoneViewAjax.do  
**Controller:**

```
PhoneMobileController.java - phoneViewAjax

        String orgnId2 = orgnId;
        // String plcySctnCd = PhoneConstant.PHONE_FOR_MSP;

        /*
         * ## 1.상품및 단품정보 조회 상품대표이미지,용량색상별 정보 등을 조회한다.
         */
        PhoneProdBasDto phoneProdBas = phoneService.findPhoneProdBasDtoByProdId(phoneProdBasDto);

        MspSalePlcyMstDto mspSalePlcyMstDto = new MspSalePlcyMstDto();
        mspSalePlcyMstDto.setPlcyTypeCd(ONLINE_FOR_MSP);
        if (PROD_LTE_CD.equals(phoneProdBas.getProdCtgId())) {
            mspSalePlcyMstDto.setPrdtSctnCd(LTE_FOR_MSP);
        } else if (PROD_3G_CD.equals(phoneProdBas.getProdCtgId())) {
            mspSalePlcyMstDto.setPrdtSctnCd(THREE_G_FOR_MSP);
        } else if (PROD_5G_CD.equals(phoneProdBas.getProdCtgId())) {
            mspSalePlcyMstDto.setPrdtSctnCd(FIVE_G_FOR_MSP);
        } else if (PROD_LTE5G_CD.equals(phoneProdBas.getProdCtgId())) {
            mspSalePlcyMstDto.setPrdtSctnCd(LTEFIVE_G_FOR_MSP);
        }
        mspSalePlcyMstDto.setPlcySctnCd(PHONE_FOR_MSP);
        mspSalePlcyMstDto.setOrgnId(orgnId);

        // 자급제폰인 경우 강제 세팅
        String sesplsYn = phoneProdBas.getSesplsYn();
        if (sesplsYn == null) {
            sesplsYn = "";
        }
        String prodType = phoneProdBas.getProdType();

        /*
         * ## 2.정책 조회 (가입유형 조회) ## 이하 아래는 정책정보를 조회 하는 부분이다. 상품별 정책은(단말할인,요금할인)에 따라 최대
         * 2개까지 나올 가능성이 있다. 해당 단품에 정책이 2개가 연결되있을경우 단말할인을 으로 선택해서 조회 한다. 정책조회 정보에 [가입유형
         * 가능여부]가 포함되있다.
         */
        String hndsetModelIdParam = hndsetModelId;
        if ("".equals(hndsetModelIdParam)) { // 단말(nrds)id가 없을경우 단품을 순회하면서 대표속성을 찾아서 세팅
            for (PhoneSntyBasDto snty : phoneProdBas.getPhoneSntyBasDtosList()) {
                if ("Y".equals(snty.getRprsPrdtYn())) {
                    hndsetModelIdParam = snty.getHndsetModelId();
                }
            }
        }

        // 현재 단품 ID 로 해당 대표모델ID를 조회한다.
        PhoneMspDto phoneMspDto = mspService.findMspPhoneInfo(hndsetModelIdParam);
        String rprsPrdtIdCurrent = phoneMspDto.getRprsPrdtId(); // 해당 단품의 대표모델ID

        // 자급제인 경우 강제 세팅
        if ("Y".equals(sesplsYn)) {
            mspSalePlcyMstDto.setOrgnId(sesplsOrgnId);
            mspSalePlcyMstDto.setPlcySctnCd("02");
        }

        MspSaleDto mspSaleDto = mspService.getMspSale(rprsPrdtIdCurrent, mspSalePlcyMstDto);

        /*
         * ## 3.단말할부기간조회 ##
         */
        List<MspSaleAgrmMst> mspSaleAgrmMstList = null;
        if ("Y".equals(sesplsYn)) {
            // 자급제인 경우 강제로 변환 처리
            // 판매정책코드
            String salePlcyCd = mspSaleDto.getMspSalePlcyMstDtoSimbol().getSalePlcyCd();
            MspSaleAgrmMst mspSaleAgrmMst = new MspSaleAgrmMst();
            mspSaleAgrmMst.setSalePlcyCd(salePlcyCd);
            mspSaleAgrmMst.setOrgnId(sesplsOrgnId); // MSP_SALE_ORGN_MST@DL_MSP - ORG.ORGN_ID = 'V000019481'

            mspSaleAgrmMstList = mspService.listMspSaleAgrmMst2(mspSaleAgrmMst);

        } else {
            mspSaleAgrmMstList = mspService.listMspSaleAgrmMst(mspSaleDto.getMspSalePlcyMstDtoSimbol().getSalePlcyCd());
        }

        /*
         * ## 4.요금리스트 조회 ##
         */
        String instNomParam = instNom;
        if ("".equals(instNomParam)) {
            if (mspSaleAgrmMstList.size() == 1) {
                instNomParam = mspSaleAgrmMstList.get(0).getInstNom();
            } else {
                instNomParam = mspSaleAgrmMstList.get(0).getInstNom();// 리스트의 요금제정보의 할부개월은 모두같은것을 조회 했기때문에 임의이 1개의를 가져와
                // set
                if ("0".equals(instNomParam)) { // 임시로 처리 하는중 데이터가 없음
                    instNomParam = mspSaleAgrmMstList.get(1).getInstNom();
                }
            }
        }

        String operTypeParam = operType;
        if ("".equals(operType)) { // 가입유형이 없을경우
            if ("Y".equals(mspSaleDto.getMspSalePlcyMstDtoSimbol().getMnpYn())) { // 번호이동이 우선수위가 제일 높다.번호이동여부가 Y 일경우
                // 업무유형을 번호이동코드로 set
                operTypeParam = OPER_PHONE_NUMBER_TRANS;
            } else if ("Y".equals(mspSaleDto.getMspSalePlcyMstDtoSimbol().getNewYn())) {
                operTypeParam = OPER_NEW;
            } else if ("Y".equals(mspSaleDto.getMspSalePlcyMstDtoSimbol().getHdnYn())) {
                operTypeParam = OPER_TYPE_EXCHANGE;
            }
        }

        // 자급제폰인 경우 강제 세팅
        if ("Y".equals(sesplsYn)) {
            orgnId2 = sesplsOrgnId; // 기관코드
            // sprtTp = ""; // 할인유형 (단말할인)
            // plcySctnCd = "02"; // 유심으로 설정
        }

        String modelMonthlyParam = modelMonthly;
        if ("null".equals(StringUtil.NVL(modelMonthlyParam, "null"))) {
            modelMonthlyParam = instNomParam;
        }
        String noAgrmYnParam = noAgrmYn;
        // List<MspSaleSubsdMstDto> chargeList =
        // mspService.listChargeInfo(rprsPrdtIdCurrent, mspSaleDto, "N",
        // orgnId2,operTypeParam,instNomParam,instNomParam,null,null,onOffType); //10
        List<MspSaleSubsdMstDto> chargeList = mspService.listChargeInfo(rprsPrdtIdCurrent, mspSaleDto, "N", orgnId2,
                operTypeParam, instNomParam, modelMonthlyParam, null, noAgrmYnParam, onOffType); // 10
        List<MspSaleSubsdMstDto> xmlList = usimService.listChargeInfoUsimXml(chargeList, "01");// 휴대폰요금제

        String rchk = phoneProdBas.getRecommendRate();
        List<MspSaleSubsdMstDto> filterList = new ArrayList<MspSaleSubsdMstDto>();
        List<MspSaleSubsdMstDto> filterXmlList = new ArrayList<MspSaleSubsdMstDto>();
        if ("".equals(chargeType) || "01".equals(chargeType)) {
            filterList = chargeList;
            filterXmlList = chargeList;
        } else {
            if ("02".equals(chargeType)) { // 추천요금제 sprtTp
                for (MspSaleSubsdMstDto item : chargeList) {
                    if (rchk != null && rchk.indexOf(item.getRateCd() + "#" + item.getSprtTp()) > -1) {
                        filterList.add(item);
                        filterXmlList.add(item);
                    }
                }
            } else if ("03".equals(chargeType)) { // 약정 (단말할인)
                for (MspSaleSubsdMstDto item : chargeList) {
                    if (PhoneConstant.PHONE_DISCOUNT_FOR_MSP.equals(item.getSprtTp())
                            && !"0".equals(item.getAgrmTrm())) {
                        filterList.add(item);
                        filterXmlList.add(item);
                    }
                }
            } else if ("04".equals(chargeType)) { // 약정 (요금할인)
                for (MspSaleSubsdMstDto item : chargeList) {
                    if (PhoneConstant.CHARGE_DISCOUNT_FOR_MSP.equals(item.getSprtTp())
                            && !"0".equals(item.getAgrmTrm())) {
                        filterList.add(item);
                        filterXmlList.add(item);
                    }
                }
            } else if ("05".equals(chargeType)) { // 무약정요금제
                for (MspSaleSubsdMstDto item : chargeList) {
                    if ("0".equals(item.getAgrmTrm())) {
                        filterList.add(item);
                        filterXmlList.add(item);
                    }
                }
            }
        }

        // 최저가로 정렬처리하기위해 임시로 상품기본객체를 생성한다.
        PhoneProdBasDto temp = new PhoneProdBasDto();
        temp.setMspSaleSubsdMstListForLowPrice(filterList);
        temp.setOrderEnum(orderEnum);
        temp.doSort();

        PhoneProdBasDto temp2 = new PhoneProdBasDto();
        temp2.setMspSaleSubsdMstListForLowPrice(xmlList);
        temp2.setOrderEnum(orderEnum);
        temp2.doSort();

        Map<String, Object> rtnJson = new HashMap<String, Object>();
        rtnJson.put("chargeList", filterList);
        rtnJson.put("chargeXmlList", xmlList);
        rtnJson.put("phoneProdBas", phoneProdBas);
```

**처리 로직 / 변환 처리:**

```
o PhoneMobileController.java - phoneViewAjax
  - PhoneFrontController.java - phoneViewNe 과 로직 통합
  - 해당 없는 로직 제외 : 자급폰 관련
```

**개발팀 전달일:** 03/22 전달  
---

**URL:** /m/product/phone/getUsimChargeAjax.do  
**Controller:**

```
PhoneMobileController.java - getUsimChargeAjax
  - 유심 요금제 조회
```

**Service:** usimService.selectJoinUsimPriceNew  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /storeUsim/joinUsimPriceNew  
**처리 로직 / 변환 처리:**

```
o /storeUsim/joinUsimPriceNew
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**URL:** /m/product/phone/phoneViewDataAjax.do  
**Controller:** PhoneMobileController.java - phoneViewDataAjax  
**Service:**

```
phoneService.findPhoneProdBasDtoByProdId
  - 상품및 단품정보 조회 상품대표이미지,용량색상별 정보 등을 조회
```

**Service / Dao / SQL / API:**

```
phoneDao.findPhoneProd
-> [M포탈-M전산 API] /phone/nmcpProdBas
```

**처리 로직 / 변환 처리:**

```
o /phone/nmcpProdBas
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  

---

## [1003]

**JSP/JS:** /mobile/appForm/appOutForm.jsp, /resources/js/mobile/appForm/appOutForm.js  
**URL:** /m/request/marketRequest.do  
**Controller:**

```
AppformController.java - marketRequest
  - 파라미터
            , @RequestParam(value = "a", required = false, defaultValue = "") String pCntpntShopId          // 조직ID
            , @RequestParam(value = "o", required = false, defaultValue = "") String pOperType              // 가입유형
            , @RequestParam(value = "s", required = false, defaultValue = "") String pModelSalePolicyCode   // 판매정책id
            , @RequestParam(value = "p", required = false, defaultValue = "") String pReqModelName          // 제품ID
            , @RequestParam(value = "y", required = false, defaultValue = "0") String pEnggMnthCnt           // 약정기간
            , @RequestParam(value = "g", required = false, defaultValue = "") String pSprtTp                // 약정기간
            , @RequestParam(value = "n", required = false, defaultValue = "") String pModelMonthly          // 할부기간
            , @RequestParam(value = "u", required = false, defaultValue = "") String openMarketReferer      // 오픈마켓 입점지
            , @RequestParam(value = "q", required = false, defaultValue = "") String cstmrId          // 사용자 ID
            , @RequestParam(value = "r", required = false, defaultValue = "") String pSocCode          // 요금상품


  - 로직
        //쿠팡 접점일때.. cstmrId 필수 .. 처리
        if (Constants.CONTPNT_SHOP_ID_COUPANG.equals(pCntpntShopId) && "".equals(cstmrId)) {
            throw new McpCommonException(INVALID_PARAMATER_EXCEPTION, "/main.do");
        }

        String pOperTypeTem = pOperType;
        //pOperType이 3이 빠져있으면 3을 추가 20160926 kjw
        if (pOperType.length() == 3) {
            pOperTypeTem = pOperType + "3";
        } else {
            pOperTypeTem = pOperType;
        }

        String temp = "";
        boolean chechopenMarket = false;
        if (openMarketReferer != null && !openMarketReferer.equals("")) {
            //공백제거 시작
            temp = openMarketReferer.replaceAll("\\p{Z}", "");
            //공백제거 끝
            //파라미터 검증 시작
            chechopenMarket = !Pattern.matches("^[a-zA-Z0-9]*$", temp);
            if (temp.length() > 10) {
                chechopenMarket = true;
            }
            //파라미터 검증 끝
        }

        //오픈마켓 입점지 파라미터 검증
        if (chechopenMarket) {
            throw new McpCommonException(ExceptionMsgConstant.INVALID_REFERER_EXCEPTION, "/main.do");
        }

        //안면인증 세션 초기화
        SessionUtils.initializeFathSession();

        // ================= STEP START =================
        // step 초기화 및 메뉴명 세팅
        SessionUtils.removeCertSession();

        StringBuilder pageSb= new StringBuilder();

        if(OPER_TYPE_NEW.equals(pOperTypeTem)) pageSb.append("Nac");
        else if(OPER_TYPE_MOVE_NUM.equals(pOperTypeTem)) pageSb.append("Mnp");
        else if(OPER_TYPE_CHANGE.equals(pOperTypeTem)) pageSb.append("Hcn");
        else if(OPER_TYPE_EXCHANGE.equals(pOperTypeTem)) pageSb.append("Hdn");

        pageSb.append("AgentForm");

        // ** 대리점은 operType을 마지막까지 바꿀 수 있음 > 상세 페이지명 설정은 주문완료 단계에서 한번 더 진행
        SessionUtils.setPageSession(pageSb.toString());
        // ================= STEP END =================

        AppformReqDto appformReqDto = new AppformReqDto();
        appformReqDto.setCntpntShopId(pCntpntShopId);
        appformReqDto.setModelSalePolicyCode(pModelSalePolicyCode);
        appformReqDto.setOperType(pOperTypeTem);
        appformReqDto.setReqModelName(pReqModelName);
        appformReqDto.setSprtTp(pSprtTp);

        AppformReqDto rtnAppformReq = appformSvc.getMarketRequest(appformReqDto);
        if (rtnAppformReq == null) {
            throw new McpCommonException(ExceptionMsgConstant.NO_EXSIST_PROD_BAS_EXCEPTION, "/main.do");
        }

        if ("01".contentEquals(rtnAppformReq.getPlcySctnCd())) {
            rtnAppformReq.setReqBuyType(Constants.REQ_BUY_TYPE_PHONE);
        } else if ("02".contentEquals(rtnAppformReq.getPlcySctnCd())) {
            rtnAppformReq.setReqBuyType(Constants.REQ_BUY_TYPE_USIM);
        }
        rtnAppformReq.setOpenMarketReferer(openMarketReferer);
        rtnAppformReq.setSiteReferer(Constants.SITE_REFERER_MARKET);
        rtnAppformReq.setCstmrJejuId(cstmrId);
        rtnAppformReq.setSocCode(pSocCode);
        rtnAppformReq.setPrdtId(rtnAppformReq.getRprsPrdtId()); //대표 상품코드를 상품코드로 설정
        rtnAppformReq.setOnOffType("0");

        if (StringUtils.isBlank(pSocCode)) {
            rtnAppformReq.setDataType("LTE");
        } else {
            MspRateMstDto rateInfo = fCommonSvc.getMspRateMst(pSocCode);
            if (rateInfo != null && rateInfo.getDataType() != null) {
                rtnAppformReq.setDataType(rateInfo.getDataType());
            }
        }

        int intEnggMnthCnt = 0;
        try {
            intEnggMnthCnt = Integer.parseInt(pEnggMnthCnt);
        } catch (NumberFormatException e) {
            intEnggMnthCnt = 0;
        }

        rtnAppformReq.setEnggMnthCnt(intEnggMnthCnt);
        rtnAppformReq.setModelMonthly(pModelMonthly);  // 할부기간

        //인가된 사용자 세션 이름 화면고정 2024-12-05 박민건
        UserSessionDto userSession = SessionUtils.getUserCookieBean();
        if (userSession != null) {
            rtnAppformReq.setCstmrName(userSession.getName());
        }

        model.addAttribute("AppformReq", rtnAppformReq);

        //3.  할부개월
        /*
         * 01 :  단말 구매   할부 개월 리스트 표현
         * 02 :   USIM(유심)단독 구매:UU  24개월 고정
         */
        List<AppformReqDto> listModelMonthly = new ArrayList<AppformReqDto>();
        listModelMonthly = appformSvc.selectModelMonthlyList(rtnAppformReq);


        model.addAttribute("listModelMonthly", listModelMonthly);


        List<AppformReqDto> listEnggMnthCnt = appformSvc.selectMonthlyListMarket(rtnAppformReq);
        model.addAttribute("listEnggMnthCnt", listEnggMnthCnt);

        //5.  색상
        List<AppformReqDto> listColorInfo = appformSvc.selectPrdtColorList(rtnAppformReq);
        model.addAttribute("listColorInfo", listColorInfo);

        // 대리점 명 확인 필요...
        Map<String,String> agentInfoOjb = appformSvc.getAgentInfoOjb(pCntpntShopId);

        if (agentInfoOjb != null) {
            String orgnNM = StringUtil.NVL(agentInfoOjb.get("ORGN_NM"), "");
            String roadnAdrZipcd = StringUtil.NVL(agentInfoOjb.get("ROADN_ADR_ZIPCD"), "");
            String roadnAdrBasSbst = StringUtil.NVL(agentInfoOjb.get("ROADN_ADR_BAS_SBST"), "");
            String roadnAdrDtlSbst = StringUtil.NVL(agentInfoOjb.get("ROADN_ADR_DTL_SBST"), "");

            /*logger.info("orgnNM==>"+orgnNM);
            logger.info("roadnAdrZipcd==>"+roadnAdrZipcd);
            logger.info("roadnAdrBasSbst==>"+roadnAdrBasSbst);
            logger.info("roadnAdrDtlSbst==>"+roadnAdrDtlSbst);*/

            model.addAttribute("agentNM", orgnNM);
            model.addAttribute("agentAddr", roadnAdrZipcd + " " + roadnAdrBasSbst + " " + roadnAdrDtlSbst  );

        } else {
            model.addAttribute("agentNM", "");
            model.addAttribute("agentAddr", "");
        }

        String agentFormLink = request.getRequestURL().append("?").append(request.getQueryString()).toString();
        SessionUtils.saveAgentFormLink(agentFormLink);

        String returnUrl = "/appForm/appOutForm";


        if ("Y".equals(NmcpServiceUtils.isMobile())) {
            //String operTpye = rtnAppformReq.getOperType();
            /*
             * if (OPER_TYPE_CHANGE.equals(operTpye) || OPER_TYPE_EXCHANGE.equals(operTpye))
             * { returnUrl = "/appForm/appOutChangeForm"; }
             */
            rtnAppformReq.setOnOffType("3");
            returnUrl = "/mobile".concat(returnUrl);
        } else {
            rtnAppformReq.setOnOffType("0");
            returnUrl = "/portal".concat(returnUrl);
        }
```

**처리 로직 / 변환 처리:**

```
o /mobile/appForm/appOutForm.jsp, /resources/js/mobile/appForm/appOutForm.js
  - 유효성 체크 참고

-----------------------------------------

o AppformController.java - marketRequest
  - 로직 적용
  - 해당 없는 로직 제외 : 자급폰 관련
```

**개발팀 전달일:** 03/22 전달  

---

## [1003-01]

**JSP/JS:** /mobile/appForm/appOutForm.jsp, /resources/js/mobile/appForm/appOutForm.js  
**URL:** /m/request/marketRequest.do  
**Controller:** AppformController.java - marketRequest  
**Service:** appformSvc.getMarketRequest  
**Service / Dao / SQL / API:**

```
appformDao.getMarketRequest
-> [M포탈-M전산 API] /appform/marketRequest
```

**처리 로직 / 변환 처리:**

```
o /appform/marketRequest
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** fCommonSvc.getMspRateMst  
**Service / Dao / SQL / API:**

```
fCommonDao.getMspRateMst
-> [M포탈-M전산 API] /common/mspRateMst
```

**처리 로직 / 변환 처리:**

```
o /common/mspRateMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
appformSvc.selectModelMonthlyList
  - 할부개월 조회
```

**Service / Dao / SQL / API:**

```
appformDao.selectModelMonthlyList
-> [M포탈-M전산 API] /appform/modelMonthlyList
```

**처리 로직 / 변환 처리:**

```
o /appform/modelMonthlyList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** appformSvc.selectMonthlyListMarket  
**Service / Dao / SQL / API:**

```
appformDao.selectMonthlyListMarket
-> [M포탈-M전산 API] /appform/monthlyListMarket
```

**처리 로직 / 변환 처리:**

```
o /appform/monthlyListMarket
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
appformSvc.selectPrdtColorList
  - 색상 조회
```

**Service / Dao / SQL / API:**

```
appformDao.selectPrdtColorList
-> [M포탈-M전산 API] /appform/prdtColorList
```

**처리 로직 / 변환 처리:**

```
o /appform/prdtColorList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
appformSvc.getAgentInfoOjb
  - 대리점명 조회
```

**Service / Dao / SQL / API:**

```
appformDao.getAgentInfoOjb
-> [M포탈-M전산 API] /appform/agentInfoOjb
```

**처리 로직 / 변환 처리:**

```
o /appform/agentInfoOjb
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용

ㅇ ITO AS-IS 소스에 로직 추가
 - 로그인 사용자 계정의 판매점에 해당하는 대리점 목록 조회 -> 화면에서 선택
```

**개발팀 전달일:** 03/22 전달  

---

## [1003-02]

**JSP/JS:** /mobile/appForm/appOutForm.jsp, /resources/js/mobile/appForm/appOutForm.js  
**URL:** /appform/selRMemberAjax.do  
**Controller:**

```
AppformController.java - selRMemberAjax

        HashMap<String, Object> rtnMap = new HashMap<String, Object>();

        String rrnShort = juoSubInfoDto.getCustomerSsn();
        if (StringUtils.isBlank(rrnShort)) {
            rtnMap.put("RESULT_CODE", "-1");
            rtnMap.put("RESULT_DESC", "비정상적인 접근입니다.");
            return rtnMap;
        }

        JuoSubInfoDto juoSubInfoRtn = appformSvc.selRMemberAjax(juoSubInfoDto);
        if(juoSubInfoRtn != null) {
            String customerSsn = juoSubInfoRtn.getCustomerSsn();
            String lglAgntSsn = juoSubInfoRtn.getLglAgntSsn();
            try {
                customerSsn = EncryptUtil.ace256Dec(customerSsn);
                lglAgntSsn = EncryptUtil.ace256Dec(lglAgntSsn);
            } catch (CryptoException e) {
                throw new McpCommonJsonException("0001", ACE_256_DECRYPT_EXCEPTION);
            }

            // 입력한 생년월일과 DB주민번호 값 비교
            if(!rrnShort.equals(customerSsn.substring(0,7))){
                rtnMap.put("RESULT_CODE", "0002");
                rtnMap.put("RESULT_DESC", "고객 정보가 일치하지 않습니다. <br> 확인 후 다시 입력 해 주세요 ");
                return rtnMap;
            }
            // 2024-12-17 인가된 사용자 체크
            Map<String, String> rtnChkAuthMap = mypageService.checkAuthUser(juoSubInfoDto.getCustomerLinkName(), customerSsn);
            if (!"0000".equals(rtnChkAuthMap.get("returnCode"))) {
                rtnMap.put("RESULT_CODE", rtnChkAuthMap.get("returnCode"));
                rtnMap.put("RESULT_MSG", rtnChkAuthMap.get("returnMsg"));
                return rtnMap;
            }
            juoSubInfoRtn.setCustomerSsn(customerSsn);
            juoSubInfoRtn.setLglAgntSsn(lglAgntSsn);
            if ("Y".equals(juoSubInfoRtn.getDvcChgYn())) {
                String ncType= ""; // 청소년 구분값

                rtnMap.put("CSTMR_TYPE", Constants.CSTMR_TYPE_NA);
                juoSubInfoRtn.setCustomerType(Constants.CSTMR_TYPE_NA);
                //외국인 청소년 구분
                if (customerSsn.length() > 6) {
                    String diviVal = customerSsn.substring(6, 7);
                    if ("|5|6|7|8".indexOf(diviVal) > -1) {
                        rtnMap.put("CSTMR_TYPE", Constants.CSTMR_TYPE_FN);
                        juoSubInfoRtn.setCustomerType(Constants.CSTMR_TYPE_FN);
                    } else {
                        //나이 확인
                        int age = NmcpServiceUtils.getAge(customerSsn, new SimpleDateFormat("yyyyMMdd", Locale.KOREA).format(new Date()));
                        if (19 > age) {
                            ncType= "0";
                            rtnMap.put("CSTMR_TYPE", Constants.CSTMR_TYPE_NM);
                            juoSubInfoRtn.setCustomerType(Constants.CSTMR_TYPE_NM);
                        }
                    }
                }

                // ============ STEP START ============
                String birthDate= (customerSsn.length() < 7) ? customerSsn : customerSsn.substring(0,6);

                // 대리인구분값, 이름, 생년월일, 기기변경 휴대폰번호, 계약번호
                String[] certKey= {"urlType", "ncType", "name", "birthDate"
                        , "mobileNo", "contractNum"};
                String[] certValue= {"changeAuth", ncType, juoSubInfoDto.getCustomerLinkName(), birthDate
                        , juoSubInfoDto.getSubscriberNo(), juoSubInfoRtn.getContractNum()};
                certService.vdlCertInfo("C", certKey, certValue);
                // ============ STEP END ============

                //session저장 처리
                SessionUtils.saveChangeAut(juoSubInfoRtn);

                rtnMap.put("RESULT_CODE", AJAX_SUCCESS);
            } else if ("E".equals(juoSubInfoRtn.getDvcChgYn())) {
                rtnMap.put("RESULT_CODE", "0004");
                rtnMap.put("RESULT_DESC", "약정기간이 6개월 이상 남아있어 온라인으로 기기변경 신청이 불가합니다.<br>기기변경을 원하실 경우 고객센터(1899-5000)로 신청을 부탁드립니다. <br>확인버튼을 누르실 경우 휴대폰 화면으로 이동합니다.");
            } else {
                rtnMap.put("RESULT_CODE", "0003");
                rtnMap.put("RESULT_DESC", "고객님께서는 기기변경 할 회선이 없습니다. <br>감사합니다.");
            }
        } else {
            rtnMap.put("RESULT_CODE", "0001");
            rtnMap.put("RESULT_DESC", "입력하신 정보는 kt M모바일에 가입된 정보가 아니거나 사용중인 상태가 아닙니다 <br> 확인 후 다시 입력 해 주세요.");
        }
        return rtnMap;
```

**처리 로직 / 변환 처리:**

```
o AppformController.java - selRMemberAjax
  - 가입자에 대한 로직을 변경하여 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** appformSvc.selRMemberAjax  
**Service / Dao / SQL / API:**

```
appformDao.selRMemberAjax
-> [M포탈-M전산 API] /appform/selRMemberAjax
```

**처리 로직 / 변환 처리:**

```
o /appform/selRMemberAjax
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
mypageService.checkAuthUser

        Map<String, String> rtnMap = new HashMap<>();
        rtnMap.put("returnCode", "0000");

        UserSessionDto session = SessionUtils.getUserCookieBean();
        if (session != null) {
            // 공백제거
            String paramName = StringUtil.NVL(name, "").replaceAll(" ", ""); // 입력한 이름
            String sessionName = session.getName().replaceAll(" ", ""); // 세션 이름

            // url 찾기
            HttpServletRequest request = ((ServletRequestAttributes) RequestContextHolder.currentRequestAttributes()).getRequest();
            String referer = StringUtil.NVL(request.getHeader("REFERER"), "");
            // 유심 구매 - 이름만 체크
            if (referer.indexOf("/reqSelfDlvry.do") > -1 || referer.indexOf("/replaceUsimView.do") > -1) {
                if (StringUtils.isBlank(paramName)) {
                    rtnMap.put("returnCode", "0010");
                    rtnMap.put("returnMsg", "이름이 입력되지 않았습니다.<br/>새로고침 후 다시 시도해주세요.");
                } else {
                    if (!paramName.equalsIgnoreCase(sessionName)) {
                        rtnMap.put("returnCode", "0010");
                        rtnMap.put("returnMsg", "회원정보와 본인인증 정보가 일치하지 않습니다.<br/>다른 명의로 신청을 원하실 경우 로그아웃 후 이용 해 주시기 바랍니다.");
                    }
                }
                return rtnMap;
            }

            // 그 외 이름, 주민번호 필수
            if (StringUtils.isBlank(paramName) || StringUtils.isBlank(userSsn)) {
                rtnMap.put("returnCode", "0010");
                rtnMap.put("returnMsg", "이름, 주민번호가 입력되지 않았습니다.<br/>새로고침 후 다시 시도해주세요.");
            } else {
                if ("00".equals(session.getUserDivision())) {
                    // 준회원
                    // 이름 체크
                    if (!paramName.equalsIgnoreCase(sessionName)) {
                        rtnMap.put("returnCode", "0010");
                        rtnMap.put("returnMsg", "회원정보와 본인인증 정보가 일치하지 않습니다.<br/>다른 명의로 신청을 원하실 경우 로그아웃 후 이용 해 주시기 바랍니다.");
                        return rtnMap;
                    }
                    NiceLogDto niceLogDto = new NiceLogDto();
                    niceLogDto.setName(name);
                    niceLogDto.setBirthDate(userSsn);
                    // nicepin di 가져오기
                    Map<String, String> nicePinRtn = nicePinService.getNicePinDi(niceLogDto);
                    if ("0000".equals(nicePinRtn.get("returnCode"))) {
                        // nicepin 연동 성공 - DI 체크
                        if (!session.getPin().equals(nicePinRtn.get("dupInfo"))) {
                            rtnMap.put("returnCode", "0010");
                            rtnMap.put("returnMsg", "회원정보와 본인인증 정보가 일치하지 않습니다.<br/>다른 명의로 신청을 원하실 경우 로그아웃 후 이용 해 주시기 바랍니다.");
                        }
                    } else {
                        // nicepin 연동 실패
                        rtnMap.put("returnCode", "9999");
                        rtnMap.put("returnMsg", nicePinRtn.get("returnMsg"));
                    }
                } else if ("01".equals(session.getUserDivision())) {
                    // 정회원
                    List<McpUserCntrMngDto> cntrList = this.selectCntrList(session.getUserId());
                    if (cntrList != null && cntrList.size() > 0) {
                        McpUserCntrMngDto cntrMngDto = cntrList.get(0);
                        // 이름, 주민번호 체크
                        String cntrName = cntrMngDto.getUserName().replaceAll(" ", "");
                        if (!paramName.equalsIgnoreCase(cntrName) || !userSsn.equals(cntrMngDto.getUnUserSSn())) {
                            rtnMap.put("returnCode", "0010");
                            rtnMap.put("returnMsg", "회원정보와 본인인증 정보가 일치하지 않습니다.<br/>다른 명의로 신청을 원하실 경우 로그아웃 후 이용 해 주시기 바랍니다.");
                        }
                    } else {
                        rtnMap.put("returnCode", "0010");
                        rtnMap.put("returnMsg", "회원정보가 확인되지 않습니다.<br/>로그아웃 후 이용 해 주시기 바랍니다.");
                    }
                } else {
                    rtnMap.put("returnCode", "0010");
                    rtnMap.put("returnMsg", "회원정보가 확인되지 않습니다.<br/>로그아웃 후 이용 해 주시기 바랍니다.");
                }
            }
        }
        return rtnMap;
```

**처리 로직 / 변환 처리:**

```
o mypageService.checkAuthUser
  - 본인인증 로직을 신분증 인증 로직으로 변경하여 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
nicePinService.getNicePinDi

        Map<String,String> resultMap = new HashMap<>();

        String url = "";
        if ("CI".equals(type)) {
            url = "/getNicePinCi.do";
        } else if ("DI".equals(type)) {
            url = "/getNicePinDi.do";
        }

        CommonHttpClient client = new CommonHttpClient(extUrl + url);
        String responseBody= null;

        // 1. prx 연동 데이터 세팅
        NameValuePair[] data = {
                new NameValuePair("sSiteCode", ciPrxSiteCode),  // NICE신용평가정보에서 발급한 사이트코드
                new NameValuePair("sSitePw", ciPrxSitePw),      // NICE신용평가정보에서 발급한 사이트패스워드
                new NameValuePair("sJumin", EncryptUtil.ace256Enc(niceLogDto.getBirthDate())), // 사용자 주민번호
                new NameValuePair("sFlag", "JID")
        };

        try {
            // 2. 연동결과 확인
            responseBody = client.postUtf8(data);

            if(StringUtil.isEmpty(responseBody)){
                throw new SocketTimeoutException();
            }

            JsonParser jsonParser = new JsonParser();
            JsonObject jsonObject = (JsonObject) jsonParser.parse(responseBody);

            // 3. returnCode 확인
            String returnCode= jsonObject.get("returnCode").getAsString();

            if("0000".equals(returnCode)) {
                // 조회 성공
                String connInfo = "";
                String dupInfo = "";
                if ("CI".equals(type)) {
                    connInfo = jsonObject.get("connInfo").getAsString();
                    connInfo = EncryptUtil.ace256Dec(connInfo);
                } else if ("DI".equals(type)) {
                    dupInfo = jsonObject.get("dupInfo").getAsString();
                    dupInfo = EncryptUtil.ace256Dec(dupInfo);
                }

                //MCP_NICE_HIST 이력 등록
                NiceLogDto paramDto = new NiceLogDto();
                paramDto.setName(niceLogDto.getName());
                paramDto.setBirthDate(niceLogDto.getBirthDate().substring(0, 6));
                paramDto.setConnInfo(connInfo);
                paramDto.setDupInfo(dupInfo);
                paramDto.setAuthType("NP");
                nicelog.insertMcpNiceHist(paramDto);

                resultMap.put("returnCode", returnCode);
                resultMap.put("connInfo", connInfo);
                resultMap.put("dupInfo", dupInfo);
            }else{
                // 조회 실패
                resultMap.put("returnCode", returnCode);
                resultMap.put("returnMsg", "시스템 장애로 인하여 통신이 원활하지 않습니다.");
            }

        }catch(SocketTimeoutException e) {
            resultMap.put("returnCode", "9999");
            resultMap.put("returnMsg", "시스템 장애로 인하여 통신이 원활하지 않습니다.");
        } catch (Exception e) {
            resultMap.put("returnCode", "9998");
            resultMap.put("returnMsg", "시스템 장애로 인하여 통신이 원활하지 않습니다.");
        }

        return resultMap;
```

**처리 로직 / 변환 처리:**

```
o nicePinService.getNicePinDi
  - 본인인증 로직을 신분증 인증 로직으로 변경하여 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**URL:** /nice/getTimeInMillisAjax.do  
**Controller:** NiceCertifyController.java - getTimeInMillis  
**처리 로직 / 변환 처리:**

```
o NiceCertifyController.java - getTimeInMillis
  - 불필요로 제외
```

**개발팀 전달일:** 03/22 전달  
---

**URL:** /getCodeNmAjax.do  
**Controller:** DbCacheController.java - getCodeNm  
**Service / Dao / SQL / API:**

```
NmcpServiceUtils.getCodeNmObj

        List<NmcpCdDtlDto> codeList = null;
        DbCacheHandler dbCacheHandler = getBean(DbCacheHandler.class);

        @SuppressWarnings("unchecked")
        Map<String, List<NmcpCdDtlDto>> codeMap = (Map<String, List<NmcpCdDtlDto>>) dbCacheHandler.getValue(CACHE_CODE);
        if (codeMap != null) {
            codeList = codeMap.get(nmcpCdDtlDto.getCdGroupId());
            if (codeList != null) {
                for (NmcpCdDtlDto bean : codeList) {
                    if (bean.getDtlCd().equals(nmcpCdDtlDto.getDtlCd())) {
                        return bean;
                    }
                }
            }
        }
        return null;
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmObj
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/22 전달  

---

## [1003-03]

**JSP/JS:** /mobile/appForm/appOutForm.jsp, /resources/js/mobile/appForm/appOutForm.js  
**URL:** /getJehuInfoAjax.do  
**Controller:** FCommonController.java - getJehuInfo  
**Service:** fCommonSvc.getJehuProdInfo  
**처리 로직 / 변환 처리:**

```
o fCommonSvc.getJehuProdInfo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
mspService.getMspRateMst
-> mspDao.getMspRateMst
-> [M포탈-M전산 API] /msp/mspRateMst
```

**처리 로직 / 변환 처리:**

```
o /msp/mspRateMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
NmcpServiceUtils.getCodeList("JehuProdType")

        List<NmcpCdDtlDto> codeList = null;
        List<NmcpCdDtlDto> codeRstList = null;
        DbCacheHandler dbCacheHandler = getBean(DbCacheHandler.class);

        @SuppressWarnings("unchecked")
        Map<String, List<NmcpCdDtlDto>> codeMap = (Map<String, List<NmcpCdDtlDto>>) dbCacheHandler.getValue(CACHE_CODE);
        if (codeMap != null) {
            codeList = codeMap.get(grpCd);
            codeRstList = new ArrayList<NmcpCdDtlDto>();

            if(codeList !=null && !codeList.isEmpty()) {
                for(NmcpCdDtlDto bean : codeList) {
                    if(DateTimeUtil.checkValidDate(bean.getPstngStartDate(), bean.getPstngEndDate())) {
                        codeRstList.add(bean);
                    }
                }
            }
        }
        return codeRstList;
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/22 전달  
---

**Service:** fCommonSvc.getJehuPartnerInfo  
**Service / Dao / SQL / API:**

```
NmcpServiceUtils.getCodeList("PartnerTerm")

        List<NmcpCdDtlDto> codeList = null;
        List<NmcpCdDtlDto> codeRstList = null;
        DbCacheHandler dbCacheHandler = getBean(DbCacheHandler.class);

        @SuppressWarnings("unchecked")
        Map<String, List<NmcpCdDtlDto>> codeMap = (Map<String, List<NmcpCdDtlDto>>) dbCacheHandler.getValue(CACHE_CODE);
        if (codeMap != null) {
            codeList = codeMap.get(grpCd);
            codeRstList = new ArrayList<NmcpCdDtlDto>();

            if(codeList !=null && !codeList.isEmpty()) {
                for(NmcpCdDtlDto bean : codeList) {
                    if(DateTimeUtil.checkValidDate(bean.getPstngStartDate(), bean.getPstngEndDate())) {
                        codeRstList.add(bean);
                    }
                }
            }
        }
        return codeRstList;
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/22 전달  

---

## [1003-05]

**JSP/JS:** /mobile/appForm/appOutForm.jsp, /resources/js/mobile/appForm/appOutForm.js  
**URL:** /msp/moscIntmMgmtAjax.do  
**Controller:**

```
AppformController.java - moscIntmMgmt
  - USIM 유효성 체크(X85)

        HashMap<String, Object> rtnMap = new HashMap<String, Object>();

        if (StringUtils.isBlank(juoSubInfo.getIccId())) {
            rtnMap.put("RESULT_CODE", "9998");
            rtnMap.put("RESULT_MSG", ExceptionMsgConstant.F_BIND_EXCEPTION);
            return rtnMap;
        }

        /**
         * 불량유심 사용 제한
         */
        int failUsimCnt = this.usimService.selectFailUsims(juoSubInfo.getIccId());
        if (failUsimCnt > 0) {
            UserSessionDto sessionDto = SessionUtils.getUserCookieBean();

            if(!ObjectUtils.isEmpty(sessionDto) &&  StringUtils.isNotEmpty(sessionDto.getUserId())){
                juoSubInfo.setCustomerId(sessionDto.getUserId());
            }else{

                juoSubInfo.setCustomerId("");
                String reqSeq= request.getParameter("reqSeq");
                String resSeq= request.getParameter("resSeq");

                if(!StringUtil.isEmpty(reqSeq) && !StringUtil.isEmpty(resSeq)){
                    NiceLogDto niceLogDto= new NiceLogDto();
                    niceLogDto.setReqSeq(reqSeq);
                    niceLogDto.setResSeq(resSeq);
                    niceLogDto.setLimitMinute(90);  // 90분 이내의 이력 조회

                    NiceLogDto smsNiceLogDto = nicelog.getMcpNiceHistWithTime(niceLogDto);
                    if(smsNiceLogDto != null && !StringUtil.isEmpty(smsNiceLogDto.getsMobileNo())){
                        juoSubInfo.setCustomerId(smsNiceLogDto.getsMobileNo());
                    }
                }
            }

            this.usimService.updateFailUsims(juoSubInfo);
            rtnMap.put("RESULT_CODE", "0004");
            rtnMap.put("RESULT_MSG", "해당 유심은 사용이 불가합니다.<br>고객센터로 연락 바랍니다.");
            return rtnMap;
        }


        /**
         * 명의도용 추가피해 방지를 위한 유심재사용
         * 해당 쿼리 조회 후 데이터가 있으면 사용불가 유심입니다.
         * 개통이 불가능한 유심입니다.
         */
        int checkValidUsimCount = 0;
        RestTemplate restTemplate = new RestTemplate();
        checkValidUsimCount = restTemplate.postForObject(apiInterfaceServer + "/appform/checkValidUsimNo", juoSubInfo.getIccId(), Integer.class);

        if (checkValidUsimCount > 0) {
            rtnMap.put("RESULT_CODE", "0003");
            rtnMap.put("RESULT_MSG", "개통이 불가능한 유심입니다.");
            return rtnMap;
        }


        try {
            MoscInqrUsimUsePsblOutDTO moscInqrUsimUsePsblOutDTO = mPlatFormService.moscIntmMgmtSO(juoSubInfo);
            if (moscInqrUsimUsePsblOutDTO != null) {
                if ("Y".equals(moscInqrUsimUsePsblOutDTO.getPsblYn())) {
                    rtnMap.put("RESULT_CODE", AJAX_SUCCESS);
                    rtnMap.put("RESULT_MSG", "사용가능");
                    //USIM 접점코드(ORGN_ID) 조회
                    String orgnId = appformSvc.getUsimOrgnId(juoSubInfo.getIccId());
                    rtnMap.put("USIM_ORGN_ID", orgnId);

                    // ============ STEP START ============
                    Map<String, String> resultMap = certService.isAuthStepApplyUrl(request);
                    if("Y".equals(resultMap.get("isAuthStep"))) {

                        String ncType= "";
                        if(request.getParameter("ncType") != null) ncType= request.getParameter("ncType");

                        // 유심번호
                        String[] certKey= {"urlType", "ncType", "reqUsimSn"};
                        String[] certValue= {"chkUsim", ncType, juoSubInfo.getIccId()};
                        certService.vdlCertInfo("C", certKey, certValue);
                    }
                    // ============ STEP END ============

                } else {
                    rtnMap.put("RESULT_CODE", "0001");
                    rtnMap.put("RESULT_MSG", moscInqrUsimUsePsblOutDTO.getRsltMsg()); //rsltMsg
                }
            } else {
                rtnMap.put("RESULT_CODE", "0002");
                rtnMap.put("RESULT_MSG", ExceptionMsgConstant.MPLATFORM_RESPONEXML_EMPTY_EXCEPTION); //rsltMsg
            }
        } catch (SocketTimeoutException e) {
            rtnMap.put("RESULT_CODE", "9999");
            rtnMap.put("RESULT_MSG", ExceptionMsgConstant.SOCKET_TIMEOUT_EXCEPTION);
        }

        return rtnMap;
```

**처리 로직 / 변환 처리:**

```
o AppformController.java - moscIntmMgmt
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
usimService.selectFailUsims
  - 불량유심 사용 제한
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /storeUsim/failUsim  
**처리 로직 / 변환 처리:**

```
o /storeUsim/failUsim
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** nicelog.getMcpNiceHistWithTime  
**Service / Dao / SQL / API:**

```
niceLogDao.getMcpNiceHistWithTime
-> [SQL] NiceLogMapper.getMcpNiceHistWithTime

        SELECT
               NICE_HIST_SEQ            --PK
             , REQ_SEQ                  --일련번호 기본키
             , RES_SEQ                  --요청 일련번호
             , AUTH_TYPE                 --요청 구분
             , NAME                     --인증성명
             , BIRTH_DATE               --인증생년월일
             , GENDER                   --GENDER
             , NATIONAL_INFO            --NATIONAL_INFO
             , DUP_INFO                 --DUP_INFO
             , CONN_INFO                --CONN_INFO
             , PARAM_R1                 --파라메터 1
             , PARAM_R2                 --파라메터 2
             , PARAM_R3                 --파라메터 3
             , END_YN                   --종료여부
             , N_REFERER                --referer
             , RIP                      --등록아이피
             , SYS_RDATE_DT             --등록일
             , SYS_RDATE                --등록일시
             , RVISN_DTTM               --수정일
             , COMPL_DTTM               --인증 완료시
             , MOBILE_CO  as sMobileCo              -- 이동사구분(모바일 인증)
             , MOBILE_NO  as sMobileNo              -- 휴대폰 번호(모바일 인증)
        FROM MCP_NICE_HIST
        WHERE 1=1
          AND SYS_RDATE_DT BETWEEN TO_CHAR(SYSDATE-1, 'YYYYMMDD') AND TO_CHAR(SYSDATE, 'YYYYMMDD')
          AND REQ_SEQ = #{reqSeq}
          AND RES_SEQ = #{resSeq}
          AND SYS_RDATE <![CDATA[>=]]> (SYSDATE-#{limitMinute}/24/60)
          AND ROWNUM = 1
```

**테이블/API:** MCP_NICE_HIST  
**처리 로직 / 변환 처리:**

```
o NiceLogMapper.getMcpNiceHistWithTime
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service:** usimService.updateFailUsims  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /storeUsim/updateFailUsim  
**처리 로직 / 변환 처리:**

```
o /storeUsim/updateFailUsim
-> 현행 ITO API 그대로 사용 : M전산/M포탈 테이블 INSERT/UPDATE/DELETE
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
[M포탈-M전산 API] /appform/checkValidUsimNo
  - 명의도용 추가피해 방지를 위한 유심재사용
```

**처리 로직 / 변환 처리:**

```
o /appform/checkValidUsimNo
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** mPlatFormService.moscIntmMgmtSO  
**Service / Dao / SQL / API:**

```
mplatFormServerAdapter.callService
-> [M플랫폼] X85
```

**테이블/API:** X85  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.moscIntmMgmtSO
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** appformSvc.getUsimOrgnId  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /msp/sellUsimMgmtOrgnId  
**처리 로직 / 변환 처리:**

```
o /msp/sellUsimMgmtOrgnId
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
certService.isAuthStepApplyUrl
  - 인증 개선 대상 url인지 검사

        Map<String, String> resultMap = new HashMap<>();
        resultMap.put("isAuthStep", "N");

        String redirectPage = "/main.do";
        if("Y".equals(NmcpServiceUtils.isMobile())) redirectPage = "/m/main.do";

        String requestUrl = (request.getHeader("referer") == null) ? "" : request.getHeader("referer");
        if("".equals(requestUrl)) throw new McpCommonException(COMMON_EXCEPTION, redirectPage);

        String uri = requestUrl.substring(request.getHeader("referer").indexOf("/", 8));
        int endIndex = uri.indexOf('?');
        if(endIndex > -1) uri = uri.substring(0, endIndex);

        // 인증 개선 대상 URL
        String[] appformUrlArr = {"appForm/appForm.do", "m/appForm/appForm.do"                  // 핸드폰, 상담사, 셀프개통, ESIM, ESIM WATCH
                                 ,"request/marketRequest.do", "m/request/marketRequest.do"      // 대리점개통
                                 ,"content/reqSharingView.do", "m/content/reqSharingView.do", "content/dataSharingStep3.do", "m/content/dataSharingStep3.do"   // 데이터쉐어링
                                 ,"mypage/myNameChg.do", "m/mypage/myNameChg.do"    // 명의변경 신청
                                 ,"cs/ownPhoNum.do", "m/cs/ownPhoNum.do"       // 가입번호 조회
                                 ,"m/appform/setUsimNoDlvey.do"          // 유심번호 등록 인증
                                 ,"mypage/paywayWiew.do", "m/mypage/paywayWiew.do"     // 납부방법변경
                                 ,"mypage/paywayView.do", "m/mypage/paywayView.do"     // 납부방법변경
                                 ,"mypage/usimSelfChg.do", "m/mypage/usimSelfChg.do"            // 유심 셀프변경
                                 ,"mypage/reqRwd.do", "m/mypage/reqRwd.do"                      // 자급제 보상서비스
                                 ,"appForm/appAgentForm.do", "m/appForm/appAgentForm.do"        // 대리점 셀프개통
                                 ,"appForm/appJehuForm.do", "m/appForm/appJehuForm.do"          // 제휴위탁온라인서식지
                                 ,"m/apply/replaceUsimSelf.do"
        };

        List<String> appformurlList = Arrays.asList(appformUrlArr);

        if(appformurlList.contains(uri.replaceAll("^/+",""))) resultMap.put("isAuthStep","Y");

        return resultMap;
```

**처리 로직 / 변환 처리:**

```
o certService.isAuthStepApplyUrl
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
ipstatisticService.getClientIp

    public String getClientIp()  {
        String clientIp = "";
        HttpServletRequest request = ((ServletRequestAttributes) RequestContextHolder.currentRequestAttributes()).getRequest();

        if(request.getHeader("X-Forwarded-For") == null) {
            clientIp = request.getRemoteAddr();
        } else {
            clientIp = request.getHeader("X-Forwarded-For");
            if (clientIp !=null && ! clientIp.equals("") && clientIp.indexOf(",")>-1) {
                clientIp =  clientIp.split("\\,")[0].trim();
            }
        }

        return clientIp;
    }
```

**처리 로직 / 변환 처리:**

```
o ipstatisticService.getClientIp
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
certDao.updateNicePin
-> [SQL] CertMapper.updateNicePin

  UPDATE NMCP_CRT_VLD_DTL SET /*CertMapper.updateNicePin*/
      NAME = #{name},
      BIRTH_DATE = #{birthDate},
      CONN_INFO = #{connInfo},
      NC_TYPE = #{ncType},
      RVISN_ID = #{rvisnId},
      RVISN_DTTM = SYSDATE
  WHERE 1 = 1
    AND CRT_SEQ = #{crtSeq}
    AND COMP_TYPE = 'E'
   AND MODU_TYPE = 'nicePin'
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o certDao.updateNicePin
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
certDao.updateMobileNo
-> [SQL] CertMapper.updateMobileNo

  UPDATE NMCP_CRT_VLD_DTL
  SET MOBILE_NO = #{mobileNo},
    RVISN_ID = #{rvisnId},
    RVISN_DTTM = SYSDATE
  WHERE CRT_SEQ = #{crtSeq}
   AND COMP_TYPE = 'E'
   AND MOBILE_NO IS NOT NULL
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o certDao.updateMobileNo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
certDao.updateEsimSeq
-> [SQL] CertMapper.updateEsimSeq

  UPDATE NMCP_CRT_VLD_DTL SET /*CertMapper.updateEsimSeq*/
      UPLOAD_PHONE_SRL_NO = #{uploadPhoneSrlNo},
      RVISN_ID = #{rvisnId},
      RVISN_DTTM = SYSDATE
  WHERE 1 = 1
      AND CRT_SEQ = #{crtSeq} 
      AND UPLOAD_PHONE_SRL_NO IS NOT NULL
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o certDao.updateEsimSeq
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
certDao.deleteCert
-> [SQL] CertMapper.deleteCert

        DELETE NMCP_CRT_VLD_DTL  /*CertMapper.deleteCert*/
        WHERE 1 = 1
         AND CRT_SEQ = #{crtSeq}
         AND MODU_TYPE = #{moduType}
     <if test= 'ncType != null and ncType != ""'>
     AND NC_TYPE = #{ncType}
     </if>
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o certDao.deleteCert
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
certDao.insertCert
-> [SQL] CertMapper.insertCert

       <selectKey keyProperty="rtnCrtSeq" resultType="java.lang.Long" order="BEFORE">
         <choose>
                 <when test="crtSeq != 0 and crtSeq != -1">
                    SELECT #{crtSeq} FROM DUAL
                 </when>
                 <otherwise>
                  SELECT SQ_CRT.NEXTVAL FROM DUAL
                 </otherwise>
            </choose>
        </selectKey>
        INSERT INTO NMCP_CRT_VLD_DTL ( /*CertMapper.insertCert*/
         CRT_SEQ,
     STEP,
     SUB_STEP,
     REGST_DT,
     AUTH_TYPE,
     MODU_TYPE,
     COMP_TYPE,
     STEP_END_YN,
     VERI_RSLT,
     REFERER,
     NAME,
     BIRTH_DATE,
     MOBILE_NO,
     CONN_INFO,
     REQUEST_KEY,
     CONTRACT_NUM,
     REQ_USIM_SN,
     REQ_BANK,
     REQ_ACCOUNT_NUMBER,
     UPLOAD_PHONE_SRL_NO,
     REQ_SEQ,
     RES_SEQ,
     NC_TYPE,
     DUP_INFO,
     CRT_ETC,
     RIP,
     REGST_ID,
     REGST_DTTM,
     URL_TYPE,
     REQ_CARD_COMPANY,
     REQ_CARD_NO,
     CRDT_CARD_TERM_DAY
        ) VALUES (
            #{rtnCrtSeq}, 
            (SELECT CASE WHEN EXISTS (SELECT 1 FROM NMCP_CRT_VLD_DTL WHERE CRT_SEQ = #{rtnCrtSeq} AND URL_TYPE = #{urlType}) THEN
      (SELECT MAX(STEP) FROM NMCP_CRT_VLD_DTL WHERE CRT_SEQ = #{rtnCrtSeq} AND URL_TYPE = #{urlType})
      ELSE (SELECT NVL(MAX(STEP), 0) + 1  FROM NMCP_CRT_VLD_DTL WHERE CRT_SEQ = #{rtnCrtSeq}) END FROM DUAL),
            (SELECT NVL(MAX(SUB_STEP), 0) + 1 FROM NMCP_CRT_VLD_DTL WHERE CRT_SEQ = #{rtnCrtSeq} AND URL_TYPE = #{urlType}),
            TO_CHAR(SYSDATE, 'YYYYMMDD'),
            #{authType},
            #{moduType},
            #{compType},
            #{stepEndYn},
            #{veriRslt},
            #{cReferer},            
            #{name},            
            #{birthDate},
            #{mobileNo}, 
            #{connInfo},
            #{requestKey},
            #{contractNum},
            #{reqUsimSn},
            #{reqBank},
            #{reqAccountNumber},
            #{uploadPhoneSrlNo},
            #{reqSeq},
            #{resSeq},
            #{ncType},
            #{dupInfo},
            #{crtEtc},
            #{rip},
            #{regstId},
            SYSDATE,
   #{urlType},
   #{reqCardCompany},
   #{reqCardNo},
   #{crdtCardTermDay}
        )
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o certDao.insertCert
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
certDao.getCompareList
-> [SQL] CertMapper.getCompareList

     SELECT    /*CertMapper.getCompareList*/
      CRT_SEQ,
    STEP,
    REGST_DT,
    AUTH_TYPE,
    MODU_TYPE,
    COMP_TYPE,
    STEP_END_YN,
    VERI_RSLT,
    REFERER,
    NAME,
    BIRTH_DATE,
    MOBILE_NO,
    CONN_INFO,
    REQUEST_KEY,
    CONTRACT_NUM,
    REQ_USIM_SN,
    REQ_BANK,
    REQ_ACCOUNT_NUMBER,
    UPLOAD_PHONE_SRL_NO,
    REQ_SEQ,
    RES_SEQ,
    NC_TYPE,
    DUP_INFO,
    CRT_ETC,
    RIP,
    REGST_ID,
    REGST_DTTM,
    RVISN_ID,
    RVISN_DTTM,
    REQ_CARD_COMPANY,
    REQ_CARD_NO,
    CRDT_CARD_TERM_DAY
     FROM NMCP_CRT_VLD_DTL
     WHERE 1 = 1
      AND CRT_SEQ = #{crtSeq}
     AND VERI_RSLT NOT IN ('N', 'X')
   <if test="ncType != null and ncType != ''">
    AND NC_TYPE = #{ncType}
   </if>
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o certDao.getCompareList
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
certDao.getModuTypeStepCnt
-> [SQL] CertMapper.getModuTypeStepCnt

  SELECT  /*CertMapper.getModuTypeStepCnt*/
      COUNT(DISTINCT STEP) CNT
  FROM NMCP_CRT_VLD_DTL
  WHERE 1 = 1
   AND CRT_SEQ = #{crtSeq}
   AND MODU_TYPE = #{moduType}
   AND COMP_TYPE IN ('E', 'C')
   <if test= 'ncType != null and ncType != ""'>
   AND NC_TYPE = #{ncType}
   </if>
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o certDao.getModuTypeStepCnt
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
certDao.getDataEsimSeq
-> [SQL] CertMapper.getDataEsimSeq

  SELECT  /*CertMapper.getDataEsimSeq*/
      COUNT(CRT_SEQ) AS CNT
  FROM NMCP_CRT_VLD_DTL
  WHERE 1 = 1
   AND CRT_SEQ = #{crtSeq}  
   AND UPLOAD_PHONE_SRL_NO IS NOT NULL
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o certDao.getDataEsimSeq
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
certDao.getDataMobileNo
-> [SQL] CertMapper.getDataMobileNo

  /*CertMapper.getDataMobileNo*/
  SELECT COUNT(CRT_SEQ)
  FROM NMCP_CRT_VLD_DTL
  WHERE CRT_SEQ = #{crtSeq}
   AND COMP_TYPE = 'E'
   AND MOBILE_NO IS NOT NULL
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o certDao.getDataMobileNo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
certDao.getContractNumByUserId
-> [SQL] CertMapper.getContractNumByUserId

  SELECT  /*CertMapper.getContractNumByUserId*/
      SVC_CNTR_NO AS CONTRACT_NUM 
  FROM MCP_USER_CNTR_MNG
  WHERE USERID = #{userId}
```

**테이블/API:** MCP_USER_CNTR_MNG  
**처리 로직 / 변환 처리:**

```
o certDao.getContractNumByUserId
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  

---

## [1003-06]

**JSP/JS:** /mobile/appForm/appOutForm.jsp, /resources/js/mobile/appForm/appOutForm.js  
**URL:** /msp/getSalePlcyAjax.do  
**Controller:**

```
AppformController.java - getSalePlcy
  - 정책정보
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /msp/mspSalePlcyMst  
**처리 로직 / 변환 처리:**

```
o /msp/mspSalePlcyMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**URL:** /msp/listChargeInfoAjax.do  
**Controller:**

```
AppformController.java - listChargeInfo
  - 요금제 정보 , 가격정보

        if (!StringUtils.isBlank(saleSubsdMstDto.getSalePlcyCd())) {
            /*
             * getSalePlcyCd정책
             * getPrdtId 대표상품 아이디
             * getOrgnId 접점 코드
             * getOperType
             *
             * #{plcySctnCd}  AS PLCY_SCTN_CD  ,--정책구분코드(01:단말,02:유심)
                #{onOffType}   AS ON_OFF_TYPE ,


             */
            if (StringUtils.isBlank(saleSubsdMstDto.getPrdtId())
                    || StringUtils.isBlank(saleSubsdMstDto.getOrgnId())
                    || StringUtils.isBlank(saleSubsdMstDto.getOperType())) {
                throw new McpCommonJsonException("0006", BIDING_EXCEPTION);
            }

            RestTemplate restTemplate = new RestTemplate();
            MspSaleSubsdMstDto[] chargeTemList = restTemplate.postForObject(apiInterfaceServer + "/msp/mspSaleSubsdMstList", saleSubsdMstDto, MspSaleSubsdMstDto[].class);
            List<MspSaleSubsdMstDto> chargeList = Arrays.asList(chargeTemList);

            //최저가로 정렬처리하기위해 임시로 상품기본객체를 생성한다.
            PhoneProdBasDto temp = new PhoneProdBasDto();
            temp.setMspSaleSubsdMstListForLowPrice(chargeList);
            temp.setOrderEnum(orderEnum);
            if (chargeList != null && chargeList.size() > 0) {
                temp.doSort();
            }

            for (MspSaleSubsdMstDto bakset : chargeList) {    //요즘제 정보에 정책할부수수료와,기본수수료값을 세팅한다.
                if (saleSubsdMstDto.getModelMonthly() > 0) {
                    bakset.setModelMonthly(saleSubsdMstDto.getModelMonthly());    //입력받은 할부기간
                }
            }

            return chargeList;
        } else {
            NmcpCdDtlDto nmcpCdDtlDto = NmcpServiceUtils.getCodeNmDto(GROUP_CODE_USIM_REGI_SITE_LIST, saleSubsdMstDto.getOrgnId());

            if (nmcpCdDtlDto == null) {
                throw new McpCommonJsonException("0001", F_BIND_EXCEPTION);
            }

            if (StringUtils.isBlank(saleSubsdMstDto.getOperType())) {
                throw new McpCommonJsonException("0006", BIDING_EXCEPTION);
            }

            if (StringUtils.isBlank(nmcpCdDtlDto.getExpnsnStrVal3())) {
                throw new McpCommonJsonException("0007", BIDING_EXCEPTION);
            }

            // 요금제
            Map<String, String> inputMap = new HashMap<String, String>();
            inputMap.put("code", nmcpCdDtlDto.getExpnsnStrVal3());
            inputMap.put("orgnId", saleSubsdMstDto.getOrgnId());
            inputMap.put("onOffType", saleSubsdMstDto.getOnOffType());
            inputMap.put("operType", saleSubsdMstDto.getOperType());
            inputMap.put("rateCd", saleSubsdMstDto.getRateCd());
            inputMap.put("prdtSctnCd", LTE_FOR_MSP);  //--getPrdtSctnCd
            //inputMap.put("prdtSctnCd", appformReqDto.getPrdtSctnCd());

            RestTemplate restTemplate = new RestTemplate();
            MspSaleSubsdMstDto[] chargeTemList = restTemplate.postForObject(apiInterfaceServer + "/prepia/rateList", inputMap, MspSaleSubsdMstDto[].class);
            List<MspSaleSubsdMstDto> chargeList = Arrays.asList(chargeTemList);
            return chargeList;
        }
```

**처리 로직 / 변환 처리:**

```
o AppformController.java - listChargeInfo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** [M포탈-M전산 API] /msp/mspSaleSubsdMstList  
**처리 로직 / 변환 처리:**

```
o /msp/mspSaleSubsdMstList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** [M포탈-M전산 API] /prepia/rateList  
**처리 로직 / 변환 처리:**

```
o /prepia/rateList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**URL:** /m/usim/selectUsimBasJoinPriceAjax.do  
**Controller:**

```
UsimController.java - selectUsimBasJoinPriceAjax
  - Usim 상품 정보 조회, 가입비 조회 , 할인 조회
```

**Service:** usimService.selectJoinUsimPriceNew  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /storeUsim/joinUsimPriceNew  
**처리 로직 / 변환 처리:**

```
o /storeUsim/joinUsimPriceNew
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** usimService.selectUsimDcamt  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /storeUsim/usimDcamt  
**처리 로직 / 변환 처리:**

```
o /storeUsim/usimDcamt
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  

---

## [1003-07]

**JSP/JS:** /mobile/appForm/appOutForm.jsp, /resources/js/mobile/appForm/appOutForm.js  
**URL:** /mypage/selectRwdProdListAjax.do  
**Controller:**

```
RwdController.java - selectRwdProdList
  - 자급제 보상 서비스 리스트
```

**Service:**

```
rwdService.selectRwdProdList
-> /mypage/selectRwdProdList
```

**처리 로직 / 변환 처리:**

```
o /mypage/selectRwdProdList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 자급제 관련 로직 불필요  
**개발팀 전달일:** 03/22 전달  
---

**URL:** /mypage/checkImeiForRwd.do  
**Controller:**

```
RwdController.java - checkImeiForRwd
  - 자급제 보상서비스 가입 시 해당 imei 사용가능 여부 체크
```

**처리 로직 / 변환 처리:**

```
o RwdController.java - checkImeiForRwd
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 자급제 관련 로직 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
rwdService.checkImeiForRwd
-> /mypage/checkImeiForRwd
```

**처리 로직 / 변환 처리:**

```
o /mypage/checkImeiForRwd
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 자급제 관련 로직 불필요  
**개발팀 전달일:** 03/22 전달  
---

**URL:** /m/mypage/rwdRegNonMemberAjax.do  
**Controller:**

```
RwdController.java - rwdRegNonMemberAjax
  - 자급제 보상 서비스 신청

        HashMap<String, String> rtnMap;
        // 자급제 보상 서비스 가입 실패 로그 기록용
        RwdFailHistDto rwdFailHistDto= new RwdFailHistDto();

        // 1. 로그인 여부 확인
        UserSessionDto userSession = SessionUtils.getUserCookieBean();
        if(userSession != null && !StringUtils.isEmpty(userSession.getUserId())) {
            rwdOrderDto.setRegstId(userSession.getUserId());
            rwdFailHistDto.setUserId(userSession.getUserId());
        }

        // 2. 신청서 정보 확인
        AppformReqDto appformReqDto= new AppformReqDto();
        appformReqDto.setRequestKey(rwdOrderDto.getRequestKey());
        AppformReqDto appformReq = appformSvc.getAppForm(appformReqDto);

        rwdFailHistDto.setRequestKey(appformReq.getRequestKey());

        if(appformReq == null){
            // RWD 신청 실패 로그 기록
            rwdFailHistDto.setFailRsn("APPFORM INFO IS NULL");
            rwdService.insertFailRwd(rwdFailHistDto);

            throw new McpCommonJsonException("0001", "개통 신청 정보가 없습니다.");
        }else if(!"Y".equals(appformReq.getClauseRwdFlag())
                || "".equals(StringUtil.NVL(appformReq.getRwdAuthInfo(), ""))
                || "".equals(StringUtil.NVL(appformReq.getRwdProdCd(), ""))){

            // RWD 신청 실패 로그 기록
            rwdFailHistDto.setResNo(appformReq.getResNo());
            rwdFailHistDto.setFailRsn("RWD SERVICE IS NOT APPLIED");
            rwdService.insertFailRwd(rwdFailHistDto);

            throw new McpCommonJsonException("0002", "자급제 보상 서비스를 신청하지 않으셨습니다.");
        }

        rwdOrderDto.setResNo(appformReq.getResNo());
        rwdOrderDto.setRwdProdCd(appformReq.getRwdProdCd());
        rwdOrderDto.setRwdAuthInfo(appformReq.getRwdAuthInfo());
        rwdFailHistDto.setResNo(appformReq.getResNo());

        // 3. 선택한 자급제 보상서비스 존재여부 체크
        RwdDto[] rwdProdList = rwdService.selectRwdProdList(rwdOrderDto.getRwdProdCd());
        if(rwdProdList== null || rwdProdList.length == 0){

            // RWD 신청 실패 로그 기록
            rwdFailHistDto.setFailRsn("RWD PROD CD IS WRONG : [CD] " + rwdOrderDto.getRwdProdCd());
            rwdService.insertFailRwd(rwdFailHistDto);

            throw new McpCommonJsonException("0003", "선택한 자급제 보상 서비스는 존재하지 않습니다.");
        }
        rwdOrderDto.setRwdProdNm(rwdProdList[0].getRwdProdNm());

        // 4. 구입가 입력 여부 (0원 입력 가능)
        String buyPric= rwdOrderDto.getBuyPric();

        try{
            long numBuyPric= Long.parseLong(buyPric);
            if(numBuyPric < 0){
                // RWD 신청 실패 로그 기록
                rwdFailHistDto.setFailRsn("BUY PRICE HAS MINUS VALUE : [PRICE] " + buyPric);
                rwdService.insertFailRwd(rwdFailHistDto);
                throw new McpCommonJsonException("0004", "자급제 핸드폰 구입가가 마이너스 값입니다.");
            }
        }catch (NumberFormatException e){
            rwdFailHistDto.setFailRsn("BUY PRICE IS NOT NUMBER : [PRICE] " + buyPric);
            rwdService.insertFailRwd(rwdFailHistDto);
            throw new McpCommonJsonException("0006", "올바른 자급제 핸드폰 구입가를 입력하여 주시기 바랍니다.");
        }

        // 5. imei, imeiTwo 입력 여부 (둘 중 하나는 필수)
        String imei= StringUtil.NVL(rwdOrderDto.getImei(), "");
        String imeiTwo= StringUtil.NVL(rwdOrderDto.getImeiTwo(), "");

        if("".equals(imei) && "".equals(imeiTwo)){
            // RWD 신청 실패 로그 기록
            rwdFailHistDto.setFailRsn("IMEI IS NULL");
            rwdService.insertFailRwd(rwdFailHistDto);

            throw new McpCommonJsonException("0005", "단말기 정보값이 없습니다.");
        }

        if( (!"".equals(imei) && imei.length() != 15) || (!"".equals(imeiTwo) && imeiTwo.length() != 15) ){
            // RWD 신청 실패 로그 기록
            rwdFailHistDto.setFailRsn("IMEI VALUE IS WRONG : [IMEI] " + imei + " , [IMEITWO] " + imeiTwo);
            rwdService.insertFailRwd(rwdFailHistDto);

            throw new McpCommonJsonException("0006", "단말기 정보값이 올바르지 않습니다.");
        }

        // imei 사용 가능 여부
        String possibleYn= rwdService.checkImeiForRwd(imei, imeiTwo);
        if(!"Y".equals(possibleYn)){
            // RWD 신청 실패 로그 기록
            rwdFailHistDto.setFailRsn("IMEI IS ALREADY USED : [IMEI] " + imei + " , [IMEITWO] " + imeiTwo);
            rwdService.insertFailRwd(rwdFailHistDto);

            throw new McpCommonJsonException("0007", "사용불가한 단말기 정보값입니다.");
        }

        // 6. 증빙서류 첨부 여부
        if(!Optional.ofNullable(rwdOrderDto.getFile()).isPresent()) {
            // RWD 신청 실패 로그 기록
            rwdFailHistDto.setFailRsn("RWD FILE IS NULL");
            rwdService.insertFailRwd(rwdFailHistDto);

            // 파일이 존재하지 않음
            throw new McpCommonJsonException("0008", "구매영수증이 없습니다.");
        }

        // 7. 증빙서류 개수 체크
        MultipartFile[] arrFile = rwdOrderDto.getFile();
        if(arrFile == null || arrFile.length != 1){
            // RWD 신청 실패 로그 기록
            rwdFailHistDto.setFailRsn("RWD FILE COUNT IS MORE THAN ONE");
            rwdService.insertFailRwd(rwdFailHistDto);

            // 첨부파일 개수 불일치
            throw new McpCommonJsonException("0009", "2개 이상의 증빙서류는 첨부할 수 없습니다.");
        }

        // 8. 증빙서류 용량과 확장자 체크
        try{
            boolean exeFlag = false;
            boolean sizeFlag = false;
            for(int i=0; i<arrFile.length; i++){
                MultipartFile file = arrFile[i];
                long maxSize = 2*1024*1024; // 2*1024*1024 ==> 2MB
                long size = file.getSize();
                String fileNm = file.getOriginalFilename();

                // 확장자체크
                String filenameExt = fileNm.substring(fileNm.lastIndexOf(".")+1);
                filenameExt = filenameExt.toLowerCase();

                String[] allowFile = {"jpg","jpeg","png","gif"};
                exeFlag= Arrays.asList(allowFile).contains(filenameExt);

                // 용량체크
                sizeFlag= (size > maxSize) ? false : true;

                if(!exeFlag || !sizeFlag){
                    break;
                }
            }

            if(!exeFlag || !sizeFlag){ // 파일 형식 또는 용량 불일치
                // RWD 신청 실패 로그 기록
                rwdFailHistDto.setFailRsn("RWD FILE SIZE/EXE IS NOT ACCEPTED");
                rwdService.insertFailRwd(rwdFailHistDto);

                throw new McpCommonJsonException("0010", "증빙서류는 2MB 이내의 jpg, gif, png 형식으로만 등록 가능합니다.");
            }
        } catch(IndexOutOfBoundsException e) {
            throw new McpCommonJsonException("0009", "IndexOutOfBoundsException error");
        } catch(Exception e){
            throw new McpCommonJsonException("0009", "증빙서류는 2MB 이내의 jpg, gif, png 형식으로만 등록 가능합니다.");
        }

        // 9. 자급제 보상서비스 order insert
        rwdOrderDto.setChnCd("2");  // 가입채널코드 세팅 (2: 홈페이지 온라인)
        rwdOrderDto.setIfTrgtCd("N");  // 연동상태 코드 (셀프케어인 경우 Y, 이외에는 N)

        // 10. 주문자 테이블에 insert와 파일 업로드 진행
        rtnMap= rwdService.saveRwdOrder(rwdOrderDto);
        return rtnMap;
```

**처리 로직 / 변환 처리:**

```
o RwdController.java - rwdRegNonMemberAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 자급제 관련 로직 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
appformSvc.getAppForm
  - 신청서 정보 확인
```

**Service / Dao / SQL / API:**

```
appformDao.getAppForm
-> [SQL] AppformMapper.getAppForm

        SELECT
            T1.REQUEST_KEY
             , CSTMR_TYPE
             , REQ_USIM_SN
             , CLAUSE_MPPS35_FLAG
             , SOC_CODE                      --요금제코드
             , OPER_TYPE                   --업무구분
             , REQ_BUY_TYPE                --구매유형
             , MODEL_ID
             , MODEL_SALE_POLICY_CODE
             , ON_OFF_TYPE
             , CSTMR_NAME               --고객정보_고객명
             , CSTMR_NATIVE_RRN         --고객정보_내국인_주민등록번호
             , CSTMR_POST               --고객정보_우편번호
             , CSTMR_ADDR               --고객정보_주소
             , CSTMR_ADDR_DTL           --고객정보_상세주소
             , CSTMR_TEL_FN             --고객정보_전화번호_앞자리
             , CSTMR_TEL_MN             --고객정보_전화번호_가운데자리
             , CSTMR_TEL_RN             --고객정보_전화번호_끝자리
             , CSTMR_MOBILE_FN          --고객정보_휴대폰번호_앞자리
             , CSTMR_MOBILE_MN          --고객정보_휴대폰번호_중간자리
             , CSTMR_MOBILE_RN          --고객정보_휴대폰번호_끝자리
             , CSTMR_MAIL               --고객정보_메일
             , SELF_CSTMR_CI
             , CSTMR_FOREIGNER_NATION   --고객정보_외국인_국적
             , CSTMR_FOREIGNER_PN       --고객정보_외국인_여권번호
             , CSTMR_FOREIGNER_SDATE    --고객정보_외국인_체류_시작날자
             , CSTMR_FOREIGNER_EDATE    --고객정보_외국인_체류_종료날자
             , SELF_CERT_TYPE           --본인인증유형
             , SELF_ISSU_EXPR_DT        --발급/만료일자
             , SELF_ISSU_NUM            --발급번호
             , MINOR_AGENT_NAME
             , MINOR_AGENT_RRN
             , MINOR_AGENT_TEL_FN
             , MINOR_AGENT_TEL_MN
             , MINOR_AGENT_TEL_RN
             , MINOR_AGENT_RELATION
             , MINOR_SELF_CERT_TYPE     --본인인증유형
             , MINOR_SELF_ISSU_EXPR_DT  --발급/만료일자
             , MINOR_SELF_ISSU_NUM      --발급번호
             , CSTMR_JEJU_ID            -- 운전면호 구분driverSelfIssuNum1
             , DLVRY_NAME               --배송정보_이름
             , DLVRY_TEL_FN             --배송정보_전화번호_앞자리
             , DLVRY_TEL_MN             --배송정보_전화번호_중간자리
             , DLVRY_TEL_RN             --배송정보_전화번호_뒷자리
             , DLVRY_MOBILE_FN          --배송정보_휴대폰번호_앞자리
             , DLVRY_MOBILE_MN          --배송정보_휴대폰번호_중간자리
             , DLVRY_MOBILE_RN          --배송정보_휴대폰번호_뒷자리
             , DLVRY_POST               --배송정보_우편번호
             , DLVRY_ADDR               --배송정보_주소
             , DLVRY_ADDR_DTL           --배송정보_상세주소
             , DLVRY_MEMO               --배송정보_요청사항
             , REQ_WANT_NUMBER
             , REQ_WANT_NUMBER2
             , REQ_WANT_NUMBER3
             , MOVE_COMPANY             --번호이동정보_변경전통신사
             , MOVE_MOBILE_FN           --번호이동정보_이동할번호_앞자리
             , MOVE_MOBILE_MN           --번호이동정보_이동할번호_중간자리
             , MOVE_MOBILE_RN           --번호이동정보_이동할번호_끝자리
             , MOVE_AUTH_TYPE           --번호이동정보_인증유형
             , MOVE_AUTH_NUMBER         --번호이동정보_인증번호4자리
             , RECOMMEND_FLAG_CD
             , RECOMMEND_INFO
             , INSR_PROD_CD
             , T1.RES_NO
             , T1.RWD_PROD_CD
             , T1.CLAUSE_RWD_FLAG
             , T1.RWD_AUTH_INFO
             , REQ_PAY_TYPE
             , CSTMR_BILL_SEND_CODE
             , REQ_BANK
             , REQ_ACCOUNT_NUMBER
             , REQ_CARD_COMPANY
             , REQ_CARD_NO
             , REQ_CARD_YY
             , REQ_CARD_MM
             , SESPLS_YN
             , CNTPNT_SHOP_ID
             , USIM_KINDS_CD             -- 유심종류(RCP2035)
             , PRNTS_CONTRACT_NO         -- 모회선 계약번호
             , SETTL_AMT                 -- 결제금액
             , MODEL_SALE_POLICY_CODE
        FROM
            MCP_REQUEST T1
           , MCP_REQUEST_SALEINFO T2
           , MCP_REQUEST_CSTMR T3
           , MCP_REQUEST_AGENT T4
           , MCP_REQUEST_DLVRY T5
           , MCP_REQUEST_MOVE T6
           , MCP_REQUEST_REQ T7
        WHERE
            T1.REQUEST_KEY = T2.REQUEST_KEY
          AND T1.REQUEST_KEY = T3.REQUEST_KEY(+)
          AND T1.REQUEST_KEY = T4.REQUEST_KEY(+)
          AND T1.REQUEST_KEY = T5.REQUEST_KEY(+)
          AND T1.REQUEST_KEY = T6.REQUEST_KEY(+)
          AND T1.REQUEST_KEY = T7.REQUEST_KEY(+)
          AND T1.REQUEST_KEY =  #{requestKey}
```

**테이블/API:** MCP_REQUEST, MCP_REQUEST_SALEINFO, MCP_REQUEST_CSTMR, MCP_REQUEST_AGENT, MCP_REQUEST_DLVRY, MCP_REQUEST_MOVE, MCP_REQUEST_REQ  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getAppForm
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
-> MCP_REQUEST_DLVRY : 배송 관련 로직은 제외
```

**기타:** 제외 - 자급제 관련 로직 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
rwdService.insertFailRwd
  - 자급제 보상서비스 가입 실패 로그 기록
```

**Service / Dao / SQL / API:**

```
rwdDao.insertFailRwd
-> [SQL] RwdMapper.insertFailRwd

        INSERT INTO NMCP_RWD_FAIL_HIST (
        HIST_SEQ,
        <if test="requestKey != 0">
            REQUEST_KEY,
        </if>
        RES_NO,
        CONTRACT_NUM,
        SYS_DATE,
        USER_ID,
        FAIL_RSN,
        ACCESS_URL,
        ACCESS_IP
        ) VALUES (
        SQ_RWD_FAIL_HIST.NEXTVAL,
        <if test="requestKey != 0">
            #{requestKey},
        </if>
        #{resNo},
        #{contractNum},
        SYSDATE,
        #{userId},
        #{failRsn},
        #{accessUrl},
        #{accessIp}
        )
```

**테이블/API:** NMCP_RWD_FAIL_HIST  
**처리 로직 / 변환 처리:**

```
o RwdMapper.insertFailRwd
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 자급제 관련 로직 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
rwdService.selectRwdProdList
  - 선택한 자급제 보상서비스 존재여부 체크
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/selectRwdProdList  
**처리 로직 / 변환 처리:**

```
o /mypage/selectRwdProdList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 자급제 관련 로직 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
rwdService.checkImeiForRwd
  - imei 사용 가능 여부
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/checkImeiForRwd  
**처리 로직 / 변환 처리:**

```
o /mypage/checkImeiForRwd
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 자급제 관련 로직 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
rwdService.saveRwdOrder
  - 주문자 테이블에 insert와 파일 업로드 진행

소스 : RwdServiceImpl-필요부분.java
```

**처리 로직 / 변환 처리:**

```
o rwdService.saveRwdOrder
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 자급제 관련 로직 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수

        Map<String, String> rtnMap= new HashMap<>();

        // 1. 파라미터 체크
        if(StringUtils.isEmpty(compType)){
            rtnMap.put("RESULT_CODE", "FAIL1");
            rtnMap.put("RESULT_DESC", STEP_INFO_NULL_EXCEPTION);
            return rtnMap;
        }

        if(keyArr == null || valueArr == null || keyArr.length < 1 || valueArr.length < 1){
            rtnMap.put("RESULT_CODE", "FAIL2");
            rtnMap.put("RESULT_DESC", STEP_INFO_NULL_EXCEPTION);
            return rtnMap;
        }

        if(keyArr.length > valueArr.length){
            rtnMap.put("RESULT_CODE", "FAIL3");
            rtnMap.put("RESULT_DESC", STEP_INFO_NULL_EXCEPTION);
            return rtnMap;
        }

        // 2. json으로 변환
        int failIdx= -1;
        StringBuilder sb = new StringBuilder("{");
        for(int i=0; i<keyArr.length; i++){

            // ncType은 null체크 제외
            if(StringUtils.isEmpty(valueArr[i])){
                if("ncType".equals(keyArr[i])) valueArr[i]= "";
                else{
                    failIdx= i;
                    break;
                }
            }

            if(i > 0) sb.append(",");
            sb.append("\"");
            sb.append(keyArr[i]);
            sb.append("\"");
            sb.append(":");
            sb.append("\"");
            sb.append(valueArr[i]);
            sb.append("\"");
        }

        // 2-1. 필수값 누락
        if(failIdx > -1){
            rtnMap.put("RESULT_CODE", "FAIL4");
            rtnMap.put("RESULT_DESC", STEP_INFO_NULL_EXCEPTION);
            logger.debug(keyArr[failIdx] +" 값이 존재하지 않습니다.");
            return rtnMap;
        }

        sb.append("}");
        String certJson= sb.toString();
        logger.debug("certJson > " + certJson);

        // 3. certDto로 변환
        try {
            CertDto certDto= new ObjectMapper().readValue(certJson, CertDto.class);

            // 3-1. compType에 따라 step 처리
            certDto.setCompType(compType);
            Map<String, Object> certReslt = this.getCertInfo(certDto);

            rtnMap.put("RESULT_CODE", (String)certReslt.get("RESULT_CODE"));
            rtnMap.put("RESULT_DESC", (String)certReslt.get("RESULT_DESC"));

        } catch (IOException e) {
            rtnMap.put("RESULT_CODE", "FAIL5");
            rtnMap.put("RESULT_DESC", "필수항목명을 다시 확인해 주시기 바랍니다.");
        }

        return rtnMap;
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 자급제 관련 로직 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
[M포탈-M전산 API] /mypage/selectRwdInfoByResNo
  - 중복신청 방지를 위해 자급제 보상 서비스 신청 이력 조회
```

**처리 로직 / 변환 처리:**

```
o /mypage/selectRwdInfoByResNo
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 자급제 관련 로직 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
[M포탈-M전산 API] /mypage/updateRwdOrder
  - 기신청건으로 update 처리 (update건은 셀프케어에선 존재x 따라서 서식지 생성 안함)
```

**처리 로직 / 변환 처리:**

```
o /mypage/updateRwdOrder
-> 현행 ITO API 그대로 사용 : M전산/M포탈 테이블 INSERT/UPDATE/DELETE
```

**기타:** 제외 - 자급제 관련 로직 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
[M포탈-M전산 API] /mypage/insertRwdOrder
  - 신규가입으로 자급제 보상서비스 order 테이블 insert
```

**처리 로직 / 변환 처리:**

```
o /mypage/insertRwdOrder
-> 현행 ITO API 그대로 사용 : M전산/M포탈 테이블 INSERT/UPDATE/DELETE
```

**기타:** 제외 - 자급제 관련 로직 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
ipstatisticService.insertAdminAccessTrace
-> fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin

        INSERT INTO MCP_ADMIN_ACCESS_TRACE (
            PARAMETER,
            ACCESS_IP,
            SYS_DT,
            SYS_RDATE,
            ACCESS_URL,
            USER_ID,
            PRCS_MDL_IND,
            PRCS_SBST,
            TRTM_RSLT_SMST
        ) VALUES (
                     #{parameter},
                     #{accessIp},
                     TO_CHAR(SYSDATE, 'YYYYMMDD'),
                     SYSDATE,
                     #{accessUrl},
                     SUBSTRB(#{userid},0,20),
                     #{prcsMdlInd},
                     SUBSTRB(#{prcsSbst},0,1000),
                     #{trtmRsltSmst}
                 )
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 자급제 관련 로직 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
prodSendScan
  - 자급제 보상서비스 서식지 생성

소스 : RwdServiceImpl-필요부분.java
```

**처리 로직 / 변환 처리:**

```
o prodSendScan
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 자급제 관련 로직 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.getAppFormPointList
  - 그룹별 서식지 위치정보 가져오기 (각 페이지 별 위치정보)
-> [SQL] AppformMapper.getAppFormPointList

        SELECT
        COLUMN_NAME
        , METAROW
        , METALINE
        , APP_KEY
        , CODEDATA
        , CODEDATA_YN
        , PAGEINDEX
        , DELETECOLUMNYN
        FROM
        MCP_APPLICATION_FORM_INFO
        ORDER BY PAGEINDEX, APP_KEY
```

**테이블/API:** MCP_APPLICATION_FORM_INFO  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getAppFormPointList
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경 : 포시에스 리포트 툴에서 사용이 필요할지 확인 후 필요한 경우 사용

ㅇ ITO AS-IS 소스에 로직 변경
 - 신규 포시에스 리포팅 시스템에 맞춰서 로직 변경
```

**기타:** 제외 - 자급제 관련 로직 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
rwdDao.updateEmvScanMstRwd
-> [SQL] RwdMapper.updateEmvScanMstRwd

        UPDATE
            EMV_SCAN_MST
        SET
            WORK_ID = 'S0012',
            DOC_TITLE = CUST_NM || '_자급제보상서비스신청'
        WHERE 1 = 1
        AND SCAN_ID = #{scanId}
```

**테이블/API:** EMV_SCAN_MST  
**처리 로직 / 변환 처리:**

```
o RwdMapper.updateEmvScanMstRwd
-> SQL Copy 후 M포탈-M전산 API 에 SQL 추가하여 데이터 INSERT/UPDATE 처리
```

**기타:** 제외 - 자급제 관련 로직 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
rwdDao.updateEmvScanFileRwd
-> [SQL] RwdMapper.updateEmvScanFileRwd

        UPDATE
            EMV_SCAN_FILE
        SET
            DOC_ID = 'E0022'
        WHERE 1 = 1
        AND SCAN_ID = #{scanId}
```

**테이블/API:** EMV_SCAN_FILE  
**처리 로직 / 변환 처리:**

```
o RwdMapper.updateEmvScanFileRwd
-> SQL Copy 후 M포탈-M전산 API 에 SQL 추가하여 데이터 INSERT/UPDATE 처리
```

**기타:** 제외 - 자급제 관련 로직 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/getOrgScanId  
**처리 로직 / 변환 처리:**

```
o /mypage/getOrgScanId
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 자급제 관련 로직 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
rwdDao.getMaxFileNum
-> [SQL] RwdMapper.getMaxFileNum

        SELECT NVL(MAX(FILE_NUMBER), -1) AS FILE_NUM
        FROM EMV_SCAN_FILE
        WHERE SCAN_ID = #{orgScanId}
```

**테이블/API:** EMV_SCAN_FILE  
**처리 로직 / 변환 처리:**

```
o RwdMapper.getMaxFileNum
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 자급제 관련 로직 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
rwdDao.getEmvScanFileList
-> [SQL] RwdMapper.getEmvScanFileList

        SELECT FILE_ID AS SCAN_FILE_ID, SCAN_ID
        FROM EMV_SCAN_FILE
        WHERE SCAN_ID = #{scanId}
        ORDER BY FILE_NUMBER
```

**테이블/API:** EMV_SCAN_FILE  
**처리 로직 / 변환 처리:**

```
o RwdMapper.getEmvScanFileList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 자급제 관련 로직 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
rwdDao.insertEmvScanFile
-> [SQL] RwdMapper.insertEmvScanFile

        INSERT INTO EMV_SCAN_FILE
        (FILE_ID, SCAN_ID, FILE_NUMBER, FILE_SIZE, FILE_EXT, DOC_ID, MAIN_DOC_CD, USE_YN, ORIGIN_DIR, RGST_DT, RGST_PRSN_ID )
        SELECT
            FILE_ID,
            #{orgScanId} AS SCAN_ID,
            #{maxFileNum} AS FILE_NUMBER,
            FILE_SIZE,
            FILE_EXT,
            DOC_ID,
            MAIN_DOC_CD,
            'Y',
            ORIGIN_DIR,
            RGST_DT,
            #{regstId}
        FROM EMV_SCAN_FILE
        WHERE SCAN_ID = #{scanId}
        AND FILE_ID = #{scanFileId}
```

**테이블/API:** EMV_SCAN_FILE  
**처리 로직 / 변환 처리:**

```
o RwdMapper.insertEmvScanFile
-> SQL Copy 후 M포탈-M전산 API 에 SQL 추가하여 데이터 INSERT/UPDATE 처리
```

**기타:** 제외 - 자급제 관련 로직 불필요  
**개발팀 전달일:** 03/22 전달  

---

## [1003-08]

**JSP/JS:** /mobile/appForm/appOutForm.jsp, /resources/js/mobile/appForm/appOutForm.js  
**URL:** /appform/selectInsrProdListAjax.do  
**Controller:**

```
AppformController.java - selectInsrProdList
 - 휴대폰 안심 서비스 정보 조회
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /appform/selectInsrProdList  
**처리 로직 / 변환 처리:**

```
o /appform/selectInsrProdList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  

---

## [1003-09]

**JSP/JS:** /mobile/appForm/appOutForm.jsp, /resources/js/mobile/appForm/appOutForm.js  
**URL:** X  
**Controller:** X  
**Service:** appformSvc.getMcpAdditionList  
**처리 로직 / 변환 처리:**

```
o 관리자에서 설정한 무료, 유료 부가서비스 목록 고정으로 조회
1. 관리자에서 설정한 무료, 유료 카테고리 부가서비스 목록 조회
2. 부가서비스 속성 정보는 기존 SQL 이용하여 가입 대상 목록 및 가입 가능 여부 체크
```

**개발팀 전달일:** 03/22 전달  

---

## [1003-10]

**JSP/JS:** /mobile/appForm/appOutForm.jsp, /resources/js/mobile/appForm/appOutForm.js  
**URL:** /appform/getMcpAdditionListAjax.do  
**Controller:**

```
AppformController.java - getMcpAdditionList
 - 부가서비스 목록 조회
```

**Service:** appformSvc.getMcpAdditionList  
**Service / Dao / SQL / API:**

```
appformDao.getMcpAdditionList
-> [SQL] AppformMapper.getMcpAdditionList

        SELECT
        A.ADDITION_KEY
        , A.ADDITION_NAME
        , A.CHARGE_FLAG
        , NVL(A.RANTAL, 0)  AS RANTAL
        , RATE_ADSVC_GDNC_SEQ AS RATE_ADSVC_PROD_REL_SEQ
        FROM
        MCP_ADDITION A
        , MCP_CODE  B
        , NMCP_RATE_ADSVC_GDNC_PROD_REL T3
        WHERE
        B.CODE_SYS = 'ADS' --코드그룹
        AND A.SOC =  T3.RATE_ADSVC_CD(+)
        AND B.USE_YN = 'Y'
        AND B.CODE = A.CHARGE_FLAG
        AND A.CHARGE_FLAG  =  #{chargeFlag}
        AND A.ACT_FLAG = 'Y'
        AND A.ADDITION_KEY <![CDATA[<> ]]>10 -- 010번호연결서비스
        <if test="osstYn != null and osstYn != ''">
            AND A.OSST_YN= #{osstYn}
        </if>
        ORDER BY  B.ORD , A.ADDITION_KEY
```

**테이블/API:** MCP_ADDITION, MCP_CODE, NMCP_RATE_ADSVC_GDNC_PROD_REL  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getMcpAdditionList
-> SQL Copy 후 스마트신청서App 에 맞게 변경하여 사용 : M포탈/전산 테이블링크하여 사용
- NMCP_RATE_ADSVC_GDNC_PROD_REL join 삭제

o 스마트서식지 카테고리 분류 테이블 join 추가
```

**개발팀 전달일:** 03/22 전달  
---

**URL:** /rate/adsvcContent.do, /m/rate/adsvcContent.do  
**Controller:**

```
RateAdsvcGdncController.java - adsvcGdncViewAjax
  - 부가서비스 상세 정보 조회
```

**Service:** rateAdsvcGdncService.getRateAdsvcGdncDtlXmlList  
**처리 로직 / 변환 처리:**

```
o rateAdsvcGdncService.getRateAdsvcGdncDtlXmlList
  - 제외
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service:** rateAdsvcGdncService.getProdBySeq  
**처리 로직 / 변환 처리:**

```
o rateAdsvcGdncService.getProdBySeq
  - 제외
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service:** rateAdsvcGdncService.getRateAdsvcGdncProdRel  
**처리 로직 / 변환 처리:**

```
o rateAdsvcGdncService.getRateAdsvcGdncProdRel
  - 제외
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/22 전달  

---

## [1003-11]

**JSP/JS:** /mobile/appForm/appOutForm.jsp, /resources/js/mobile/appForm/appOutForm.js  
**URL:** /appform/containsGoldNumbersAjax.do  
**Controller:** AppformController.java - containsGoldNumbersAjax  
**Service:**

```
appformSvc.containsGoldNumbers

        NmcpCdDtlDto nmcpCdDtlDto = new NmcpCdDtlDto();
        nmcpCdDtlDto.setCdGroupId("GoldNumberList");
        List<String> goldNumberList = fCommonSvc.getCodeList(nmcpCdDtlDto).stream()
                .map(NmcpCdDtlDto::getDtlCd)
                .collect(Collectors.toList());

        List<String> filteredNumbers = reqWantNumbers.stream()
                .filter(goldNumberList::contains)
                .collect(Collectors.toList());

        if (!filteredNumbers.isEmpty()) {
            StringBuilder sb = new StringBuilder();
            sb.append("[").append(String.join(", ", filteredNumbers)).append("]<br>");
            sb.append(ExceptionMsgConstant.CONTAINS_GOLD_NUMBER_EXCEPTION);
            throw new McpCommonJsonException("9999", sb.toString());
        }
```

**처리 로직 / 변환 처리:**

```
o appformSvc.containsGoldNumbers
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
fCommonSvc.getCodeList
  - CD_GROUP_ID = 'GoldNumberList'
```

**Service / Dao / SQL / API:**

```
fCommonDao.getCodeList
-> [SQL] CommCodeMapper.getCodeList

        SELECT
            CD_GROUP_ID
            ,DTL_CD
            ,DTL_CD_NM
            ,DTL_CD_DESC
            ,INDC_ODRG
            ,USE_YN
            ,EXPNSN_STR_VAL1
            ,EXPNSN_STR_VAL2
            ,EXPNSN_STR_VAL3
            ,CRET_ID
            ,AMD_ID
            ,CRET_DT
            ,AMD_DT
            ,UP_GRP_CD
        FROM NMCP_CD_DTL
        WHERE 1=1
            <choose>
                <when test="cdGroupId == 'UserPromotion'">
                      AND CRET_DT = (
                        SELECT MAX(CRET_DT)
                        FROM NMCP_CD_DTL
                        WHERE CD_GROUP_ID = #{cdGroupId}
                        AND USE_YN = 'Y'
                        AND TO_DATE(PSTNG_END_DATE, 'yyyymmddhh24miss') <![CDATA[>]]> SYSDATE
                        AND TO_DATE(PSTNG_START_DATE, 'yyyymmddhh24miss') <![CDATA[<=]]> SYSDATE
                          )
                </when>
                <otherwise>
                     AND CD_GROUP_ID = #{cdGroupId}
                     AND USE_YN = 'Y'
                     AND TO_DATE(PSTNG_END_DATE, 'yyyymmddhh24miss') <![CDATA[>]]> SYSDATE
                </otherwise>
            </choose>
            <if test="expnsnStrVal1 != null and expnsnStrVal1 != ''">
                AND EXPNSN_STR_VAL1 = #{expnsnStrVal1}
            </if>
            ORDER BY INDC_ODRG
```

**테이블/API:** NMCP_CD_DTL  
**처리 로직 / 변환 처리:**

```
o CommCodeMapper.getCodeList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/22 전달  

---

## [1003-12]

**JSP/JS:** /mobile/appForm/appOutForm.jsp, /resources/js/mobile/appForm/appOutForm.js  
**URL:** /nice/accountCheckAjax.do  
**Controller:**

```
NiceCertifyController.java - checkNiceAccount
  - 계좌 번호 유효성 체크 서비스

        String result = null;
        HashMap<String, Object> rtnMap = new HashMap<String, Object>();
        if ( "LOCAL".equals(serverName)) {

            // ============ STEP START ============
            Map<String, String> resultMap = certService.isAuthStepApplyUrl(request);
            if("Y".equals(resultMap.get("isAuthStep"))) {

                String ncType= "";
                if(request.getParameter("ncType") != null) ncType= request.getParameter("ncType");

                // account인증 이력 존재여부 확인
                if(0 < certService.getModuTypeStepCnt("account", ncType)){
                    // 계좌인증 관련 스텝 초기화
                    CertDto certDto = new CertDto();
                    certDto.setModuType("account");
                    certDto.setCompType("G");
                    certDto.setNcType(ncType);
                    certService.getCertInfo(certDto);
                }

                // 인증종류, 대리인구분, 계좌번호, 은행코드, 이름
                String[] certKey= {"urlType", "moduType", "ncType", "reqAccountNumber", "reqBank", "name"};
                String[] certValue= {"chkAccount", "account", ncType, EncryptUtil.ace256Enc(niceResDto.getAccountNo()), niceResDto.getBankCode(), niceResDto.getName()};

                // service가 3인 경우 이름 필수x
                if("3".equals(niceResDto.getService())){
                    certKey= Arrays.copyOfRange(certKey, 0, 5);
                    certService.vdlCertInfo("C", certKey, certValue);
                }else{
                    certService.vdlCertInfo("C", certKey, certValue);

                    // 인증종류, 대리인구분, 이름
                    certKey= new String[]{"urlType", "moduType", "ncType", "name"};
                    certValue= new String[]{"compAccount", "account", ncType, niceResDto.getName()};

                    Map<String,String> vldReslt= certService.vdlCertInfo("D", certKey, certValue);
                    if(!AJAX_SUCCESS.equals(vldReslt.get("RESULT_CODE"))) {
                        throw new McpCommonJsonException("STEP01", vldReslt.get("RESULT_DESC"));
                    }
                }
            }
            // ============ STEP END ============

            rtnMap.put("RESULT_CODE", AJAX_SUCCESS);
            return rtnMap;

            //result = niceCertifySvc.checkNiceAccount(niceResDto);
        } else {
            result = niceCertifySvc.checkNiceAccount(niceResDto);
        }



        String[] results = result.split("\\|");


        NiceLogDto nicelogDto= new NiceLogDto();
        nicelogDto.setnReferer(request.getHeader("referer"));
        nicelogDto.setnAuthType("A");

        if (results!=null && results.length > 0 && "0000".equals(results[1])) {
            //인증성공
            //나이스 로그 기록 20160403
            nicelogDto.setnResult("O");
            nicelog.insert(request , niceResDto , nicelogDto);
            rtnMap.put("RESULT_CODE", AJAX_SUCCESS);

            // ============ STEP START ============
            Map<String, String> resultMap = certService.isAuthStepApplyUrl(request);
            if("Y".equals(resultMap.get("isAuthStep"))) {

                String ncType= "";
                if(request.getParameter("ncType") != null) ncType= request.getParameter("ncType");

                // account인증 이력 존재여부 확인
                if(0 < certService.getModuTypeStepCnt("account", ncType)){
                    // 계좌인증 관련 스텝 초기화
                    CertDto certDto = new CertDto();
                    certDto.setModuType("account");
                    certDto.setCompType("G");
                    certDto.setNcType(ncType);
                    certService.getCertInfo(certDto);
                }

                // 인증종류, 대리인구분, 계좌번호, 은행코드, 이름
                String[] certKey= {"urlType", "moduType", "ncType", "reqAccountNumber", "reqBank", "name"};
                String[] certValue= {"chkAccount", "account", ncType, EncryptUtil.ace256Enc(niceResDto.getAccountNo()), niceResDto.getBankCode(), niceResDto.getName()};

                // service가 3인 경우 이름 필수x
                if("3".equals(niceResDto.getService())){
                    certKey= Arrays.copyOfRange(certKey, 0, 5);
                    certService.vdlCertInfo("C", certKey, certValue);
                }else{
                    certService.vdlCertInfo("C", certKey, certValue);

                    // 인증종류, 대리인구분, 이름
                    certKey= new String[]{"urlType", "moduType", "ncType", "name"};
                    certValue= new String[]{"compAccount", "account", ncType, niceResDto.getName()};

                    Map<String,String> vldReslt= certService.vdlCertInfo("D", certKey, certValue);
                    if(!AJAX_SUCCESS.equals(vldReslt.get("RESULT_CODE"))) {
                        throw new McpCommonJsonException("STEP01", vldReslt.get("RESULT_DESC"));
                    }
                }
            }
            // ============ STEP END ============

        } else {
            //인증실패
            //나이스 로그 기록 20160403 나이스 에로코드가있을때만 로그에 넣는다
            nicelogDto.setnResult("X");
            nicelog.insert(request , niceResDto , nicelogDto);
            rtnMap.put("RESULT_CODE", "0001");
        }
        return rtnMap;
```

**처리 로직 / 변환 처리:**

```
o NiceCertifyController.java - checkNiceAccount
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
niceCertifySvc.checkNiceAccount

        Random numGen = null;
        try {
            numGen = SecureRandom.getInstance("SHA1PRNG");
        } catch (NoSuchAlgorithmException e) {
            throw new McpCommonException("NoSuchAlgorithmException");
        }

        String result = "";
        SimpleDateFormat sdf = new SimpleDateFormat("yyyyMMdd",Locale.KOREA);
        String strOrderNo   = sdf.format(new Date()) + (Math.round(numGen.nextDouble() * 10000000000L) + "");           //주문번호 : 매 요청마다 중복되지 않도록 유의

        CommonHttpClient client = new CommonHttpClient(extUrl+"/rlnmCheck.do");
        NameValuePair[] data = {
                new NameValuePair("niceUid",NICE_UID),
                new NameValuePair("svcPwd",niceUidPassword),
                new NameValuePair("service",niceResDto.getService()),  //서비스구분 1=계좌소유주확인 2=계좌성명확인 3=계좌유효성확인
                new NameValuePair("strGbn","1"),  // 1 : 개인, 2: 사업자
                new NameValuePair("strResId",niceResDto.getResId()), //주민번호(사업자 번호,법인번호)
                new NameValuePair("strNm",StringUtil.substringByBytes(niceResDto.getName(),0,60) ), //계좌소유주명
                new NameValuePair("strBankCode",niceResDto.getBankCode()), //은행코드(전문참조)
                new NameValuePair("strAccountNo",niceResDto.getAccountNo()), //계좌번호
                new NameValuePair("svcGbn",niceResDto.getSvcGbn()),   //업무구분(전문참조)
                new NameValuePair("strOrderNo",strOrderNo),
                new NameValuePair("svc_cls",niceResDto.getSvcCls()),  //내-외국인구분
                new NameValuePair("inq_rsn","10") // 조회사유 - 10:회원가입 20:기존회원가입 30:성인인증 40:비회원확인 90:기타사유
        };
        try {
            result = client.postUtf8(data);
            //result = client.post(data, "UTF-8");
        } catch (SocketTimeoutException e) {
            throw new McpCommonException("SocketTimeoutException");
        }

        //data:"service=3&svcGbn=4&strBankCode=" + $('#reqBankTmp').val() + "&strAccountNo=" +  $('#reqAccountNumberTmp').val() + "&USERNM=" + $('#reqAccountNameTmp').val(),

       /*
        * 확인용도 주석 처리
        private String service   ; //서비스구분 1=계좌소유주확인 2=계좌성명확인 3=계좌유효성확인
        private String resId     ; //주민번호(사업자 번호,법인번호)
        private String bankCode  ; //은행코드(전문참조)
        private String accountNo ; //계좌번호
        private String svcGbn    ; //업무구분(전문참조)
        private String svcCls    ; //내-외국인구분

        service 서비스구분 1: 소유주 확인, 2: 예금주명 확인, 3: 계좌 유효성 확인
        svcGbn 업무구분 5: 소유주 확인, 2: 예금주명 확인, 4: 계좌 유효성 확인
        */
```

**Service / Dao / SQL / API:**

```
[외부] {ext.url}+"/rlnmCheck.do
```

**테이블/API:**

```
[외부] {ext.url}+"/rlnmCheck.do
```

**처리 로직 / 변환 처리:**

```
o niceCertifySvc.checkNiceAccount
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
nicelog.insert

        // TODO Auto-generated method stub


        //접속 구분 P:피씨 , M:모바일

        //파리미터로 받은 값이 있으면
        if(null!=nicelogDto.getnReferer() && !nicelogDto.getnReferer().equals("")){
            if(nicelogDto.getnReferer().indexOf("/m/")==-1){
                nicelogDto.setnCertify("P");
            }else{
                nicelogDto.setnCertify("M");
            }
        //referer 존재할때
        }else if(null!=request.getHeader("referer") && !request.getHeader("referer").equals("")){

            if(request.getHeader("referer").indexOf("/m/")==-1){
                nicelogDto.setnCertify("P");
            }else{
                nicelogDto.setnCertify("M");
            }
        }else{
            nicelogDto.setnCertify("Q");
        }

        if (nicelogDto.getnReferer() != null) {
            //호출 페이지
            String [] referer = nicelogDto.getnReferer().split(request.getServerName());
            nicelogDto.setnReferer(referer[referer.length-1]);
        }

        //인증성공시
        if(request.getHeader("X-Forwarded-For") == null) {
            nicelogDto.setnIp(request.getRemoteAddr());
        } else {
            String ip = request.getHeader("X-Forwarded-For");
            if(ip !=null && !ip.equals("") && ip.indexOf(",")>-1){
                ip =  ip.split("\\,")[0].trim();
            }
            nicelogDto.setnIp(ip);//아이피
        }

        if(null!=nicelogDto.getnAuthType() && !nicelogDto.getnAuthType().equals("")){
            nicelogDto.setnAuthType(nicelogDto.getnAuthType());//인증수단
        }else{
            nicelogDto.setnAuthType(niceDto.getAuthType());//인증수단
        }

        nicelogDto.setnName(niceDto.getName());//이름
        nicelogDto.setnGender(niceDto.getGender());//성별

        if(null!=niceDto.getBirthDate() && !niceDto.getBirthDate().trim().equals("")){
            nicelogDto.setnBirthDate(EncryptUtil.ace256Enc(niceDto.getBirthDate()));//생년월일
        }

        if(null!=niceDto.getBankCode() && !niceDto.getBankCode().trim().equals("")){
            nicelogDto.setnBankCode(niceDto.getBankCode());//은행정보
        }

        if(null!=niceDto.getAccountNo() && !niceDto.getAccountNo().trim().equals("")){
            nicelogDto.setnAccountNo(EncryptUtil.ace256Enc(niceDto.getAccountNo()));//계좌정보
        } else {
            if(!StringUtils.isBlank(niceDto.getDupInfo())){
                nicelogDto.setnAccountNo(niceDto.getDupInfo());
            }
        }
        if(null!=nicelogDto.getnResult()){
            nicelogDto.setnResult(nicelogDto.getnResult());//결과
        }else{
            nicelogDto.setnResult("O");
        }
        niceLogDao.insert(nicelogDto);
```

**Service / Dao / SQL / API:**

```
niceLogDao.insert
-> [SQL] NiceLogMapper.insertLog

    INSERT INTO NMCP_NICE_LOG (
       N_CERTIFY, N_AUTH_TYPE, N_IP,
       N_NAME, N_GENDER, N_BIRTH_DATE,
       N_BANK_CODE, N_ACCOUNT_NO, N_RESULT,
       N_ERROR_CODE, N_CRET_DT, N_REFERER)
    VALUES (
     #{nCertify} ,#{nAuthType} , #{nIp} ,
     #{nName} ,#{nGender} , #{nBirthDate} ,
     #{nBankCode} , #{nAccountNo} , #{nResult} ,
     #{nErrorCode} ,SYSDATE, #{nReferer}
     )
```

**테이블/API:** NMCP_NICE_LOG  
**처리 로직 / 변환 처리:**

```
o NiceLogMapper.insertLog
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경

-----------------------------------------

o niceLogDao.insert
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
certService.isAuthStepApplyUrl
  - 인증 개선 대상 url인지 검사

        Map<String, String> resultMap = new HashMap<>();
        resultMap.put("isAuthStep", "N");

        String redirectPage = "/main.do";
        if("Y".equals(NmcpServiceUtils.isMobile())) redirectPage = "/m/main.do";

        String requestUrl = (request.getHeader("referer") == null) ? "" : request.getHeader("referer");
        if("".equals(requestUrl)) throw new McpCommonException(COMMON_EXCEPTION, redirectPage);

        String uri = requestUrl.substring(request.getHeader("referer").indexOf("/", 8));
        int endIndex = uri.indexOf('?');
        if(endIndex > -1) uri = uri.substring(0, endIndex);

        // 인증 개선 대상 URL
        String[] appformUrlArr = {"appForm/appForm.do", "m/appForm/appForm.do"                  // 핸드폰, 상담사, 셀프개통, ESIM, ESIM WATCH
                                 ,"request/marketRequest.do", "m/request/marketRequest.do"      // 대리점개통
                                 ,"content/reqSharingView.do", "m/content/reqSharingView.do", "content/dataSharingStep3.do", "m/content/dataSharingStep3.do"   // 데이터쉐어링
                                 ,"mypage/myNameChg.do", "m/mypage/myNameChg.do"    // 명의변경 신청
                                 ,"cs/ownPhoNum.do", "m/cs/ownPhoNum.do"       // 가입번호 조회
                                 ,"m/appform/setUsimNoDlvey.do"          // 유심번호 등록 인증
                                 ,"mypage/paywayWiew.do", "m/mypage/paywayWiew.do"     // 납부방법변경
                                 ,"mypage/paywayView.do", "m/mypage/paywayView.do"     // 납부방법변경
                                 ,"mypage/usimSelfChg.do", "m/mypage/usimSelfChg.do"            // 유심 셀프변경
                                 ,"mypage/reqRwd.do", "m/mypage/reqRwd.do"                      // 자급제 보상서비스
                                 ,"appForm/appAgentForm.do", "m/appForm/appAgentForm.do"        // 대리점 셀프개통
                                 ,"appForm/appJehuForm.do", "m/appForm/appJehuForm.do"          // 제휴위탁온라인서식지
                                 ,"m/apply/replaceUsimSelf.do"
        };

        List<String> appformurlList = Arrays.asList(appformUrlArr);

        if(appformurlList.contains(uri.replaceAll("^/+",""))) resultMap.put("isAuthStep","Y");

        return resultMap;
```

**처리 로직 / 변환 처리:**

```
o certService.isAuthStepApplyUrl
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
certService.getModuTypeStepCnt
  - moduType별 step cnt

        //1.moduType 없으면 리턴
        if(StringUtils.isBlank(moduType)) return 0;
        //2.세션 CERT_SEQ 가져오고 없으면 리턴
        long crtSeq = SessionUtils.getCertSession();
        if(crtSeq == 0) return 0;

        //3.dto에 set해서 get 보내고 리턴
        CertDto reqDto = new CertDto();
        reqDto.setCrtSeq(crtSeq);
        reqDto.setModuType(moduType);
        reqDto.setNcType(ncType);
        return certDao.getModuTypeStepCnt(reqDto);
```

**Service / Dao / SQL / API:**

```
certDao.getModuTypeStepCnt
-> [SQL] CertMapper.getModuTypeStepCnt

  SELECT  /*CertMapper.getModuTypeStepCnt*/
      COUNT(DISTINCT STEP) CNT
  FROM NMCP_CRT_VLD_DTL
  WHERE 1 = 1
   AND CRT_SEQ = #{crtSeq}
   AND MODU_TYPE = #{moduType}
   AND COMP_TYPE IN ('E', 'C')
   <if test= 'ncType != null and ncType != ""'>
   AND NC_TYPE = #{ncType}
   </if>
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getModuTypeStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  

---

## [1003-13]

**JSP/JS:** /mobile/appForm/appOutForm.jsp, /resources/js/mobile/appForm/appOutForm.js  
**URL:** /crdtCardAthnInfoAjax.do  
**Controller:**

```
AppformController.java - crdtCardAthnInfo
  - 신용카드 인증조회(X91)

        HashMap<String, Object> rtnMap = new HashMap<String, Object>();
        //신용카드 인증조회(X91)
        String brthDate = appformReqDto.getReqCardRrn();
        String crdtCardNo = appformReqDto.getReqCardNo();
        try {
            brthDate = EncryptUtil.ace256Dec(brthDate);
            crdtCardNo = EncryptUtil.ace256Dec(crdtCardNo);
        } catch (CryptoException e) {
            brthDate= "";
        }
        brthDate =  NmcpServiceUtils.getSsnDate(brthDate); // YYYYMMDD 형태
        String crdtCardTermDay = appformReqDto.getReqCardYy() + appformReqDto.getReqCardMm();
        String custNm = appformReqDto.getReqCardName();

        /*logger.info("crdtCardNo==>"+crdtCardNo);
        logger.info("brthDate==>"+brthDate);
        logger.info("crdtCardTermDay==>"+crdtCardTermDay);
        logger.info("custNm==>"+custNm);*/

        MoscCrdtCardAthnInDto moscCrdtCardAthnIn = null;
        try {
            moscCrdtCardAthnIn = mPlatFormService.moscCrdtCardAthnInfo(crdtCardNo , crdtCardTermDay ,brthDate , custNm );
            if(moscCrdtCardAthnIn.isSuccess()) {
                if("Y".equals(moscCrdtCardAthnIn.getTrtResult()) ) {
                    String crdtCardKindCd = moscCrdtCardAthnIn.getCrdtCardKindCd();
                    String crdtCardKindNm = "";

                    rtnMap.put("GLOBAL_NO", moscCrdtCardAthnIn.getGlobalNo());
                    rtnMap.put("TRT_MSG", moscCrdtCardAthnIn.getTrtMsg());
                    rtnMap.put("CRDT_CARD_KIND_CD", crdtCardKindCd);
                    rtnMap.put("CRDT_CARD_NM", moscCrdtCardAthnIn.getCrdtCardNm());

                    //공통 코드 존재 여부 확인 ... getXmlMessageOP0
                    List<NmcpCdDtlDto> crdtCardList = NmcpServiceUtils.getCodeList("CRD");//카드 정보

                    if ( crdtCardList != null) {
                        for (NmcpCdDtlDto crdtCardInfo : crdtCardList) {
                            if (crdtCardInfo.getExpnsnStrVal1().equals(crdtCardKindCd)) {
                                crdtCardKindNm = crdtCardInfo.getDtlCd();
                                break;
                            }
                        }
                    }

                    if (!"".equals(crdtCardKindNm)) {

                        // ============ STEP START ============
                        String ncType= "";
                        if(request.getParameter("ncType") != null) ncType= request.getParameter("ncType");

                        // 카드 인증 이력 존재여부 확인
                        if(0 < certService.getModuTypeStepCnt("card", ncType)){
                            // 카드 관련 스텝 초기화
                            CertDto certDto = new CertDto();
                            certDto.setModuType("card");
                            certDto.setCompType("G");
                            certDto.setNcType(ncType);
                            certService.getCertInfo(certDto);
                        }

                        // 인증종류, 대리인구분, 카드번호, 카드유효기간(년도+월), 카드회사코드, 주민번호, 이름
                        String[] certKey= {"urlType", "moduType", "ncType", "reqCardNo", "crdtCardTermDay", "reqCardCompany"
                                , "birthDate", "name" };
                        String[] certValue= {"chkCard", "card", ncType, appformReqDto.getReqCardNo(), crdtCardTermDay, crdtCardKindNm
                                , appformReqDto.getReqCardRrn(), appformReqDto.getReqCardName()};

                        certService.vdlCertInfo("C", certKey, certValue);

                        // 인증종류, 대리인구분, 주민번호, 이름
                        certKey= new String[]{"urlType", "moduType", "ncType", "birthDate", "name"};
                        certValue= new String[]{"compCard", "card", ncType, appformReqDto.getReqCardRrn(), appformReqDto.getReqCardName()};

                        Map<String,String> vldReslt= certService.vdlCertInfo("D", certKey, certValue);
                        if(!AJAX_SUCCESS.equals(vldReslt.get("RESULT_CODE"))) {
                            rtnMap.put("RESULT_CODE", "STEP01");
                            rtnMap.put("ALTER_MSG", vldReslt.get("RESULT_DESC"));
                            return rtnMap;
                        }
                        // ============ STEP END ============

                        rtnMap.put("RESULT_CODE", Constants.AJAX_SUCCESS);
                        rtnMap.put("CRDT_CARD_CODE_NM", crdtCardKindNm);
                        rtnMap.put("ALTER_MSG", "신용카드 유효성 체크에 성공하였습니다.");
                    } else {
                        rtnMap.put("RESULT_CODE", "00003");
                        rtnMap.put("TRT_MSG", "공통코드(CRD) 카드 정보가 없습니다. ");
                        rtnMap.put("ALTER_MSG", "신용카드 유효성 체크에 실패하였습니다.<br/>다른 카드로 변경하여 등록 해 주세요. ");
                    }
                } else {
                    String trtMsg = moscCrdtCardAthnIn.getTrtMsg();
                    if (trtMsg.contains("주민번호")) {
                        rtnMap.put("RESULT_CODE", "00004");
                        rtnMap.put("GLOBAL_NO", moscCrdtCardAthnIn.getGlobalNo());
                        rtnMap.put("TRT_MSG", trtMsg);
                        rtnMap.put("ALTER_MSG", "최초 요금 납부등록은<br/> 가입자 본인 명의의 카드/계좌로만 가능합니다.<br/>재 확인 후 시도 바랍니다.");
                    } else {
                        rtnMap.put("RESULT_CODE", "00002");
                        rtnMap.put("GLOBAL_NO", moscCrdtCardAthnIn.getGlobalNo());
                        rtnMap.put("TRT_MSG", trtMsg);
                        rtnMap.put("ALTER_MSG", "신용카드 유효성 체크에 실패하였습니다.<br/>신용카드 정보 확인 후 다시 입력 해 주세요. ");
                    }
                }
            } else {
                //??? 연동 오류 일때?????
                rtnMap.put("RESULT_CODE", "00001");
                rtnMap.put("GLOBAL_NO", moscCrdtCardAthnIn.getGlobalNo());
                //rtnMap.put("ALTER_MSG", "처리중인 업무가 있습니다. 잠시 후 다시 시도해 주시기 바랍니다..");
                rtnMap.put("ALTER_MSG", "신용카드 유효성 체크에 실패하였습니다.<br/>신용카드 정보 확인 후 다시 입력 해 주세요.");
            }
        } catch (SocketTimeoutException e){
            // ???
            rtnMap.put("RESULT_CODE", "99999");
            rtnMap.put("TRT_MSG", SOCKET_TIMEOUT_EXCEPTION);
            rtnMap.put("ALTER_MSG", "처리중인 업무가 있습니다. 잠시 후 다시 시도해 주시기 바랍니다.");
            //throw new McpCommonException(SOCKET_TIMEOUT_EXCEPTION);
        }
        return rtnMap;
```

**처리 로직 / 변환 처리:**

```
o AppformController.java - crdtCardAthnInfo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** mPlatFormService.moscCrdtCardAthnInfo  
**Service / Dao / SQL / API:**

```
mplatFormServerAdapter.callServiceNe
-> [M플랫폼] X91
```

**테이블/API:** X91  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.moscCrdtCardAthnInfo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  

---

## [1003-15]

**JSP/JS:** /mobile/appForm/appOutForm.jsp, /resources/js/mobile/appForm/appOutForm.js  
**URL:** /m/appForm/saveAppformAjax.do  
**Controller:**

```
AppformController.java - saveAppformDb
  - 서식지 저장
  - 소스 : AppformController-saveAppformDb.java
```

**처리 로직 / 변환 처리:**

```
o AppformController.java - saveAppformDb
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

o 스마트신청서 테이블에 등록 후 m플랫폼 API 호출하여 전송 처리
  - M 전산 로직 바로 실행
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
NmcpServiceUtils.getCodeNmDto
  - 예외 요금제인지 확인
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:**

```
제외 - 판매점은 제외
공통모듈 - 공통코드 조회 / 홍성민C
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
appformSvc.getLimitForm
  - 신규개통 이력 확인
```

**Service / Dao / SQL / API:**

```
appformDao.getLimitForm
-> [M포탈-M전산 API] /appform/limitForm
```

**처리 로직 / 변환 처리:**

```
o /appform/limitForm
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** ipstatisticService.insertAdminAccessTrace  
**Service / Dao / SQL / API:**

```
fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin

        INSERT INTO MCP_ADMIN_ACCESS_TRACE (
            PARAMETER,
            ACCESS_IP,
            SYS_DT,
            SYS_RDATE,
            ACCESS_URL,
            USER_ID,
            PRCS_MDL_IND,
            PRCS_SBST,
            TRTM_RSLT_SMST
        ) VALUES (
                     #{parameter},
                     #{accessIp},
                     TO_CHAR(SYSDATE, 'YYYYMMDD'),
                     SYSDATE,
                     #{accessUrl},
                     SUBSTRB(#{userid},0,20),
                     #{prcsMdlInd},
                     SUBSTRB(#{prcsSbst},0,1000),
                     #{trtmRsltSmst}
                 )
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 호출 로그 적재  
**개발팀 전달일:** 03/22 전달  
---

**Service:** mypageService.checkAuthUser  
**처리 로직 / 변환 처리:**

```
o mypageService.checkAuthUser
  - 본인인증 로직을 신분증 인증 로직으로 변경하여 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** fnSetDataOfeSim  
**처리 로직 / 변환 처리:**

```
o fnSetDataOfeSim
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
fnSetDataOfeSim
-> appformSvc.getUploadPhoneInfo
```

**Service / Dao / SQL / API:**

```
appformDao.getUploadPhoneInfo
-> [SQL] AppformMapper.getUploadPhoneInfo
```

**테이블/API:** MCP_UPLOAD_PHONE_INFO  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getUploadPhoneInfo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
fnSetDataOfeSim
-> appformSvc.getCopyMcpRequest
```

**Service / Dao / SQL / API:**

```
appformDao.getCopyMcpRequest
-> [SQ] AppformMapper.getCopyMcpRequest
```

**테이블/API:** MCP_REQUEST, MCP_REQUEST_CSTMR, MCP_REQUEST_REQ, MCP_REQUEST_SALEINFO, MCP_REQUEST_AGENT  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getCopyMcpRequest
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
fnSetDataOfeSim
-> myinfoService.kosMoscBillInfo
```

**Service / Dao / SQL / API:**

```
mPlatFormService.kosMoscBillInfo
-> mplatFormServerAdapter.callService
-> [M플랫폼] X49
```

**테이블/API:** X49  
**처리 로직 / 변환 처리:**

```
o myinfoService.kosMoscBillInfo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** eventCodeSvc.getEventchk  
**기타:** 제외 - 이벤트 제외  
**개발팀 전달일:** 03/22 전달  
---

**Service:** appformSvc.getUsimOrgnId  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /msp/sellUsimMgmtOrgnId  
**처리 로직 / 변환 처리:**

```
o /msp/sellUsimMgmtOrgnId
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 사은품  
**개발팀 전달일:** 03/22 전달  
---

**Service:** giftSvc.giftBasList  
**기타:** 제외 - 사은품  
**개발팀 전달일:** 03/22 전달  
---

**Service:** NmcpServiceUtils.getCodeNm  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNm
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** [M포탈-M전산 API] /appform/checkLimitOpenFormCount  
**처리 로직 / 변환 처리:**

```
o /appform/checkLimitOpenFormCount
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** ipstatisticService.insertAdminAccessTrace  
**Service / Dao / SQL / API:**

```
fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin

        INSERT INTO MCP_ADMIN_ACCESS_TRACE (
            PARAMETER,
            ACCESS_IP,
            SYS_DT,
            SYS_RDATE,
            ACCESS_URL,
            USER_ID,
            PRCS_MDL_IND,
            PRCS_SBST,
            TRTM_RSLT_SMST
        ) VALUES (
                     #{parameter},
                     #{accessIp},
                     TO_CHAR(SYSDATE, 'YYYYMMDD'),
                     SYSDATE,
                     #{accessUrl},
                     SUBSTRB(#{userid},0,20),
                     #{prcsMdlInd},
                     SUBSTRB(#{prcsSbst},0,1000),
                     #{trtmRsltSmst}
                 )
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 호출 로그 적재  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
appformSvc.containsGoldNumbers

    public void containsGoldNumbers(List<String> reqWantNumbers) {
        NmcpCdDtlDto nmcpCdDtlDto = new NmcpCdDtlDto();
        nmcpCdDtlDto.setCdGroupId("GoldNumberList");
        List<String> goldNumberList = fCommonSvc.getCodeList(nmcpCdDtlDto).stream()
                .map(NmcpCdDtlDto::getDtlCd)
                .collect(Collectors.toList());

        List<String> filteredNumbers = reqWantNumbers.stream()
                .filter(goldNumberList::contains)
                .collect(Collectors.toList());

        if (!filteredNumbers.isEmpty()) {
            StringBuilder sb = new StringBuilder();
            sb.append("[").append(String.join(", ", filteredNumbers)).append("]<br>");
            sb.append(ExceptionMsgConstant.CONTAINS_GOLD_NUMBER_EXCEPTION);
            throw new McpCommonJsonException("9999", sb.toString());
        }
    }
```

**처리 로직 / 변환 처리:**

```
o appformSvc.containsGoldNumbers
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
fCommonSvc.getCodeList
-> fCommonDao.getCodeList
-> [SQL] CommCodeMapper.getCodeList
```

**테이블/API:** NMCP_CD_DTL  
**처리 로직 / 변환 처리:**

```
o CommCodeMapper.getCodeList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/22 전달  
---

**Service:** usimService.getSimInfo  
**처리 로직 / 변환 처리:**

```
o usimService.getSimInfo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
selectJoinUsimPriceNew
-> [M포탈-M전산 API] /storeUsim/joinUsimPriceNew
```

**처리 로직 / 변환 처리:**

```
o /storeUsim/joinUsimPriceNew
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** ipstatisticService.insertAdminAccessTrace  
**Service / Dao / SQL / API:**

```
fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin

        INSERT INTO MCP_ADMIN_ACCESS_TRACE (
            PARAMETER,
            ACCESS_IP,
            SYS_DT,
            SYS_RDATE,
            ACCESS_URL,
            USER_ID,
            PRCS_MDL_IND,
            PRCS_SBST,
            TRTM_RSLT_SMST
        ) VALUES (
                     #{parameter},
                     #{accessIp},
                     TO_CHAR(SYSDATE, 'YYYYMMDD'),
                     SYSDATE,
                     #{accessUrl},
                     SUBSTRB(#{userid},0,20),
                     #{prcsMdlInd},
                     SUBSTRB(#{prcsSbst},0,1000),
                     #{trtmRsltSmst}
                 )
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 호출 로그 적재  
**개발팀 전달일:** 03/22 전달  
---

**Service:** ipstatisticService.insertAdminAccessTrace  
**Service / Dao / SQL / API:**

```
fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin

        INSERT INTO MCP_ADMIN_ACCESS_TRACE (
            PARAMETER,
            ACCESS_IP,
            SYS_DT,
            SYS_RDATE,
            ACCESS_URL,
            USER_ID,
            PRCS_MDL_IND,
            PRCS_SBST,
            TRTM_RSLT_SMST
        ) VALUES (
                     #{parameter},
                     #{accessIp},
                     TO_CHAR(SYSDATE, 'YYYYMMDD'),
                     SYSDATE,
                     #{accessUrl},
                     SUBSTRB(#{userid},0,20),
                     #{prcsMdlInd},
                     SUBSTRB(#{prcsSbst},0,1000),
                     #{trtmRsltSmst}
                 )
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 호출 로그 적재  
**개발팀 전달일:** 03/22 전달  
---

**Service:** ipstatisticService.insertAdminAccessTrace  
**Service / Dao / SQL / API:**

```
fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin

        INSERT INTO MCP_ADMIN_ACCESS_TRACE (
            PARAMETER,
            ACCESS_IP,
            SYS_DT,
            SYS_RDATE,
            ACCESS_URL,
            USER_ID,
            PRCS_MDL_IND,
            PRCS_SBST,
            TRTM_RSLT_SMST
        ) VALUES (
                     #{parameter},
                     #{accessIp},
                     TO_CHAR(SYSDATE, 'YYYYMMDD'),
                     SYSDATE,
                     #{accessUrl},
                     SUBSTRB(#{userid},0,20),
                     #{prcsMdlInd},
                     SUBSTRB(#{prcsSbst},0,1000),
                     #{trtmRsltSmst}
                 )
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 호출 로그 적재  
**개발팀 전달일:** 03/22 전달  
---

**Service:** certService.getModuTypeStepCnt  
**Service / Dao / SQL / API:**

```
certDao.getModuTypeStepCnt
-> [SQL] CertMapper.getModuTypeStepCnt

  SELECT  /*CertMapper.getModuTypeStepCnt*/
      COUNT(DISTINCT STEP) CNT
  FROM NMCP_CRT_VLD_DTL
  WHERE 1 = 1
   AND CRT_SEQ = #{crtSeq}
   AND MODU_TYPE = #{moduType}
   AND COMP_TYPE IN ('E', 'C')
   <if test= 'ncType != null and ncType != ""'>
   AND NC_TYPE = #{ncType}
   </if>
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getModuTypeStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
certService.getCertInfo
  - 인증정보 비교해서 map으로 리턴
```

**처리 로직 / 변환 처리:**

```
o certService.getCertInfo
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 본인인증  
**개발팀 전달일:** 03/22 전달  
---

**Service:** ipstatisticService.insertAdminAccessTrace  
**Service / Dao / SQL / API:**

```
fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin

        INSERT INTO MCP_ADMIN_ACCESS_TRACE (
            PARAMETER,
            ACCESS_IP,
            SYS_DT,
            SYS_RDATE,
            ACCESS_URL,
            USER_ID,
            PRCS_MDL_IND,
            PRCS_SBST,
            TRTM_RSLT_SMST
        ) VALUES (
                     #{parameter},
                     #{accessIp},
                     TO_CHAR(SYSDATE, 'YYYYMMDD'),
                     SYSDATE,
                     #{accessUrl},
                     SUBSTRB(#{userid},0,20),
                     #{prcsMdlInd},
                     SUBSTRB(#{prcsSbst},0,1000),
                     #{trtmRsltSmst}
                 )
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 호출 로그 적재  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** [M포탈-M전산 API] /msp/mspSaleSubsdMstList  
**처리 로직 / 변환 처리:**

```
o /msp/mspSaleSubsdMstList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
phoneService.findPhoneProdBasDtoByProdId
  - 상품및 단품정보 조회 상품대표이미지,용량색상별 정보 등을 조회
```

**Service / Dao / SQL / API:**

```
phoneDao.findPhoneProd
-> [M포탈-M전산 API] /phone/nmcpProdBas
```

**처리 로직 / 변환 처리:**

```
o /phone/nmcpProdBas
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** NmcpServiceUtils.getCodeNmDto  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/22 전달  
---

**Service:** NmcpServiceUtils.getCodeNmDto  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
NmcpServiceUtils.getCodeNmDto
- BE0020
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 제외 - 카드혜택 제외  
**개발팀 전달일:** 03/22 전달  
---

**Service:** ipstatisticService.insertAdminAccessTrace  
**Service / Dao / SQL / API:**

```
fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin

        INSERT INTO MCP_ADMIN_ACCESS_TRACE (
            PARAMETER,
            ACCESS_IP,
            SYS_DT,
            SYS_RDATE,
            ACCESS_URL,
            USER_ID,
            PRCS_MDL_IND,
            PRCS_SBST,
            TRTM_RSLT_SMST
        ) VALUES (
                     #{parameter},
                     #{accessIp},
                     TO_CHAR(SYSDATE, 'YYYYMMDD'),
                     SYSDATE,
                     #{accessUrl},
                     SUBSTRB(#{userid},0,20),
                     #{prcsMdlInd},
                     SUBSTRB(#{prcsSbst},0,1000),
                     #{trtmRsltSmst}
                 )
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 호출 로그 적재  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
mypageService.selectCntrList
  - 회선정보
```

**Service / Dao / SQL / API:**

```
mypageService.selectCntrList
-> [M포탈-M전산 API] /mypage/cntrList
```

**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 회원 로직 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service:** myBenefitService.selectCustPoint  
**기타:** 제외 - 포인트 제외  
**개발팀 전달일:** 03/22 전달  
---

**Service:** ipstatisticService.getClientIp  
**처리 로직 / 변환 처리:**

```
o ipstatisticService.getClientIp
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
appformSvc.getAgentCode
  - 대리점 코드 조회
```

**Service / Dao / SQL / API:**

```
appformDao.getAgentCode
-> [M포탈-M전산 API] /appform/agentCode
```

**처리 로직 / 변환 처리:**

```
o /appform/agentCode
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** appformSvc.getDisPrmtId  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /appform/getDisPrmtId  
**처리 로직 / 변환 처리:**

```
o /appform/getDisPrmtId
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** [M포탈-M전산 API] /msp/juoSubIngoCount  
**처리 로직 / 변환 처리:**

```
o /msp/juoSubIngoCount
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** certService.updateCrtReferer  
**Service / Dao / SQL / API:**

```
certDao.updateCrtReferer
-> [SQL] CertMapper.updateCrtReferer
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.updateCrtReferer
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** certService.getModuTypeStepCnt  
**Service / Dao / SQL / API:**

```
certDao.getModuTypeStepCnt
-> [SQL] CertMapper.getModuTypeStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getModuTypeStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
certService.getCertInfo
  - 인증정보 비교해서 map으로 리턴
```

**처리 로직 / 변환 처리:**

```
o certService.getCertInfo
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 본인인증  
**개발팀 전달일:** 03/22 전달  
---

**Service:** certService.getModuTypeStepCnt  
**Service / Dao / SQL / API:**

```
certDao.getModuTypeStepCnt
-> [SQL] CertMapper.getModuTypeStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getModuTypeStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
certService.getCertInfo
  - 인증정보 비교해서 map으로 리턴
```

**처리 로직 / 변환 처리:**

```
o certService.getCertInfo
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 본인인증  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
appformSvc.crtUpdateSimpleAppFormInfo
  - 셀프개통
- 소스 : AppformSvcImpl-필요부분.java
```

**처리 로직 / 변환 처리:**

```
o appformSvc.crtUpdateSimpleAppFormInfo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
certService.getModuTypeStepCnt
-> certDao.getModuTypeStepCnt
-> [SQL] CertMapper.getModuTypeStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getModuTypeStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 본인인증  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
appformSvc.crtSaveAppFormInfo
  - 셀프개통 이외
- 소스 : AppformSvcImpl-필요부분.java
```

**처리 로직 / 변환 처리:**

```
o appformSvc.crtSaveAppFormInfo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
certService.getModuTypeStepCnt
-> certDao.getModuTypeStepCnt
-> [SQL] CertMapper.getModuTypeStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getModuTypeStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 본인인증  
**개발팀 전달일:** 03/22 전달  
---

**Service:** appformSvc.saveAppform  
**처리 로직 / 변환 처리:**

```
o appformSvc.saveAppform
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

o M전산 M플랫폼 전송 API 호출로 대체
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.generateRequestKey
-> [SQL] AppformMapper.generateRequestKey
```

**테이블/API:** SQ_RCP_REQUEST_KEY_01  
**처리 로직 / 변환 처리:**

```
o AppformMapper.generateRequestKey
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
certService.getModuTypeStepCnt
-> certDao.getModuTypeStepCnt
-> [SQL] CertMapper.getModuTypeStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getModuTypeStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
certService.getCertInfo
  - 인증정보 비교해서 map으로 리턴
```

**처리 로직 / 변환 처리:**

```
o certService.getCertInfo
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 본인인증  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
crtSaveAppFormStep
  - 상담사 개통 최종 step 확인
```

**처리 로직 / 변환 처리:**

```
o crtSaveAppFormStep
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

o M전산 M플랫폼 전송 API 호출로 대체
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
crtSaveAppFormStep
-> certService.getModuTypeStepCnt
-> certDao.getModuTypeStepCnt
-> [SQL] CertMapper.getModuTypeStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getModuTypeStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 본인인증  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.generateResNo
-> [SQL] AppformMapper.generateResNo
```

**테이블/API:** SQ_RCP_RES_NO_01  
**처리 로직 / 변환 처리:**

```
o AppformMapper.generateResNo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경 : 임시등록 신청서일련번호 SQ_RCP_RES_NO_01 사용하지 않고 실제 신청서 일련번호와 같이 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** fathService.validateFathSession  
**처리 로직 / 변환 처리:**

```
o fathService.validateFathSession
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** fathService.updateFathMcpRequestOsst  
**처리 로직 / 변환 처리:**

```
o fathService.updateFathMcpRequestOsst
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
fathService.updateFathMcpRequestOsst
-> fathDao.updateFathMcpRequestOsst
-> [SQL] FathMapper.updateFathMcpRequestOsst
```

**테이블/API:** MCP_REQUEST_OSST  
**처리 로직 / 변환 처리:**

```
o FathMapper.updateFathMcpRequestOsst
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.getMspPrdtCode
-> [M포탈-M전산 API] /appform/mspPrdtCode
```

**처리 로직 / 변환 처리:**

```
o /appform/mspPrdtCode
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.getMspPrdtCode
-> [M포탈-M전산 API] /appform/mspPrdtCode
```

**처리 로직 / 변환 처리:**

```
o /appform/mspPrdtCode
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** NmcpServiceUtils.getCodeList(Constants.USIM_PROD_DTL_ID_GROP_CODE)  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** insertRequestSelfDlvry  
**처리 로직 / 변환 처리:**

```
o insertRequestSelfDlvry
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
insertRequestSelfDlvry
-> appformDao.getRequestSelfDlvrKey
-> [SQL] AppformMapper.getRequestSelfDlvrKey
```

**테이블/API:** SQ_REQUEST_SELF_DLVR_KEY  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getRequestSelfDlvrKey
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
insertRequestSelfDlvry
-> appformDao.insertRequestSelfDlvry
-> [SQL] AppformMapper.insertRequestSelfDlvry
```

**테이블/API:** MCP_REQUEST_SELF_DLVRY  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertRequestSelfDlvry
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** NmcpServiceUtils.getCodeNmDto(JEHU_AGENT_LIST  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** NmcpServiceUtils.getCodeNmDto("AcenLimit", "useLimit")  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:**

```
제외 - 상담사 상담
공통모듈 - 공통코드 조회 / 홍성민C
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
chkAcenReqCondition
  - Acen 연동 대상 조건 확인
```

**처리 로직 / 변환 처리:**

```
o chkAcenReqCondition
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 상담사 상담  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
chkAcenReqCondition
-> NmcpServiceUtils.getCodeNmDto("AcenLimit", "useEsim")
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:**

```
제외 - 상담사 상담
공통모듈 - 공통코드 조회 / 홍성민C
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
insertAcenReqTrg
  - Acen 연동 대상 insert
```

**처리 로직 / 변환 처리:**

```
o insertAcenReqTrg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 상담사 상담  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
insertAcenReqTrg
-> appformDao.insertAcenReqTrg
-> [SQL] AppformMapper.insertAcenReqTrg
```

**테이블/API:** NMCP_ACEN_REQ  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertAcenReqTrg
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 상담사 상담  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequest
-> [SQL] AppformMapper.insertMcpRequestOsst
```

**테이블/API:** MCP_REQUEST_OSST  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestOsst
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestCstmr
-> [SQL] AppformMapper.insertMcpRequestCstmr
```

**테이블/API:** MCP_REQUEST_CSTMR  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestCstmr
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestAgent
-> [SQL] AppformMapper.insertMcpRequestAgent
```

**테이블/API:** MCP_REQUEST_AGENT  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestAgent
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestMove
-> [SQL] AppformMapper.insertMcpRequestMove
```

**테이블/API:** MCP_REQUEST_MOVE  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestMove
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestPayment
-> [SQL] AppformMapper.insertMcpRequestPayment
```

**테이블/API:** MCP_REQUEST_PAYMENT  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestPayment
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 선불  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestSaleinfo
-> [SQL] AppformMapper.insertMcpRequestSaleinfo
```

**테이블/API:** MCP_REQUEST_SALEINFO  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestSaleinfo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestChange
-> [SQL] AppformMapper.insertMcpRequestChange
```

**테이블/API:** MCP_REQUEST_CHANGE  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestChange
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
-> MCP_REQUEST_CHANGE : 해당 테이블 미사용하고, 명의변경 관련 테이블로 통합 처리
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestDlvry
-> [SQL] AppformMapper.insertMcpRequestDlvry
```

**테이블/API:** MCP_REQUEST_DLVRY  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestDlvry
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestReq
-> [SQL] AppformMapper.insertMcpRequestReq
```

**테이블/API:** MCP_REQUEST_REQ  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestReq
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestPayInfo
-> [SQL] AppformMapper.insertMcpRequestPayInfo
```

**테이블/API:** MCP_REQUEST_PAY_INFO  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestPayInfo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 결제 제외  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestDvcChg
-> [SQL] AppformMapper.insertMcpRequestDvcChg
```

**테이블/API:** MCP_REQUEST_DVC_CHG  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestDvcChg
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestAddition
-> [SQL] AppformMapper.insertMcpRequestAddition
```

**테이블/API:** MCP_REQUEST_ADDITION  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestAddition
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestKtInter
-> [SQL] AppformMapper.insertMcpRequestKtInter
```

**테이블/API:** MCP_REQUEST_KT_INTER  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestKtInter
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 유선 결합  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.checkGiftReqCount
-> [SQL] AppformMapper.checkGiftReqCount
```

**테이블/API:** NMCP_GIFT_REQ_TXN  
**처리 로직 / 변환 처리:**

```
o AppformMapper.checkGiftReqCount
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 사은품  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertGiftReqTxn
-> [SQL] AppformMapper.insertGiftReqTxn
```

**테이블/API:** NMCP_GIFT_REQ_TXN  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertGiftReqTxn
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 사은품  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.checkGiftReqCount
-> [SQL] AppformMapper.checkGiftReqCount
```

**테이블/API:** NMCP_GIFT_REQ_TXN  
**처리 로직 / 변환 처리:**

```
o AppformMapper.checkGiftReqCount
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 사은품  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertGiftReqTxn
-> [SQL] AppformMapper.insertGiftReqTxn
```

**테이블/API:** NMCP_GIFT_REQ_TXN  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertGiftReqTxn
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 사은품  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestAdditionPromotion
-> [M포탈-M전산 API] /appform/mcpRequestAdditionPromotion
```

**처리 로직 / 변환 처리:**

```
o /appform/mcpRequestAdditionPromotion
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestState
-> [SQL] AppformMapper.insertMcpRequestState
```

**테이블/API:** MCP_REQUEST_STATE  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestState
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertNmcpRequestApd
-> [SQL] AppformMapper.insertNmcpRequestApd
```

**테이블/API:** NMCP_REQUEST_APD  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertNmcpRequestApd
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 자급제 제외  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertNmcpRequestApdDlvry
-> [SQL] AppformMapper.insertNmcpRequestApdDlvry
```

**테이블/API:** NMCP_REQUEST_APD_DLVRY  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertNmcpRequestApdDlvry
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 자급제 제외  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertNmcpRequestApdSaleinfo
-> [SQL] AppformMapper.insertNmcpRequestApdSaleinfo
```

**테이블/API:** NMCP_REQUEST_APD_SALEINFO  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertNmcpRequestApdSaleinfo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 자급제 제외  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertNmcpRequestApdState
-> [SQL] AppformMapper.insertNmcpRequestApdState
```

**테이블/API:** NMCP_REQUEST_APD_STATE  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertNmcpRequestApdState
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 자급제 제외  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** pointService.editPoint  
**기타:** 제외 - 포인트 제외  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** pointService.editPoint  
**기타:** 제외 - 포인트 제외  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestCommend
-> [SQL] AppformMapper.insertMcpRequestCommend
```

**테이블/API:** MCP_REQUEST_COMMEND  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestCommend
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 추천인 제외  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.updateMcpRequestNowDlvryHist
-> [SQL] AppformMapper.updateMcpRequestNowDlvryHist
```

**테이블/API:** MCP_REQUEST_NOW_DLVRY_HIST  
**처리 로직 / 변환 처리:**

```
o AppformMapper.updateMcpRequestNowDlvryHist
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.getMcpReqDtl
-> [SQL] AppformMapper.getMcpReqDtl
```

**테이블/API:** MCP_REQUEST REQ, MCP_REQUEST_CSTMR, MCP_REQUEST_SALEINFO, MCP_REQUEST_MOVE, MCP_REQUEST_DLVRY  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getMcpReqDtl
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
  - MCP_REQUEST_DLVRY : 배송 관련 로직은 제외
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.chkMcpReqDtl
-> [SQL] AppformMapper.chkMcpReqDtl
```

**테이블/API:** MCP_REQUEST_DTL  
**처리 로직 / 변환 처리:**

```
o AppformMapper.chkMcpReqDtl
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
-> MCP_REQUEST_DTL : 해당 테이블 미사용하고 각 신청서 관련 테이블 데이터로 등록 처리
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.updateMcpReqDtl
-> [SQL] AppformMapper.updateMcpReqDtl
```

**테이블/API:** MCP_REQUEST_DTL  
**처리 로직 / 변환 처리:**

```
o AppformMapper.updateMcpReqDtl
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
-> MCP_REQUEST_DTL : 해당 테이블 미사용하고 각 신청서 관련 테이블 데이터로 등록 처리
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpReqDtl
-> [SQL] AppformMapper.insertMcpReqDtl
```

**테이블/API:** MCP_REQUEST_DTL  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpReqDtl
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
-> MCP_REQUEST_DTL : 해당 테이블 미사용하고 각 신청서 관련 테이블 데이터로 등록 처리
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertKtCounsel
-> [SQL] AppformMapper.insertKtCounsel
```

**테이블/API:** NMCP_KT_COUNSEL_REQ  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertKtCounsel
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 상담사 상담  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
NmcpServiceUtils.getCodeList(Constants.SEND_LMS_CNTPNT_GROP_CODE)
  - SMS발송 접점코드
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/22 전달  
---

**Service:** fCommonSvc.getMspSmsTemplateMst  
**Service / Dao / SQL / API:**

```
fCommonSvc.getMspSmsTemplateMst
-> fCommonDao.getMspSmsTemplateMst
-> [M포탈-M전산 API] /common/mspSmsTemplateMst
```

**처리 로직 / 변환 처리:**

```
o /common/mspSmsTemplateMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** smsSvc.sendKakaoNoti  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /sms/addKakaoNoti  
**처리 로직 / 변환 처리:**

```
o /sms/addKakaoNoti
-> 현행 ITO API 그대로 사용 : M전산/M포탈 테이블 INSERT/UPDATE/DELETE
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** NmcpServiceUtils.getCodeList(Constants.SEND_SMS_CNTPNT_AGENT_CODE)  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/22 전달  
---

**Service:** fCommonSvc.getMspSmsTemplateMst  
**Service / Dao / SQL / API:**

```
fCommonSvc.getMspSmsTemplateMst
-> fCommonDao.getMspSmsTemplateMst
-> [M포탈-M전산 API] /common/mspSmsTemplateMst
```

**처리 로직 / 변환 처리:**

```
o /common/mspSmsTemplateMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** smsSvc.sendLms  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /sms/addNewSms  
**처리 로직 / 변환 처리:**

```
o /sms/addNewSms
-> 현행 ITO API 그대로 사용 : M전산/M포탈 테이블 INSERT/UPDATE/DELETE
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** ipstatisticService.insertAdminAccessTrace  
**Service / Dao / SQL / API:**

```
fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin

        INSERT INTO MCP_ADMIN_ACCESS_TRACE (
            PARAMETER,
            ACCESS_IP,
            SYS_DT,
            SYS_RDATE,
            ACCESS_URL,
            USER_ID,
            PRCS_MDL_IND,
            PRCS_SBST,
            TRTM_RSLT_SMST
        ) VALUES (
                     #{parameter},
                     #{accessIp},
                     TO_CHAR(SYSDATE, 'YYYYMMDD'),
                     SYSDATE,
                     #{accessUrl},
                     SUBSTRB(#{userid},0,20),
                     #{prcsMdlInd},
                     SUBSTRB(#{prcsSbst},0,1000),
                     #{trtmRsltSmst}
                 )
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 호출 로그 적재  
**개발팀 전달일:** 03/22 전달  

---

## [1003-14]

**JSP/JS:**

```
/mobile/appForm/appOutForm.jsp, /resources/js/mobile/appForm/appOutForm.js, 
/resources/js/mobile/appForm/eSimFormDesignView.js
```

**URL:** /getOcrImgReadyAjax.do  
**Controller:**

```
OcrController.java - getOcrImgReadyAjax

        Map<String,Object> hm = new HashMap<String, Object>();
        String retCode = "99";
        String retMsg = "오류가 발생했습니다. 다시 시도해 주세요.";
        String url = OCRIMGserver.concat("/api/v3/imei/ocr");
        // 1. 파일관련되서 로직 추가
        MultipartFile file = fileRequest.getFile("image");
        if(file ==null ) {
            retCode = "99";
            retMsg = "이미지파일을 확인해 주세요.";
            hm.put("retCode", retCode);
            hm.put("retMsg", retMsg);
            return hm;
        }
        String originalFilename = file.getOriginalFilename();
        JSONObject jObject = null;
        int status = -1;
        String message = "";
        String eid = "";
        String imei1 = ""; // 안드로이드
        String imei2 = "";
//  String meid = "";
        int statusCode = 0;
        int uploadPhoneSrlNo = 0;

        OkHttpClient client = new OkHttpClient().newBuilder().build();
        Response response = null;
        try {

            RequestBody requestBody = new MultipartBody.Builder().setType(MultipartBody.FORM)
                    .addFormDataPart("image", originalFilename, RequestBody.create(MultipartBody.FORM, file.getBytes()))
                    .build();
            Request request = new Request.Builder().url(url)
                    .post(requestBody)
                    .addHeader("Content-Type", "application/json")
                    .build();
            response = client.newCall(request).execute();

            if(response !=null) {
                if(response.isSuccessful()) {
                    statusCode = response.code();
                    jObject = new JSONObject(response.body().string());
                }
                response.body().close();
            }


        } catch(IOException e){
            logger.error("Exception 111 e : {}", e.getMessage());
        } catch(Exception e) {
            logger.error("Exception 2222 e : {}", e.getMessage());
        } finally {
            if (response != null ) {
                response.close();
            }
        }

        // 3.데이터 검증
        try {
            logger.info("responseStrJson==>"+ObjectUtils.convertObjectToString(jObject));
            logger.info("statusCode==>"+statusCode);

//         JSONObject jObject = new JSONObject(response.body().string());

            if(statusCode==200) {
                retCode = "0000";
                retMsg = "";

                try {
                    status = jObject.getInt("status");
                } catch(JSONException e){
                    logger.info("status JSONException");
                } catch(Exception e) {
                    logger.info("status error");
                }

                try {
                    message = jObject.getString("message");
                } catch(JSONException e){
                    logger.info("message JSONException");
                } catch(Exception e) {
                    logger.info("message error");
                }

                JSONObject phoneDataObject = null;

                try {
                    phoneDataObject = (JSONObject) jObject.get("result");
                } catch(JSONException e){
                    logger.info("result JSONException");
                } catch(Exception e) {
                    logger.info("result error");
                }

                if(phoneDataObject !=null){
                    try {
                        eid = StringUtil.onlyNum(phoneDataObject.getString("eid"));
                    } catch(JSONException e){
                        logger.info("eid JSONException");
                    } catch(Exception e) {
                        logger.info("eid error");
                    }

                    try {
                        imei1 = StringUtil.NVL(phoneDataObject.getString("imei1"),"");
                    } catch(JSONException e){
                        logger.info("imei1 JSONException");
                    } catch(Exception e) {
                        logger.info("imei1 error");
                    }
                    try {
                        imei2 = StringUtil.NVL(phoneDataObject.getString("imei2"),"");
                    } catch(JSONException e){
                        logger.info("imei2 JSONException");
                    } catch(Exception e) {
                        logger.info("imei2 error");
                    }
//        meid = phoneDataObject.getString("meid");
                }

            } else {
                retCode = "0002";
                message = jObject.getString("message");
                retMsg = "eSIM 개통을 위해서는 EID 및 IMEI2값이 필요합니다.<br/>eSIM 지원 기기 여부를 확인 바랍니다.";
            }

        } catch(JSONException e){
            retMsg = e.getMessage();
            retMsg = "이미지 파일을 확인 해 주세요.";
            retCode = "98";
        } catch(Exception e) {
            retMsg = e.getMessage();
            retMsg = "이미지 파일을 확인 해 주세요.";
            retCode = "99";
        }

        hm.put("retCode", retCode);
        hm.put("retMsg", retMsg);
        hm.put("eid", eid);
        hm.put("imei1", imei1);
        hm.put("imei2", imei2);
        hm.put("uploadPhoneSrlNo", uploadPhoneSrlNo);
        return hm;
```

**처리 로직 / 변환 처리:**

```
o OcrController.java - getOcrImgReadyAjax
  - 신규 OCR 솔루션에 맞춰서 M포탈/M전산 소스 로직 스마트신청서App 에 적용

ㅇ ITO AS-IS 소스에 로직 변경
 - 신규 OCR 솔루션에 맞춰서 로직 변경
```

**개발팀 전달일:** 03/22 전달  
---

**JSP/JS:** /resources/js/mobile/appForm/eSimFormDesignView.js  
**URL:** /m/getOcrReadyAjax.do  
**Controller:** OcrController.java - getOcrReadyAjax  
**처리 로직 / 변환 처리:**

```
o OcrController.java - getOcrReadyAjax
  - 신규 OCR 솔루션에 맞춰서 M포탈/M전산 소스 로직 스마트신청서App 에 적용

ㅇ ITO AS-IS 소스에 로직 변경
 - 신규 OCR 솔루션에 맞춰서 로직 변경
```

**개발팀 전달일:** 03/22 전달  
---

**JSP/JS:** /resources/js/mobile/appForm/eSimFormDesignView.js  
**URL:** /m/getOcrResultAjax.do  
**Controller:** OcrController.java - getOcrResultAjax  
**처리 로직 / 변환 처리:**

```
o OcrController.java - getOcrResultAjax
  - 신규 OCR 솔루션에 맞춰서 M포탈/M전산 소스 로직 스마트신청서App 에 적용

ㅇ ITO AS-IS 소스에 로직 변경
 - 신규 OCR 솔루션에 맞춰서 로직 변경
```

**개발팀 전달일:** 03/22 전달  

---

## [1003-22]

**JSP/JS:**

```
/mobile/appForm/appFormDesignView.jsp, /resources/js/mobile/appForm/appFormDesignView.js

- <c:if test="${prdtIndCd eq '10'}"> 영역
```

**URL:** /m/appForm/appFormDesignView.do?prdtIndCd=10  
**처리 로직 / 변환 처리:**

```
o 스크립트 로직
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**JSP/JS:** /resources/js/mobile/appForm/appFormDesignView.js  
**URL:** /m/getOcrEidReadyAjax.do  
**Controller:** OcrController.java - getOcrEidReadyAjax  
**처리 로직 / 변환 처리:**

```
o OcrController.java - getOcrEidReadyAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**JSP/JS:** /resources/js/mobile/appForm/appFormDesignView.js  
**URL:** /appForm/eSimChkAjax.do  
**Controller:** EsimController.java - eSimChkAjax  
**처리 로직 / 변환 처리:**

```
o EsimController.java - eSimChkAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** eSimSvc.checkAbuseImeiList  
**Service / Dao / SQL / API:**

```
existsAbuseImei
-> [M포탈-M전산 API] /appform/existsAbuseImei
```

**처리 로직 / 변환 처리:**

```
o /appform/existsAbuseImei
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
saveAbuseImeiHist
-> ipstatisticService.getClientIp
```

**처리 로직 / 변환 처리:**

```
o ipstatisticService.getClientIp
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
saveAbuseImeiHist
-> esimDao.insertAbuseImeiHist
-> [SQL] EsimMapper.insertAbuseImeiHist
```

**테이블/API:** NMCP_ABUSE_IMEI_HIST  
**처리 로직 / 변환 처리:**

```
o EsimMapper.insertAbuseImeiHist
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** mcpUploadPhoneInfo  
**Service / Dao / SQL / API:** ipstatisticService.getClientIp  
**처리 로직 / 변환 처리:**

```
o ipstatisticService.getClientIp
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
eSimSvc.insertMcpUploadPhoneInfoDto
-> esimDao.insertMcpUploadPhoneInfoDto
-> [SQL] EsimMapper.insertMcpUploadPhoneInfoDto
```

**테이블/API:** MCP_UPLOAD_PHONE_INFO  
**처리 로직 / 변환 처리:**

```
o EsimMapper.insertMcpUploadPhoneInfoDto
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** eSimSvc.eSimChk  
**처리 로직 / 변환 처리:**

```
o eSimSvc.eSimChk
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** getY13  
**처리 로직 / 변환 처리:**

```
o getY13
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
getY13
-> mplatFormService.moscRetvIntmOrrgInfo
```

**테이블/API:** Y13  
**처리 로직 / 변환 처리:**

```
o mplatFormService.moscRetvIntmOrrgInfo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
getY13
-> esimDao.insertMcpEsimOmdTrace
-> [SQL] EsimMapper.insertMcpEsimOmdTrace
```

**테이블/API:** MCP_ESIM_OMD_TRACE  
**처리 로직 / 변환 처리:**

```
o EsimMapper.insertMcpEsimOmdTrace
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** getY12  
**처리 로직 / 변환 처리:**

```
o getY12
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
getY12
-> mplatFormService.moscRetvIntmMdlSpecInfo
```

**테이블/API:** Y12  
**처리 로직 / 변환 처리:**

```
o mplatFormService.moscRetvIntmMdlSpecInfo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
getY12
-> esimDao.insertMcpEsimOmdTrace
-> [SQL] EsimMapper.insertMcpEsimOmdTrace
```

**테이블/API:** MCP_ESIM_OMD_TRACE  
**처리 로직 / 변환 처리:**

```
o EsimMapper.insertMcpEsimOmdTrace
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** getY14  
**처리 로직 / 변환 처리:**

```
o getY14
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
getY14
-> mplatFormService.moscBfacChkOmdIntm
```

**테이블/API:** Y14  
**처리 로직 / 변환 처리:**

```
o mplatFormService.moscBfacChkOmdIntm
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
getY14
->esimDao.insertMcpEsimOmdTrace
-> [SQL] EsimMapper.insertMcpEsimOmdTrace
```

**테이블/API:** MCP_ESIM_OMD_TRACE  
**처리 로직 / 변환 처리:**

```
o EsimMapper.insertMcpEsimOmdTrace
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
getY14
->esimDao.updateServiceRst
-> [SQL] EsimMapper.updateServiceRst
```

**테이블/API:** MCP_UPLOAD_PHONE_INFO  
**처리 로직 / 변환 처리:**

```
o EsimMapper.updateServiceRst
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**JSP/JS:** /resources/js/mobile/appForm/appFormDesignView.js  
**URL:** /getCodeListAjax.do  
**Controller:** DbCacheController.java - getCodeList  
**처리 로직 / 변환 처리:**

```
o DbCacheController.java - getCodeList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/22 전달  
---

**JSP/JS:** /resources/js/mobile/appForm/appFormDesignView.js  
**URL:** /appForm/omdRegAjax.do  
**Controller:** EsimController.java - omdRegAjax  
**처리 로직 / 변환 처리:**

```
o EsimController.java - omdRegAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** eSimSvc.checkAbuseImeiList  
**Service / Dao / SQL / API:**

```
existsAbuseImei
-> [M포탈-M전산 API] /appform/existsAbuseImei
```

**처리 로직 / 변환 처리:**

```
o /appform/existsAbuseImei
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
saveAbuseImeiHist
-> ipstatisticService.getClientIp
```

**처리 로직 / 변환 처리:**

```
o ipstatisticService.getClientIp
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
saveAbuseImeiHist
-> esimDao.insertAbuseImeiHist
-> [SQL] EsimMapper.insertAbuseImeiHist
```

**테이블/API:** NMCP_ABUSE_IMEI_HIST  
**처리 로직 / 변환 처리:**

```
o EsimMapper.insertAbuseImeiHist
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** eSimSvc.omdReg  
**처리 로직 / 변환 처리:**

```
o eSimSvc.omdReg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
esimDao.doubleChk
-> [SQL] EsimMapper.doubleChk
```

**테이블/API:** MCP_UPLOAD_PHONE_INFO  
**처리 로직 / 변환 처리:**

```
o EsimMapper.doubleChk
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** getY14  
**처리 로직 / 변환 처리:**

```
o getY14
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
getY14
-> mplatFormService.moscBfacChkOmdIntm
```

**테이블/API:** Y14  
**처리 로직 / 변환 처리:**

```
o mplatFormService.moscBfacChkOmdIntm
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
getY14
->esimDao.insertMcpEsimOmdTrace
-> [SQL] EsimMapper.insertMcpEsimOmdTrace
```

**테이블/API:** MCP_ESIM_OMD_TRACE  
**처리 로직 / 변환 처리:**

```
o EsimMapper.insertMcpEsimOmdTrace
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
getY14
->esimDao.updateServiceRst
-> [SQL] EsimMapper.updateServiceRst
```

**테이블/API:** MCP_UPLOAD_PHONE_INFO  
**처리 로직 / 변환 처리:**

```
o EsimMapper.updateServiceRst
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** getY15  
**처리 로직 / 변환 처리:**

```
o getY15
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
getY15
-> mplatFormService.moscTrtOmdIntm
```

**테이블/API:** Y15  
**처리 로직 / 변환 처리:**

```
o mplatFormService.moscTrtOmdIntm
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
getY15
-> esimDao.insertMcpEsimOmdTrace
-> [SQL] EsimMapper.insertMcpEsimOmdTrace
```

**테이블/API:** MCP_ESIM_OMD_TRACE  
**처리 로직 / 변환 처리:**

```
o EsimMapper.insertMcpEsimOmdTrace
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
getY15
-> esimDao.updateServiceRst
-> [SQL] EsimMapper.updateServiceRst
```

**테이블/API:** MCP_UPLOAD_PHONE_INFO  
**처리 로직 / 변환 처리:**

```
o EsimMapper.updateServiceRst
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** getY12  
**처리 로직 / 변환 처리:**

```
o getY12
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
getY12
-> mplatFormService.moscRetvIntmMdlSpecInfo
```

**테이블/API:** Y12  
**처리 로직 / 변환 처리:**

```
o mplatFormService.moscRetvIntmMdlSpecInfo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
getY12
-> esimDao.insertMcpEsimOmdTrace
-> [SQL] EsimMapper.insertMcpEsimOmdTrace
```

**테이블/API:** MCP_ESIM_OMD_TRACE  
**처리 로직 / 변환 처리:**

```
o EsimMapper.insertMcpEsimOmdTrace
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** updateMcpUploadPhoneInfo  
**처리 로직 / 변환 처리:**

```
o updateMcpUploadPhoneInfo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
updateMcpUploadPhoneInfo
-> esimDao.updateMcpUploadPhoneInfoDto
-> [SQL] EsimMapper.updateMcpUploadPhoneInfoDto
```

**테이블/API:** MCP_UPLOAD_PHONE_INFO  
**처리 로직 / 변환 처리:**

```
o EsimMapper.updateMcpUploadPhoneInfoDto
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  

---

## [1003-16]

**JSP/JS:** /mobile/appForm/appOutForm.jsp, /resources/js/mobile/appForm/appOutForm.js  
**URL:** /getCodeListAjax.do  
**Controller:** DbCacheController.java - getCodeList  
**Service / Dao / SQL / API:**

```
NmcpServiceUtils.getCodeList

        List<NmcpCdDtlDto> codeList = null;
        List<NmcpCdDtlDto> codeRstList = null;
        DbCacheHandler dbCacheHandler = getBean(DbCacheHandler.class);

        @SuppressWarnings("unchecked")
        Map<String, List<NmcpCdDtlDto>> codeMap = (Map<String, List<NmcpCdDtlDto>>) dbCacheHandler.getValue(CACHE_CODE);
        if (codeMap != null) {
            codeList = codeMap.get(grpCd);
            codeRstList = new ArrayList<NmcpCdDtlDto>();

            if(codeList !=null && !codeList.isEmpty()) {
                for(NmcpCdDtlDto bean : codeList) {
                    if(DateTimeUtil.checkValidDate(bean.getPstngStartDate(), bean.getPstngEndDate())) {
                        codeRstList.add(bean);
                    }
                }
            }
        }
        return codeRstList;
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/22 전달  

---

## [1003-17]

**JSP/JS:** /mobile/appForm/appOutForm.jsp, /resources/js/mobile/appForm/appOutForm.js  
**URL:** /m/getCodeNmAjax.do  
**Controller:** DbCacheController.java - getCodeNm  
**Service / Dao / SQL / API:**

```
NmcpServiceUtils.getCodeNmObj

        List<NmcpCdDtlDto> codeList = null;
        DbCacheHandler dbCacheHandler = getBean(DbCacheHandler.class);

        @SuppressWarnings("unchecked")
        Map<String, List<NmcpCdDtlDto>> codeMap = (Map<String, List<NmcpCdDtlDto>>) dbCacheHandler.getValue(CACHE_CODE);
        if (codeMap != null) {
            codeList = codeMap.get(nmcpCdDtlDto.getCdGroupId());
            if (codeList != null) {
                for (NmcpCdDtlDto bean : codeList) {
                    if (bean.getDtlCd().equals(nmcpCdDtlDto.getDtlCd())) {
                        return bean;
                    }
                }
            }
        }
        return null;
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmObj
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/22 전달  

---

## [1003-18]

**JSP/JS:** /mobile/appForm/appOutForm.jsp, /resources/js/mobile/appForm/appOutForm.js  
**URL:** /auth/resetProdAuth.do  
**Controller:**

```
NiceCertifyController.java - resetProdAuth

        Map<String, String> rtnMap = new HashMap<>();

        // 0. 파라미터 검사
        if(!(INSR_PROD.equals(moduleType) || RWD_PROD.equals(moduleType))){
            throw new McpCommonJsonException("0001", F_BIND_EXCEPTION);
        }

        // 1. moduleType 인증 이력 존재여부 확인
        if(0 < certService.getModuTypeStepCnt(moduleType, "")){
            // 1-1. moduleType 인증 관련 스텝 초기화
            CertDto certDto = new CertDto();
            certDto.setModuType(moduleType);
            certDto.setCompType("G");
            certService.getCertInfo(certDto);
        }

        rtnMap.put("RESULT_CODE", AJAX_SUCCESS);
        return rtnMap;
```

**처리 로직 / 변환 처리:**

```
o NiceCertifyController.java - resetProdAuth
  - 인증에 맞춰서 M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
certService.getModuTypeStepCnt
  - moduType별 step cnt

        //1.moduType 없으면 리턴
        if(StringUtils.isBlank(moduType)) return 0;
        //2.세션 CERT_SEQ 가져오고 없으면 리턴
        long crtSeq = SessionUtils.getCertSession();
        if(crtSeq == 0) return 0;

        //3.dto에 set해서 get 보내고 리턴
        CertDto reqDto = new CertDto();
        reqDto.setCrtSeq(crtSeq);
        reqDto.setModuType(moduType);
        reqDto.setNcType(ncType);
        return certDao.getModuTypeStepCnt(reqDto);
```

**Service / Dao / SQL / API:**

```
certDao.getModuTypeStepCnt
-> [SQL] CertMapper.getModuTypeStepCnt

  SELECT  /*CertMapper.getModuTypeStepCnt*/
      COUNT(DISTINCT STEP) CNT
  FROM NMCP_CRT_VLD_DTL
  WHERE 1 = 1
   AND CRT_SEQ = #{crtSeq}
   AND MODU_TYPE = #{moduType}
   AND COMP_TYPE IN ('E', 'C')
   <if test= 'ncType != null and ncType != ""'>
   AND NC_TYPE = #{ncType}
   </if>
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getModuTypeStepCnt
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 본인인증  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
certService.getCertInfo
  - 인증정보 비교해서 map으로 리턴

        Map<String,Object> rtnMap = new HashMap<String, Object>();

        //1.반드시 필요한 정보 compType, urlType 없으면 리턴
        if(StringUtils.isBlank(certDto.getCompType())) {
            rtnMap.put("RESULT_CODE", "99999"); //예상할수 없는 결과 == 예외
            rtnMap.put("RESULT_DESC", "[STEP]처음부터 다시 시도해 주세요.");
            return rtnMap;
        }

        //2.세션 CERT_SEQ 가져오기
        long crtSeq = SessionUtils.getCertSession();
        logger.info("[CERT] 세션에서 가져온 crtSeq : " + crtSeq);
        certDto.setCrtSeq(crtSeq); //세션 있으면 crtSeq, 없으면 0 저장

        //3.parameter로 받은 dto 비교해서 결과 값 받기
        String strRtn = this.compCertInfo(certDto);
        //비교 결과 값 없으면 리턴
        if(StringUtils.isBlank(strRtn)) {
            rtnMap.put("RESULT_CODE", "99999"); //예외
            rtnMap.put("RESULT_DESC", "[STEP]처음부터 다시 시도해 주세요.");
            return rtnMap;
        }

        //4.결과값에 따라 insert/merge/delete 준비
        //4-1.리턴받은 비교 결과 값 set
        certDto.setVeriRslt(strRtn);
        //4-2.로그인 세션 가져와서 id set
        UserSessionDto loginSession = SessionUtils.getUserCookieBean();
        if(null != loginSession) {
            certDto.setRegstId(loginSession.getUserId());
            certDto.setRvisnId(loginSession.getUserId());
        }

        //4-3.referer 없으면 세션에서 PAGE_TYPE set - 어느 메뉴에서 진입했는지 확인용
        if(StringUtils.isBlank(certDto.getcReferer())) {
            certDto.setcReferer(SessionUtils.getPageSession());
        }

        //4-4.사용자 ip set
        certDto.setRip(ipstatisticService.getClientIp());

        /* strRtn
            * Y : 인증비교 결과 맞음
            * N : 인증비교 결과 틀림
            * X : 비교 대상이 없음
            * I : 단순 insert
            * U : 단순 update
            * D : 단순 delete  */
        //4-5.strRtn 값에 따라 insert/merge/delete
        if("U".equals(strRtn)) { //compType이 E일 때 U 또는 I 리턴(특수경우)
            try {
                //moduType이 nicePin일 때 이름, 생년월일, CI update - 20240215 수정
                if("nicePin".equals(certDto.getModuType())) {
                    certDao.updateNicePin(certDto);
                }else if(!StringUtils.isBlank(certDto.getMobileNo())){
                    certDao.updateMobileNo(certDto);
                }else {
                    certDao.updateEsimSeq(certDto);
                }
            }catch(DataAccessException e) {
                strRtn = "E";
                logger.error("[CERT][getCertInfo.updateEsimSeq.DataAccessException", e.getMessage());
            }catch(Exception e) {
                strRtn = "E";
                logger.error("[CERT][getCertInfo.updateEsimSeq.Exception]", e.getMessage());
            }
        }else if("D".equals(strRtn)) { //compType이 G일때
            try {
                certDao.deleteCert(certDto);
            }catch(DataAccessException e) {
                strRtn = "E";
                logger.error("[CERT][getCertInfo.deleteCert.DataAccessException", e.getMessage());
            }catch(Exception e) {
                strRtn = "E";
                logger.error("[CERT][getCertInfo.deleteCert.Exception]", e.getMessage());
            }
        }else { //Y,N,X,I NMCP_CRT_VLD_DTL insert
            try {
                long rtnCrtSeq = certDao.insertCert(certDto);
                //insert 시 세션에 crtSeq 저장
                SessionUtils.setCertSession(rtnCrtSeq);
                //insert 후에 null set
                certDto.setReqSeq(null);
                certDto.setResSeq(null);
                certDto.setCrtEtc(null);
            }catch(DataAccessException e) {
                strRtn = "E";
                logger.error("[CERT][getCertInfo.insertCert.DataAccessException", e.getMessage());
            }catch(Exception e) {
                strRtn = "E";
                logger.error("[CERT][getCertInfo.insertCert.Exception]", e.getMessage());
            }
        }

        /* rtnMap
         * 00000 : [STEP]인증한 정보가 동일합니다.
         * 00001 : [STEP]본인 인증한 정보가 틀립니다.<br>새로고침 후 다시 시도해 주세요.
         * 00002 : [STEP]비교 대상이 없어 진행이 불가합니다.<br>처음부터 다시 시도해 주세요.
         * 00003 : [STEP]오류입니다.<br>이 메시지가 계속되면 처음부터 다시 시도해 주세요.
         * 99999 : [STEP]처음부터 다시 시도해 주세요. */
        //6.리턴값에 따라 Map 설정(단계 중 실패하거나 최종단계 종료 시 CERT_SEQ 세션 비우기)
        if("Y".equals(strRtn)) {
            rtnMap.put("RESULT_CODE", "00000"); //Constants.AJAX_SUCCESS
            rtnMap.put("RESULT_DESC", "[STEP]인증한 정보가 동일합니다.");
        }else if("N".equals(strRtn)) {
            logger.info("[CERT rtnMap][N:00001][crtSeq:" + crtSeq);
            rtnMap.put("RESULT_CODE", "00001");
            rtnMap.put("RESULT_DESC", "[STEP]본인 인증한 정보가 틀립니다.<br>새로고침 후 다시 시도해 주세요."); //==NICE_CERT_EXCEPTION
        }else if("X".equals(strRtn)) {
            logger.info("[CERT rtnMap][X:00002][crtSeq:" + crtSeq);
            rtnMap.put("RESULT_CODE", "00002");
            rtnMap.put("RESULT_DESC", "[STEP]비교 대상이 없어 진행이 불가합니다.<br>처음부터 다시 시도해 주세요.");
        }else if("E".equals(strRtn)) {
            logger.info("[CERT rtnMap][E:00003][crtSeq:" + crtSeq);
            rtnMap.put("RESULT_CODE", "00003");
            rtnMap.put("RESULT_DESC", "[STEP]오류입니다.<br>이 메시지가 계속되면 처음부터 다시 시도해 주세요.");
        }else {
            logger.info("[CERT rtnMap][exception:99999][crtSeq:" + crtSeq);
            rtnMap.put("RESULT_CODE", "99999"); //예외
            rtnMap.put("RESULT_DESC", "[STEP]처음부터 다시 시도해 주세요.");
        }

        return rtnMap;
```

**처리 로직 / 변환 처리:**

```
o certService.getCertInfo
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 본인인증  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
certDao.updateNicePin
-> [SQL] CertMapper.updateNicePin

  UPDATE NMCP_CRT_VLD_DTL SET /*CertMapper.updateNicePin*/
      NAME = #{name},
      BIRTH_DATE = #{birthDate},
      CONN_INFO = #{connInfo},
      NC_TYPE = #{ncType},
      RVISN_ID = #{rvisnId},
      RVISN_DTTM = SYSDATE
  WHERE 1 = 1
    AND CRT_SEQ = #{crtSeq}
    AND COMP_TYPE = 'E'
   AND MODU_TYPE = 'nicePin'
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.updateNicePin
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 본인인증  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
certDao.updateMobileNo
-> [SQL] CertMapper.updateMobileNo

  /*CertMapper.updateMobileNo*/
  UPDATE NMCP_CRT_VLD_DTL
  SET MOBILE_NO = #{mobileNo},
    RVISN_ID = #{rvisnId},
    RVISN_DTTM = SYSDATE
  WHERE CRT_SEQ = #{crtSeq}
   AND COMP_TYPE = 'E'
   AND MOBILE_NO IS NOT NULL
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.updateMobileNo
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 본인인증  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
certDao.updateEsimSeq
-> [SQL] CertMapper.updateEsimSeq

  UPDATE NMCP_CRT_VLD_DTL SET /*CertMapper.updateEsimSeq*/
      UPLOAD_PHONE_SRL_NO = #{uploadPhoneSrlNo},
      RVISN_ID = #{rvisnId},
      RVISN_DTTM = SYSDATE
  WHERE 1 = 1
      AND CRT_SEQ = #{crtSeq} 
      AND UPLOAD_PHONE_SRL_NO IS NOT NULL
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.updateEsimSeq
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 본인인증  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
certDao.deleteCert
-> [SQL] CertMapper.deleteCert

        DELETE NMCP_CRT_VLD_DTL  /*CertMapper.deleteCert*/
        WHERE 1 = 1
         AND CRT_SEQ = #{crtSeq}
         AND MODU_TYPE = #{moduType}
     <if test= 'ncType != null and ncType != ""'>
     AND NC_TYPE = #{ncType}
     </if>
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.deleteCert
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 본인인증  
**개발팀 전달일:** 03/22 전달  

---

## [1003-19]

**JSP/JS:** /mobile/appForm/appOutForm.jsp, /resources/js/mobile/appForm/appOutForm.js  
**URL:** /nice/niceAccountOtpNameAjax.do  
**Controller:**

```
NiceCertifyController.java - niceAccountOtpName

       String result = null;
        HashMap<String, Object> rtnMap = new HashMap<String, Object>();
        result = niceCertifySvc.checkNiceAccount(niceResDto);

        String[] results = result.split("\\|");


        NiceLogDto nicelogDto= new NiceLogDto();

        if (results!=null && results.length > 1 && "0000".equals(results[1])) {
            //계좌 OTP NAME 생성
            result = niceCertifySvc.niceAccountOtpName(niceResDto);

            JSONObject jObject = null;
            try {
                jObject = new JSONObject(result);
                JSONObject jObjectBody = jObject.getJSONObject("dataBody");

                if(jObjectBody.getString("result_cd").equals("0000")) {
                    rtnMap.put("RESULT_CODE", AJAX_SUCCESS);

                    //화면 HIDDEN에 저장 필요
                    //OTP CONFIRM에 사용됨
                    rtnMap.put("request_no", jObjectBody.getString("request_no"));
                    rtnMap.put("res_uniq_id", jObjectBody.getString("res_uniq_id"));

                } else {
                    rtnMap.put("RESULT_CODE", "0002");
                }

                //로그 이력 저정
                nicelogDto.setAuthType("A");
                nicelogDto.setName(niceResDto.getName());
                nicelogDto.setBirthDate(niceResDto.getAccountNo());
                nicelogDto.setParamR2(niceResDto.getBankCode());
                nicelogDto.setParamR1(jObjectBody.getString("result_cd"));
                nicelogDto.setParamR3("passReq");
                nicelogDto.setReqSeq(jObjectBody.getString("request_no"));
                nicelogDto.setResSeq(jObjectBody.getString("res_uniq_id"));
                nicelogDto.setsMobileCo(jObjectBody.getString("mobile_co"));
                nicelogDto.setsMobileNo(jObjectBody.getString("mobile_no"));
                nicelog.insertMcpNiceHist(nicelogDto);

            } catch(JSONException e) {
                rtnMap.put("RESULT_CODE", "9998");
            } catch (Exception e) {
                rtnMap.put("RESULT_CODE", "9999");
            }


        } else {
            //유효하지 않은 계좌 번호 입니다.
            rtnMap.put("RESULT_CODE", "0001");
        }
        return rtnMap;
```

**처리 로직 / 변환 처리:**

```
o NiceCertifyController.java - niceAccountOtpName
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 계좌+PASS 인증  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
niceCertifySvc.checkNiceAccount

        Random numGen = null;
        try {
            numGen = SecureRandom.getInstance("SHA1PRNG");
        } catch (NoSuchAlgorithmException e) {
            throw new McpCommonException("NoSuchAlgorithmException");
        }

        String result = "";
        SimpleDateFormat sdf = new SimpleDateFormat("yyyyMMdd",Locale.KOREA);
        String strOrderNo   = sdf.format(new Date()) + (Math.round(numGen.nextDouble() * 10000000000L) + "");           //주문번호 : 매 요청마다 중복되지 않도록 유의

        CommonHttpClient client = new CommonHttpClient(extUrl+"/rlnmCheck.do");
        NameValuePair[] data = {
                new NameValuePair("niceUid",NICE_UID),
                new NameValuePair("svcPwd",niceUidPassword),
                new NameValuePair("service",niceResDto.getService()),  //서비스구분 1=계좌소유주확인 2=계좌성명확인 3=계좌유효성확인
                new NameValuePair("strGbn","1"),  // 1 : 개인, 2: 사업자
                new NameValuePair("strResId",niceResDto.getResId()), //주민번호(사업자 번호,법인번호)
                new NameValuePair("strNm",StringUtil.substringByBytes(niceResDto.getName(),0,60) ), //계좌소유주명
                new NameValuePair("strBankCode",niceResDto.getBankCode()), //은행코드(전문참조)
                new NameValuePair("strAccountNo",niceResDto.getAccountNo()), //계좌번호
                new NameValuePair("svcGbn",niceResDto.getSvcGbn()),   //업무구분(전문참조)
                new NameValuePair("strOrderNo",strOrderNo),
                new NameValuePair("svc_cls",niceResDto.getSvcCls()),  //내-외국인구분
                new NameValuePair("inq_rsn","10") // 조회사유 - 10:회원가입 20:기존회원가입 30:성인인증 40:비회원확인 90:기타사유
        };
        try {
            result = client.postUtf8(data);
            //result = client.post(data, "UTF-8");
        } catch (SocketTimeoutException e) {
            throw new McpCommonException("SocketTimeoutException");
        }

        //data:"service=3&svcGbn=4&strBankCode=" + $('#reqBankTmp').val() + "&strAccountNo=" +  $('#reqAccountNumberTmp').val() + "&USERNM=" + $('#reqAccountNameTmp').val(),

       /*
        * 확인용도 주석 처리
        private String service   ; //서비스구분 1=계좌소유주확인 2=계좌성명확인 3=계좌유효성확인
        private String resId     ; //주민번호(사업자 번호,법인번호)
        private String bankCode  ; //은행코드(전문참조)
        private String accountNo ; //계좌번호
        private String svcGbn    ; //업무구분(전문참조)
        private String svcCls    ; //내-외국인구분

        service 서비스구분 1: 소유주 확인, 2: 예금주명 확인, 3: 계좌 유효성 확인
        svcGbn 업무구분 5: 소유주 확인, 2: 예금주명 확인, 4: 계좌 유효성 확인
        */
```

**Service / Dao / SQL / API:**

```
[외부] {ext.url}+"/rlnmCheck.do
```

**테이블/API:**

```
[외부] {ext.url}+"/rlnmCheck.do
```

**처리 로직 / 변환 처리:**

```
o niceCertifySvc.checkNiceAccount
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 계좌+PASS 인증  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
niceCertifySvc.niceAccountOtpName

        Random numGen = null;
        try {
            numGen = SecureRandom.getInstance("SHA1PRNG");
        } catch (NoSuchAlgorithmException e) {
            throw new McpCommonException("NoSuchAlgorithmException");
        }

        String result = "";
        SimpleDateFormat sdf = new SimpleDateFormat("yyyyMMdd",Locale.KOREA);
        String strRequestNo   = sdf.format(new Date()) + (Math.round(numGen.nextDouble() * 10000000000L) + "");           //주문번호 : 매 요청마다 중복되지 않도록 유의

        CommonHttpClient client = new CommonHttpClient(extUrl+"/niceAccountOtpName.do");

        NameValuePair[] data = {
                new NameValuePair("clientId",clientId),
                new NameValuePair("clientSecret",clientSecret),
                new NameValuePair("strNm",niceResDto.getName()), //계좌소유주명
                new NameValuePair("strBankCode",niceResDto.getBankCode()), //은행코드(전문참조)
                new NameValuePair("strAccountNo",niceResDto.getAccountNo()), //계좌번호
                new NameValuePair("strRequestNo",strRequestNo),                   //요청고유번호

        };
        try {
            result = client.postUtf8(data);
        } catch (SocketTimeoutException e) {
            throw new McpCommonException("SocketTimeoutException");
        }

        //data:"service=3&svcGbn=4&strBankCode=" + $('#reqBankTmp').val() + "&strAccountNo=" +  $('#reqAccountNumberTmp').val() + "&USERNM=" + $('#reqAccountNameTmp').val(),

       /*
        * 확인용도 주석 처리
        private String service   ; //서비스구분 1=계좌소유주확인 2=계좌성명확인 3=계좌유효성확인
        private String resId     ; //주민번호(사업자 번호,법인번호)
        private String bankCode  ; //은행코드(전문참조)
        private String accountNo ; //계좌번호
        private String svcGbn    ; //업무구분(전문참조)
        private String svcCls    ; //내-외국인구분
        */

        return result;
```

**Service / Dao / SQL / API:**

```
[외부] {ext.url}+"/niceAccountOtpName.do
```

**테이블/API:**

```
[외부] {ext.url}+"/niceAccountOtpName.do
```

**처리 로직 / 변환 처리:**

```
o niceCertifySvc.niceAccountOtpName
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 계좌+PASS 인증  
**개발팀 전달일:** 03/22 전달  
---

**Service:** nicelog.insertMcpNiceHist  
**처리 로직 / 변환 처리:**

```
o nicelog.insertMcpNiceHist
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 계좌+PASS 인증  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
ipstatisticService.getClientIp

        String clientIp = "";
        HttpServletRequest request = ((ServletRequestAttributes) RequestContextHolder.currentRequestAttributes()).getRequest();

        if(request.getHeader("X-Forwarded-For") == null) {
            clientIp = request.getRemoteAddr();
        } else {
            clientIp = request.getHeader("X-Forwarded-For");
            if (clientIp !=null && ! clientIp.equals("") && clientIp.indexOf(",")>-1) {
                clientIp =  clientIp.split("\\,")[0].trim();
            }
        }

        return clientIp;
```

**처리 로직 / 변환 처리:**

```
o ipstatisticService.getClientIp
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 계좌+PASS 인증  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
ipstatisticService.getReferer

        String referer = "";
        HttpServletRequest request = ((ServletRequestAttributes) RequestContextHolder.currentRequestAttributes()).getRequest();
        referer = request.getHeader("referer");
        return referer;
```

**처리 로직 / 변환 처리:**

```
o ipstatisticService.getReferer
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 계좌+PASS 인증  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
niceLogDao.insertMcpNiceHist
-> [SQL] NiceLogMapper.insertMcpNiceHist

        <selectKey keyProperty="niceHistSeq" resultType="Long" order="BEFORE">
            SELECT SQ_HIST_SEQ.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO MCP_NICE_HIST (
              NICE_HIST_SEQ            --PK
            , REQ_SEQ                  --일련번호 기본키
            , RES_SEQ                  --요청 일련번호
            , AUTH_TYPE                 --요청 구분
            , NAME                     --인증성명
            , BIRTH_DATE               --인증생년월일
            , GENDER                   --GENDER
            , NATIONAL_INFO            --NATIONAL_INFO
            , DUP_INFO                 --DUP_INFO
            , CONN_INFO                --CONN_INFO
            , PARAM_R1                 --파라메터 1
            , PARAM_R2                 --파라메터 2
            , PARAM_R3                 --파라메터 3
            , END_YN                   --종료여부
            , N_REFERER                --referer
            , RIP                      --등록아이피
            , SYS_RDATE_DT             --등록일
            , SYS_RDATE                --등록일시
            , RVISN_DTTM               --수정일
            , COMPL_DTTM               --인증 완료시
            , MOBILE_CO                -- 이동사구분(모바일 인증)
            , MOBILE_NO                -- 휴대폰 번호(모바일 인증)
        ) VALUES (
            #{niceHistSeq}
            , #{reqSeq}
            , #{resSeq}
            , #{authType}
            , #{name}
            , #{birthDate}
            , #{gender}
            , #{nationalInfo}
            , #{dupInfo}
            , #{connInfo}
            , #{paramR1}
            , #{paramR2}
            , #{paramR3}
            , 'N'
            , #{nReferer}
            , #{rip}
            , TO_CHAR(SYSDATE, 'YYYYMMDD')
            <choose>
                 <when test="sysRdate != null">
                    , #{sysRdate}
                 </when>
                 <otherwise>
                    , SYSDATE
                 </otherwise>
             </choose>
            , SYSDATE
            , SYSDATE
            , #{sMobileCo}
            , #{sMobileNo}
        )
```

**테이블/API:** MCP_NICE_HIST  
**처리 로직 / 변환 처리:**

```
o NiceLogMapper.insertMcpNiceHist
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 계좌+PASS 인증  
**개발팀 전달일:** 03/22 전달  
---

**URL:** /nice/niceAccountOtpConfirmAjax.do  
**Controller:**

```
NiceCertifyController.java - niceAccountOtpConfirm

        String result = null;
        HashMap<String, Object> rtnMap = new HashMap<String, Object>();
        result = niceCertifySvc.niceAccountOtpConfirm(niceResDto);

        //String[] results = result.split("\\|");


        NiceLogDto nicelogDto= new NiceLogDto();
        nicelogDto.setnReferer(request.getHeader("referer"));
        nicelogDto.setnAuthType("A");

        JSONObject jObject = null;
        try {
            jObject = new JSONObject(result);

            JSONObject jObjectBody = jObject.getJSONObject("dataBody");

            if(jObjectBody.getString("rsp_cd").equals("P000") && jObjectBody.getString("result_cd").equals("0000")) {
                //정상응답코드
                //OTP 인증 성공
                rtnMap.put("RESULT_CODE", AJAX_SUCCESS);
            }else {
                rtnMap.put("RESULT_CODE", "0001");
            }

             //로그 이력 저정
            /*rsp_cd  :   P000
            request_no  :   20210617561599961
            result_cd   :   0000
            res_uniq_id :   AC2106171136191000249545
            err_msg :
             var varData = ajaxCommon.getSerializedData({
               bankCode:$("#reqBankAut").val()
               ,accountNo:$.trim($("#reqAccountAut").val())
               ,name:$.trim($("#cstmrName").val())
               ,requestNo:$.trim($("#requestNo").val())
               ,resUniqId:$.trim($("#resUniqId").val())
               ,otp:$.trim($("#textOpt").val())
           });
            */
            nicelogDto.setName(niceResDto.getName());
            nicelogDto.setAuthType("A");
            nicelogDto.setParamR1(jObjectBody.getString("rsp_cd"));
            nicelogDto.setParamR2(jObjectBody.getString("result_cd"));
            nicelogDto.setParamR3("passCheck");
            nicelogDto.setReqSeq(jObjectBody.getString("request_no"));
            nicelogDto.setResSeq(jObjectBody.getString("res_uniq_id"));
            nicelogDto.setsMobileCo(jObjectBody.getString("mobile_co"));
            nicelogDto.setsMobileNo(jObjectBody.getString("mobile_no"));
            nicelog.insertMcpNiceHist(nicelogDto);

        } catch (JSONException e) {
            rtnMap.put("RESULT_CODE", "0002");
        }

        return rtnMap;
```

**처리 로직 / 변환 처리:**

```
o NiceCertifyController.java - niceAccountOtpConfirm
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 계좌+PASS 인증  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
niceCertifySvc.niceAccountOtpConfirm

        Random numGen = null;
        try {
            numGen = SecureRandom.getInstance("SHA1PRNG");
        } catch (NoSuchAlgorithmException e) {
            throw new McpCommonException("NoSuchAlgorithmException");
        }

        String result = "";
        SimpleDateFormat sdf = new SimpleDateFormat("yyyyMMdd",Locale.KOREA);
        String strOrderNo   = sdf.format(new Date()) + (Math.round(numGen.nextDouble() * 10000000000L) + "");           //주문번호 : 매 요청마다 중복되지 않도록 유의

        CommonHttpClient client = new CommonHttpClient(extUrl+"/niceAccountOtpConfirm.do");

        NameValuePair[] data = {
                new NameValuePair("clientId",clientId),
                new NameValuePair("clientSecret",clientSecret),
                new NameValuePair("strRequestNo",niceResDto.getRequestNo()), //요청고유번호
                new NameValuePair("strResUniqId",niceResDto.getResUniqId()), //응답고유번호
                new NameValuePair("otp",niceResDto.getOtp()),                         //otp

        };
        try {
            result = client.postUtf8(data);
        } catch (SocketTimeoutException e) {
            throw new McpCommonException("SocketTimeoutException");
        }


        return result;
```

**Service / Dao / SQL / API:**

```
[외부] {ext.url}+"/niceAccountOtpConfirm.do
```

**테이블/API:**

```
[외부] {ext.url}+"/niceAccountOtpConfirm.do
```

**처리 로직 / 변환 처리:**

```
o niceCertifySvc.niceAccountOtpConfirm
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 계좌+PASS 인증  
**개발팀 전달일:** 03/22 전달  
---

**Service:** nicelog.insertMcpNiceHist  
**처리 로직 / 변환 처리:**

```
o nicelog.insertMcpNiceHist
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 계좌+PASS 인증  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
ipstatisticService.getClientIp

        String clientIp = "";
        HttpServletRequest request = ((ServletRequestAttributes) RequestContextHolder.currentRequestAttributes()).getRequest();

        if(request.getHeader("X-Forwarded-For") == null) {
            clientIp = request.getRemoteAddr();
        } else {
            clientIp = request.getHeader("X-Forwarded-For");
            if (clientIp !=null && ! clientIp.equals("") && clientIp.indexOf(",")>-1) {
                clientIp =  clientIp.split("\\,")[0].trim();
            }
        }

        return clientIp;
```

**처리 로직 / 변환 처리:**

```
o ipstatisticService.getClientIp
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 계좌+PASS 인증  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
ipstatisticService.getReferer

        String referer = "";
        HttpServletRequest request = ((ServletRequestAttributes) RequestContextHolder.currentRequestAttributes()).getRequest();
        referer = request.getHeader("referer");
        return referer;
```

**처리 로직 / 변환 처리:**

```
o ipstatisticService.getReferer
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 계좌+PASS 인증  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
niceLogDao.insertMcpNiceHist
-> [SQL] NiceLogMapper.insertMcpNiceHist

        <selectKey keyProperty="niceHistSeq" resultType="Long" order="BEFORE">
            SELECT SQ_HIST_SEQ.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO MCP_NICE_HIST (
              NICE_HIST_SEQ            --PK
            , REQ_SEQ                  --일련번호 기본키
            , RES_SEQ                  --요청 일련번호
            , AUTH_TYPE                 --요청 구분
            , NAME                     --인증성명
            , BIRTH_DATE               --인증생년월일
            , GENDER                   --GENDER
            , NATIONAL_INFO            --NATIONAL_INFO
            , DUP_INFO                 --DUP_INFO
            , CONN_INFO                --CONN_INFO
            , PARAM_R1                 --파라메터 1
            , PARAM_R2                 --파라메터 2
            , PARAM_R3                 --파라메터 3
            , END_YN                   --종료여부
            , N_REFERER                --referer
            , RIP                      --등록아이피
            , SYS_RDATE_DT             --등록일
            , SYS_RDATE                --등록일시
            , RVISN_DTTM               --수정일
            , COMPL_DTTM               --인증 완료시
            , MOBILE_CO                -- 이동사구분(모바일 인증)
            , MOBILE_NO                -- 휴대폰 번호(모바일 인증)
        ) VALUES (
            #{niceHistSeq}
            , #{reqSeq}
            , #{resSeq}
            , #{authType}
            , #{name}
            , #{birthDate}
            , #{gender}
            , #{nationalInfo}
            , #{dupInfo}
            , #{connInfo}
            , #{paramR1}
            , #{paramR2}
            , #{paramR3}
            , 'N'
            , #{nReferer}
            , #{rip}
            , TO_CHAR(SYSDATE, 'YYYYMMDD')
            <choose>
                 <when test="sysRdate != null">
                    , #{sysRdate}
                 </when>
                 <otherwise>
                    , SYSDATE
                 </otherwise>
             </choose>
            , SYSDATE
            , SYSDATE
            , #{sMobileCo}
            , #{sMobileNo}
        )
```

**테이블/API:** MCP_NICE_HIST  
**처리 로직 / 변환 처리:**

```
o NiceLogMapper.insertMcpNiceHist
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 계좌+PASS 인증  
**개발팀 전달일:** 03/22 전달  

---

## [1003-20]

**JSP/JS:** /mobile/appForm/appOutForm.jsp, /resources/js/mobile/appForm/appOutForm.js  
**URL:** /appform/isEnabledFT1.do  
**Controller:** AppformController.java - isEnabledFT1  
**Service:**

```
appformSvc.isEnabledFT1

    public boolean isEnabledFT1() {
        NmcpCdDtlDto nmcpCdDtlDto = NmcpServiceUtils.getCodeNmDto("fathCertPolicy", "FT1MCP");
        if (nmcpCdDtlDto == null) {
            return false;
        }

        if (!"Y".equals(nmcpCdDtlDto.getDtlCdNm())) {
            return false;
        }

        try {
            if (!isMiddleDateTime2(nmcpCdDtlDto.getPstngStartDate(), nmcpCdDtlDto.getPstngEndDate())) {
                return false;
            }
        } catch (ParseException e) {
            return false;
        }

        return true;
    }
```

**처리 로직 / 변환 처리:**

```
o appformSvc.isEnabledFT1
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
NmcpServiceUtils.getCodeNmDto

    public static NmcpCdDtlDto getCodeNmDto(String grpCd, String dtlCd) {

        NmcpCdDtlDto result = null;

        List<NmcpCdDtlDto> codeList = null;
        DbCacheHandler dbCacheHandler = getBean(DbCacheHandler.class);

        @SuppressWarnings("unchecked")
        Map<String, List<NmcpCdDtlDto>> codeMap = (Map<String, List<NmcpCdDtlDto>>) dbCacheHandler.getValue(CACHE_CODE);
        if (codeMap != null) {
            codeList = codeMap.get(grpCd);
            if (codeList != null) {
                for (NmcpCdDtlDto bean : codeList) {
                    if (bean.getDtlCd().equals(dtlCd)) {
                        return bean;
                    }
                }
            }
        }
        return null;
    }
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/22 전달  
---

**URL:** /appform/requestCustFathTxnSkipAjax.do  
**Controller:** AppformController.java - requestCustFathTxnSkipAjax  
**Service:**

```
appformSvc.processOsstFt1Service

    public HashMap<String, Object> processOsstFt1Service(OsstFathReqDto osstFathReqDto) {
        HashMap<String, Object> rtnMap = new HashMap<>();

        if (!isEnabledFT1()) {
            throw new McpCommonJsonException("1004", "안면인증 SKIP이 불가능합니다.");
        }

        String fathTransacId = SessionUtils.getFathSession().getTransacId();
        if(StringUtils.isEmpty(fathTransacId)) {
            throw new McpCommonJsonException("9991", FATH_CERT_EXPIR_EXCEPTION);
        }
        osstFathReqDto.setFathTransacId(fathTransacId);
        osstFathReqDto.setCpntId(SessionUtils.getFathSession().getCpntId());

        if ("P".equals(osstFathReqDto.getGubun())) {
            if(StringUtils.isEmpty(osstFathReqDto.getOperType())){
                throw new McpCommonJsonException("9992", F_BIND_EXCEPTION);
            }
            //신청서 조회
            FathFormInfo fathFormInfo = fathService.getFathFormInfo(osstFathReqDto.getResNo(), osstFathReqDto.getOperType());
            if(fathFormInfo == null) {
                throw new McpCommonJsonException("9993", F_BIND_EXCEPTION);
            }
            osstFathReqDto.setOrgId(fathFormInfo.getCntpntShopId());
        } else {
            //임시예약번호
            String tempResNo = SessionUtils.getFathSession().getTempResNo();
            if(StringUtils.isEmpty(tempResNo)) {
                throw new McpCommonJsonException("9994", FATH_CERT_EXPIR_EXCEPTION);
            }
            osstFathReqDto.setResNo(tempResNo);
        }

        //대리점 코드 조회
        String agentCode = this.getAgentCode(osstFathReqDto.getOrgId());
        if (StringUtils.isBlank(agentCode)) {
            throw new McpCommonJsonException("9995", "시스템 오류로 메세지가 반복되면 고객센터로 문의부탁드립니다.");
        }
        osstFathReqDto.setAsgnAgncId(agentCode);

        MSimpleOsstXmlFt1VO simpleOsstXmlFt1VO = null;
        try {
            simpleOsstXmlFt1VO = this.sendOsstFt1Service(osstFathReqDto);
            if (!simpleOsstXmlFt1VO.isSuccess()) { //연동실패
                rtnMap.put("RESULT_CODE", "9996");
                rtnMap.put("RESULT_MSG", "시스템에 문제가 발생하였습니다. 다음에 다시 진행 부탁드립니다.");
                return rtnMap;
            }
        } catch (Exception e) {
            rtnMap.put("RESULT_CODE", "9997");
            rtnMap.put("RESULT_MSG", "시스템에 문제가 발생하였습니다. 다음에 다시 진행 부탁드립니다.");
            //이력 정보 저장 처리
            McpIpStatisticDto mcpIpStatisticDto = new McpIpStatisticDto();
            mcpIpStatisticDto.setPrcsMdlInd("FT1_ERROR");
            mcpIpStatisticDto.setPrcsSbst("Exception[Exception]");
            ipstatisticService.insertAdminAccessTrace(mcpIpStatisticDto);
            return rtnMap;
        }

        if ("0000".equals(simpleOsstXmlFt1VO.getRsltCd())) {
            fathService.updateNameChgFathSkip(osstFathReqDto.getResNo());
            rtnMap.put("RESULT_CODE", AJAX_SUCCESS);
            rtnMap.put("RESULT_MSG", "안면인증 SKIP이 완료되었습니다.");
        } else {
            rtnMap.put("RESULT_CODE", simpleOsstXmlFt1VO.getRsltCd());
        }
        return rtnMap;
    }
```

**처리 로직 / 변환 처리:**

```
o appformSvc.processOsstFt1Service
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
fathService.getFathFormInfo
-> fathDao.getNameChgInfo
-> [SQL] FathMapper.getNameChgInfo

안면인증 명의변경 신청서 조회
        SELECT  CHG.CSTMR_TYPE
                ,CASE WHEN CHG.CSTMR_TYPE = 'NM' THEN AGT.NEE_AGENT_SELF_CERT_TYPE  /* 미성년자*/
                     ELSE CHG.SELF_CERT_TYPE
                END AS SELF_CERT_TYPE
                ,'1100011741' AS CNTPNT_SHOP_ID
                ,CASE WHEN CHG.CSTMR_TYPE = 'NM' THEN AGT.NEE_AGENT_NAME  /* 미성년자*/
                      ELSE CHG.CSTMR_NAME
                 END AS CSTMR_NAME
                ,CASE WHEN CHG.CSTMR_TYPE = 'NM' THEN AGT.NEE_AGENT_RRN  /* 미성년자*/
                      ELSE CHG.CSTMR_NATIVE_RRN
                 END AS CSTMR_RRN
                ,NVL(CHG.FATH_TEL_NO, CHG.CSTMR_RECEIVE_TEL_FN || CHG.CSTMR_RECEIVE_TEL_MN || CHG.CSTMR_RECEIVE_TEL_RN) AS FATH_TEL_NO
        FROM    NMCP_CUST_REQUEST_NAME_CHG CHG
                ,NMCP_CUST_REQ_NAME_CHG_AGENT AGT
        WHERE   CHG.CUST_REQ_SEQ = AGT.CUST_REQ_SEQ(+)
        AND     MCN_RES_NO = #{resNo}
```

**테이블/API:** NMCP_CUST_REQUEST_NAME_CHG, NMCP_CUST_REQ_NAME_CHG_AGENT  
**처리 로직 / 변환 처리:**

```
o FathMapper.getNameChgInfo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
-> NMCP_CUST_REQ_NAME_CHG_AGENT : MSF_REQUEST_AGENT 가입신청서 대리인 테이블로 통합
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
fathService.getFathFormInfo
-> fathDao.getFathAppForm
-> [SQL] FathMapper.getFathAppForm

안면인증 신청서 조회

        SELECT  T1.CSTMR_TYPE
                ,CASE WHEN T1.CSTMR_TYPE = 'NM' THEN T3.MINOR_SELF_CERT_TYPE  /* 미성년자*/
                     ELSE T2.SELF_CERT_TYPE
                END AS SELF_CERT_TYPE
                ,T1.CNTPNT_SHOP_ID
                ,CASE WHEN T1.CSTMR_TYPE = 'NM' THEN T3.MINOR_AGENT_NAME  /* 미성년자*/
                      ELSE T2.CSTMR_NAME
                END AS CSTMR_NAME
                ,CASE WHEN T1.CSTMR_TYPE = 'NM' THEN T3.MINOR_AGENT_RRN  /* 미성년자*/
                      WHEN T1.CSTMR_TYPE = 'NA' THEN T2.CSTMR_NATIVE_RRN /* 내국인*/
                      ELSE T2.CSTMR_FOREIGNER_RRN                        /* 외국인*/
                END AS CSTMR_RRN
                ,FATH_MOBILE_FN || FATH_MOBILE_MN || FATH_MOBILE_RN AS FATH_TEL_NO
        FROM    MCP_REQUEST T1
               ,MCP_REQUEST_CSTMR T2
               ,MCP_REQUEST_AGENT T3
        WHERE   T1.REQUEST_KEY = T2.REQUEST_KEY
          AND   T1.REQUEST_KEY = T3.REQUEST_KEY(+)
          AND   T1.RES_NO =  #{resNo}
```

**테이블/API:** MCP_REQUEST, MCP_REQUEST_CSTMR, MCP_REQUEST_AGENT  
**처리 로직 / 변환 처리:**

```
o FathMapper.getFathAppForm
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
getAgentCode
-> appformDao.getAgentCode
-> [M포탈-M전산 API] /appform/agentCode
```

**처리 로직 / 변환 처리:**

```
o /appform/agentCode
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
sendOsstFt1Service
-> mplatFormOsstServerAdapter.callService
-> [M플랫폼] FT1

        MSimpleOsstXmlFt1VO simpleOsstXmlFt1VO =  new MSimpleOsstXmlFt1VO();
        HashMap<String, String> param = new HashMap<String, String>();
        param.put("appEventCd", Constants.EVENT_CODE_FATH_SKIP);
        param.put("fathTransacId", osstFathReqDto.getFathTransacId());
        param.put("asgnAgncId", osstFathReqDto.getAsgnAgncId());

        if(!StringUtils.isEmpty(osstFathReqDto.getResNo())) {
            param.put("resNo", osstFathReqDto.getResNo());
        }

        mplatFormOsstServerAdapter.callService(param,simpleOsstXmlFt1VO,100000);
        return simpleOsstXmlFt1VO;
```

**테이블/API:** FT1  
**처리 로직 / 변환 처리:**

```
o sendOsstFt1Service
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
ipstatisticService.insertAdminAccessTrace
-> fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin

        INSERT INTO MCP_ADMIN_ACCESS_TRACE (
            PARAMETER,
            ACCESS_IP,
            SYS_DT,
            SYS_RDATE,
            ACCESS_URL,
            USER_ID,
            PRCS_MDL_IND,
            PRCS_SBST,
            TRTM_RSLT_SMST
        ) VALUES (
                     #{parameter},
                     #{accessIp},
                     TO_CHAR(SYSDATE, 'YYYYMMDD'),
                     SYSDATE,
                     #{accessUrl},
                     SUBSTRB(#{userid},0,20),
                     #{prcsMdlInd},
                     SUBSTRB(#{prcsSbst},0,1000),
                     #{trtmRsltSmst}
                 )
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 호출 로그 적재  
**개발팀 전달일:** 03/22 전달  
---

**URL:** /appform/reqFathUrlRqtAjax.do  
**Controller:**

```
AppformController.java - reqFathUrlRqtAjax
  - 고객 안면인증 URL 요청 (FS8)
  - 소스 : AppformController-reqFathUrlRqtAjax.java
```

**처리 로직 / 변환 처리:**

```
o AppformController.java - reqFathUrlRqtAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
mypageUserService.selectContractObj

    public Map<String, String> selectContractObj(String name, String phone , String contractNum) {

        Map<String, String> rtnMap = null;

        RestTemplate restTemplate = new RestTemplate();
        Map<String, String> params = new HashMap<String, String>();

        params.put("name", name);
        params.put("mobileNo", phone);
        params.put("contractNum", contractNum);
        rtnMap = restTemplate.postForObject(apiInterfaceServer + "/mypage/selectContractObj", params, Map.class);

        return rtnMap;
    }
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/selectContractObj  
**처리 로직 / 변환 처리:**

```
o /mypage/selectContractObj
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
fathService.findAndSaveSessionCpntId

 public String findAndSaveSessionCpntId(String cntpntShopId) {
  String cpntId = SessionUtils.getFathSession().getCpntId();
  if (!StringUtils.isEmpty(cpntId)) {
   return cpntId;
  }

  if (orgnId.equals(cntpntShopId)) {
   cpntId = cntpntShopId;
  } else { //직영온라인이 아닌 경우
   //(대리점)안면인증 가능 접점ID조회
   cpntId = fathDao.getCpntId(cntpntShopId);
  }
  SessionUtils.saveFathCpntId(cpntId);

  return cpntId;
 }
```

**Service / Dao / SQL / API:**

```
fathDao.getCpntId
-> [M포탈-M전산 API] /appform/getCpntId
```

**처리 로직 / 변환 처리:**

```
o /appform/getCpntId
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
appformSvc.getAgentCode
  - 대리점 코드 조회
```

**Service / Dao / SQL / API:**

```
appformDao.getAgentCode
-> [M포탈-M전산 API] /appform/agentCode
```

**처리 로직 / 변환 처리:**

```
o /appform/agentCode
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
appformSvc.processOsstFs8Service

    public HashMap<String, Object> processOsstFs8Service(OsstFathReqDto osstFathReqDto) {
        HashMap<String, Object> rtnMap = new HashMap<>();

        MSimpleOsstXmlFs8VO simpleOsstXmlFs8VO = null; 
        
        try {
            simpleOsstXmlFs8VO = this.sendOsstFs8Service(osstFathReqDto);

            if (!simpleOsstXmlFs8VO.isSuccess()) {
                rtnMap.put("RESULT_CODE", "0001");
                rtnMap.put("RESULT_MSG", "시스템에 문제가 발생하였습니다. 다음에 다시 진행 부탁드립니다.");
                return rtnMap;
            }
        } catch (Exception e) {
            rtnMap.put("RESULT_CODE", "0003");
            rtnMap.put("RESULT_MSG", "시스템에 문제가 발생하였습니다. 다음에 다시 진행 부탁드립니다.");
            //이력 정보 저장 처리
            McpIpStatisticDto mcpIpStatisticDto = new McpIpStatisticDto();
            mcpIpStatisticDto.setPrcsMdlInd("FS8_ERROR");
            mcpIpStatisticDto.setPrcsSbst("Exception[Exception]");
            ipstatisticService.insertAdminAccessTrace(mcpIpStatisticDto);
        }

        SessionUtils.increaseFathTryCount(); //URL전송받기 성공시 시도횟수 +

        String resltCd = simpleOsstXmlFs8VO.getResltCd();
        HashMap<String, Object> resltRtnMap = fathService.fathResltRtn(resltCd, osstFathReqDto);
        if(!AJAX_SUCCESS.equals(resltRtnMap.get("RESULT_CODE"))) {
            return resltRtnMap;
        }

        SessionUtils.saveFathTransacId(simpleOsstXmlFs8VO.getFathTransacId());   // 트랜잭션 ID 세션 저장
        //CD02, CD05는 FS9 연동하여 연동결과 리턴
        if("CD02".equals(resltCd) || "CD05".equals(resltCd)) {
            //FS9는 트래픽1회로 제한 (연동 이력이 존재하면 재연동하지않는다.)
            if("Y".equals(SessionUtils.getFathSession().getIsFs9())) {
                throw new McpCommonJsonException("9997", "이미 안면인증 결과확인을 하셨습니다.");
            }
            return this.processOsstFs9Service(osstFathReqDto, resltCd);
        }

        rtnMap.put("RESULT_CODE", AJAX_SUCCESS);
        StringBuilder sb = new StringBuilder("안면인증 URL이 전송되었습니다.");
        if (this.isEnabledFT1()) {
            sb.append("<br><br>※ 현재는 시범기간으로 안면인증을 SKIP 하실 수 있습니다.");
        }
        rtnMap.put("RESULT_MSG", sb.toString());
        rtnMap.put("RESLT_SBST", simpleOsstXmlFs8VO.getResltSbst());
        rtnMap.put("FATH_TRANSAC_ID", simpleOsstXmlFs8VO.getFathTransacId());
        rtnMap.put("RESLT_CD", simpleOsstXmlFs8VO.getResltCd());
        if ("STG".equals(serverName)) {
            rtnMap.put("URL_ADR", simpleOsstXmlFs8VO.getUrlAdr());
        }

        fathService.sendMsgFathUrl(simpleOsstXmlFs8VO.getUrlAdr(), osstFathReqDto.getSmsRcvTelNo());

        // 셀프안면인증 개인화 URL인 경우 신청서에 트랜잭션 ID 저장
        if("P".equals(osstFathReqDto.getGubun())) {
            AppformReqDto appformReqDto = new AppformReqDto();
            appformReqDto.setResNo(osstFathReqDto.getResNo());
            appformReqDto.setFathTransacId(String.valueOf(rtnMap.get("FATH_TRANSAC_ID")));
            appformReqDto.setOperType(osstFathReqDto.getOperType());
            appformReqDto.setCpntId(osstFathReqDto.getCpntId());
            fathService.updateFathTransacIdByResNo(appformReqDto);
        }
        
        return rtnMap;
    }
```

**처리 로직 / 변환 처리:**

```
o appformSvc.processOsstFs8Service
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
sendOsstFs8Service
-> mplatFormOsstServerAdapter.callService
-> [M플랫폼] FS8
```

**테이블/API:** FS8  
**처리 로직 / 변환 처리:**

```
o sendOsstFs8Service
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
ipstatisticService.insertAdminAccessTrace
-> fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin

        INSERT INTO MCP_ADMIN_ACCESS_TRACE (
            PARAMETER,
            ACCESS_IP,
            SYS_DT,
            SYS_RDATE,
            ACCESS_URL,
            USER_ID,
            PRCS_MDL_IND,
            PRCS_SBST,
            TRTM_RSLT_SMST
        ) VALUES (
                     #{parameter},
                     #{accessIp},
                     TO_CHAR(SYSDATE, 'YYYYMMDD'),
                     SYSDATE,
                     #{accessUrl},
                     SUBSTRB(#{userid},0,20),
                     #{prcsMdlInd},
                     SUBSTRB(#{prcsSbst},0,1000),
                     #{trtmRsltSmst}
                 )
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 호출 로그 적재  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
fathService.fathResltRtn

 public HashMap<String, Object> fathResltRtn(String resltCd, OsstFathReqDto osstFathReqDto){
   
  /* 
  0000 안면인증 필수
  CD01 안면인증 불필요 - 오픈일자 미도래
  CD02 안면인증 불필요 - 안면인증 스킵 권한 보유자
  CD03 안면인증 미오픈 대면 사업자 (2026-02-11 기준 미사용)
  CD04 안면인증 불필요 - MIS 장애 스킵 개통 불가
  CD05 안면인증 필수 - MIS 장애 스킵
  CD06 안면인증 불필요 - 안면인증 미대상 접점
  CD07 안면인증 불필요 - 안면인증 미대상 신분증
  CD08 안면인증 불필요 - 안면인증 실패 스킵 권한 보유자
  CD09 안면인증 불필요 - 대리인 개통 안면인증 스킵 권한 보유자
  * 안면인증 필수 = 오더 처리 시 안면인증트랜잭션ID 수록
  * 응답값 CD02 / CD08 / CD09 의 경우, 온라인 개통/명변/우수기변 시 KOS를 통해서 처리 필수(MP 통해 오더 업무 처리 불가)
  * 응답값 CD04 의 경우, MIS장애로 오더 업무 불가한 상태.  이후 MIS장애가 해소되면 안면인증 대상여부 조회부터 재수행 필수.
  */

  HashMap<String, Object> rtnMap = new HashMap<>();
  
  
  if(!"P".equals(osstFathReqDto.getGubun())) { //신청서 작성시점인 경우 (개인화 URL은 해당되지않음)
   String stbznPerdYn = SessionUtils.getFathSession().getStbznPerdYn();
   String onOffType = osstFathReqDto.getOnOffType();
   if(StringUtils.isEmpty(stbznPerdYn)) {
    rtnMap.put("RESULT_CODE", "9991");
    rtnMap.put("RESULT_MSG", FATH_CERT_EXPIR_EXCEPTION);
    return rtnMap;
   }
   if("N".equals(stbznPerdYn) && ("5".equals(onOffType) || "7".equals(onOffType))) { //안정화 기간 미적용이고 셀프개통인 경우
    List<String> happyCallList = Arrays.asList("CD02", "CD04", "CD08", "CD09"); //셀프개통 불가 -> 해피콜신청서로 전환 
    if(happyCallList.contains(resltCd)) {
     rtnMap.put("RESULT_CODE", "00001");
     rtnMap.put("RESULT_MSG", "시스템에 문제가 발생하여 현재 상담사 개통 신청만 가능합니다. <br>상담사 개통신청으로 진행합니다.");
     return rtnMap;
    }
   }
  }  
  
  //CD02 안면인증 불필요 - 안면인증 스킵 권한 보유자는 상담사 개통 시 FS8, FS9 수행을 위해 안면인증 로직을 적용함
  List<String> trgList = Arrays.asList("0000", "CD02", "CD05"); //안면인증 적용대상 
  if(trgList.contains(resltCd)) {
   rtnMap.put("RESULT_CODE", AJAX_SUCCESS);
  } else {
   rtnMap.put("RESULT_CODE", "00002");
   rtnMap.put("RESULT_MSG", "안면인증 없이 신분증정보 입력으로 진행됩니다. <br> 향후 추가 인증이 필요할 수 있습니다.");
  }
  return rtnMap;
 
  
 }
```

**처리 로직 / 변환 처리:**

```
o fathService.fathResltRtn
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
processOsstFs9Service
-> 소스 : AppformController-reqFathUrlRqtAjax.java
```

**처리 로직 / 변환 처리:**

```
o processOsstFs9Service
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
fathService.getFathFormInfo
-> fathDao.getNameChgInfo
-> [SQL] FathMapper.getNameChgInfo

안면인증 명의변경 신청서 조회
        SELECT  CHG.CSTMR_TYPE
                ,CASE WHEN CHG.CSTMR_TYPE = 'NM' THEN AGT.NEE_AGENT_SELF_CERT_TYPE  /* 미성년자*/
                     ELSE CHG.SELF_CERT_TYPE
                END AS SELF_CERT_TYPE
                ,'1100011741' AS CNTPNT_SHOP_ID
                ,CASE WHEN CHG.CSTMR_TYPE = 'NM' THEN AGT.NEE_AGENT_NAME  /* 미성년자*/
                      ELSE CHG.CSTMR_NAME
                 END AS CSTMR_NAME
                ,CASE WHEN CHG.CSTMR_TYPE = 'NM' THEN AGT.NEE_AGENT_RRN  /* 미성년자*/
                      ELSE CHG.CSTMR_NATIVE_RRN
                 END AS CSTMR_RRN
                ,NVL(CHG.FATH_TEL_NO, CHG.CSTMR_RECEIVE_TEL_FN || CHG.CSTMR_RECEIVE_TEL_MN || CHG.CSTMR_RECEIVE_TEL_RN) AS FATH_TEL_NO
        FROM    NMCP_CUST_REQUEST_NAME_CHG CHG
                ,NMCP_CUST_REQ_NAME_CHG_AGENT AGT
        WHERE   CHG.CUST_REQ_SEQ = AGT.CUST_REQ_SEQ(+)
        AND     MCN_RES_NO = #{resNo}
```

**테이블/API:** NMCP_CUST_REQUEST_NAME_CHG, NMCP_CUST_REQ_NAME_CHG_AGENT  
**처리 로직 / 변환 처리:**

```
o FathMapper.getNameChgInfo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
-> NMCP_CUST_REQ_NAME_CHG_AGENT : MSF_REQUEST_AGENT 가입신청서 대리인 테이블로 통합
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
fathService.getFathFormInfo
-> fathDao.getFathAppForm
-> [SQL] FathMapper.getFathAppForm

안면인증 신청서 조회

        SELECT  T1.CSTMR_TYPE
                ,CASE WHEN T1.CSTMR_TYPE = 'NM' THEN T3.MINOR_SELF_CERT_TYPE  /* 미성년자*/
                     ELSE T2.SELF_CERT_TYPE
                END AS SELF_CERT_TYPE
                ,T1.CNTPNT_SHOP_ID
                ,CASE WHEN T1.CSTMR_TYPE = 'NM' THEN T3.MINOR_AGENT_NAME  /* 미성년자*/
                      ELSE T2.CSTMR_NAME
                END AS CSTMR_NAME
                ,CASE WHEN T1.CSTMR_TYPE = 'NM' THEN T3.MINOR_AGENT_RRN  /* 미성년자*/
                      WHEN T1.CSTMR_TYPE = 'NA' THEN T2.CSTMR_NATIVE_RRN /* 내국인*/
                      ELSE T2.CSTMR_FOREIGNER_RRN                        /* 외국인*/
                END AS CSTMR_RRN
                ,FATH_MOBILE_FN || FATH_MOBILE_MN || FATH_MOBILE_RN AS FATH_TEL_NO
        FROM    MCP_REQUEST T1
               ,MCP_REQUEST_CSTMR T2
               ,MCP_REQUEST_AGENT T3
        WHERE   T1.REQUEST_KEY = T2.REQUEST_KEY
          AND   T1.REQUEST_KEY = T3.REQUEST_KEY(+)
          AND   T1.RES_NO =  #{resNo}
```

**테이블/API:** MCP_REQUEST, MCP_REQUEST_CSTMR, MCP_REQUEST_AGENT  
**처리 로직 / 변환 처리:**

```
o FathMapper.getFathAppForm
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
getAgentCode
-> appformDao.getAgentCode
-> [M포탈-M전산 API] /appform/agentCode
```

**처리 로직 / 변환 처리:**

```
o /appform/agentCode
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
sendOsstFs9Service
-> mplatFormOsstServerAdapter.callService
-> [M플랫폼] FS9

-> 소스 : AppformController-reqFathUrlRqtAjax.java
```

**테이블/API:** FS9  
**처리 로직 / 변환 처리:**

```
o sendOsstFs9Service
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
fathService.fathSuccRtn
-> 소스 : FathServiceImpl-fathSuccRtn.java
```

**처리 로직 / 변환 처리:**

```
o fathService.fathSuccRtn
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
fathService.fathSuccRtn
-> fathDao.updateNameChgComplete
-> [SQL] FathMapper.updateNameChgComplete

        UPDATE NMCP_CUST_REQUEST_NAME_CHG SET
            FATH_CMPLT_NTFY_DT = #{fathCmpltNtfyDt},
            MCN_STATE_CODE = '03',
            RVISN_ID = 'SELF',
            RVISN_DTTM = SYSDATE
        WHERE MCN_RES_NO = #{resNo}
```

**테이블/API:** NMCP_CUST_REQUEST_NAME_CHG  
**처리 로직 / 변환 처리:**

```
o FathMapper.updateNameChgComplete
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
fathService.fathSuccRtn
-> fathDao.updateNameChgSelfCertInfoMinor
-> [SQL] FathMapper.updateNameChgSelfCertInfoMinor

        UPDATE  NMCP_CUST_REQ_NAME_CHG_AGENT
        SET     NEE_AGENT_SELF_CERT_TYPE      = #{selfCertType},
                NEE_AGENT_SELF_ISSU_EXPR_DT   = #{selfIssuExprDt},
                NEE_AGENT_SELF_ISSU_NUM       = #{selfIssuNum},
                RVISN_ID                      = 'SELF',
                RVISN_DTTM                    = SYSDATE
        WHERE   CUST_REQ_SEQ                  = (SELECT CUST_REQ_SEQ FROM NMCP_CUST_REQUEST_NAME_CHG WHERE MCN_RES_NO = #{mcnResNo})
```

**테이블/API:** NMCP_CUST_REQ_NAME_CHG_AGENT  
**처리 로직 / 변환 처리:**

```
o FathMapper.updateNameChgSelfCertInfoMinor
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
-> NMCP_CUST_REQ_NAME_CHG_AGENT : MSF_REQUEST_AGENT 가입신청서 대리인 테이블로 통합
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
fathService.fathSuccRtn
-> fathDao.updateNameChgSelfCertInfo
-> [SQL] FathMapper.updateNameChgSelfCertInfo

        UPDATE  NMCP_CUST_REQUEST_NAME_CHG
        SET     SELF_CERT_TYPE      = #{selfCertType},
                SELF_ISSU_EXPR_DT   = #{selfIssuExprDt},
                SELF_ISSU_NUM       = #{selfIssuNum},
                RVISN_ID            = 'SELF',
                RVISN_DTTM          = SYSDATE
        WHERE   MCN_RES_NO          = #{mcnResNo}
```

**테이블/API:** NMCP_CUST_REQUEST_NAME_CHG  
**처리 로직 / 변환 처리:**

```
o FathMapper.updateNameChgSelfCertInfo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
fathService.fathSuccRtn
-> fathDao.updateSelfCertInfoMinor
-> [SQL] FathMapper.updateSelfCertInfoMinor

        UPDATE  MCP_REQUEST_AGENT
        SET     MINOR_SELF_CERT_TYPE      = #{selfCertType},
                MINOR_SELF_ISSU_EXPR_DT   = #{selfIssuExprDt},
                MINOR_SELF_ISSU_NUM       = #{selfIssuNum},
                RVISN_ID        = 'SELF',
                RVISN_DTTM          = SYSDATE
        WHERE  REQUEST_KEY         = (SELECT REQUEST_KEY FROM MCP_REQUEST WHERE RES_NO = #{resNo})
```

**테이블/API:** MCP_REQUEST_AGENT  
**처리 로직 / 변환 처리:**

```
o FathMapper.updateSelfCertInfoMinor
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
fathService.fathSuccRtn
-> fathDao.updateSelfCertInfo
-> [SQL] FathMapper.updateSelfCertInfo

        UPDATE  MCP_REQUEST_CSTMR
        SET     SELF_CERT_TYPE      = #{selfCertType},
                SELF_ISSU_EXPR_DT   = #{selfIssuExprDt},
                SELF_ISSU_NUM       = #{selfIssuNum},
                RVISN_ID        = 'SELF',
                RVISN_DTTM          = SYSDATE
        WHERE  REQUEST_KEY         = (SELECT REQUEST_KEY FROM MCP_REQUEST WHERE RES_NO = #{resNo})
```

**테이블/API:** MCP_REQUEST_CSTMR  
**처리 로직 / 변환 처리:**

```
o FathMapper.updateSelfCertInfo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
fathService.fathFailRtn

 public HashMap<String, Object> fathFailRtn(OsstFathReqDto osstFathReqDto) {
  HashMap<String, Object> rtnMap = new HashMap<>();
  
  if("P".equals(osstFathReqDto.getGubun())) {
   rtnMap.put("RESULT_CODE", "00002");
   rtnMap.put("RESULT_MSG", "안면인증 실패 하였습니다. <br> 향후 추가 인증이 필요할 수 있습니다.  <br>(개통은 계속 진행 됩니다.)");
   return rtnMap;
  }
  
  String stbznPerdYn = SessionUtils.getFathSession().getStbznPerdYn();
  String onOffType = osstFathReqDto.getOnOffType();
  if(StringUtils.isEmpty(stbznPerdYn)) {
   throw new McpCommonJsonException("9991", FATH_CERT_EXPIR_EXCEPTION);
  }

  if("5".equals(onOffType) || "7".equals(onOffType)) { //셀프개통인 경우
   if("Y".equals(stbznPerdYn)) {
    rtnMap.put("RESULT_CODE", "00002");
    rtnMap.put("RESULT_MSG", "안면인증 실패 하였습니다. <br> 향후 추가 인증이 필요할 수 있습니다.  <br>(개통은 계속 진행 됩니다.)"); 
   } else {
    rtnMap.put("RESULT_CODE", "00001");
    rtnMap.put("RESULT_MSG", "안면인증 실패 하여<br> 현재 상담사 개통 신청만 가능합니다. <br>상담사 개통신청으로 진행합니다.");
   }
  } else { //상담사 개통인 경우 
   rtnMap.put("RESULT_CODE", "00002");
   rtnMap.put("RESULT_MSG", "안면인증 실패 하였습니다. <br> 향후 추가 인증이 필요할 수 있습니다.  <br>(개통은 계속 진행 됩니다.)");
  }
  return rtnMap;
 }
```

**처리 로직 / 변환 처리:**

```
o fathService.fathFailRtn
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
fathService.sendMsgFathUrl
-> fCommonSvc.getMspSmsTemplateMst
-> fCommonDao.getMspSmsTemplateMst
-> [M포탈-M전산 API] /common/mspSmsTemplateMst


 public void sendMsgFathUrl(String urlAdr, String smsRcvTelNo) {
  MspSmsTemplateMstDto mspSmsTemplateMstDto = fCommonSvc.getMspSmsTemplateMst(443);
  String text = mspSmsTemplateMstDto.getText();
  String smsMsg = text.replaceAll(Pattern.quote("#{url}"), urlAdr);
  smsSvc.sendLms(mspSmsTemplateMstDto.getSubject(), smsRcvTelNo, smsMsg,
    mspSmsTemplateMstDto.getCallback(),String.valueOf(443),"SYSTEM");
 }
```

**처리 로직 / 변환 처리:**

```
o /common/mspSmsTemplateMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**URL:** /appform/reqFathTxnRetvAjax.do  
**Controller:**

```
AppformController.java - reqFathTxnRetvAjax
  - 고객 안면인증 내역 조회

        HashMap<String, Object> rtnMap = new HashMap<>();

        // 세션의 트랜잭션 ID
        String fathTransacId = SessionUtils.getFathSession().getTransacId();
        if(StringUtils.isEmpty(fathTransacId)) {
            throw new McpCommonJsonException("9999", FATH_CERT_EXPIR_EXCEPTION);
        }
        // 안면인증 결과 PUSH 조회
        FathDto fathDto = fathService.selectFathResltPush(fathTransacId);

        if(fathDto == null) { //PUSH알림이 들어오지 않은경우
            //최초 안면인증확인 버튼 클릭시점 세션에 저장
            if(SessionUtils.getFathSession().getFathResltFirstReqAt() == null) {
                SessionUtils.saveFathResltFirstReqAt();
                throw new McpCommonJsonException("9998", FATH_CERT_RESLT1_EXCEPTION);
            }
            //FS9는 트래픽1회로 제한 (연동 이력이 존재하면 재연동하지않는다.)
            if("Y".equals(SessionUtils.getFathSession().getIsFs9())) {
                throw new McpCommonJsonException("9997", "이미 안면인증 결과확인을 하셨습니다.");
            }
            //공통코드 안면인증 PUSH 타임아웃 초과시간(2026-02-03 기준 10분)
            long fathResltTimeOut = Integer.parseInt(NmcpServiceUtils.getCodeNm("fathCertPolicy", "fathResltTimeOut")) * 60 * 1000;
            Date now = new Date();
            long passTimeMs  = now.getTime() - SessionUtils.getFathSession().getFathResltFirstReqAt().getTime();

            if(passTimeMs > fathResltTimeOut) { //타임아웃 시간 초과 시 FS9 연동
                return appformSvc.processOsstFs9Service(osstFathReqDto, "");
            } else {
                throw new McpCommonJsonException("9995", FATH_CERT_RESLT1_EXCEPTION);
            }
        }
        // 안면인증 PUSH 결과 성공인 경우
        if("0000".equals(fathDto.getFathResltCd())) {
            //복호화
            try {
                fathDto.setCustNm(EncryptUtil.ace256Dec(fathDto.getCustNm()));              //안면인증 이름
                fathDto.setCustIdfyNo(EncryptUtil.ace256Dec(fathDto.getCustIdfyNo()));      //안면인증 주민번호
                fathDto.setDriveLicnsNo(EncryptUtil.ace256Dec(fathDto.getDriveLicnsNo()));  //안면인증 운전면허번호
            } catch (CryptoException e) {
                throw new McpCommonJsonException("0001", ACE_256_DECRYPT_EXCEPTION);
            }
            rtnMap = fathService.fathSuccRtn(fathDto, osstFathReqDto);
        } else {
            rtnMap = fathService.fathFailRtn(osstFathReqDto);
        }
        return rtnMap;
```

**처리 로직 / 변환 처리:**

```
o AppformController.java - reqFathTxnRetvAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
fathService.selectFathResltPush
  - 안면인증 결과 PUSH 조회
```

**Service / Dao / SQL / API:**

```
fathDao.selectFathResltPush
-> [SQL] FathMapper.selectFathResltPush

        SELECT  FATH_TRANSAC_ID,  
                SLS_CMPC_CD,           
                RETV_CD_VAL,          
                CUST_NM,              
                CUST_IDFY_NO,         
                ISS_DATE_VAL,         
                DRIVE_LICNS_NO,       
                MBL_IDCARD_QR_IMG,   
                IDCARD_CONF_WAY,      
                DIST_RSRTN_YN,        
                FATH_PROGR_STEP_CD,
                FATH_CMPLT_NTFY_DT,
                FATH_URL_RQT_DT,          
                FATH_RESLT_CD,        
                FATH_RESLT_SBST,
                FATH_RQTR_ID
        FROM    NMCP_FATH_RESLT_PUSH
        WHERE FATH_TRANSAC_ID = #{fathTransacId}
        AND     SEQ = (SELECT MAX(SEQ) FROM NMCP_FATH_RESLT_PUSH WHERE  FATH_TRANSAC_ID = #{fathTransacId})
```

**테이블/API:** NMCP_FATH_RESLT_PUSH  
**처리 로직 / 변환 처리:**

```
o FathMapper.selectFathResltPush
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
fathService.fathSuccRtn
-> 소스 : FathServiceImpl-fathSuccRtn.java
```

**처리 로직 / 변환 처리:**

```
o fathService.fathSuccRtn
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
fathService.fathFailRtn

 public HashMap<String, Object> fathFailRtn(OsstFathReqDto osstFathReqDto) {
  HashMap<String, Object> rtnMap = new HashMap<>();
  
  if("P".equals(osstFathReqDto.getGubun())) {
   rtnMap.put("RESULT_CODE", "00002");
   rtnMap.put("RESULT_MSG", "안면인증 실패 하였습니다. <br> 향후 추가 인증이 필요할 수 있습니다.  <br>(개통은 계속 진행 됩니다.)");
   return rtnMap;
  }
  
  String stbznPerdYn = SessionUtils.getFathSession().getStbznPerdYn();
  String onOffType = osstFathReqDto.getOnOffType();
  if(StringUtils.isEmpty(stbznPerdYn)) {
   throw new McpCommonJsonException("9991", FATH_CERT_EXPIR_EXCEPTION);
  }

  if("5".equals(onOffType) || "7".equals(onOffType)) { //셀프개통인 경우
   if("Y".equals(stbznPerdYn)) {
    rtnMap.put("RESULT_CODE", "00002");
    rtnMap.put("RESULT_MSG", "안면인증 실패 하였습니다. <br> 향후 추가 인증이 필요할 수 있습니다.  <br>(개통은 계속 진행 됩니다.)"); 
   } else {
    rtnMap.put("RESULT_CODE", "00001");
    rtnMap.put("RESULT_MSG", "안면인증 실패 하여<br> 현재 상담사 개통 신청만 가능합니다. <br>상담사 개통신청으로 진행합니다.");
   }
  } else { //상담사 개통인 경우 
   rtnMap.put("RESULT_CODE", "00002");
   rtnMap.put("RESULT_MSG", "안면인증 실패 하였습니다. <br> 향후 추가 인증이 필요할 수 있습니다.  <br>(개통은 계속 진행 됩니다.)");
  }
  return rtnMap;
 }
```

**처리 로직 / 변환 처리:**

```
o fathService.fathFailRtn
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**URL:** /appform/reqFathTgtYnAjax.do  
**Controller:**

```
AppformController.java - reqFathTgtYnAjax
  - 고객 안면인증 대상 여부 조회 (FT0)

        if (StringUtils.isEmpty(osstFathReqDto.getOnlineOfflnDivCd()) ||
                 StringUtils.isEmpty(osstFathReqDto.getOrgId()) ||
                 StringUtils.isEmpty(osstFathReqDto.getSelfCertType())) {
            throw new McpCommonJsonException("1001", F_BIND_EXCEPTION);
        }

        HashMap<String, Object> rtnMap = new HashMap<>();
        // 안면인증 사용 공통코드 확인
        if(!"Y".equals(fathService.getFathUseYn())) {
            throw new McpCommonJsonException("00002", "안면인증 미사용");
        }
        // 안면인증 대상 신분증 여부확인
        String fathCertIdTypeYn = NmcpServiceUtils.getCodeNm("fathCertIdType", osstFathReqDto.getSelfCertType());
        if(!"Y".equals(fathCertIdTypeYn)) {
            throw new McpCommonJsonException("00002", "안면인증 미대상");
        }


        //이미 안면인증 완료를 하여 세션에 안면인증 완료일이 들어가있는경우
        if(!StringUtil.isEmpty(SessionUtils.getFathSession().getCmpltNtfyDt())) {
            throw new McpCommonJsonException("1002", "이미 안면인증을 완료하였습니다.");
        }

        String cpntId = fathService.findAndSaveSessionCpntId(osstFathReqDto.getOrgId());
        if (StringUtils.isEmpty(cpntId)) {
            String onOffType = osstFathReqDto.getOnOffType();
            if("5".equals(onOffType) || "7".equals(onOffType)) {
                throw new McpCommonJsonException("00001", "안면인증 대상이 아닙니다. <br> 현재 상담사 개통 신청만 가능합니다. <br>상담사 개통신청으로 진행합니다.");
            } else {
                throw new McpCommonJsonException("00002", "안면인증 없이 신분증정보 입력으로 진행됩니다. <br> 향후 추가 인증이 필요할 수 있습니다.");
            }
        }
        osstFathReqDto.setCpntId(cpntId);

        //안면인증 세션 초기화
        SessionUtils.initializeFathSession();
        String tempResNo = fathService.generateTempResNo();
        SessionUtils.saveFathTempResNo(tempResNo);

        osstFathReqDto.setResNo(tempResNo);

        osstFathReqDto.setRetvCdVal(FATH_RETV_CD_VAL.get(osstFathReqDto.getSelfCertType()));
        //대리점 코드 조회
        String agentCode = "";
        agentCode = appformSvc.getAgentCode(osstFathReqDto.getOrgId());
        if (StringUtils.isBlank(agentCode)) {
            throw new McpCommonJsonException("1004", "시스템 오류로 메세지가 반복되면 고객센터로 문의부탁드립니다.");
        } else {
            osstFathReqDto.setAsgnAgncId(agentCode);
        }

        return appformSvc.processOsstFt0Service(osstFathReqDto);
```

**처리 로직 / 변환 처리:**

```
o AppformController.java - reqFathTgtYnAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
NmcpServiceUtils.getCodeNm
  - 안면인증 대상 신분증 여부확인

    public static String getCodeNm(String grpCd, String dtlCd) {
        List<NmcpCdDtlDto> codeList = null;
        DbCacheHandler dbCacheHandler = getBean(DbCacheHandler.class);

        @SuppressWarnings("unchecked")
        Map<String, List<NmcpCdDtlDto>> codeMap = (Map<String, List<NmcpCdDtlDto>>) dbCacheHandler.getValue(CACHE_CODE);
        if (codeMap != null) {
            codeList = codeMap.get(grpCd);
            if (codeList != null) {
                for (NmcpCdDtlDto bean : codeList) {
                    if (bean.getDtlCd().equals(dtlCd)) {
                        return bean.getDtlCdNm();
                    }
                }
            }
        }
        return "";
    }
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNm
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
fathService.findAndSaveSessionCpntId

 public String findAndSaveSessionCpntId(String cntpntShopId) {
  String cpntId = SessionUtils.getFathSession().getCpntId();
  if (!StringUtils.isEmpty(cpntId)) {
   return cpntId;
  }

  if (orgnId.equals(cntpntShopId)) {
   cpntId = cntpntShopId;
  } else { //직영온라인이 아닌 경우
   //(대리점)안면인증 가능 접점ID조회
   cpntId = fathDao.getCpntId(cntpntShopId);
  }
  SessionUtils.saveFathCpntId(cpntId);

  return cpntId;
 }
```

**Service / Dao / SQL / API:**

```
fathDao.getCpntId
-> [M포탈-M전산 API] /appform/getCpntId
```

**처리 로직 / 변환 처리:**

```
o /appform/getCpntId
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
appformSvc.getAgentCode
  - 대리점 코드 조회
```

**Service / Dao / SQL / API:**

```
appformDao.getAgentCode
-> [M포탈-M전산 API] /appform/agentCode
```

**처리 로직 / 변환 처리:**

```
o /appform/agentCode
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
appformSvc.processOsstFt0Service

    public HashMap<String, Object> processOsstFt0Service(OsstFathReqDto osstFathReqDto) {
        HashMap<String, Object> rtnMap = new HashMap<>();

        int maxRetryCount = Integer.parseInt(NmcpServiceUtils.getCodeNm("fathCertPolicy", "reTryFT0"));
        for (int i = 0; i < maxRetryCount; i++) {

            try {
                //Thread.sleep(3000);
                MSimpleOsstXmlFt0VO simpleOsstXmlFt0VO = this.sendOsstFt0Service(osstFathReqDto);
                if (simpleOsstXmlFt0VO.isSuccess()) {
                    
                    //안면인증 안정화기간 여부
                    String stbznPerdYn = simpleOsstXmlFt0VO.getStbznPerdYn();
                    SessionUtils.saveStbznPerdYn(stbznPerdYn);

                    String trtResltCd = simpleOsstXmlFt0VO.getTrtResltCd();
                    return fathService.fathResltRtn(trtResltCd, osstFathReqDto);
                } 
            } catch (Exception e) {
                logger.error("OSST FT0 연동 재시도 #" + (i+1) );
               
                //Exception 반복문 재시도 
            }
        }
        //최종 성공아닐경우 연동실패로 리턴
        rtnMap.put("RESULT_CODE", "0001");
        rtnMap.put("RESULT_MSG", "시스템에 문제가 발생하였습니다. 다음에 다시 진행 부탁드립니다.");

        //이력 정보 저장 처리
        McpIpStatisticDto mcpIpStatisticDto = new McpIpStatisticDto();
        mcpIpStatisticDto.setPrcsMdlInd("FT0_ERROR");
        mcpIpStatisticDto.setPrcsSbst("Exception[Exception]");
        ipstatisticService.insertAdminAccessTrace(mcpIpStatisticDto);
        
        return rtnMap;
    }
```

**처리 로직 / 변환 처리:**

```
o appformSvc.processOsstFt0Service
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
NmcpServiceUtils.getCodeNm

    public static String getCodeNm(String grpCd, String dtlCd) {
        List<NmcpCdDtlDto> codeList = null;
        DbCacheHandler dbCacheHandler = getBean(DbCacheHandler.class);

        @SuppressWarnings("unchecked")
        Map<String, List<NmcpCdDtlDto>> codeMap = (Map<String, List<NmcpCdDtlDto>>) dbCacheHandler.getValue(CACHE_CODE);
        if (codeMap != null) {
            codeList = codeMap.get(grpCd);
            if (codeList != null) {
                for (NmcpCdDtlDto bean : codeList) {
                    if (bean.getDtlCd().equals(dtlCd)) {
                        return bean.getDtlCdNm();
                    }
                }
            }
        }
        return "";
    }
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNm
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
sendOsstFt0Service
-> mplatFormOsstServerAdapter.callService
-> [M플랫폼] FT0

    private MSimpleOsstXmlFt0VO sendOsstFt0Service(OsstFathReqDto osstFathReqDto) throws SelfServiceException, SocketTimeoutException ,McpMplatFormException{
        MSimpleOsstXmlFt0VO simpleOsstXmlFt0VO =  new MSimpleOsstXmlFt0VO();
        HashMap<String, String> param = new HashMap<String, String>();
        param.put("appEventCd", Constants.EVENT_CODE_FATH_TGT_YN);
        param.put("onlineOfflnDivCd", osstFathReqDto.getOnlineOfflnDivCd());
        param.put("orgId", osstFathReqDto.getOrgId());
        param.put("retvCdVal", osstFathReqDto.getRetvCdVal());
        param.put("asgnAgncId", osstFathReqDto.getAsgnAgncId());
        param.put("cpntId", osstFathReqDto.getCpntId());
        param.put("resNo", osstFathReqDto.getResNo());

        mplatFormOsstServerAdapter.callService(param,simpleOsstXmlFt0VO,100000);
        return simpleOsstXmlFt0VO;
    }
```

**테이블/API:** FT0  
**처리 로직 / 변환 처리:**

```
o sendOsstFt0Service
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
ipstatisticService.insertAdminAccessTrace
-> fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin

        INSERT INTO MCP_ADMIN_ACCESS_TRACE (
            PARAMETER,
            ACCESS_IP,
            SYS_DT,
            SYS_RDATE,
            ACCESS_URL,
            USER_ID,
            PRCS_MDL_IND,
            PRCS_SBST,
            TRTM_RSLT_SMST
        ) VALUES (
                     #{parameter},
                     #{accessIp},
                     TO_CHAR(SYSDATE, 'YYYYMMDD'),
                     SYSDATE,
                     #{accessUrl},
                     SUBSTRB(#{userid},0,20),
                     #{prcsMdlInd},
                     SUBSTRB(#{prcsSbst},0,1000),
                     #{trtmRsltSmst}
                 )
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 호출 로그 적재  
**개발팀 전달일:** 03/22 전달  
---

**URL:** /auth/getReqAuthAjax.do  
**Controller:**

```
NiceCertifyController.java - getReqAuthAjax
  - 소스 : NiceCertifyController-getReqAuthAjax.java
```

**처리 로직 / 변환 처리:**

```
o NiceCertifyController.java - getReqAuthAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
nicelog.getMcpNiceHistWithReqSeq

    public NiceLogDto getMcpNiceHistWithReqSeq(NiceLogDto niceLogDto) {
        NiceLogDto rtnLogDto = niceLogDao.getMcpNiceHistWithReqSeq(niceLogDto);
        
        if (rtnLogDto == null) {
            /*
            * 오늘 날짜에 없으면 
            *  이전 날짜로 다시 검색
            *  23시 59분 58초 이후에 검색 못 할 수도 있음
             */
            rtnLogDto = niceLogDao.getMcpNiceHistWithReqSeq2(niceLogDto);
        }
        
        return rtnLogDto ;
    }
```

**Service / Dao / SQL / API:**

```
niceLogDao.getMcpNiceHistWithReqSeq
-> [SQL] NiceLogMapper.getMcpNiceHistWithReqSeq

        SELECT
            NICE_HIST_SEQ            --PK
            , REQ_SEQ                  --일련번호 기본키
             , RES_SEQ                  --요청 일련번호
             , AUTH_TYPE                 --요청 구분
             , NAME                     --인증성명
             , BIRTH_DATE               --인증생년월일
             , GENDER                   --GENDER
             , NATIONAL_INFO            --NATIONAL_INFO
             , DUP_INFO                 --DUP_INFO
             , CONN_INFO                --CONN_INFO
             , PARAM_R1                 --파라메터 1
             , PARAM_R2                 --파라메터 2
             , PARAM_R3                 --파라메터 3
             , END_YN                   --종료여부
             , N_REFERER                --referer
             , RIP                      --등록아이피
             , SYS_RDATE_DT             --등록일
             , SYS_RDATE                --등록일시
             , RVISN_DTTM               --수정일
             , COMPL_DTTM               --인증 완료시
             , MOBILE_CO  as sMobileCo              -- 이동사구분(모바일 인증)
             , MOBILE_NO  as sMobileNo              -- 휴대폰 번호(모바일 인증)
        FROM MCP_NICE_HIST
        WHERE 1=1
          AND SYS_RDATE_DT = TO_CHAR(SYSDATE, 'YYYYMMDD')
          AND REQ_SEQ = #{reqSeq}
          AND RES_SEQ = #{resSeq}
          AND SYS_RDATE >= (SYSDATE-2/24/60) -- 2분전 DATA만 유효
          AND ROWNUM =1
```

**테이블/API:** MCP_NICE_HIST  
**처리 로직 / 변환 처리:**

```
o NiceLogMapper.getMcpNiceHistWithReqSeq
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
niceLogDao.getMcpNiceHistWithReqSeq2
-> [SQL] NiceLogMapper.getMcpNiceHistWithReqSeq2

        SELECT
            NICE_HIST_SEQ            --PK
             , REQ_SEQ                  --일련번호 기본키
             , RES_SEQ                  --요청 일련번호
             , AUTH_TYPE                 --요청 구분
             , NAME                     --인증성명
             , BIRTH_DATE               --인증생년월일
             , GENDER                   --GENDER
             , NATIONAL_INFO            --NATIONAL_INFO
             , DUP_INFO                 --DUP_INFO
             , CONN_INFO                --CONN_INFO
             , PARAM_R1                 --파라메터 1
             , PARAM_R2                 --파라메터 2
             , PARAM_R3                 --파라메터 3
             , END_YN                   --종료여부
             , N_REFERER                --referer
             , RIP                      --등록아이피
             , SYS_RDATE_DT             --등록일
             , SYS_RDATE                --등록일시
             , RVISN_DTTM               --수정일
             , COMPL_DTTM               --인증 완료시
             , MOBILE_CO  as sMobileCo              -- 이동사구분(모바일 인증)
             , MOBILE_NO  as sMobileNo              -- 휴대폰 번호(모바일 인증)
        FROM MCP_NICE_HIST
        WHERE 1=1
          AND SYS_RDATE_DT = TO_CHAR(SYSDATE-1, 'YYYYMMDD')
          AND REQ_SEQ = #{reqSeq}
          AND RES_SEQ = #{resSeq}
          AND SYS_RDATE >= (SYSDATE-2/24/60) -- 2분전 DATA만 유효
          AND ROWNUM =1
```

**테이블/API:** MCP_NICE_HIST  
**처리 로직 / 변환 처리:**

```
o NiceLogMapper.getMcpNiceHistWithReqSeq2
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
certService.isAuthStepApplyUrl
  - 인증 개선 대상 url인지 검사

        Map<String, String> resultMap = new HashMap<>();
        resultMap.put("isAuthStep", "N");

        String redirectPage = "/main.do";
        if("Y".equals(NmcpServiceUtils.isMobile())) redirectPage = "/m/main.do";

        String requestUrl = (request.getHeader("referer") == null) ? "" : request.getHeader("referer");
        if("".equals(requestUrl)) throw new McpCommonException(COMMON_EXCEPTION, redirectPage);

        String uri = requestUrl.substring(request.getHeader("referer").indexOf("/", 8));
        int endIndex = uri.indexOf('?');
        if(endIndex > -1) uri = uri.substring(0, endIndex);

        // 인증 개선 대상 URL
        String[] appformUrlArr = {"appForm/appForm.do", "m/appForm/appForm.do"                  // 핸드폰, 상담사, 셀프개통, ESIM, ESIM WATCH
                                 ,"request/marketRequest.do", "m/request/marketRequest.do"      // 대리점개통
                                 ,"content/reqSharingView.do", "m/content/reqSharingView.do", "content/dataSharingStep3.do", "m/content/dataSharingStep3.do"   // 데이터쉐어링
                                 ,"mypage/myNameChg.do", "m/mypage/myNameChg.do"    // 명의변경 신청
                                 ,"cs/ownPhoNum.do", "m/cs/ownPhoNum.do"       // 가입번호 조회
                                 ,"m/appform/setUsimNoDlvey.do"          // 유심번호 등록 인증
                                 ,"mypage/paywayWiew.do", "m/mypage/paywayWiew.do"     // 납부방법변경
                                 ,"mypage/paywayView.do", "m/mypage/paywayView.do"     // 납부방법변경
                                 ,"mypage/usimSelfChg.do", "m/mypage/usimSelfChg.do"            // 유심 셀프변경
                                 ,"mypage/reqRwd.do", "m/mypage/reqRwd.do"                      // 자급제 보상서비스
                                 ,"appForm/appAgentForm.do", "m/appForm/appAgentForm.do"        // 대리점 셀프개통
                                 ,"appForm/appJehuForm.do", "m/appForm/appJehuForm.do"          // 제휴위탁온라인서식지
                                 ,"m/apply/replaceUsimSelf.do"
        };

        List<String> appformurlList = Arrays.asList(appformUrlArr);

        if(appformurlList.contains(uri.replaceAll("^/+",""))) resultMap.put("isAuthStep","Y");

        return resultMap;
```

**처리 로직 / 변환 처리:**

```
o certService.isAuthStepApplyUrl
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 제외  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
niceCertifySvc.chkAgentInfo

    public Map<String, String> chkAgentInfo(String cstmrName, NiceResDto childniceResDto, NiceLogDto agentNiceLogRtn,UserSessionDto userSession) {

        Map<String,String> vldReslt= new HashMap<>();

        if(childniceResDto == null){
            vldReslt.put("RESULT_CODE", "AUTH04");
            vldReslt.put("RESULT_DESC", NICE_CERT_EXCEPTION_INSR);
            return vldReslt;
        }

        // 화면에 입력한 법정대리인 이름과 인증한 법정대리인 이름 비교
        String niceUserNm= agentNiceLogRtn.getName().toUpperCase().replaceAll(" ", "");
        String inputCstmrName= StringUtil.NVL(cstmrName, "").toUpperCase().replaceAll(" ", "");

        if(!niceUserNm.equals(inputCstmrName)){
            vldReslt.put("RESULT_CODE", "AUTH05");
            vldReslt.put("RESULT_DESC", "법정대리인 정보가 일치하지 않습니다");
            return vldReslt;
        }


        // 어린이 인증값(이름,핸드폰번호)으로 계약정보 조회
        JuoSubInfoDto juoSubInfoRtn = mypageUserService.selectJuoSubInfo(childniceResDto.getName(),childniceResDto.getsMobileNo());

        String userDivision = "";
        if(userSession != null) userDivision = userSession.getUserDivision();

        // 계약정보가 null 인데 정회원인 경우 (회원정보 수정)
        if(juoSubInfoRtn == null && "01".equals(userDivision)) {
            // 세션ID로 정회원목록 조회
            List<McpUserCntrMngDto> cntrList = mypageService.selectCntrList(userSession.getUserId());
            if(cntrList == null || cntrList.size() < 0){
                vldReslt.put("RESULT_CODE", "AUTH07");
                vldReslt.put("RESULT_DESC", "정회원 정보가 존재하지 않습니다. 고객센터에 문의 하시기 바랍니다.");
                return vldReslt;
            }
            // 정회원 정보로 계약정보 재 조회
            juoSubInfoRtn = mypageUserService.selectJuoSubInfo(cntrList.get(0).getUserName(), cntrList.get(0).getCntrMobileNo());
            if(juoSubInfoRtn == null) {
                vldReslt.put("RESULT_CODE", "AUTH08");
                vldReslt.put("RESULT_DESC", "계약정보가 존재하지 않습니다. 고객센터에 문의 하시기 바랍니다. ");
                return vldReslt;
            }
        }
        // 기변인 경우 계약정보 없으면 실패
        JuoSubInfoDto changeAut = SessionUtils.getChangeAutCookieBean();
        if (changeAut != null && !StringUtils.isBlank(changeAut.getContractNum()) && juoSubInfoRtn == null) {
            vldReslt.put("RESULT_CODE", "AUTH08");
            vldReslt.put("RESULT_DESC", "입력하신 정보는 kt M모바일에 가입된 정보가 아니거나 사용중인 상태가 아닙니다 <br> 확인 후 다시 입력 해 주세요.");
            return vldReslt;
        }

        // 계약정보존재
        if(juoSubInfoRtn != null && ("I".equals(juoSubInfoRtn.getCustomerType()) || "O".equals(juoSubInfoRtn.getCustomerType()))){
            String dbLegalCi = juoSubInfoRtn.getLegalCi(); //DB 법정대리인 Ci값
            // 기변이면 dbLegalCi가 존재하면 비교 / 비어있으면 비교하지않고 통과
            if (changeAut != null && !StringUtils.isBlank(changeAut.getContractNum())) {
                if (!StringUtils.isBlank(dbLegalCi) && !agentNiceLogRtn.getConnInfo().equals(dbLegalCi)) {
                    vldReslt.put("RESULT_CODE", "AUTH06");
                    vldReslt.put("RESULT_DESC", "미성년자의 경우 기기변경 휴대폰 번호를 개통할 당시의 법정대리인 정보로 본인인증이 필요합니다. <br><br>" +
                            "법정대리인 정보 확인 후 다시 시도를 부탁드리며, 불가할 경우 고객센터(1899-5000)으로 문의를 부탁드립니다.");
                    return vldReslt;
                } else {
                    //dbLegalCi값이 비어있을경우
                    if(StringUtils.isBlank(dbLegalCi)) {
                        vldReslt.put("RESULT_CODE", "AUTH06");
                        vldReslt.put("RESULT_DESC", "법정대리인 정보 확인까지 몇분정도 걸릴수 있습니다. 잠시 후 다시 시도해주세요.");
                        return vldReslt;
                    }
                }
            } else {
                //dbLegalCi값이 비어있을경우
                if(StringUtils.isBlank(dbLegalCi)) {
                    vldReslt.put("RESULT_CODE", "AUTH06");
                    vldReslt.put("RESULT_DESC", "법정대리인 정보 확인까지 몇분정도 걸릴수 있습니다. 잠시 후 다시 시도해주세요.");
                    return vldReslt;
                }
                // 계약정보의 법정대리인 Ci값과 인증한 Ci값이 다른경우
                if(!agentNiceLogRtn.getConnInfo().equals(dbLegalCi)) {
                    vldReslt.put("RESULT_CODE", "AUTH06");
                    vldReslt.put("RESULT_DESC", "부 혹은 모와 동일한 법정대리인의 휴대폰인증을 해야 합니다.");
                    return vldReslt;
                }
            }
        } else {
            /**
             * 기변일때 법정 대리인 나이 확인 하지 않음...
             * 회원 가입 / 수정 할때 법정 대리인 정보 나이 확인 처리
             */
            //법정대리인의 나이 확인
            int agentAge = 0;
            try{
                String agentBirthDate = agentNiceLogRtn.getBirthDate().length() > 8 ? EncryptUtil.ace256Dec(agentNiceLogRtn.getBirthDate()) : agentNiceLogRtn.getBirthDate();
                agentAge = NmcpServiceUtils.getBirthDateToAge(agentBirthDate, new SimpleDateFormat("yyyyMMdd", Locale.KOREA).format(new Date()));
            } catch (CryptoException e) {
                logger.error("Exception e : {}", e.getMessage());
            }

            if(agentAge < 19) {
                vldReslt.put("RESULT_CODE", "AUTH05");
                vldReslt.put("RESULT_DESC", "법정대리인의 나이는 만 19세 이상이어야 합니다.");
                return vldReslt;
            }
        }




        vldReslt.put("RESULT_CODE", AJAX_SUCCESS);
        vldReslt.put("RESULT_DESC", "성공");
        return vldReslt;
    }
```

**처리 로직 / 변환 처리:**

```
o niceCertifySvc.chkAgentInfo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
mypageUserService.selectJuoSubInfo
-> [M포탈-M전산 API] /mypage/juoSubInfo
```

**처리 로직 / 변환 처리:**

```
o /mypage/juoSubInfo
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.selectCntrList
-> [M포탈-M전산 API] /mypage/cntrList

    public List<McpUserCntrMngDto> selectCntrList(String userId) {
        //---- API 호출 S ----//
        //MultiValueMap<String, String> params = new LinkedMultiValueMap<String, String>();
        Map<String, String> params = new HashMap<String, String>();
        params.put("userId", userId);
        UserSessionDto userSessionDto1 = SessionUtils.getUserCookieBean();
        if (userSessionDto1 != null) {
            params.put("customerId", userSessionDto1.getCustomerId());
        }

        RestTemplate restTemplate = new RestTemplate();
        //McpUserCntrMngDto[] resultList = restTemplate.postForObject(apiInterfaceServer + "/mypage/cntrList", userId, McpUserCntrMngDto[].class);
        McpUserCntrMngDto[] resultList = restTemplate.postForObject(apiInterfaceServer + "/mypage/cntrList", params, McpUserCntrMngDto[].class);

        UserSessionDto userSessionDto2 = SessionUtils.getUserCookieBean();

        List<McpUserCntrMngDto> list = null;
        if (Optional.ofNullable(resultList).isPresent() && resultList.length != 0) {
            list = Arrays.asList(resultList);

            // UC0 유심셀프변경 화면에서 미성년자 체크를 위해 추가
            for (McpUserCntrMngDto mcpUserCntrMngDto : list) {
                String strUnUserSSn = mcpUserCntrMngDto.getUnUserSSn();
                mcpUserCntrMngDto.setAge(Integer.toString(getAge(strUnUserSSn)));
                if (strUnUserSSn != null && strUnUserSSn.length() > 5) {
                    mcpUserCntrMngDto.setBirth(strUnUserSSn.substring(0, 6));
                } else if (strUnUserSSn != null) {
                    mcpUserCntrMngDto.setBirth(strUnUserSSn);
                }

            }
        }


        //---- API 호출 E ----//
        return list;
    }
```

**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service:** nicelog.getMcpNiceTryHist  
**Service / Dao / SQL / API:**

```
niceLogDao.getMcpNiceTryHist
-> [SQL] NiceLogMapper.getMcpNiceTryHist

        SELECT
            NICE_TRY_HIST_SEQ       /*순번 (PK)*/
          , REQ_SEQ                 /*요청 일련번호*/
          , RES_SEQ                 /*응답 일련번호*/
          , AUTH_TYPE               /*본인인증 유형*/
          , NAME                    /*인증성명*/
          , BIRTH_DATE              /*인증 생년월일*/
          , CONN_INFO               /*CID*/
          , N_REFERER               /*접속 URL*/
          , RIP                     /*접속 IP*/
          , SYS_RDATE_DT            /*생성일*/
          , SYS_RDATE               /*인증 시작일시*/
          , RVISN_DTTM              /*수정일시*/
          , SUCC_YN                 /*성공,실패여부*/
          , FNL_SUCC_YN             /*최종 성공,실패 여부 (본인인증에 사용한 정보와 고객정보 일치 여부)*/
        FROM MCP_NICE_TRY_HIST
        WHERE 1=1
        AND SYS_RDATE_DT BETWEEN TO_CHAR(SYSDATE-1, 'YYYYMMDD') AND TO_CHAR(SYSDATE, 'YYYYMMDD')
        AND REQ_SEQ = #{reqSeq}
        AND RES_SEQ = #{resSeq}
        AND SYS_RDATE <![CDATA[>=]]> (SYSDATE-5/24/60) -- 5분전 DATA만 유효
        AND ROWNUM = 1
```

**테이블/API:** MCP_NICE_TRY_HIST  
**처리 로직 / 변환 처리:**

```
o NiceLogMapper.getMcpNiceTryHist
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  
---

**Service:** nicelog.updateMcpNiceTryHist  
**Service / Dao / SQL / API:**

```
niceLogDao.updateMcpNiceTryHist
-> [SQL] NiceLogMapper.updateMcpNiceTryHist

        UPDATE MCP_NICE_TRY_HIST SET
            RVISN_DTTM = SYSDATE
        <if test="reqSeq != null and reqSeq != ''">
            , REQ_SEQ = #{reqSeq}
        </if>
        <if test="resSeq != null and resSeq != ''">
            , RES_SEQ = #{resSeq}
        </if>
        <if test="name != null and name != ''">
            , NAME = #{name}
        </if>
        <if test="birthDate != null and birthDate != ''">
            , BIRTH_DATE = #{birthDate}
        </if>
        <if test="connInfo != null and connInfo != ''">
            , CONN_INFO = #{connInfo}
        </if>
        <if test="nReferer != null and nReferer != ''">
            , N_REFERER = #{nReferer}
        </if>
        <if test="sysRdate != null and sysRdate != ''">
            , SYS_RDATE = #{sysRdate}
        </if>
        <if test="fnlSuccYn != null and fnlSuccYn != ''">
            , FNL_SUCC_YN = #{fnlSuccYn}
        </if>
        WHERE 1=1
        AND NICE_TRY_HIST_SEQ = #{niceTryHistSeq}
```

**테이블/API:** MCP_NICE_TRY_HIST  
**처리 로직 / 변환 처리:**

```
o NiceLogMapper.updateMcpNiceTryHist
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/22 전달  

---

## [1003-21]

**JSP/JS:** /mobile/appForm/appOutFormComplete.jsp, /resources/js/mobile/appForm/appFormComplete.js  
**URL:** /m/appForm/appOutFormComplete.do  
**Controller:**

```
AppformController.java - appOutFormComplete
- 소스 : AppformController-appOutFormComplete.java
```

**처리 로직 / 변환 처리:**

```
o AppformController.java - appOutFormComplete
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
appformSvc.getAppForm
  - 신청서 정보 확인
```

**Service / Dao / SQL / API:**

```
appformDao.getAppForm
-> [SQL] AppformMapper.getAppForm

        SELECT
            T1.REQUEST_KEY
             , CSTMR_TYPE
             , REQ_USIM_SN
             , CLAUSE_MPPS35_FLAG
             , SOC_CODE                      --요금제코드
             , OPER_TYPE                   --업무구분
             , REQ_BUY_TYPE                --구매유형
             , MODEL_ID
             , MODEL_SALE_POLICY_CODE
             , ON_OFF_TYPE
             , CSTMR_NAME               --고객정보_고객명
             , CSTMR_NATIVE_RRN         --고객정보_내국인_주민등록번호
             , CSTMR_POST               --고객정보_우편번호
             , CSTMR_ADDR               --고객정보_주소
             , CSTMR_ADDR_DTL           --고객정보_상세주소
             , CSTMR_TEL_FN             --고객정보_전화번호_앞자리
             , CSTMR_TEL_MN             --고객정보_전화번호_가운데자리
             , CSTMR_TEL_RN             --고객정보_전화번호_끝자리
             , CSTMR_MOBILE_FN          --고객정보_휴대폰번호_앞자리
             , CSTMR_MOBILE_MN          --고객정보_휴대폰번호_중간자리
             , CSTMR_MOBILE_RN          --고객정보_휴대폰번호_끝자리
             , CSTMR_MAIL               --고객정보_메일
             , SELF_CSTMR_CI
             , CSTMR_FOREIGNER_NATION   --고객정보_외국인_국적
             , CSTMR_FOREIGNER_PN       --고객정보_외국인_여권번호
             , CSTMR_FOREIGNER_SDATE    --고객정보_외국인_체류_시작날자
             , CSTMR_FOREIGNER_EDATE    --고객정보_외국인_체류_종료날자
             , SELF_CERT_TYPE           --본인인증유형
             , SELF_ISSU_EXPR_DT        --발급/만료일자
             , SELF_ISSU_NUM            --발급번호
             , MINOR_AGENT_NAME
             , MINOR_AGENT_RRN
             , MINOR_AGENT_TEL_FN
             , MINOR_AGENT_TEL_MN
             , MINOR_AGENT_TEL_RN
             , MINOR_AGENT_RELATION
             , MINOR_SELF_CERT_TYPE     --본인인증유형
             , MINOR_SELF_ISSU_EXPR_DT  --발급/만료일자
             , MINOR_SELF_ISSU_NUM      --발급번호
             , CSTMR_JEJU_ID            -- 운전면호 구분driverSelfIssuNum1
             , DLVRY_NAME               --배송정보_이름
             , DLVRY_TEL_FN             --배송정보_전화번호_앞자리
             , DLVRY_TEL_MN             --배송정보_전화번호_중간자리
             , DLVRY_TEL_RN             --배송정보_전화번호_뒷자리
             , DLVRY_MOBILE_FN          --배송정보_휴대폰번호_앞자리
             , DLVRY_MOBILE_MN          --배송정보_휴대폰번호_중간자리
             , DLVRY_MOBILE_RN          --배송정보_휴대폰번호_뒷자리
             , DLVRY_POST               --배송정보_우편번호
             , DLVRY_ADDR               --배송정보_주소
             , DLVRY_ADDR_DTL           --배송정보_상세주소
             , DLVRY_MEMO               --배송정보_요청사항
             , REQ_WANT_NUMBER
             , REQ_WANT_NUMBER2
             , REQ_WANT_NUMBER3
             , MOVE_COMPANY             --번호이동정보_변경전통신사
             , MOVE_MOBILE_FN           --번호이동정보_이동할번호_앞자리
             , MOVE_MOBILE_MN           --번호이동정보_이동할번호_중간자리
             , MOVE_MOBILE_RN           --번호이동정보_이동할번호_끝자리
             , MOVE_AUTH_TYPE           --번호이동정보_인증유형
             , MOVE_AUTH_NUMBER         --번호이동정보_인증번호4자리
             , RECOMMEND_FLAG_CD
             , RECOMMEND_INFO
             , INSR_PROD_CD
             , T1.RES_NO
             , T1.RWD_PROD_CD
             , T1.CLAUSE_RWD_FLAG
             , T1.RWD_AUTH_INFO
             , REQ_PAY_TYPE
             , CSTMR_BILL_SEND_CODE
             , REQ_BANK
             , REQ_ACCOUNT_NUMBER
             , REQ_CARD_COMPANY
             , REQ_CARD_NO
             , REQ_CARD_YY
             , REQ_CARD_MM
             , SESPLS_YN
             , CNTPNT_SHOP_ID
             , USIM_KINDS_CD             -- 유심종류(RCP2035)
             , PRNTS_CONTRACT_NO         -- 모회선 계약번호
             , SETTL_AMT                 -- 결제금액
             , MODEL_SALE_POLICY_CODE
        FROM
            MCP_REQUEST T1
           , MCP_REQUEST_SALEINFO T2
           , MCP_REQUEST_CSTMR T3
           , MCP_REQUEST_AGENT T4
           , MCP_REQUEST_DLVRY T5
           , MCP_REQUEST_MOVE T6
           , MCP_REQUEST_REQ T7
        WHERE
            T1.REQUEST_KEY = T2.REQUEST_KEY
          AND T1.REQUEST_KEY = T3.REQUEST_KEY(+)
          AND T1.REQUEST_KEY = T4.REQUEST_KEY(+)
          AND T1.REQUEST_KEY = T5.REQUEST_KEY(+)
          AND T1.REQUEST_KEY = T6.REQUEST_KEY(+)
          AND T1.REQUEST_KEY = T7.REQUEST_KEY(+)
          AND T1.REQUEST_KEY =  #{requestKey}
```

**테이블/API:** MCP_REQUEST, MCP_REQUEST_SALEINFO, MCP_REQUEST_CSTMR, MCP_REQUEST_AGENT, MCP_REQUEST_DLVRY, MCP_REQUEST_MOVE, MCP_REQUEST_REQ  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getAppForm
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
  - MCP_REQUEST_DLVRY : 배송 관련 로직은 제외
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
appformSvc.getAgentInfoOjb
  - 대리점명 조회
```

**Service / Dao / SQL / API:**

```
appformDao.getAgentInfoOjb
-> [M포탈-M전산 API] /appform/agentInfoOjb
```

**처리 로직 / 변환 처리:**

```
o /appform/agentInfoOjb
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** [M포탈-M전산 API] /msp/mspPhoneInfo  
**처리 로직 / 변환 처리:**

```
o /msp/mspPhoneInfo
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/22 전달  
---

**URL:** /appForm/getAgentFormLinkAjax.do  
**Controller:**

```
AppformController.java - getAgentFormLinkAjax

    public Map<String, String> getAgentFormLinkAjax(HttpServletRequest request) {

        Map<String, String> rtnMap = new HashMap<>();

        // 대리점 신청서 URL 세션 조회
        String agentFormLink = SessionUtils.getAgentFormLink();

        if (agentFormLink != null && !"".equals(agentFormLink)) {
            rtnMap.put("RESULT_CODE", AJAX_SUCCESS);
            rtnMap.put("agentFormLink", agentFormLink);
        } else {
            throw new McpCommonJsonException(NO_SESSION_EXCEPTION);
        }

        return rtnMap;
    }
```

**처리 로직 / 변환 처리:**

```
o AppformController.java - getAgentFormLinkAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**URL:** /appform/sendScanAjax.do  
**Controller:**

```
AppformController.java - sendScan
 - SCAN 서버 전송
 - 소스 : AppformController-sendScan.java
```

**처리 로직 / 변환 처리:**

```
o AppformController.java - sendScan
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** appformSvc.getMcpRequest  
**Service / Dao / SQL / API:**

```
appformDao.getMcpRequest
-> [SQL] AppformMapper.getMcpRequestSelfDlvry

        SELECT
        SELF_DLVRY_IDX
        , USIM_PROD_ID
        , USIM_PROD_DTL_ID
        , CSTMR_NAME
        , CSTMR_NATIVE_RRN
        , ONLINE_AUTH_TYPE
        , ONLINE_AUTH_INFO
        , ONLINE_AUTH_DI
        , DLVRY_NAME
        , DLVRY_TEL_FN
        , DLVRY_TEL_MN
        , DLVRY_TEL_RN
        , DLVRY_POST
        , DLVRY_ADDR
        , DLVRY_ADDR_DTL
        , DLVRY_MEMO
        , SELF_MEMO
        , REQ_USIM_SN
        , DLVRY_STATE_CODE
        , SELF_STATE_CODE
        , DLVRY_NO
        , TB_CD
        , CONTRACT_NUM
        , CRET_ID
        , RVISN_ID
        , RVISN_DTTM
        , SYS_RDATE
        , REQ_BUY_QNTY
        , USIM_UCOST
        , USIM_PRICE
        , DLVRY_TYPE
        FROM MCP_REQUEST_SELF_DLVRY
        WHERE 1=1
        <if test="selfDlvryIdx != null and selfDlvryIdx > 0 ">
            AND SELF_DLVRY_IDX = #{selfDlvryIdx}
        </if>
        <if test="onlineAuthDi !=null and onlineAuthDi != ''" >
            AND ONLINE_AUTH_DI = #{onlineAuthDi}
        </if>
        <if test=" !(onlineAuthDi !=null and onlineAuthDi != '') " >
            AND CSTMR_NAME = #{cstmrName} AND DLVRY_TEL_FN || DLVRY_TEL_MN || DLVRY_TEL_RN = #{dlvryTelFn}||#{dlvryTelMn}||#{dlvryTelRn}
        </if>
        <if test="sysRdateS !=null and sysRdateS != '' and sysRdateE !=null and sysRdateE != ''" >
            AND SYS_RDATE BETWEEN  TO_DATE(#{sysRdateS}||'000000', 'YYYYMMDDHH24MISS') AND TO_DATE(#{sysRdateE}||'235959', 'YYYYMMDDHH24MISS')
        </if>
        ORDER BY SELF_DLVRY_IDX DESC
```

**테이블/API:** MCP_REQUEST_SELF_DLVRY  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getMcpRequestSelfDlvry
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
scanSvc.prodSendScan
  - SCAN 서버에 서식지 데이터 생성 및 전송
 - 소스 : ScanSvcImpl-prodSendScan.java
```

**처리 로직 / 변환 처리:**

```
o scanSvc.prodSendScan
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

ㅇ ITO AS-IS 소스에 로직 변경
 - 신규 포시에스 리포팅 시스템에 맞춰서 로직 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.getAppFormData
-> [M포탈-M전산 API] /appform/requestJoinDataByRjoinKeyNew
```

**처리 로직 / 변환 처리:**

```
o /appform/requestJoinDataByRjoinKeyNew
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용

ㅇ ITO AS-IS 소스에 로직 변경
 - 신규 포시에스 리포팅 시스템에 맞춰서 로직 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** createXml  
**처리 로직 / 변환 처리:**

```
o createXml
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

ㅇ ITO AS-IS 소스에 로직 변경
 - 신규 포시에스 리포팅 시스템에 맞춰서 로직 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
createXml
-> getGroupCode
-> NmcpServiceUtils.getCodeList
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
createXml
-> appformDao.getAppFormPointList
-> [SQL] AppformMapper.getAppFormPointList
```

**테이블/API:** MCP_APPLICATION_FORM_INFO  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getAppFormPointList
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경 : 포시에스 리포트 툴에서 사용이 필요할지 확인 후 필요한 경우 사용

ㅇ ITO AS-IS 소스에 로직 변경
 - 신규 포시에스 리포팅 시스템에 맞춰서 로직 변경
```

**기타:** 확인 필요 - 포시에스 솔루션에서 필요할지 확인  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** xmlFileSend  
**처리 로직 / 변환 처리:**

```
o xmlFileSend
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

ㅇ ITO AS-IS 소스에 로직 변경
 - 신규 포시에스 리포팅 시스템에 맞춰서 로직 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
xmlFileSend
-> ipstatisticService.insertAdminAccessTrace
-> fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 호출 로그 적재  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.updateAppFormXmlYn
-> [SQL] AppformMapper.updateAppFormXmlYn

        UPDATE
        MCP_REQUEST
        SET
        APP_FORM_XML_YN = 'Y'
        WHERE
        REQUEST_KEY = #{requestKey}
```

**테이블/API:** MCP_REQUEST  
**처리 로직 / 변환 처리:**

```
o AppformMapper.updateAppFormXmlYn
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경

ㅇ ITO AS-IS 소스에 로직 변경
 - 신규 포시에스 리포팅 시스템에 맞춰서 로직 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** ipstatisticService.insertAdminAccessTrace  
**Service / Dao / SQL / API:**

```
fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin

        INSERT INTO MCP_ADMIN_ACCESS_TRACE (
            PARAMETER,
            ACCESS_IP,
            SYS_DT,
            SYS_RDATE,
            ACCESS_URL,
            USER_ID,
            PRCS_MDL_IND,
            PRCS_SBST,
            TRTM_RSLT_SMST
        ) VALUES (
                     #{parameter},
                     #{accessIp},
                     TO_CHAR(SYSDATE, 'YYYYMMDD'),
                     SYSDATE,
                     #{accessUrl},
                     SUBSTRB(#{userid},0,20),
                     #{prcsMdlInd},
                     SUBSTRB(#{prcsSbst},0,1000),
                     #{trtmRsltSmst}
                 )
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 호출 로그 적재  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
ipstatisticService.getReferer

        String referer = "";
        HttpServletRequest request = ((ServletRequestAttributes) RequestContextHolder.currentRequestAttributes()).getRequest();
        referer = request.getHeader("referer");
        return referer;
```

**처리 로직 / 변환 처리:**

```
o ipstatisticService.getReferer
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
NmcpServiceUtils.getCodeList

        List<NmcpCdDtlDto> codeList = null;
        List<NmcpCdDtlDto> codeRstList = null;
        DbCacheHandler dbCacheHandler = getBean(DbCacheHandler.class);

        @SuppressWarnings("unchecked")
        Map<String, List<NmcpCdDtlDto>> codeMap = (Map<String, List<NmcpCdDtlDto>>) dbCacheHandler.getValue(CACHE_CODE);
        if (codeMap != null) {
            codeList = codeMap.get(grpCd);
            codeRstList = new ArrayList<NmcpCdDtlDto>();

            if(codeList !=null && !codeList.isEmpty()) {
                for(NmcpCdDtlDto bean : codeList) {
                    if(DateTimeUtil.checkValidDate(bean.getPstngStartDate(), bean.getPstngEndDate())) {
                        codeRstList.add(bean);
                    }
                }
            }
        }
        return codeRstList;
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 제외  
**개발팀 전달일:** 03/22 전달  
---

**URL:** /nice/popNiceAuth.do  
**Controller:** NiceCertifyController.java - popNiceAuth  
**기타:** 제외 - 본인인증  
**개발팀 전달일:** 03/22 전달  
---

**URL:** /m/dlvryInfo.do  
**Controller:** FCommonController.java - dlvryInfo  
**기타:** 제외 - 배송  
**개발팀 전달일:** 03/22 전달  

---

## [1010]

**URL:** /appForm/insertAppFormTempSave.do  
**Controller:**

```
AppformController.java - insertAppFormTempSave
  - 임시저장 처리
```

**Service:** appformSvc.getTempRequestKey  
**Service / Dao / SQL / API:**

```
appformDao.getTempRequestKey
-> [SQL] AppformMapper.getTempRequestKey

        SELECT SQ_REQUEST_TEMP_KEY_SEQ.NEXTVAL FROM DUAL
```

**테이블/API:** SQ_REQUEST_TEMP_KEY_SEQ  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getTempRequestKey
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경 : 임시등록 신청서일련번호 SQ_RCP_RES_NO_01 사용하지 않고 실제 신청서 일련번호와 같이 사용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** appformSvc.insertAppFormTempSave  
**Service / Dao / SQL / API:**

```
appformDao.insertAppFormTempSave
-> [SQL] AppformMapper.insertAppFormTempSave

        INSERT INTO NMCP_REQUEST_TEMP
        (
            REQUEST_KEY
        , MANAGER_CODE
        , AGENT_CODE
        , SERVICE_TYPE
        , OPER_TYPE
        , CSTMR_TYPE
        , RES_CODE
        , RES_MSG
        , RES_NO
        , CLAUSE_PRI_COLLECT_FLAG
        , CLAUSE_PRI_OFFER_FLAG
        , CLAUSE_ESS_COLLECT_FLAG
        , CLAUSE_PRI_TRUST_FLAG
        , CLAUSE_PRI_AD_FLAG
        , CLAUSE_CONFIDENCE_FLAG
        , ONLINE_AUTH_TYPE
        , ONLINE_AUTH_INFO
        , PSTATE
        , REQUEST_STATE_CODE
        , OPEN_NO
        , FILE01
        , FILE01_MASK
        , FAXYN
        , FAXNUM
        , SCAN_ID
        , ON_OFF_TYPE
        , RIP
        , OPEN_REQ_DATE
        , SYS_RDATE
        , REQ_WANT_NUMBER
        , REQ_WANT_NUMBER2
        , REQ_WANT_NUMBER3
        , REQ_BUY_TYPE
        , REQ_MODEL_NAME
        , REQ_MODEL_COLOR
        , REQ_PHONE_SN
        , REQ_USIM_SN
        , REQ_PAY_TYPE
        , REQ_ADDITION
        , SHOP_CD
        , APP_FORM_YN
        , REQ_IN_DAY
        , CONTRACT_NUM
        , ETC_SPECIAL
        , REQ_USIM_NAME
        , PHONE_PAYMENT
        , REQ_ADDITION_PRICE
        , APP_FORM_XML_YN
        , SPC_CODE
        , CNTPNT_SHOP_ID
        , SHOP_USM_ID
        , MEMO
        , REC_YN
        , OPEN_MARKET_REFERER
        , SO_CD
        , NW_BLCK_AGRM_YN
        , APP_BLCK_AGRM_YN
        , APP_CD
        , SO_TRNS_AGRM_YN
        , CLAUSE_JEHU_FLAG
        , SHOP_NM
        , PROD_ID
        , CRET_ID
        , BANNER_CD
        , SNTY_COLOR_CD
        , SNTY_CAPAC_CD
        , PROD_TYPE
        , CLAUSE_RENTAL_MODEL_CP
        , CLAUSE_RENTAL_MODEL_CP_PR
        , CLAUSE_RENTAL_SERVICE
        , CLAUSE_MPPS35_FLAG
        , CLAUSE_FINANCE_FLAG
        , RVISN_ID
        , RVISN_DTTM
        , PROMOTION_CD
        , REAL_SHOP_NM
        , USIM_KINDS_CD
        , INSR_CD
        , CLAUSE_INSURANCE_FLAG
        , PROD_NM
        , INSR_PROD_CD
        , CLAUSE_INSR_PROD_FLAG
        , INSR_AUTH_INFO
        , RWD_PROD_CD
        , CLAUSE_RWD_FLAG
        , RWD_AUTH_INFO
        , USER_ID
        , NM
        , PIN
        , MOBILE_NO
        , PRE_URL_TYPE
        , PRDT_SCTN_CD
        , INDC_ODRG
        , TMP_STEP
        , UPLOAD_PHONE_SRL_NO
        , OPEN_MARKET_ID
        , EVNT_CD_PRMT
        , KT_COUNSEL_AGREE
        )
        VALUES
            (
                #{requestKey}
            , #{managerCode}
            , #{agentCode}
            , #{serviceType}
            , #{operType}
            , #{cstmrType}
            , #{resCode}
            , #{resMsg}
            , #{resNo}
            , #{clausePriCollectFlag}
            , #{clausePriOfferFlag}
            , #{clauseEssCollectFlag}
            , #{clausePriTrustFlag}
            , #{clausePriAdFlag}
            , #{clauseConfidenceFlag}
            , #{onlineAuthType}
            , #{onlineAuthInfo}
            , #{pstate}
            , #{requestStateCode}
            , #{openNo}
            , #{file01}
            , #{file01Mask}
            , #{faxyn}
            , #{faxnum}
            , #{scanId}
            , #{onOffType}
            , #{rip}
            , #{openReqDate}
            , SYSDATE
            , #{reqWantNumber}
            , #{reqWantNumber2}
            , #{reqWantNumber3}
            , #{reqBuyType}
            , #{reqModelName}
            , #{reqModelColor}
            , #{reqPhoneSn}
            , #{reqUsimSn}
            , #{reqPayType}
            , #{reqAddition}
            , #{shopCd}
            , #{appFormYn}
            , #{reqInDay}
            , #{contractNum}
            , #{etcSpecial}
            , #{reqUsimName}
            , #{phonePayment}
            , #{reqAdditionPrice}
            , #{appFormXmlYn}
            , #{spcCode}
            , #{cntpntShopId}
            , #{shopUsmId}
            , #{memo}
            , #{recYn}
            , #{openMarketReferer}
            , #{soCd}
            , #{nwBlckAgrmYn}
            , #{appBlckAgrmYn}
            , #{appCd}
            , #{soTrnsAgrmYn}
            , #{clauseJehuFlag}
            , #{shopNm}
            , #{prodId}
            , #{cretId}
            , #{bannerCd}
            , #{sntyColorCd}
            , #{sntyCapacCd}
            , #{prodType}
            , #{clauseRentalModelCp}
            , #{clauseRentalModelCpPr}
            , #{clauseRentalService}
            , #{clauseMpps35Flag}
            , #{clauseFinanceFlag}
            , #{rvisnId}
            , #{rvisnDttm}
            , #{promotionCd}
            , #{realShopNm}
            , #{usimKindsCd}
            , #{insrCd}
            , #{clauseInsuranceFlag}
            , #{prodNm}
            , #{insrProdCd}
            , #{clauseInsrProdFlag}
            , #{insrAuthInfo}
            , #{rwdProdCd}
            , #{clauseRwdFlag}
            , #{rwdAuthInfo}
            , #{userId}
            , #{nm}
            , #{pin}
            , #{mobileNo}
            , #{preUrlType}
            , #{prdtSctnCd}
            , #{indcOdrg}
            , #{tmpStep}
            , #{uploadPhoneSrlNo}
            , #{openMarketId}
            , #{evntCdPrmt}
            , #{ktCounselAgree}
            )
```

**테이블/API:** NMCP_REQUEST_TEMP  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertAppFormTempSave
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/22 전달  

---

## [1011]

**JSP/JS:** /mobile/popup/jusoPopup.jsp  
**URL:** /m/addPopup.do  
**Controller:** FCommonController.java - addPopup  
**처리 로직 / 변환 처리:**

```
o FCommonController.java - addPopup
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**URL:** /addrlink/addrLinkApi.do  
**Controller:**

```
FCommonController.java - addrLinkApi

    public HttpEntity<byte[]> addrLinkApi(
            @RequestParam(value = "currentPage") String currentPage
            ,@RequestParam(value = "countPerPage") String countPerPage
            ,@RequestParam(value = "keyword") String keyword
            , ModelMap model) {


        String keywordTemp = keyword ;
        try {
            keywordTemp= URLEncoder.encode(keyword, "UTF-8");
        } catch (Exception e) {
            logger.error("addrLinkApi error");
        }

        keywordTemp = keywordTemp.toUpperCase();
        keywordTemp = keywordTemp.replaceAll("OR", "");
        keywordTemp = keywordTemp.replaceAll("SELECT", "");
        keywordTemp = keywordTemp.replaceAll("INSERT", "");
        keywordTemp = keywordTemp.replaceAll("DELETE", "");
        keywordTemp = keywordTemp.replaceAll("UPDATE", "");
        keywordTemp = keywordTemp.replaceAll("CREATE", "");
        keywordTemp = keywordTemp.replaceAll("DROP", "");
        keywordTemp = keywordTemp.replaceAll("EXEC", "");
        keywordTemp = keywordTemp.replaceAll("UNION", "");
        keywordTemp = keywordTemp.replaceAll("FETCH", "");
        keywordTemp = keywordTemp.replaceAll("DECLARE", "");
        keywordTemp =keywordTemp.replaceAll("TRUNCATE", "");




        String param = "";
        try {
            param = "currentPage="+currentPage+"&countPerPage="+countPerPage+"&keyword="+keywordTemp+"&confmKey="+jusoKey ;
        } catch (Exception e) {
            logger.error("addrLinkApi error");
        }

        String url = propertiesService + "?" + param;

        HttpEntity<byte[]> result = null;
        CommonHttpClient client = new CommonHttpClient(url);
        try {
            String xml = client.get();

            byte[] documentBody = xml.getBytes("UTF-8");

            HttpHeaders header = new HttpHeaders();
            header.setContentType(new MediaType("application", "xml"));
            header.setContentLength(documentBody.length);
            result = new HttpEntity<byte[]>(documentBody, header);

        } catch (Exception e) {
            logger.error("addrLinkApi error");
        }

        return result;
    }
```

**Service / Dao / SQL / API:**

```
[외부] o ${juso.interface.server} : 
http://000.000.000.000:00000/app/search/addrSearchApi.d
```

**테이블/API:**

```
[외부] o ${juso.interface.server} : 
http://000.000.000.000:00000/app/search/addrSearchApi.d
```

**처리 로직 / 변환 처리:**

```
o FCommonController.java - addrLinkApi
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  

---

## [9001]

**URL:** X  
**Controller:** ㅇ ITO AS-IS 소스 참고하지 않고 신규 개발  
**처리 로직 / 변환 처리:**

```
ㅇ 해당 업무별로 공통코드에 정의된 공통그룹ID 에 해당하는 약관 목록 조회
1. DTL_CD_DESC = 'Y' 인 항목은 HIDE 처리
2. 약관 항목 화면에 출력
   - SORT_ODRG 순서대로 출력
   - DTL_CD_NM - 약관명
   - EXPNSN_STR_VAL3 -> Y : 필수, N : 선택, 고지 : 2
   - EXPNSN_STR_VAL1, EXPNSN_STR_VAL2 값이 없는 경우는 하위 약관 타이틀로 출력
ㅇ HIDE 처리된 특정 약관은 조건별로 해당하는 약관 SHOW 처리
   - 번호이동, 미성년자, 5G요금제, 제휴요금, M모바일/KT 등
```

**개발팀 전달일:** 03/22 전달  

---

## [9002]

**URL:** [참고] /termsPop.do  
**Controller:** ㅇ ITO AS-IS 소스 참고하지 않고 신규 개발  
**처리 로직 / 변환 처리:**

```
ㅇ ITO AS-IS 소스 참고하지 않고 신규 개발
ㅇ 공통코드에 정의된 공통그룹ID, 상세코드 에 해당하는 약관 상세 조회
   - EXPNSN_STR_VAL1 : M포탈 공통그룹 값
   - EXPNSN_STR_VAL2 : M포탈 상세코드 값
```

**기타:** 미확정 - 관리자 추가할 경우 조회 기준 변경  
**개발팀 전달일:** 03/22 전달  

---

## [셀프개통]

**URL:** /appform/reqPreOpenCheckAjax.do  
**기타:** 제외 - 신청서 직접 전송 처리  
**개발팀 전달일:** 03/22 전달  

---

## [m전산 온라인신청서 전송 처리]

**Controller:** https://scm.ktmmobile.com/repo/msp/MSP/code/sources/head/trunk/ktisMvno/  
**기타:** 제외 - 신청서 직접 전송 처리  
**개발팀 전달일:** 03/23 전달  

---

## [N7001]

**처리 로직 / 변환 처리:**

```
o 청구계정ID 유효성 체크
   -> m전산 청구계정 정보로 동일 고객 내 청구계정 조회하도록 체크

        SELECT BAN
          FROM (SELECT BAN, CUSTOMER_ID 
                  FROM MSP_JUO_BAN_INFO@DL_MSP 
                 WHERE BL_BILLING_METHOD IN ('C', 'D') -- 납부방법 : 신용카드, 자동이체
                   AND COL_DELINQ_STATUS = 'N' -- 미납여부 : 정상
               ) A
         INNER JOIN (SELECT CUSTOMER_ID FROM MSP_JUO_CUS_INFO@DL_MSP WHERE CUSTOMER_SSN = '고객식별번호') B
            ON A.CUSTOMER_ID = B.CUSTOMER_ID
        ;
```

**개발팀 전달일:** 03/23 전달  

---

## [N7002]

**URL:** 접점코드로 대리점 코드 조회  
**Service:**

```
appformSvc.getAgentCode
  - 대리점 코드 조회
```

**Service / Dao / SQL / API:**

```
appformDao.getAgentCode
-> [M포탈-M전산 API] /appform/agentCode
```

**처리 로직 / 변환 처리:**

```
o /appform/agentCode
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/23 전달  

---

## [N7003]

**Service:** 개통일자 조회  
**처리 로직 / 변환 처리:**

```
o MSP_JUO_SUB_INFO@DL_MSP
 -- 개통일자 : LST_COM_ACTV_DATE
```

**개발팀 전달일:** 03/23 전달  

---

## [N7004]

**URL:** /mypage/channelInfo  
**Controller:** MypageController.java - channelInfo  
**Service:** 개통 대리점 조회  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/channelInfo  
**처리 로직 / 변환 처리:**

```
o /mypage/channelInfo
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/23 전달  

---

## [2001-01]

**JSP/JS:** /mobile/mypage/myNameChg.jsp, /resources/js/mobile/mypage/myNameChg.js  
**URL:** /m/mypage/myNameChg.do  
**Controller:**

```
MyNameChgController.java - myNameChg
  - 명의변경
```

**처리 로직 / 변환 처리:**

```
o MyNameChgController.java - myNameChg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service:**

```
myNameChgService.selectCntrListNmChg
  - MCP 휴대폰 회선관리 리스트를 가지고 온다. (명의변경)
```

**처리 로직 / 변환 처리:**

```
o myNameChgService.selectCntrListNmChg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNmChg  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNmChg
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/23 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service:** maskingSvc.insertMaskingReleaseHist  
**Service / Dao / SQL / API:**

```
maskingDao.insertMaskingReleaseHist
-> [SQL] MaskingMapper.insertMaskingReleaseHist
```

**테이블/API:** MCP_MASKING_RELEASE_HIST  
**처리 로직 / 변환 처리:**

```
o MaskingMapper.insertMaskingReleaseHist
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/23 전달  
---

**Service:** fCommonSvc.getJehuProdInfo  
**처리 로직 / 변환 처리:**

```
o fCommonSvc.getJehuProdInfo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
mspService.getMspRateMst
-> mspDao.getMspRateMst
-> [M포탈-M전산 API] /msp/mspRateMst
```

**처리 로직 / 변환 처리:**

```
o /msp/mspRateMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
NmcpServiceUtils.getCodeList("JehuProdType")

        List<NmcpCdDtlDto> codeList = null;
        List<NmcpCdDtlDto> codeRstList = null;
        DbCacheHandler dbCacheHandler = getBean(DbCacheHandler.class);

        @SuppressWarnings("unchecked")
        Map<String, List<NmcpCdDtlDto>> codeMap = (Map<String, List<NmcpCdDtlDto>>) dbCacheHandler.getValue(CACHE_CODE);
        if (codeMap != null) {
            codeList = codeMap.get(grpCd);
            codeRstList = new ArrayList<NmcpCdDtlDto>();

            if(codeList !=null && !codeList.isEmpty()) {
                for(NmcpCdDtlDto bean : codeList) {
                    if(DateTimeUtil.checkValidDate(bean.getPstngStartDate(), bean.getPstngEndDate())) {
                        codeRstList.add(bean);
                    }
                }
            }
        }
        return codeRstList;
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/23 전달  

---

## [2001-02]

**JSP/JS:** /mobile/mypage/myNameChg.jsp, /resources/js/mobile/mypage/myNameChg.js  
**URL:** /mypage/myNameChgRequest.do  
**Controller:**

```
MyNameChgController.java - myNameChgRequest
  - 명의변경 신청
```

**처리 로직 / 변환 처리:**

```
o MyNameChgController.java - myNameChgRequest
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service:**

```
certAuthChgRequest
  -  명의변경 신청 전, 사용자 입력값 비교 로직 추가
```

**처리 로직 / 변환 처리:**

```
o certAuthChgRequest
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service:**

```
myNameChgService.selectCntrListNmChg
  - MCP 휴대폰 회선관리 리스트를 가지고 온다. (명의변경)
```

**처리 로직 / 변환 처리:**

```
o myNameChgService.selectCntrListNmChg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNmChg  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNmChg
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/23 전달  
---

**Service:** certService.getModuTypeStepCnt  
**Service / Dao / SQL / API:**

```
certDao.getModuTypeStepCnt
-> [SQL] CertMapper.getModuTypeStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getModuTypeStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/23 전달  
---

**Service:** myNameChgService.myNameChgRequest  
**처리 로직 / 변환 처리:**

```
o myNameChgService.myNameChgRequest
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
nicelog.getMcpNiceHistWithTime
-> niceLogDao.getMcpNiceHistWithTime
-> [SQL] NiceLogMapper.getMcpNiceHistWithTime
```

**테이블/API:** MCP_NICE_HIST  
**처리 로직 / 변환 처리:**

```
o NiceLogMapper.getMcpNiceHistWithTime
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:** fathService.validateFathSession  
**처리 로직 / 변환 처리:**

```
o fathService.validateFathSession
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:** fathService.updateFathMcpRequestOsst  
**처리 로직 / 변환 처리:**

```
o fathService.updateFathMcpRequestOsst
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
fathService.updateFathMcpRequestOsst
-> fathDao.updateFathMcpRequestOsst
-> [SQL] FathMapper.updateFathMcpRequestOsst
```

**테이블/API:** MCP_REQUEST_OSST  
**처리 로직 / 변환 처리:**

```
o FathMapper.updateFathMcpRequestOsst
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
myNameChgDao.insertNmcpCustReqMst
-> [SQL] MyNameChgMapper.insertNmcpCustReqMst
```

**테이블/API:** NMCP_CUST_REQUEST_MST  
**처리 로직 / 변환 처리:**

```
o MyNameChgMapper.insertNmcpCustReqMst
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
myNameChgDao.insertNmcpCustReqNameChg
-> [SQL] MyNameChgMapper.insertNmcpCustReqNameChg
```

**테이블/API:** NMCP_CUST_REQUEST_NAME_CHG  
**처리 로직 / 변환 처리:**

```
o MyNameChgMapper.insertNmcpCustReqNameChg
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
myNameChgDao.insertNmcpCustReqNameChgAgent
-> [SQL] MyNameChgMapper.insertNmcpCustReqNameChgAgent
```

**테이블/API:** NMCP_CUST_REQ_NAME_CHG_AGENT  
**처리 로직 / 변환 처리:**

```
o MyNameChgMapper.insertNmcpCustReqNameChgAgent
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
myNameChgDao.updateNmcpCustReqMst
-> [SQL] MyNameChgMapper.updateNmcpCustReqMst
```

**테이블/API:** NMCP_CUST_REQUEST_MST  
**처리 로직 / 변환 처리:**

```
o MyNameChgMapper.updateNmcpCustReqMst
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
myNameChgDao.updateNmcpCustReqNameChg
-> [SQL] MyNameChgMapper.updateNmcpCustReqNameChg
```

**테이블/API:** NMCP_CUST_REQUEST_NAME_CHG  
**처리 로직 / 변환 처리:**

```
o MyNameChgMapper.updateNmcpCustReqNameChg
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/23 전달  
---

**Service:**

```
custRequestScanService.prodSendScan
  - SCAN 서버에 서식지 데이터 생성 및 전송
  - "NC"
```

**처리 로직 / 변환 처리:**

```
o custRequestScanService.prodSendScan
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
custRequestDao.getReqNmChgData
  - 명의변경
  - reqType = "NC"
-> [SQL] CustRequestMapper.getReqNmChgData
```

**테이블/API:** NMCP_CUST_REQUEST_MST, NMCP_CUST_REQUEST_NAME_CHG, NMCP_CUST_REQ_NAME_CHG_AGENT  
**처리 로직 / 변환 처리:**

```
o CustRequestMapper.getReqNmChgData
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
settingSsnData
  - 주민번호 분석 값 설정
```

**처리 로직 / 변환 처리:**

```
o settingSsnData
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
settingData
  - 각종 설정 값 설정
```

**처리 로직 / 변환 처리:**

```
o settingData
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
createXml
  - XML 파일 생성
```

**처리 로직 / 변환 처리:**

```
o createXml
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
createXml
-> getGroupCode
```

**처리 로직 / 변환 처리:**

```
o getGroupCode
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
createXml
-> custRequestDao.getAppFormPointList
-> [SQL] CustRequestMapper.getAppFormGroupPointList
```

**테이블/API:** NMCP_APP_GROUP_MST, NMCP_APP_GROUP_INFO, NMCP_APP_FORM_INFO  
**처리 로직 / 변환 처리:**

```
o CustRequestMapper.getAppFormGroupPointList
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경

ㅇ ITO AS-IS 소스에 로직 변경
 - 신규 포시에스 리포팅 시스템에 맞춰서 로직 변경
```

**기타:** 확인 필요 - 포시에스 솔루션에서 필요할지 확인  
**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
createXml
-> saveXML
```

**처리 로직 / 변환 처리:**

```
o saveXML
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
xmlFileSend
  - XML 파일 전송 및 삭제 처리
```

**처리 로직 / 변환 처리:**

```
o xmlFileSend
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
xmlFileSend
-> ipstatisticService.insertAdminAccessTrace
-> fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
custRequestDao.updateReqNmChgScanId
  - 명의변경
  - reqType = "NC"
-> [SQL] CustRequestMapper.updateReqNmChgScanId
```

**테이블/API:** NMCP_CUST_REQUEST_NAME_CHG  
**처리 로직 / 변환 처리:**

```
o CustRequestMapper.updateReqNmChgScanId
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
custRequestDao.updateEmvScanMstNC
  - 명의변경
  - reqType = "NC"
-> [SQL] CustRequestMapper.updateEmvScanMstNC
```

**테이블/API:** EMV_SCAN_MST  
**처리 로직 / 변환 처리:**

```
o CustRequestMapper.updateEmvScanMstNC
-> SQL Copy 후 M포탈-M전산 API 에 SQL 추가하여 데이터 INSERT/UPDATE 처리
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
custRequestDao.updateEmvScanFileNC
  - 명의변경
  - reqType = "NC"
-> [SQL] CustRequestMapper.updateEmvScanFileNC
```

**테이블/API:** EMV_SCAN_FILE  
**처리 로직 / 변환 처리:**

```
o CustRequestMapper.updateEmvScanFileNC
-> SQL Copy 후 M포탈-M전산 API 에 SQL 추가하여 데이터 INSERT/UPDATE 처리
```

**개발팀 전달일:** 03/23 전달  
---

**Service:** ipstatisticService.insertAdminAccessTrace  
**Service / Dao / SQL / API:**

```
fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 호출 로그 적재  
**개발팀 전달일:** 03/23 전달  

---

## [2001-03]

**JSP/JS:** /mobile/mypage/myNameChg.jsp, /resources/js/mobile/mypage/myNameChg.js  
**URL:** /mypage/grantorReqChk.do  
**Controller:**

```
MyNameChgController.java - grantorReqChk
  - 양도인 신청가능여부 체크
```

**처리 로직 / 변환 처리:**

```
o MyNameChgController.java - grantorReqChk
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/23 전달  
---

**Service:** myNameChgService.grantorReqChk  
**처리 로직 / 변환 처리:**

```
o myNameChgService.grantorReqChk
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNmChg  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNmChg
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
farPricePlanService.moscWireUseTimeInfo
-> mPlatFormService.moscWireUseTimeInfo
-> [M플랫폼] X83
  - 회선 사용기간조회
```

**테이블/API:** X83  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.moscWireUseTimeInfo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  

---

## [2001-04]

**JSP/JS:** /mobile/mypage/myNameChg.jsp, /resources/js/mobile/mypage/myNameChg.js  
**URL:** /mypage/nameChgChkTelNo.do  
**Controller:** MyNameChgController.java - nameChgChkTelNo  
**처리 로직 / 변환 처리:**

```
o MyNameChgController.java - nameChgChkTelNo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service:** myNameChgService.nameChgChkTelNo  
**처리 로직 / 변환 처리:**

```
o myNameChgService.nameChgChkTelNo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNmChg  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNmChg
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/23 전달  

---

## [2001-05]

**JSP/JS:** /mobile/mypage/myNameChg.jsp, /resources/js/mobile/mypage/myNameChg.js  
**URL:** /mypage/userInfoChk.do  
**Controller:**

```
MyNameChgController.java - userInfoChk
  - 입력한 회원 정보(이름,주민번호)와 회선정보가 일치하는지 체크
```

**처리 로직 / 변환 처리:**

```
o MyNameChgController.java - userInfoChk
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service:**

```
myNameChgService.selectCntrListNmChg
  - MCP 휴대폰 회선관리 리스트를 가지고 온다. (명의변경)
```

**처리 로직 / 변환 처리:**

```
o myNameChgService.selectCntrListNmChg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNmChg  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNmChg
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/23 전달  

---

## [3001-01]

**JSP/JS:** /mobile/mypage/cancelConsult.jsp, /resources/js/mobile/mypage/cancelConsult.js  
**URL:** /m/mypage/cancelConsult.do  
**Controller:**

```
CancelConsultController.java - cancelConsult
  - 해지 상담 신청
```

**처리 로직 / 변환 처리:**

```
o CancelConsultController.java - cancelConsult
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service:**

```
mypageService.selectCntrList
  - 회선정보
```

**Service / Dao / SQL / API:**

```
mypageService.selectCntrList
-> [M포탈-M전산 API] /mypage/cntrList
```

**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 회원 로직 불필요  
**개발팀 전달일:** 03/23 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service:** cancelConsultSvc.selectCancelConsultList  
**Service / Dao / SQL / API:**

```
cancelConsultDao.selectCancelConsultList
-> [SQL] CancelConsultMapper.selectCancelConsultList
```

**테이블/API:** MCP_CANCEL_REQUEST  
**처리 로직 / 변환 처리:**

```
o CancelConsultMapper.selectCancelConsultList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용
```

**개발팀 전달일:** 03/23 전달  
---

**Service:** maskingSvc.insertMaskingReleaseHist  
**Service / Dao / SQL / API:**

```
maskingDao.insertMaskingReleaseHist
-> [SQL] MaskingMapper.insertMaskingReleaseHist
```

**테이블/API:** MCP_MASKING_RELEASE_HIST  
**처리 로직 / 변환 처리:**

```
o MaskingMapper.insertMaskingReleaseHist
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/23 전달  

---

## [3001-02]

**JSP/JS:** /mobile/mypage/cancelConsult.jsp, /resources/js/mobile/mypage/cancelConsult.js  
**URL:** /mypage/cancelConsultAjax.do  
**Controller:**

```
CancelConsultController.java - cancelConsultAjax
  - 해지 상담 신청
```

**처리 로직 / 변환 처리:**

```
o CancelConsultController.java - cancelConsultAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service:**

```
mypageService.selectCntrList
  - 회선정보
```

**Service / Dao / SQL / API:**

```
mypageService.selectCntrList
-> [M포탈-M전산 API] /mypage/cntrList
```

**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 회원 로직 불필요  
**개발팀 전달일:** 03/23 전달  
---

**Service:**

```
cancelConsultSvc.countCancelConsult
  - 동일 접수 여부
```

**Service / Dao / SQL / API:**

```
cancelConsultDao.countCancelConsult
-> [SQL] CancelConsultMapper.countCancelConsult
```

**테이블/API:** MCP_CANCEL_REQUEST  
**처리 로직 / 변환 처리:**

```
o CancelConsultMapper.countCancelConsult
-> SQL Copy 후 스마트신청서App 에 동일하게 사용
```

**개발팀 전달일:** 03/23 전달  
---

**Service:** certService.getStepCnt  
**Service / Dao / SQL / API:**

```
certDao.getStepCnt
-> [SQL] CertMapper.getStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/23 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/23 전달  
---

**Service:** cancelConsultSvc.cancelConsultRequest  
**Service / Dao / SQL / API:**

```
cancelConsultDao.insertNmcpCustReqMst
-> [SQL] CancelConsultMapper.insertNmcpCustReqMst
```

**테이블/API:** NMCP_CUST_REQUEST_MST  
**처리 로직 / 변환 처리:**

```
o CancelConsultMapper.insertNmcpCustReqMst
-> SQL Copy 후 M포탈-M전산 API 에 SQL 추가하여 데이터 INSERT/UPDATE 처리
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
cancelConsultDao.insertCancelConsult
-> [SQL] CancelConsultMapper.insertCancelConsult
```

**테이블/API:** MCP_CANCEL_REQUEST  
**처리 로직 / 변환 처리:**

```
o CancelConsultMapper.insertCancelConsult
-> SQL Copy 후 M포탈-M전산 API 에 SQL 추가하여 데이터 INSERT/UPDATE 처리
```

**개발팀 전달일:** 03/23 전달  
---

**Service:**

```
custRequestScanService.prodSendScan
  - SCAN 서버에 서식지 데이터 생성 및 전송
  - "CC"
```

**처리 로직 / 변환 처리:**

```
o custRequestScanService.prodSendScan
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
custRequestDao.getCancelConsultData
  - 해지상담신청
  - reqType = "CC"
-> [SQL] CustRequestMapper.getCancelConsultData
```

**테이블/API:** NMCP_CUST_REQUEST_MST, MCP_CANCEL_REQUEST  
**처리 로직 / 변환 처리:**

```
o CustRequestMapper.getCancelConsultData
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
settingSsnData
  - 주민번호 분석 값 설정
```

**처리 로직 / 변환 처리:**

```
o settingSsnData
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
settingData
  - 각종 설정 값 설정
```

**처리 로직 / 변환 처리:**

```
o settingData
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
createXml
  - XML 파일 생성
```

**처리 로직 / 변환 처리:**

```
o createXml
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
createXml
-> getGroupCode
```

**처리 로직 / 변환 처리:**

```
o getGroupCode
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
createXml
-> custRequestDao.getAppFormPointList
-> [SQL] CustRequestMapper.getAppFormGroupPointList
```

**테이블/API:** NMCP_APP_GROUP_MST, NMCP_APP_GROUP_INFO, NMCP_APP_FORM_INFO  
**처리 로직 / 변환 처리:**

```
o CustRequestMapper.getAppFormGroupPointList
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경

ㅇ ITO AS-IS 소스에 로직 변경
 - 신규 포시에스 리포팅 시스템에 맞춰서 로직 변경
```

**기타:** 확인 필요 - 포시에스 솔루션에서 필요할지 확인  
**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
createXml
-> saveXML
```

**처리 로직 / 변환 처리:**

```
o saveXML
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
xmlFileSend
  - XML 파일 전송 및 삭제 처리
```

**처리 로직 / 변환 처리:**

```
o xmlFileSend
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
xmlFileSend
-> ipstatisticService.insertAdminAccessTrace
-> fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
custRequestDao.updateCancelReqScanId
  - 해지상담신청
  - reqType = "CC"
-> [SQL] CustRequestMapper.updateCancelReqScanId
```

**테이블/API:** MCP_CANCEL_REQUEST  
**처리 로직 / 변환 처리:**

```
o CustRequestMapper.updateCancelReqScanId
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
custRequestDao.updateEmvScanMstCC
  - 해지상담신청
  - reqType = "CC"
-> [SQL] CustRequestMapper.updateEmvScanMstCC
```

**테이블/API:** EMV_SCAN_MST  
**처리 로직 / 변환 처리:**

```
o CustRequestMapper.updateEmvScanMstCC
-> SQL Copy 후 M포탈-M전산 API 에 SQL 추가하여 데이터 INSERT/UPDATE 처리
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
custRequestDao.updateEmvScanFileCC
  - 해지상담신청
  - reqType = "CC"
-> [SQL] CustRequestMapper.updateEmvScanFileCC
```

**테이블/API:** EMV_SCAN_FILE  
**처리 로직 / 변환 처리:**

```
o CustRequestMapper.updateEmvScanFileCC
-> SQL Copy 후 M포탈-M전산 API 에 SQL 추가하여 데이터 INSERT/UPDATE 처리
```

**개발팀 전달일:** 03/23 전달  
---

**Service:** ipstatisticService.insertAdminAccessTrace  
**Service / Dao / SQL / API:**

```
fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 호출 로그 적재  
**개발팀 전달일:** 03/23 전달  

---

## [쉐어링]

**JSP/JS:** /mobile/content/mySharingCntrInfo.jsp  
**URL:** /m/mySharingCntrInfo.do  
**Controller:**

```
MyShareDataController.java - doMySharingCntrInfo
  - 인증전
```

**처리 로직 / 변환 처리:**

```
o MyShareDataController.java - doMySharingCntrInfo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 해당없음  
**개발팀 전달일:** 03/24 전달  

---

## [쉐어링]

**JSP/JS:** /mobile/content/dataSharingStep1.jsp  
**URL:** /m/content/dataSharingStep1.do  
**Controller:**

```
MyShareDataController.java - dataSharingStep1
  - 쉐어링 인증전
```

**처리 로직 / 변환 처리:**

```
o MyShareDataController.java - dataSharingStep1
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 해당없음  
**개발팀 전달일:** 03/24 전달  

---

## [쉐어링]

**JSP/JS:** /mobile/content/mySharingView.jsp, /resources/js/mobile/content/mySharingView.js  
**URL:** /m/content/mySharingView.do  
**Controller:**

```
MyShareDataController.java - doMySharingView
  - 인증후
```

**처리 로직 / 변환 처리:**

```
o MyShareDataController.java - doMySharingView
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 중복이므로 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrListNoLogin
  - 비회원로그인
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNoLogin  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNoLogin
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 중복이므로 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrList
  - 회선정보
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 회원 로직 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** checkUserType  
**처리 로직 / 변환 처리:**

```
o checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 중복이므로 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectSocDesc
  - 현재 요금제 조회
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/socDesc  
**처리 로직 / 변환 처리:**

```
o /mypage/socDesc
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 중복이므로 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCustomerType
  - 고객구분여부
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/customerType  
**처리 로직 / 변환 처리:**

```
o /mypage/customerType
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 중복이므로 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
myShareDataSvc.mosharingList
  - 데이터쉐어링 결합중인 대상 조회
```

**Service / Dao / SQL / API:**

```
mplatFormService.mosharingList
-> mplatFormServerAdapter.callService
-> [M플랫폼] X23
```

**테이블/API:** X71  
**처리 로직 / 변환 처리:**

```
o mplatFormService.mosharingList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 중복이므로 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
myinfoService.farChangewayInfo
  - 청구계약조회
```

**Service / Dao / SQL / API:**

```
mPlatFormService.farChangewayInfo
-> mplatFormServerAdapter.callService
-> [M플랫폼] X23
```

**테이블/API:** X23  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.farChangewayInfo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 중복이므로 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 제외 - 중복이므로 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
maskingSvc.insertMaskingReleaseHist
  - 마스킹해제
```

**Service / Dao / SQL / API:**

```
maskingDao.insertMaskingReleaseHist
-> [SQL] MaskingMapper.insertMaskingReleaseHist
```

**테이블/API:** MCP_MASKING_RELEASE_HIST  
**처리 로직 / 변환 처리:**

```
o MaskingMapper.insertMaskingReleaseHist
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 중복이므로 제외  
**개발팀 전달일:** 03/24 전달  

---

## [4001-01]

**JSP/JS:** /mobile/content/reqSharingView.jsp, /resources/js/mobile/content/reqSharingView.js  
**URL:** /m/content/reqSharingView.do  
**Controller:**

```
MyShareDataController.java - dorReqSharingView
  - 쉐어링 신청
```

**처리 로직 / 변환 처리:**

```
o MyShareDataController.java - dorReqSharingView
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrListNoLogin
  - 비회원로그인
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNoLogin  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNoLogin
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrList
  - 회선정보
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 회원 로직 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** checkUserType  
**처리 로직 / 변환 처리:**

```
o checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:** nicePinService.getNicePinCi  
**Service / Dao / SQL / API:** certService.vdlCertInfo  
**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 본인인증  
**개발팀 전달일:** 03/24 전달  

---

## [4001-05]

**JSP/JS:** /mobile/content/dataSharingStep2.jsp  
**URL:** /m/content/dataSharingStep2.do  
**Controller:**

```
MyShareDataController.java - dataSharingStep2
  - 쉐어링 인증후
```

**처리 로직 / 변환 처리:**

```
o MyShareDataController.java - dataSharingStep2
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrListNoLogin
  - 비회원로그인
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNoLogin  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNoLogin
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrList
  - 회선정보
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 회원 로직 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** checkUserType  
**처리 로직 / 변환 처리:**

```
o checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectSocDesc
  - 요금제 조회
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/socDesc  
**처리 로직 / 변환 처리:**

```
o /mypage/socDesc
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCustomerType
  - 고객구분여부
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/customerType  
**처리 로직 / 변환 처리:**

```
o /mypage/customerType
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
myShareDataSvc.mosharingList
  - 데이터쉐어링 결합중인 대상 조회
```

**Service / Dao / SQL / API:**

```
mplatFormService.mosharingList
-> mplatFormServerAdapter.callService
-> [M플랫폼] X23
```

**테이블/API:** X71  
**처리 로직 / 변환 처리:**

```
o mplatFormService.mosharingList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
myinfoService.farChangewayInfo
  - 청구계약조회
```

**Service / Dao / SQL / API:**

```
mPlatFormService.farChangewayInfo
-> mplatFormServerAdapter.callService
-> [M플랫폼] X23
```

**테이블/API:** X23  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.farChangewayInfo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** appformSvc.isSimpleApplyObj  
**처리 로직 / 변환 처리:**

```
o appformSvc.isSimpleApplyObj
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** NmcpServiceUtils.getCodeNmDto  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
maskingSvc.insertMaskingReleaseHist
  - 마스킹해제
```

**Service / Dao / SQL / API:**

```
maskingDao.insertMaskingReleaseHist
-> [SQL] MaskingMapper.insertMaskingReleaseHist
```

**테이블/API:** MCP_MASKING_RELEASE_HIST  
**처리 로직 / 변환 처리:**

```
o MaskingMapper.insertMaskingReleaseHist
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  

---

## [4001-06]

**JSP/JS:** /mobile/content/dataSharingStep2.jsp  
**URL:** /appForm/isSimpleApplyAjax.do  
**Controller:** AppformController.java - isSimpleApplyObj  
**Service:** appformSvc.isSimpleApplyObj  
**처리 로직 / 변환 처리:**

```
o appformSvc.isSimpleApplyObj
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** NmcpServiceUtils.getCodeNmDto  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/24 전달  

---

## [4001-02]

**JSP/JS:** /mobile/content/reqSharingView.jsp, /resources/js/mobile/content/reqSharingView.js  
**URL:** /m/content/insertOpenRequestAjax.do  
**Controller:**

```
MyShareDataController.java - doinsertOpenRequestAjax
  - 데이터 쉐어링
```

**처리 로직 / 변환 처리:**

```
o MyShareDataController.java - doinsertOpenRequestAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrListNoLogin
  - 비회원로그인
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNoLogin  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNoLogin
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrList
  - 회선정보
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 회원 로직 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
myShareDataSvc.moscDataSharingChk
  - 개통 사전체크
```

**Service / Dao / SQL / API:**

```
mplatFormService.moscDataSharingChk
-> mplatFormServerAdapter.callService
-> [M플랫폼] X69
```

**테이블/API:** X69  
**처리 로직 / 변환 처리:**

```
o mplatFormService.moscDataSharingChk
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
myShareDataSvc.moscDataSharingSave
  - 쉐어링 가입
```

**Service / Dao / SQL / API:**

```
mplatFormService.moscDataSharingSave
-> mplatFormServerAdapter.callService
-> [M플랫폼] X70
```

**테이블/API:** X70  
**처리 로직 / 변환 처리:**

```
o mplatFormService.moscDataSharingSave
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  

---

## [4001-03]

**JSP/JS:** /mobile/content/reqSharingView.jsp, /resources/js/mobile/content/reqSharingView.js  
**URL:** /m/appform/saveDataSharingSimpleAjax.do  
**Controller:** AppformController.java - saveDataSharingSimple  
**처리 로직 / 변환 처리:**

```
o AppformController.java - saveDataSharingSimple
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrList
  - 회선정보
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 회원 로직 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrListNoLogin
  - 비회원로그인
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNoLogin  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNoLogin
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectBanSel  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/selectBanSel  
**처리 로직 / 변환 처리:**

```
o /mypage/selectBanSel
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
appformSvc.getCopyMcpRequest
  - 기존에 저장한 정보 확인
```

**Service / Dao / SQL / API:**

```
appformDao.getCopyMcpRequest
-> [SQ] AppformMapper.getCopyMcpRequest
```

**테이블/API:** MCP_REQUEST, MCP_REQUEST_CSTMR, MCP_REQUEST_REQ, MCP_REQUEST_SALEINFO, MCP_REQUEST_AGENT  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getCopyMcpRequest
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경

o 현행 SQL에 로직 추가
 - 스마트신청서 App 에서 조회해서 없으면, M포탈 테이블에서 조회
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** NmcpServiceUtils.getCodeNmDto  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/24 전달  
---

**Service:** usimService.selectJoinUsimPriceNew  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /storeUsim/joinUsimPriceNew  
**처리 로직 / 변환 처리:**

```
o /storeUsim/joinUsimPriceNew
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
appformSvc.getAgentCode
  - 대리점 코드 조회
```

**Service / Dao / SQL / API:**

```
appformDao.getAgentCode
-> [M포탈-M전산 API] /appform/agentCode
```

**처리 로직 / 변환 처리:**

```
o /appform/agentCode
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** appformSvc.getChrgPrmtId  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /appform/getChrgPrmtId  
**처리 로직 / 변환 처리:**

```
o /appform/getChrgPrmtId
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.getModuTypeStepCnt
  - moduType별 step 춧
```

**Service / Dao / SQL / API:**

```
certDao.getModuTypeStepCnt
-> [SQL] CertMapper.getModuTypeStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getModuTypeStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:** appformSvc.saveAppform  
**처리 로직 / 변환 처리:**

```
o appformSvc.saveAppform
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

o M전산 M플랫폼 전송 API 호출로 대체
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.generateRequestKey
-> [SQL] AppformMapper.generateRequestKey
```

**테이블/API:** SQ_RCP_REQUEST_KEY_01  
**처리 로직 / 변환 처리:**

```
o AppformMapper.generateRequestKey
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
certService.getModuTypeStepCnt
-> certDao.getModuTypeStepCnt
-> [SQL] CertMapper.getModuTypeStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getModuTypeStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
certService.getCertInfo
  - 인증정보 비교해서 map으로 리턴
```

**처리 로직 / 변환 처리:**

```
o certService.getCertInfo
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 본인인증  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
crtSaveAppFormStep
  - 상담사 개통 최종 step 확인
```

**처리 로직 / 변환 처리:**

```
o crtSaveAppFormStep
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

o M전산 M플랫폼 전송 API 호출로 대체
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
crtSaveAppFormStep
-> certService.getModuTypeStepCnt
-> certDao.getModuTypeStepCnt
-> [SQL] CertMapper.getModuTypeStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getModuTypeStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 본인인증  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.generateResNo
-> [SQL] AppformMapper.generateResNo
```

**테이블/API:** SQ_RCP_RES_NO_01  
**처리 로직 / 변환 처리:**

```
o AppformMapper.generateResNo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경 : 임시등록 신청서일련번호 SQ_RCP_RES_NO_01 사용하지 않고 실제 신청서 일련번호와 같이 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** fathService.validateFathSession  
**처리 로직 / 변환 처리:**

```
o fathService.validateFathSession
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** fathService.updateFathMcpRequestOsst  
**처리 로직 / 변환 처리:**

```
o fathService.updateFathMcpRequestOsst
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
fathService.updateFathMcpRequestOsst
-> fathDao.updateFathMcpRequestOsst
-> [SQL] FathMapper.updateFathMcpRequestOsst
```

**테이블/API:** MCP_REQUEST_OSST  
**처리 로직 / 변환 처리:**

```
o FathMapper.updateFathMcpRequestOsst
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.getMspPrdtCode
-> [M포탈-M전산 API] /appform/mspPrdtCode
```

**처리 로직 / 변환 처리:**

```
o /appform/mspPrdtCode
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.getMspPrdtCode
-> [M포탈-M전산 API] /appform/mspPrdtCode
```

**처리 로직 / 변환 처리:**

```
o /appform/mspPrdtCode
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** NmcpServiceUtils.getCodeList(Constants.USIM_PROD_DTL_ID_GROP_CODE)  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** insertRequestSelfDlvry  
**처리 로직 / 변환 처리:**

```
o insertRequestSelfDlvry
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
insertRequestSelfDlvry
-> appformDao.getRequestSelfDlvrKey
-> [SQL] AppformMapper.getRequestSelfDlvrKey
```

**테이블/API:** SQ_REQUEST_SELF_DLVR_KEY  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getRequestSelfDlvrKey
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
insertRequestSelfDlvry
-> appformDao.insertRequestSelfDlvry
-> [SQL] AppformMapper.insertRequestSelfDlvry
```

**테이블/API:** MCP_REQUEST_SELF_DLVRY  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertRequestSelfDlvry
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** NmcpServiceUtils.getCodeNmDto(JEHU_AGENT_LIST  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** NmcpServiceUtils.getCodeNmDto("AcenLimit", "useLimit")  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:**

```
제외 - 상담사 상담
공통모듈 - 공통코드 조회 / 홍성민C
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
chkAcenReqCondition
  - Acen 연동 대상 조건 확인
```

**처리 로직 / 변환 처리:**

```
o chkAcenReqCondition
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 상담사 상담  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
chkAcenReqCondition
-> NmcpServiceUtils.getCodeNmDto("AcenLimit", "useEsim")
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:**

```
제외 - 상담사 상담
공통모듈 - 공통코드 조회 / 홍성민C
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
insertAcenReqTrg
  - Acen 연동 대상 insert
```

**처리 로직 / 변환 처리:**

```
o insertAcenReqTrg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 상담사 상담  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
insertAcenReqTrg
-> appformDao.insertAcenReqTrg
-> [SQL] AppformMapper.insertAcenReqTrg
```

**테이블/API:** NMCP_ACEN_REQ  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertAcenReqTrg
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 상담사 상담  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequest
-> [SQL] AppformMapper.insertMcpRequestOsst
```

**테이블/API:** MCP_REQUEST_OSST  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestOsst
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestCstmr
-> [SQL] AppformMapper.insertMcpRequestCstmr
```

**테이블/API:** MCP_REQUEST_CSTMR  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestCstmr
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestAgent
-> [SQL] AppformMapper.insertMcpRequestAgent
```

**테이블/API:** MCP_REQUEST_AGENT  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestAgent
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestMove
-> [SQL] AppformMapper.insertMcpRequestMove
```

**테이블/API:** MCP_REQUEST_MOVE  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestMove
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestPayment
-> [SQL] AppformMapper.insertMcpRequestPayment
```

**테이블/API:** MCP_REQUEST_PAYMENT  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestPayment
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 선불  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestSaleinfo
-> [SQL] AppformMapper.insertMcpRequestSaleinfo
```

**테이블/API:** MCP_REQUEST_SALEINFO  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestSaleinfo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestChange
-> [SQL] AppformMapper.insertMcpRequestChange
```

**테이블/API:** MCP_REQUEST_CHANGE  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestChange
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
-> MCP_REQUEST_CHANGE : 해당 테이블 미사용하고, 명의변경 관련 테이블로 통합 처리
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestDlvry
-> [SQL] AppformMapper.insertMcpRequestDlvry
```

**테이블/API:** MCP_REQUEST_DLVRY  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestDlvry
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestReq
-> [SQL] AppformMapper.insertMcpRequestReq
```

**테이블/API:** MCP_REQUEST_REQ  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestReq
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestPayInfo
-> [SQL] AppformMapper.insertMcpRequestPayInfo
```

**테이블/API:** MCP_REQUEST_PAY_INFO  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestPayInfo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 결제 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestDvcChg
-> [SQL] AppformMapper.insertMcpRequestDvcChg
```

**테이블/API:** MCP_REQUEST_DVC_CHG  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestDvcChg
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestAddition
-> [SQL] AppformMapper.insertMcpRequestAddition
```

**테이블/API:** MCP_REQUEST_ADDITION  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestAddition
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestKtInter
-> [SQL] AppformMapper.insertMcpRequestKtInter
```

**테이블/API:** MCP_REQUEST_KT_INTER  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestKtInter
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 유선 결합  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.checkGiftReqCount
-> [SQL] AppformMapper.checkGiftReqCount
```

**테이블/API:** NMCP_GIFT_REQ_TXN  
**처리 로직 / 변환 처리:**

```
o AppformMapper.checkGiftReqCount
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 사은품  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertGiftReqTxn
-> [SQL] AppformMapper.insertGiftReqTxn
```

**테이블/API:** NMCP_GIFT_REQ_TXN  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertGiftReqTxn
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 사은품  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.checkGiftReqCount
-> [SQL] AppformMapper.checkGiftReqCount
```

**테이블/API:** NMCP_GIFT_REQ_TXN  
**처리 로직 / 변환 처리:**

```
o AppformMapper.checkGiftReqCount
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 사은품  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertGiftReqTxn
-> [SQL] AppformMapper.insertGiftReqTxn
```

**테이블/API:** NMCP_GIFT_REQ_TXN  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertGiftReqTxn
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 사은품  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestAdditionPromotion
-> [M포탈-M전산 API] /appform/mcpRequestAdditionPromotion
```

**처리 로직 / 변환 처리:**

```
o /appform/mcpRequestAdditionPromotion
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestState
-> [SQL] AppformMapper.insertMcpRequestState
```

**테이블/API:** MCP_REQUEST_STATE  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestState
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertNmcpRequestApd
-> [SQL] AppformMapper.insertNmcpRequestApd
```

**테이블/API:** NMCP_REQUEST_APD  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertNmcpRequestApd
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 자급제 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertNmcpRequestApdDlvry
-> [SQL] AppformMapper.insertNmcpRequestApdDlvry
```

**테이블/API:** NMCP_REQUEST_APD_DLVRY  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertNmcpRequestApdDlvry
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 자급제 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertNmcpRequestApdSaleinfo
-> [SQL] AppformMapper.insertNmcpRequestApdSaleinfo
```

**테이블/API:** NMCP_REQUEST_APD_SALEINFO  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertNmcpRequestApdSaleinfo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 자급제 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertNmcpRequestApdState
-> [SQL] AppformMapper.insertNmcpRequestApdState
```

**테이블/API:** NMCP_REQUEST_APD_STATE  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertNmcpRequestApdState
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 자급제 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** pointService.editPoint  
**기타:** 제외 - 포인트 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** pointService.editPoint  
**기타:** 제외 - 포인트 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestCommend
-> [SQL] AppformMapper.insertMcpRequestCommend
```

**테이블/API:** MCP_REQUEST_COMMEND  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestCommend
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 추천인 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.updateMcpRequestNowDlvryHist
-> [SQL] AppformMapper.updateMcpRequestNowDlvryHist
```

**테이블/API:** MCP_REQUEST_NOW_DLVRY_HIST  
**처리 로직 / 변환 처리:**

```
o AppformMapper.updateMcpRequestNowDlvryHist
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.getMcpReqDtl
-> [SQL] AppformMapper.getMcpReqDtl
```

**테이블/API:** MCP_REQUEST REQ, MCP_REQUEST_CSTMR, MCP_REQUEST_SALEINFO, MCP_REQUEST_MOVE, MCP_REQUEST_DLVRY  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getMcpReqDtl
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
  - MCP_REQUEST_DLVRY : 배송 관련 로직은 제외
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.chkMcpReqDtl
-> [SQL] AppformMapper.chkMcpReqDtl
```

**테이블/API:** MCP_REQUEST_DTL  
**처리 로직 / 변환 처리:**

```
o AppformMapper.chkMcpReqDtl
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
-> MCP_REQUEST_DTL : 해당 테이블 미사용하고 각 신청서 관련 테이블 데이터로 등록 처리
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.updateMcpReqDtl
-> [SQL] AppformMapper.updateMcpReqDtl
```

**테이블/API:** MCP_REQUEST_DTL  
**처리 로직 / 변환 처리:**

```
o AppformMapper.updateMcpReqDtl
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
-> MCP_REQUEST_DTL : 해당 테이블 미사용하고 각 신청서 관련 테이블 데이터로 등록 처리
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpReqDtl
-> [SQL] AppformMapper.insertMcpReqDtl
```

**테이블/API:** MCP_REQUEST_DTL  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpReqDtl
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
-> MCP_REQUEST_DTL : 해당 테이블 미사용하고 각 신청서 관련 테이블 데이터로 등록 처리
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertKtCounsel
-> [SQL] AppformMapper.insertKtCounsel
```

**테이블/API:** NMCP_KT_COUNSEL_REQ  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertKtCounsel
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 상담사 상담  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
appformSvc.sendOsstService
- Constants.EVENT_CODE_PRE_CHECK
```

**Service / Dao / SQL / API:**

```
mplatFormOsstServerAdapter.callService
-> [M플랫폼] PC0
```

**테이블/API:** PC0  
**처리 로직 / 변환 처리:**

```
o appformSvc.sendOsstService
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** appformSvc.getRequestOsst  
**Service / Dao / SQL / API:**

```
appformDao.getRequestOsst
-> [SQL] AppformMapper.getRequestOsst
```

**테이블/API:** MCP_REQUEST_OSST  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getRequestOsst
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** appformSvc.chkRealPc2Result  
**처리 로직 / 변환 처리:**

```
o appformSvc.chkRealPc2Result
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
sendOsstSt1Service
-> mplatFormOsstServerAdapter.callService
- Constants.EVENT_CODE_PRE_SCH
-> [M플랫폼] ST1
```

**테이블/API:** ST1  
**처리 로직 / 변환 처리:**

```
o sendOsstSt1Service
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mPlatFormService.MoscSvcContService  
**Service / Dao / SQL / API:**

```
mplatFormServerAdapter.callService
-> [M플랫폼] FT1
```

**테이블/API:** Y39  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.MoscSvcContService
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:** fnSearchNumber  
**처리 로직 / 변환 처리:**

```
o fnSearchNumber
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformSvc.updateMcpRequest
-> appformDao.updateMcpRequest
-> [SQL] AppformMapper.updateMcpRequest
```

**테이블/API:** MCP_REQUEST  
**처리 로직 / 변환 처리:**

```
o AppformMapper.updateMcpRequest
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformSvc.getPhoneNoList
-> mplatFormOsstServerAdapter.callService
-> [M플랫폼] NU1
- EVENT_CODE_SEARCH_NUMBER
```

**테이블/API:** NU1  
**처리 로직 / 변환 처리:**

```
o appformSvc.getPhoneNoList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** appformSvc.insertMcpRequestOsst  
**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestOsst
-> [SQL] AppformMapper.insertMcpRequestOsst
```

**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestOsst
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
appformSvc.sendOsstService
- EVENT_CODE_NUMBER_REG
```

**Service / Dao / SQL / API:**

```
mplatFormOsstServerAdapter.callService
-> [M플랫폼] NU2
```

**테이블/API:** NU2  
**처리 로직 / 변환 처리:**

```
o appformSvc.sendOsstService
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** appformSvc.insertDisPrmtApd  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /appform/insertDisPrmtApd  
**처리 로직 / 변환 처리:**

```
o /appform/insertDisPrmtApd
-> 현행 ITO API 그대로 사용 : M전산/M포탈 테이블 INSERT/UPDATE/DELETE
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** appformSvc.getRequestOsst  
**Service / Dao / SQL / API:**

```
appformDao.getRequestOsst
-> [SQL] AppformMapper.getRequestOsst
```

**테이블/API:** MCP_REQUEST_OSST  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getRequestOsst
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  

---

## [4001-04]

**JSP/JS:** /mobile/content/reqSharingView.jsp, /resources/js/mobile/content/reqSharingView.js  
**URL:** /appform/sendScanAjax.do  
**Controller:**

```
AppformController.java - sendScan
 - SCAN 서버 전송
 - 소스 : AppformController-sendScan.java
```

**처리 로직 / 변환 처리:**

```
o AppformController.java - sendScan
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** appformSvc.getMcpRequest  
**Service / Dao / SQL / API:**

```
appformDao.getMcpRequest
-> [SQL] AppformMapper.getMcpRequestSelfDlvry

        SELECT
        SELF_DLVRY_IDX
        , USIM_PROD_ID
        , USIM_PROD_DTL_ID
        , CSTMR_NAME
        , CSTMR_NATIVE_RRN
        , ONLINE_AUTH_TYPE
        , ONLINE_AUTH_INFO
        , ONLINE_AUTH_DI
        , DLVRY_NAME
        , DLVRY_TEL_FN
        , DLVRY_TEL_MN
        , DLVRY_TEL_RN
        , DLVRY_POST
        , DLVRY_ADDR
        , DLVRY_ADDR_DTL
        , DLVRY_MEMO
        , SELF_MEMO
        , REQ_USIM_SN
        , DLVRY_STATE_CODE
        , SELF_STATE_CODE
        , DLVRY_NO
        , TB_CD
        , CONTRACT_NUM
        , CRET_ID
        , RVISN_ID
        , RVISN_DTTM
        , SYS_RDATE
        , REQ_BUY_QNTY
        , USIM_UCOST
        , USIM_PRICE
        , DLVRY_TYPE
        FROM MCP_REQUEST_SELF_DLVRY
        WHERE 1=1
        <if test="selfDlvryIdx != null and selfDlvryIdx > 0 ">
            AND SELF_DLVRY_IDX = #{selfDlvryIdx}
        </if>
        <if test="onlineAuthDi !=null and onlineAuthDi != ''" >
            AND ONLINE_AUTH_DI = #{onlineAuthDi}
        </if>
        <if test=" !(onlineAuthDi !=null and onlineAuthDi != '') " >
            AND CSTMR_NAME = #{cstmrName} AND DLVRY_TEL_FN || DLVRY_TEL_MN || DLVRY_TEL_RN = #{dlvryTelFn}||#{dlvryTelMn}||#{dlvryTelRn}
        </if>
        <if test="sysRdateS !=null and sysRdateS != '' and sysRdateE !=null and sysRdateE != ''" >
            AND SYS_RDATE BETWEEN  TO_DATE(#{sysRdateS}||'000000', 'YYYYMMDDHH24MISS') AND TO_DATE(#{sysRdateE}||'235959', 'YYYYMMDDHH24MISS')
        </if>
        ORDER BY SELF_DLVRY_IDX DESC
```

**테이블/API:** MCP_REQUEST_SELF_DLVRY  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getMcpRequestSelfDlvry
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
scanSvc.prodSendScan
  - SCAN 서버에 서식지 데이터 생성 및 전송
 - 소스 : ScanSvcImpl-prodSendScan.java
```

**처리 로직 / 변환 처리:**

```
o scanSvc.prodSendScan
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

ㅇ ITO AS-IS 소스에 로직 변경
 - 신규 포시에스 리포팅 시스템에 맞춰서 로직 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.getAppFormData
-> [M포탈-M전산 API] /appform/requestJoinDataByRjoinKeyNew
```

**처리 로직 / 변환 처리:**

```
o /appform/requestJoinDataByRjoinKeyNew
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용

ㅇ ITO AS-IS 소스에 로직 변경
 - 신규 포시에스 리포팅 시스템에 맞춰서 로직 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** createXml  
**처리 로직 / 변환 처리:**

```
o createXml
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

ㅇ ITO AS-IS 소스에 로직 변경
 - 신규 포시에스 리포팅 시스템에 맞춰서 로직 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
createXml
-> getGroupCode
-> NmcpServiceUtils.getCodeList
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
createXml
-> appformDao.getAppFormPointList
-> [SQL] AppformMapper.getAppFormPointList
```

**테이블/API:** MCP_APPLICATION_FORM_INFO  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getAppFormPointList
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경 : 포시에스 리포트 툴에서 사용이 필요할지 확인 후 필요한 경우 사용

ㅇ ITO AS-IS 소스에 로직 변경
 - 신규 포시에스 리포팅 시스템에 맞춰서 로직 변경
```

**기타:** 확인 필요 - 포시에스 솔루션에서 필요할지 확인  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:** xmlFileSend  
**처리 로직 / 변환 처리:**

```
o xmlFileSend
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

ㅇ ITO AS-IS 소스에 로직 변경
 - 신규 포시에스 리포팅 시스템에 맞춰서 로직 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
xmlFileSend
-> ipstatisticService.insertAdminAccessTrace
-> fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 호출 로그 적재  
**개발팀 전달일:** 03/22 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.updateAppFormXmlYn
-> [SQL] AppformMapper.updateAppFormXmlYn

        UPDATE
        MCP_REQUEST
        SET
        APP_FORM_XML_YN = 'Y'
        WHERE
        REQUEST_KEY = #{requestKey}
```

**테이블/API:** MCP_REQUEST  
**처리 로직 / 변환 처리:**

```
o AppformMapper.updateAppFormXmlYn
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경

ㅇ ITO AS-IS 소스에 로직 변경
 - 신규 포시에스 리포팅 시스템에 맞춰서 로직 변경
```

**개발팀 전달일:** 03/22 전달  
---

**Service:** ipstatisticService.insertAdminAccessTrace  
**Service / Dao / SQL / API:**

```
fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin

        INSERT INTO MCP_ADMIN_ACCESS_TRACE (
            PARAMETER,
            ACCESS_IP,
            SYS_DT,
            SYS_RDATE,
            ACCESS_URL,
            USER_ID,
            PRCS_MDL_IND,
            PRCS_SBST,
            TRTM_RSLT_SMST
        ) VALUES (
                     #{parameter},
                     #{accessIp},
                     TO_CHAR(SYSDATE, 'YYYYMMDD'),
                     SYSDATE,
                     #{accessUrl},
                     SUBSTRB(#{userid},0,20),
                     #{prcsMdlInd},
                     SUBSTRB(#{prcsSbst},0,1000),
                     #{trtmRsltSmst}
                 )
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 호출 로그 적재  
**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
ipstatisticService.getReferer

        String referer = "";
        HttpServletRequest request = ((ServletRequestAttributes) RequestContextHolder.currentRequestAttributes()).getRequest();
        referer = request.getHeader("referer");
        return referer;
```

**처리 로직 / 변환 처리:**

```
o ipstatisticService.getReferer
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/22 전달  
---

**Service:**

```
NmcpServiceUtils.getCodeList

        List<NmcpCdDtlDto> codeList = null;
        List<NmcpCdDtlDto> codeRstList = null;
        DbCacheHandler dbCacheHandler = getBean(DbCacheHandler.class);

        @SuppressWarnings("unchecked")
        Map<String, List<NmcpCdDtlDto>> codeMap = (Map<String, List<NmcpCdDtlDto>>) dbCacheHandler.getValue(CACHE_CODE);
        if (codeMap != null) {
            codeList = codeMap.get(grpCd);
            codeRstList = new ArrayList<NmcpCdDtlDto>();

            if(codeList !=null && !codeList.isEmpty()) {
                for(NmcpCdDtlDto bean : codeList) {
                    if(DateTimeUtil.checkValidDate(bean.getPstngStartDate(), bean.getPstngEndDate())) {
                        codeRstList.add(bean);
                    }
                }
            }
        }
        return codeRstList;
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 제외  
**개발팀 전달일:** 03/22 전달  

---

## [쉐어링]

**JSP/JS:** /mobile/content/reqSharingCompleteView.jsp  
**URL:** /m/content/reqSharingCompleteView.do  
**Controller:**

```
MyShareDataController.java - doinsertOpenRequestAjax
  - 데이터 쉐어링 완료
```

**처리 로직 / 변환 처리:**

```
o MyShareDataController.java - doinsertOpenRequestAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrListNoLogin
  - 비회원로그인
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNoLogin  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNoLogin
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrList
  - 회선정보
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 회원 로직 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectSocDesc
  - 요금제 조회
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/socDesc  
**처리 로직 / 변환 처리:**

```
o /mypage/socDesc
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectSocDesc
  - 요금제 조회
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/socDesc  
**처리 로직 / 변환 처리:**

```
o /mypage/socDesc
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  

---

## [쉐어링]

**JSP/JS:** /mobile/content/dataSharingStep3.jsp, /resources/js/mobile/content/dataSharingStep3.js  
**URL:** /m/content/dataSharingStep3.do  
**Controller:**

```
MyShareDataController.java - dataSharingStep3
  - 쉐어링 신청
```

**처리 로직 / 변환 처리:**

```
o MyShareDataController.java - dataSharingStep3
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 중복이므로 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrListNoLogin
  - 비회원로그인
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNoLogin  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNoLogin
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 중복이므로 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrList
  - 회선정보
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 중복이므로 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service:** checkUserType  
**처리 로직 / 변환 처리:**

```
o checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 중복이므로 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 제외 - 중복이므로 제외  
**개발팀 전달일:** 03/24 전달  

---

## [4001-07]

**JSP/JS:** /mobile/content/dataSharingStep3.jsp, /resources/js/mobile/content/dataSharingStep3.js  
**URL:** /appform/saveDataSharingAjax.do  
**Controller:** AppformController.java - saveDataSharing  
**처리 로직 / 변환 처리:**

```
o AppformController.java - saveDataSharing
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
  - data설정 처리
```

**처리 로직 / 변환 처리:**

```
o fnSetDataOfdataSharing
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
-> mypageService.selectCntrList
  - 회선정보
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 회원 로직 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
-> mypageService.selectCntrListNoLogin
  - 비회원로그인
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNoLogin  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNoLogin
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
-> appformSvc.getCopyMcpRequest
```

**Service / Dao / SQL / API:**

```
appformDao.getCopyMcpRequest
-> [SQ] AppformMapper.getCopyMcpRequest
```

**테이블/API:** MCP_REQUEST, MCP_REQUEST_CSTMR, MCP_REQUEST_REQ, MCP_REQUEST_SALEINFO, MCP_REQUEST_AGENT  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getCopyMcpRequest
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경

o 스마트신청서 App 에서 조회해서 없으면, M포탈 테이블에서 조회
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
-> NmcpServiceUtils.getCodeNmDto
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
-> usimService.selectJoinUsimPriceNew
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /storeUsim/joinUsimPriceNew  
**처리 로직 / 변환 처리:**

```
o /storeUsim/joinUsimPriceNew
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
-> appformSvc.getAgentCode
```

**Service / Dao / SQL / API:**

```
appformDao.getAgentCode
-> [M포탈-M전산 API] /appform/agentCode
```

**처리 로직 / 변환 처리:**

```
o /appform/agentCode
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
-> appformSvc.getChrgPrmtId
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /appform/getChrgPrmtId  
**처리 로직 / 변환 처리:**

```
o /appform/getChrgPrmtId
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
-> certService.getModuTypeStepCnt
```

**Service / Dao / SQL / API:**

```
certDao.getModuTypeStepCnt
-> [SQL] CertMapper.getModuTypeStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getModuTypeStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
-> certService.vdlCertInfo
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
-> appformSvc.saveAppform
```

**처리 로직 / 변환 처리:**

```
o appformSvc.saveAppform
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

o M전산 M플랫폼 전송 API 호출로 대체
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.generateRequestKey
-> [SQL] AppformMapper.generateRequestKey
```

**테이블/API:** SQ_RCP_REQUEST_KEY_01  
**처리 로직 / 변환 처리:**

```
o AppformMapper.generateRequestKey
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
certService.getModuTypeStepCnt
-> certDao.getModuTypeStepCnt
-> [SQL] CertMapper.getModuTypeStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getModuTypeStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
certService.getCertInfo
  - 인증정보 비교해서 map으로 리턴
```

**처리 로직 / 변환 처리:**

```
o certService.getCertInfo
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 본인인증  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
crtSaveAppFormStep
  - 상담사 개통 최종 step 확인
```

**처리 로직 / 변환 처리:**

```
o crtSaveAppFormStep
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

o M전산 M플랫폼 전송 API 호출로 대체
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
crtSaveAppFormStep
-> certService.getModuTypeStepCnt
-> certDao.getModuTypeStepCnt
-> [SQL] CertMapper.getModuTypeStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getModuTypeStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 본인인증  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.generateResNo
-> [SQL] AppformMapper.generateResNo
```

**테이블/API:** SQ_RCP_RES_NO_01  
**처리 로직 / 변환 처리:**

```
o AppformMapper.generateResNo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경 : 임시등록 신청서일련번호 SQ_RCP_RES_NO_01 사용하지 않고 실제 신청서 일련번호와 같이 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** fathService.validateFathSession  
**처리 로직 / 변환 처리:**

```
o fathService.validateFathSession
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** fathService.updateFathMcpRequestOsst  
**처리 로직 / 변환 처리:**

```
o fathService.updateFathMcpRequestOsst
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
fathService.updateFathMcpRequestOsst
-> fathDao.updateFathMcpRequestOsst
-> [SQL] FathMapper.updateFathMcpRequestOsst
```

**테이블/API:** MCP_REQUEST_OSST  
**처리 로직 / 변환 처리:**

```
o FathMapper.updateFathMcpRequestOsst
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.getMspPrdtCode
-> [M포탈-M전산 API] /appform/mspPrdtCode
```

**처리 로직 / 변환 처리:**

```
o /appform/mspPrdtCode
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.getMspPrdtCode
-> [M포탈-M전산 API] /appform/mspPrdtCode
```

**처리 로직 / 변환 처리:**

```
o /appform/mspPrdtCode
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** NmcpServiceUtils.getCodeList(Constants.USIM_PROD_DTL_ID_GROP_CODE)  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** insertRequestSelfDlvry  
**처리 로직 / 변환 처리:**

```
o insertRequestSelfDlvry
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
insertRequestSelfDlvry
-> appformDao.getRequestSelfDlvrKey
-> [SQL] AppformMapper.getRequestSelfDlvrKey
```

**테이블/API:** SQ_REQUEST_SELF_DLVR_KEY  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getRequestSelfDlvrKey
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
insertRequestSelfDlvry
-> appformDao.insertRequestSelfDlvry
-> [SQL] AppformMapper.insertRequestSelfDlvry
```

**테이블/API:** MCP_REQUEST_SELF_DLVRY  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertRequestSelfDlvry
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** NmcpServiceUtils.getCodeNmDto(JEHU_AGENT_LIST  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** NmcpServiceUtils.getCodeNmDto("AcenLimit", "useLimit")  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:**

```
제외 - 상담사 상담
공통모듈 - 공통코드 조회 / 홍성민C
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
chkAcenReqCondition
  - Acen 연동 대상 조건 확인
```

**처리 로직 / 변환 처리:**

```
o chkAcenReqCondition
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 상담사 상담  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
chkAcenReqCondition
-> NmcpServiceUtils.getCodeNmDto("AcenLimit", "useEsim")
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:**

```
제외 - 상담사 상담
공통모듈 - 공통코드 조회 / 홍성민C
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
insertAcenReqTrg
  - Acen 연동 대상 insert
```

**처리 로직 / 변환 처리:**

```
o insertAcenReqTrg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 상담사 상담  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
insertAcenReqTrg
-> appformDao.insertAcenReqTrg
-> [SQL] AppformMapper.insertAcenReqTrg
```

**테이블/API:** NMCP_ACEN_REQ  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertAcenReqTrg
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 상담사 상담  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequest
-> [SQL] AppformMapper.insertMcpRequestOsst
```

**테이블/API:** MCP_REQUEST_OSST  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestOsst
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestCstmr
-> [SQL] AppformMapper.insertMcpRequestCstmr
```

**테이블/API:** MCP_REQUEST_CSTMR  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestCstmr
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestAgent
-> [SQL] AppformMapper.insertMcpRequestAgent
```

**테이블/API:** MCP_REQUEST_AGENT  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestAgent
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestMove
-> [SQL] AppformMapper.insertMcpRequestMove
```

**테이블/API:** MCP_REQUEST_MOVE  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestMove
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestPayment
-> [SQL] AppformMapper.insertMcpRequestPayment
```

**테이블/API:** MCP_REQUEST_PAYMENT  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestPayment
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 선불  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestSaleinfo
-> [SQL] AppformMapper.insertMcpRequestSaleinfo
```

**테이블/API:** MCP_REQUEST_SALEINFO  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestSaleinfo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestChange
-> [SQL] AppformMapper.insertMcpRequestChange
```

**테이블/API:** MCP_REQUEST_CHANGE  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestChange
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
-> MCP_REQUEST_CHANGE : 해당 테이블 미사용하고, 명의변경 관련 테이블로 통합 처리
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestDlvry
-> [SQL] AppformMapper.insertMcpRequestDlvry
```

**테이블/API:** MCP_REQUEST_DLVRY  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestDlvry
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestReq
-> [SQL] AppformMapper.insertMcpRequestReq
```

**테이블/API:** MCP_REQUEST_REQ  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestReq
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestPayInfo
-> [SQL] AppformMapper.insertMcpRequestPayInfo
```

**테이블/API:** MCP_REQUEST_PAY_INFO  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestPayInfo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 결제 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestDvcChg
-> [SQL] AppformMapper.insertMcpRequestDvcChg
```

**테이블/API:** MCP_REQUEST_DVC_CHG  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestDvcChg
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestAddition
-> [SQL] AppformMapper.insertMcpRequestAddition
```

**테이블/API:** MCP_REQUEST_ADDITION  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestAddition
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestKtInter
-> [SQL] AppformMapper.insertMcpRequestKtInter
```

**테이블/API:** MCP_REQUEST_KT_INTER  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestKtInter
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 유선 결합  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.checkGiftReqCount
-> [SQL] AppformMapper.checkGiftReqCount
```

**테이블/API:** NMCP_GIFT_REQ_TXN  
**처리 로직 / 변환 처리:**

```
o AppformMapper.checkGiftReqCount
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 사은품  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertGiftReqTxn
-> [SQL] AppformMapper.insertGiftReqTxn
```

**테이블/API:** NMCP_GIFT_REQ_TXN  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertGiftReqTxn
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 사은품  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.checkGiftReqCount
-> [SQL] AppformMapper.checkGiftReqCount
```

**테이블/API:** NMCP_GIFT_REQ_TXN  
**처리 로직 / 변환 처리:**

```
o AppformMapper.checkGiftReqCount
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 사은품  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertGiftReqTxn
-> [SQL] AppformMapper.insertGiftReqTxn
```

**테이블/API:** NMCP_GIFT_REQ_TXN  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertGiftReqTxn
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 사은품  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestAdditionPromotion
-> [M포탈-M전산 API] /appform/mcpRequestAdditionPromotion
```

**처리 로직 / 변환 처리:**

```
o /appform/mcpRequestAdditionPromotion
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestState
-> [SQL] AppformMapper.insertMcpRequestState
```

**테이블/API:** MCP_REQUEST_STATE  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestState
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertNmcpRequestApd
-> [SQL] AppformMapper.insertNmcpRequestApd
```

**테이블/API:** NMCP_REQUEST_APD  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertNmcpRequestApd
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 자급제 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertNmcpRequestApdDlvry
-> [SQL] AppformMapper.insertNmcpRequestApdDlvry
```

**테이블/API:** NMCP_REQUEST_APD_DLVRY  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertNmcpRequestApdDlvry
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 자급제 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertNmcpRequestApdSaleinfo
-> [SQL] AppformMapper.insertNmcpRequestApdSaleinfo
```

**테이블/API:** NMCP_REQUEST_APD_SALEINFO  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertNmcpRequestApdSaleinfo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 자급제 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertNmcpRequestApdState
-> [SQL] AppformMapper.insertNmcpRequestApdState
```

**테이블/API:** NMCP_REQUEST_APD_STATE  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertNmcpRequestApdState
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 자급제 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** pointService.editPoint  
**기타:** 제외 - 포인트 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** pointService.editPoint  
**기타:** 제외 - 포인트 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestCommend
-> [SQL] AppformMapper.insertMcpRequestCommend
```

**테이블/API:** MCP_REQUEST_COMMEND  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestCommend
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 추천인 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.updateMcpRequestNowDlvryHist
-> [SQL] AppformMapper.updateMcpRequestNowDlvryHist
```

**테이블/API:** MCP_REQUEST_NOW_DLVRY_HIST  
**처리 로직 / 변환 처리:**

```
o AppformMapper.updateMcpRequestNowDlvryHist
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.getMcpReqDtl
-> [SQL] AppformMapper.getMcpReqDtl
```

**테이블/API:** MCP_REQUEST REQ, MCP_REQUEST_CSTMR, MCP_REQUEST_SALEINFO, MCP_REQUEST_MOVE, MCP_REQUEST_DLVRY  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getMcpReqDtl
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
  - MCP_REQUEST_DLVRY : 배송 관련 로직은 제외
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.chkMcpReqDtl
-> [SQL] AppformMapper.chkMcpReqDtl
```

**테이블/API:** MCP_REQUEST_DTL  
**처리 로직 / 변환 처리:**

```
o AppformMapper.chkMcpReqDtl
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
-> MCP_REQUEST_DTL : 해당 테이블 미사용하고 각 신청서 관련 테이블 데이터로 등록 처리
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.updateMcpReqDtl
-> [SQL] AppformMapper.updateMcpReqDtl
```

**테이블/API:** MCP_REQUEST_DTL  
**처리 로직 / 변환 처리:**

```
o AppformMapper.updateMcpReqDtl
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
-> MCP_REQUEST_DTL : 해당 테이블 미사용하고 각 신청서 관련 테이블 데이터로 등록 처리
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpReqDtl
-> [SQL] AppformMapper.insertMcpReqDtl
```

**테이블/API:** MCP_REQUEST_DTL  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpReqDtl
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
-> MCP_REQUEST_DTL : 해당 테이블 미사용하고 각 신청서 관련 테이블 데이터로 등록 처리
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertKtCounsel
-> [SQL] AppformMapper.insertKtCounsel
```

**테이블/API:** NMCP_KT_COUNSEL_REQ  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertKtCounsel
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 상담사 상담  
**개발팀 전달일:** 03/24 전달  
---

**Service:** NmcpServiceUtils.getCodeList  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/24 전달  
---

**Service:** fCommonSvc.getMspSmsTemplateMst  
**Service / Dao / SQL / API:**

```
fCommonSvc.getMspSmsTemplateMst
-> fCommonDao.getMspSmsTemplateMst
-> [M포탈-M전산 API] /common/mspSmsTemplateMst
```

**처리 로직 / 변환 처리:**

```
o /common/mspSmsTemplateMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** smsSvc.sendKakaoNoti  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /sms/addKakaoNoti  
**처리 로직 / 변환 처리:**

```
o /sms/addKakaoNoti
-> 현행 ITO API 그대로 사용 : M전산/M포탈 테이블 INSERT/UPDATE/DELETE
```

**개발팀 전달일:** 03/24 전달  

---

## [4001-08]

**JSP/JS:** /mobile/content/dataSharingStep3.jsp, /resources/js/mobile/content/dataSharingStep3.js  
**URL:** /appform/saveDataSharingStep1Ajax.do  
**Controller:** AppformController.java - saveDataSharingStep1  
**처리 로직 / 변환 처리:**

```
o AppformController.java - saveDataSharingStep1
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
  - data설정 처리
```

**처리 로직 / 변환 처리:**

```
o fnSetDataOfdataSharing
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
-> mypageService.selectCntrList
  - 회선정보
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 회원 로직 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
-> mypageService.selectCntrListNoLogin
  - 비회원로그인
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNoLogin  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNoLogin
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
-> appformSvc.getCopyMcpRequest
```

**Service / Dao / SQL / API:**

```
appformDao.getCopyMcpRequest
-> [SQ] AppformMapper.getCopyMcpRequest
```

**테이블/API:** MCP_REQUEST, MCP_REQUEST_CSTMR, MCP_REQUEST_REQ, MCP_REQUEST_SALEINFO, MCP_REQUEST_AGENT  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getCopyMcpRequest
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경

o 스마트신청서 App 에서 조회해서 없으면, M포탈 테이블에서 조회
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
-> NmcpServiceUtils.getCodeNmDto
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
-> usimService.selectJoinUsimPriceNew
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /storeUsim/joinUsimPriceNew  
**처리 로직 / 변환 처리:**

```
o /storeUsim/joinUsimPriceNew
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
-> appformSvc.getAgentCode
```

**Service / Dao / SQL / API:**

```
appformDao.getAgentCode
-> [M포탈-M전산 API] /appform/agentCode
```

**처리 로직 / 변환 처리:**

```
o /appform/agentCode
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
-> appformSvc.getChrgPrmtId
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /appform/getChrgPrmtId  
**처리 로직 / 변환 처리:**

```
o /appform/getChrgPrmtId
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
-> certService.getModuTypeStepCnt
```

**Service / Dao / SQL / API:**

```
certDao.getModuTypeStepCnt
-> [SQL] CertMapper.getModuTypeStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getModuTypeStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
-> certService.vdlCertInfo
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
-> appformSvc.saveAppform
```

**처리 로직 / 변환 처리:**

```
o appformSvc.saveAppform
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

o M전산 M플랫폼 전송 API 호출로 대체
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.generateRequestKey
-> [SQL] AppformMapper.generateRequestKey
```

**테이블/API:** SQ_RCP_REQUEST_KEY_01  
**처리 로직 / 변환 처리:**

```
o AppformMapper.generateRequestKey
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
certService.getModuTypeStepCnt
-> certDao.getModuTypeStepCnt
-> [SQL] CertMapper.getModuTypeStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getModuTypeStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
certService.getCertInfo
  - 인증정보 비교해서 map으로 리턴
```

**처리 로직 / 변환 처리:**

```
o certService.getCertInfo
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 본인인증  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
crtSaveAppFormStep
  - 상담사 개통 최종 step 확인
```

**처리 로직 / 변환 처리:**

```
o crtSaveAppFormStep
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

o M전산 M플랫폼 전송 API 호출로 대체
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
crtSaveAppFormStep
-> certService.getModuTypeStepCnt
-> certDao.getModuTypeStepCnt
-> [SQL] CertMapper.getModuTypeStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getModuTypeStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 본인인증  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.generateResNo
-> [SQL] AppformMapper.generateResNo
```

**테이블/API:** SQ_RCP_RES_NO_01  
**처리 로직 / 변환 처리:**

```
o AppformMapper.generateResNo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경 : 임시등록 신청서일련번호 SQ_RCP_RES_NO_01 사용하지 않고 실제 신청서 일련번호와 같이 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** fathService.validateFathSession  
**처리 로직 / 변환 처리:**

```
o fathService.validateFathSession
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** fathService.updateFathMcpRequestOsst  
**처리 로직 / 변환 처리:**

```
o fathService.updateFathMcpRequestOsst
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
fathService.updateFathMcpRequestOsst
-> fathDao.updateFathMcpRequestOsst
-> [SQL] FathMapper.updateFathMcpRequestOsst
```

**테이블/API:** MCP_REQUEST_OSST  
**처리 로직 / 변환 처리:**

```
o FathMapper.updateFathMcpRequestOsst
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.getMspPrdtCode
-> [M포탈-M전산 API] /appform/mspPrdtCode
```

**처리 로직 / 변환 처리:**

```
o /appform/mspPrdtCode
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.getMspPrdtCode
-> [M포탈-M전산 API] /appform/mspPrdtCode
```

**처리 로직 / 변환 처리:**

```
o /appform/mspPrdtCode
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** NmcpServiceUtils.getCodeList(Constants.USIM_PROD_DTL_ID_GROP_CODE)  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** insertRequestSelfDlvry  
**처리 로직 / 변환 처리:**

```
o insertRequestSelfDlvry
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
insertRequestSelfDlvry
-> appformDao.getRequestSelfDlvrKey
-> [SQL] AppformMapper.getRequestSelfDlvrKey
```

**테이블/API:** SQ_REQUEST_SELF_DLVR_KEY  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getRequestSelfDlvrKey
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
insertRequestSelfDlvry
-> appformDao.insertRequestSelfDlvry
-> [SQL] AppformMapper.insertRequestSelfDlvry
```

**테이블/API:** MCP_REQUEST_SELF_DLVRY  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertRequestSelfDlvry
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** NmcpServiceUtils.getCodeNmDto(JEHU_AGENT_LIST  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** NmcpServiceUtils.getCodeNmDto("AcenLimit", "useLimit")  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:**

```
제외 - 상담사 상담
공통모듈 - 공통코드 조회 / 홍성민C
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
chkAcenReqCondition
  - Acen 연동 대상 조건 확인
```

**처리 로직 / 변환 처리:**

```
o chkAcenReqCondition
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 상담사 상담  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
chkAcenReqCondition
-> NmcpServiceUtils.getCodeNmDto("AcenLimit", "useEsim")
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeNmDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:**

```
제외 - 상담사 상담
공통모듈 - 공통코드 조회 / 홍성민C
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
insertAcenReqTrg
  - Acen 연동 대상 insert
```

**처리 로직 / 변환 처리:**

```
o insertAcenReqTrg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 상담사 상담  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
insertAcenReqTrg
-> appformDao.insertAcenReqTrg
-> [SQL] AppformMapper.insertAcenReqTrg
```

**테이블/API:** NMCP_ACEN_REQ  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertAcenReqTrg
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 상담사 상담  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequest
-> [SQL] AppformMapper.insertMcpRequestOsst
```

**테이블/API:** MCP_REQUEST_OSST  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestOsst
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestCstmr
-> [SQL] AppformMapper.insertMcpRequestCstmr
```

**테이블/API:** MCP_REQUEST_CSTMR  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestCstmr
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestAgent
-> [SQL] AppformMapper.insertMcpRequestAgent
```

**테이블/API:** MCP_REQUEST_AGENT  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestAgent
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestMove
-> [SQL] AppformMapper.insertMcpRequestMove
```

**테이블/API:** MCP_REQUEST_MOVE  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestMove
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestPayment
-> [SQL] AppformMapper.insertMcpRequestPayment
```

**테이블/API:** MCP_REQUEST_PAYMENT  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestPayment
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 선불  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestSaleinfo
-> [SQL] AppformMapper.insertMcpRequestSaleinfo
```

**테이블/API:** MCP_REQUEST_SALEINFO  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestSaleinfo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestChange
-> [SQL] AppformMapper.insertMcpRequestChange
```

**테이블/API:** MCP_REQUEST_CHANGE  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestChange
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
-> MCP_REQUEST_CHANGE : 해당 테이블 미사용하고, 명의변경 관련 테이블로 통합 처리
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestDlvry
-> [SQL] AppformMapper.insertMcpRequestDlvry
```

**테이블/API:** MCP_REQUEST_DLVRY  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestDlvry
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestReq
-> [SQL] AppformMapper.insertMcpRequestReq
```

**테이블/API:** MCP_REQUEST_REQ  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestReq
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestPayInfo
-> [SQL] AppformMapper.insertMcpRequestPayInfo
```

**테이블/API:** MCP_REQUEST_PAY_INFO  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestPayInfo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 결제 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestDvcChg
-> [SQL] AppformMapper.insertMcpRequestDvcChg
```

**테이블/API:** MCP_REQUEST_DVC_CHG  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestDvcChg
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestAddition
-> [SQL] AppformMapper.insertMcpRequestAddition
```

**테이블/API:** MCP_REQUEST_ADDITION  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestAddition
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestKtInter
-> [SQL] AppformMapper.insertMcpRequestKtInter
```

**테이블/API:** MCP_REQUEST_KT_INTER  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestKtInter
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 유선 결합  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.checkGiftReqCount
-> [SQL] AppformMapper.checkGiftReqCount
```

**테이블/API:** NMCP_GIFT_REQ_TXN  
**처리 로직 / 변환 처리:**

```
o AppformMapper.checkGiftReqCount
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 사은품  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertGiftReqTxn
-> [SQL] AppformMapper.insertGiftReqTxn
```

**테이블/API:** NMCP_GIFT_REQ_TXN  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertGiftReqTxn
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 사은품  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.checkGiftReqCount
-> [SQL] AppformMapper.checkGiftReqCount
```

**테이블/API:** NMCP_GIFT_REQ_TXN  
**처리 로직 / 변환 처리:**

```
o AppformMapper.checkGiftReqCount
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 사은품  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertGiftReqTxn
-> [SQL] AppformMapper.insertGiftReqTxn
```

**테이블/API:** NMCP_GIFT_REQ_TXN  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertGiftReqTxn
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 사은품  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestAdditionPromotion
-> [M포탈-M전산 API] /appform/mcpRequestAdditionPromotion
```

**처리 로직 / 변환 처리:**

```
o /appform/mcpRequestAdditionPromotion
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestState
-> [SQL] AppformMapper.insertMcpRequestState
```

**테이블/API:** MCP_REQUEST_STATE  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestState
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertNmcpRequestApd
-> [SQL] AppformMapper.insertNmcpRequestApd
```

**테이블/API:** NMCP_REQUEST_APD  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertNmcpRequestApd
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 자급제 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertNmcpRequestApdDlvry
-> [SQL] AppformMapper.insertNmcpRequestApdDlvry
```

**테이블/API:** NMCP_REQUEST_APD_DLVRY  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertNmcpRequestApdDlvry
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 자급제 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertNmcpRequestApdSaleinfo
-> [SQL] AppformMapper.insertNmcpRequestApdSaleinfo
```

**테이블/API:** NMCP_REQUEST_APD_SALEINFO  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertNmcpRequestApdSaleinfo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 자급제 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertNmcpRequestApdState
-> [SQL] AppformMapper.insertNmcpRequestApdState
```

**테이블/API:** NMCP_REQUEST_APD_STATE  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertNmcpRequestApdState
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 자급제 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** pointService.editPoint  
**기타:** 제외 - 포인트 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** pointService.editPoint  
**기타:** 제외 - 포인트 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestCommend
-> [SQL] AppformMapper.insertMcpRequestCommend
```

**테이블/API:** MCP_REQUEST_COMMEND  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestCommend
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 추천인 제외  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.updateMcpRequestNowDlvryHist
-> [SQL] AppformMapper.updateMcpRequestNowDlvryHist
```

**테이블/API:** MCP_REQUEST_NOW_DLVRY_HIST  
**처리 로직 / 변환 처리:**

```
o AppformMapper.updateMcpRequestNowDlvryHist
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 배송  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.getMcpReqDtl
-> [SQL] AppformMapper.getMcpReqDtl
```

**테이블/API:** MCP_REQUEST REQ, MCP_REQUEST_CSTMR, MCP_REQUEST_SALEINFO, MCP_REQUEST_MOVE, MCP_REQUEST_DLVRY  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getMcpReqDtl
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
  - MCP_REQUEST_DLVRY : 배송 관련 로직은 제외
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.chkMcpReqDtl
-> [SQL] AppformMapper.chkMcpReqDtl
```

**테이블/API:** MCP_REQUEST_DTL  
**처리 로직 / 변환 처리:**

```
o AppformMapper.chkMcpReqDtl
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
-> MCP_REQUEST_DTL : 해당 테이블 미사용하고 각 신청서 관련 테이블 데이터로 등록 처리
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.updateMcpReqDtl
-> [SQL] AppformMapper.updateMcpReqDtl
```

**테이블/API:** MCP_REQUEST_DTL  
**처리 로직 / 변환 처리:**

```
o AppformMapper.updateMcpReqDtl
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
-> MCP_REQUEST_DTL : 해당 테이블 미사용하고 각 신청서 관련 테이블 데이터로 등록 처리
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpReqDtl
-> [SQL] AppformMapper.insertMcpReqDtl
```

**테이블/API:** MCP_REQUEST_DTL  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpReqDtl
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
-> MCP_REQUEST_DTL : 해당 테이블 미사용하고 각 신청서 관련 테이블 데이터로 등록 처리
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertKtCounsel
-> [SQL] AppformMapper.insertKtCounsel
```

**테이블/API:** NMCP_KT_COUNSEL_REQ  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertKtCounsel
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 상담사 상담  
**개발팀 전달일:** 03/24 전달  
---

**Service:** appformSvc.requestOsstCount  
**Service / Dao / SQL / API:**

```
appformDao.requestOsstCount
-> [SQL] AppformMapper.requestOsstCount
```

**테이블/API:** MCP_REQUEST_OSST  
**처리 로직 / 변환 처리:**

```
o AppformMapper.requestOsstCount
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
appformSvc.sendOsstService
- Constants.EVENT_CODE_PRE_CHECK
```

**Service / Dao / SQL / API:**

```
mplatFormOsstServerAdapter.callService
-> [M플랫폼] PC0
```

**테이블/API:** PC0  
**처리 로직 / 변환 처리:**

```
o appformSvc.sendOsstService
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  

---

## [4001-09]

**JSP/JS:** /mobile/content/dataSharingStep3.jsp, /resources/js/mobile/content/dataSharingStep3.js  
**URL:** /appform/conPreCheckAjax.do  
**Controller:**

```
AppformController.java - conPreCheck
 - 개통사전 체크 확인 /  개통 및 수납 확인
```

**처리 로직 / 변환 처리:**

```
o AppformController.java - conPreCheck
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** appformSvc.getRequestOsst  
**Service / Dao / SQL / API:**

```
appformDao.getRequestOsst
-> [SQL] AppformMapper.getRequestOsst
```

**테이블/API:** MCP_REQUEST_OSST  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getRequestOsst
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** appformSvc.chkRealPc2Result  
**처리 로직 / 변환 처리:**

```
o appformSvc.chkRealPc2Result
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
sendOsstSt1Service
-> mplatFormOsstServerAdapter.callService
- Constants.EVENT_CODE_PRE_SCH
-> [M플랫폼] ST1
```

**테이블/API:** ST1  
**처리 로직 / 변환 처리:**

```
o sendOsstSt1Service
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mPlatFormService.MoscSvcContService  
**Service / Dao / SQL / API:**

```
mplatFormServerAdapter.callService
-> [M플랫폼] FT1
```

**테이블/API:** Y39  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.MoscSvcContService
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  

---

## [4001-10]

**JSP/JS:** /mobile/content/dataSharingStep3.jsp, /resources/js/mobile/content/dataSharingStep3.js  
**URL:** /appform/saveDataSharingStep2Ajax.do  
**Controller:**

```
AppformController.java - saveDataSharingStep2
 - 번호
```

**처리 로직 / 변환 처리:**

```
o AppformController.java - saveDataSharingStep2
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
-> mypageService.selectCntrList
  - 회선정보
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 회원 로직 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrListNoLogin
  - 비회원로그인
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNoLogin  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNoLogin
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** fnSearchNumber  
**처리 로직 / 변환 처리:**

```
o fnSearchNumber
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformSvc.updateMcpRequest
-> appformDao.updateMcpRequest
-> [SQL] AppformMapper.updateMcpRequest
```

**테이블/API:** MCP_REQUEST  
**처리 로직 / 변환 처리:**

```
o AppformMapper.updateMcpRequest
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformSvc.getPhoneNoList
-> mplatFormOsstServerAdapter.callService
-> [M플랫폼] NU1
- EVENT_CODE_SEARCH_NUMBER
```

**테이블/API:** NU1  
**처리 로직 / 변환 처리:**

```
o appformSvc.getPhoneNoList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** appformSvc.insertMcpRequestOsst  
**Service / Dao / SQL / API:**

```
appformDao.insertMcpRequestOsst
-> [SQL] AppformMapper.insertMcpRequestOsst
```

**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpRequestOsst
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
appformSvc.sendOsstService
- EVENT_CODE_NUMBER_REG
```

**Service / Dao / SQL / API:**

```
mplatFormOsstServerAdapter.callService
-> [M플랫폼] NU2
```

**테이블/API:** NU2  
**처리 로직 / 변환 처리:**

```
o appformSvc.sendOsstService
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  

---

## [4001-11]

**JSP/JS:** /mobile/content/dataSharingStep3.jsp, /resources/js/mobile/content/dataSharingStep3.js  
**URL:** /appform/saveDataSharingStep3Ajax.do  
**Controller:**

```
AppformController.java - saveDataSharingStep3
 - 개통및수납
```

**처리 로직 / 변환 처리:**

```
o AppformController.java - saveDataSharingStep3
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fnSetDataOfdataSharing
-> mypageService.selectCntrList
  - 회선정보
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 회원 로직 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrListNoLogin
  - 비회원로그인
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNoLogin  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNoLogin
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectBanSel  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/selectBanSel  
**처리 로직 / 변환 처리:**

```
o /mypage/selectBanSel
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
appformSvc.sendOsstService
- Constants.EVENT_CODE_REQ_OPEN
```

**Service / Dao / SQL / API:**

```
mplatFormOsstServerAdapter.callService
-> [M플랫폼] OP0
```

**테이블/API:** OP0  
**처리 로직 / 변환 처리:**

```
o appformSvc.sendOsstService
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** appformSvc.insertDisPrmtApd  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /appform/insertDisPrmtApd  
**처리 로직 / 변환 처리:**

```
o /appform/insertDisPrmtApd
-> 현행 ITO API 그대로 사용 : M전산/M포탈 테이블 INSERT/UPDATE/DELETE
```

**개발팀 전달일:** 03/24 전달  

---

## [쉐어링]

**JSP/JS:** /mobile/content/dataSharingStep4.jsp  
**URL:** /m/content/dataSharingStep4.do  
**Controller:**

```
MyShareDataController.java - dataSharingStep4
  - 쉐어링 완료
```

**처리 로직 / 변환 처리:**

```
o MyShareDataController.java - dataSharingStep4
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrListNoLogin
  - 비회원로그인
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNoLogin  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNoLogin
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrList
  - 회선정보
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 회원 로직 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectSocDesc
  - 요금제 조회
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/socDesc  
**처리 로직 / 변환 처리:**

```
o /mypage/socDesc
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectSocDesc
  - 요금제 조회
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/socDesc  
**처리 로직 / 변환 처리:**

```
o /mypage/socDesc
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  

---

## [쉐어링]

**URL:** /m/content/preOpenCheckAjax.do  
**Controller:**

```
MyShareDataController.java - domyCntrListAjax
  - 데이터 쉐어링 사전체크
```

**처리 로직 / 변환 처리:**

```
o MyShareDataController.java - domyCntrListAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrListNoLogin
  - 비회원로그인
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNoLogin  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNoLogin
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrList
  - 회선정보
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 회원 로직 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** checkUserType  
**처리 로직 / 변환 처리:**

```
o checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
myShareDataSvc.mosharingList
  - 데이터쉐어링 결합중인 대상 조회
```

**Service / Dao / SQL / API:**

```
mplatFormService.mosharingList
-> mplatFormServerAdapter.callService
-> [M플랫폼] X23
```

**테이블/API:** X71  
**처리 로직 / 변환 처리:**

```
o mplatFormService.mosharingList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  

---

## [7002-01]

**JSP/JS:** /WEB-INF/views/mobile/mypage/sendCertSms.jsp, /resources/js/mobile/mypage/sendCertSms.js  
**URL:** /m/sms/sendCertSmsAjax.do  
**Controller:**

```
SmsController.java - sendCertSmsAjax
  - sms인증
```

**처리 로직 / 변환 처리:**

```
o SmsController.java - sendCertSmsAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageUserService.selectConSsnObj  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/selectConSsnObj  
**처리 로직 / 변환 처리:**

```
o /mypage/selectConSsnObj
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageUserService.selectContractObj  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/selectContractObj  
**처리 로직 / 변환 처리:**

```
o /mypage/selectContractObj
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageUserService.selectPrePayment
  - 선불 요금제 여부 조회
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/prePayment  
**처리 로직 / 변환 처리:**

```
o /mypage/prePayment
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** certService.smsAuthCertMenuType  
**처리 로직 / 변환 처리:**

```
o certService.smsAuthCertMenuType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.getModuTypeStepCnt
  - moduType별 step
```

**Service / Dao / SQL / API:**

```
certDao.getModuTypeStepCnt
-> [SQL] CertMapper.getModuTypeStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getModuTypeStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.getCertInfo
  - 인증정보 비교해서 map으로 리턴
```

**처리 로직 / 변환 처리:**

```
o certService.getCertInfo
  - 본인인증 불필요로하므로 적용 제외
```

**기타:** 제외 - 본인인증  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:** fCommonSvc.sendAuthSms2  
**처리 로직 / 변환 처리:**

```
o fCommonSvc.sendAuthSms2
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** NmcpServiceUtils.getCodeList("SMSRESTRICT")  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** [M포탈-M전산 API] /sms/qStackNewCnt  
**처리 로직 / 변환 처리:**

```
o /sms/qStackNewCnt
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** smsSvc.sendSmsForAuth  
**처리 로직 / 변환 처리:**

```
o smsSvc.sendSmsForAuth
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
smsSvc.sendSmsForAuth
-> sendSms
```

**처리 로직 / 변환 처리:**

```
o sendSms
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
smsSvc.sendSmsForAuth
-> sendSms
-> [M포탈-M전산 API] /sms/addSms
```

**처리 로직 / 변환 처리:**

```
o /sms/addSms
-> 현행 ITO API 그대로 사용 : M전산/M포탈 테이블 INSERT/UPDATE/DELETE
```

**개발팀 전달일:** 03/24 전달  

---

## [7002-02]

**JSP/JS:** /WEB-INF/views/mobile/mypage/sendCertSms.jsp, /resources/js/mobile/mypage/sendCertSms.js  
**URL:** /m/sms/checkCertSmsAjax.do  
**Controller:**

```
SmsController.java - checkCertSmsAjax
  - sms 번호 확인
```

**처리 로직 / 변환 처리:**

```
o SmsController.java - checkCertSmsAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrListNoLogin
  - 비회원로그인
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNoLogin  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNoLogin
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** certService.smsAuthCertMenuType  
**처리 로직 / 변환 처리:**

```
o certService.smsAuthCertMenuType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** certService.getStepCnt  
**Service / Dao / SQL / API:**

```
certDao.getStepCnt
-> [SQL] CertMapper.getStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**JSP/JS:** /WEB-INF/views/mobile/mypage/sendCertSms.jsp, /resources/js/mobile/mypage/sendCertSms.js
**URL:** /m/event/frndSendAjax.do
**기타:** 제외 - 불필요
**개발팀 전달일:** 03/24 전달

---

## [7003-01]

> **출처:** MMSP-AN-01 기능목록 (2026-03-26 추가)
> **구분:** 서식지App / 부가기능 / 접수완료 신청서
> **요구사항ID:** REQ_010502

**기능:** 접수완료 신청서 목록 화면 및 목록 조회

**처리 로직 / 변환 처리:**

```
o 접수완료 신청서 목록 화면 구성
o 접수완료 신청서 목록 조회
  - 연동: 없음
  - 테이블: m전산DB + m포탈DB - 조회
  - 개발방향: kt 소스 참고
```

**의사결정/확인필요:**

```
[유통영업팀 확인] (2026.03.10)
o 조회 기간을 전일~당일까지만 가능하게 할 것인지 (조회기간 확인 필요)
=> 유통영업팀 확인 후 피드백 예정
```

---

## [7003-02]

> **출처:** MMSP-AN-01 기능목록 (2026-03-26 추가)
> **구분:** 서식지App / 부가기능 / 임시저장
> **요구사항ID:** REQ_010501

**기능:** 임시저장 목록 화면 및 목록 조회

**처리 로직 / 변환 처리:**

```
o 임시저장 목록 화면 구성
o 임시저장 목록 조회
  - 연동: 없음
  - 테이블: 스마트서식지DB - m포탈레이아웃 사용
  - 개발방향: kt 소스 참고
```

---

## [7003-03]

> **출처:** MMSP-AN-01 기능목록 (2026-03-26 추가)
> **구분:** 배치 / 신청서 관리
> **요구사항ID:** REQ_030101

**기능:** 임시저장 데이터 삭제 (배치)

**처리 로직 / 변환 처리:**

```
o 임시저장 데이터 주기적 삭제 처리
  - 매일 1회 배치 자동 실행
  - 현재일 기준 D-8 이전 데이터 모두 삭제 처리
  - 연동: 없음
  - 테이블: 스마트서식지DB - m포탈레이아웃 사용

[참고]
o m포탈 임시저장 삭제 배치 소스 참고하여 스마트서식지DB 테이블에 맞춰 변경
```

---

## [7003-04]

> **출처:** MMSP-AN-01 기능목록 (2026-03-26 추가)
> **구분:** 서식지App / 신규/변경 신청서
> **요구사항ID:** REQ_010501

**기능:** 임시저장 데이터 조회

**처리 로직 / 변환 처리:**

```
o 임시저장 데이터 조회
  - 연동: 없음 (/)
  - 테이블: 스마트서식지DB - m포탈레이아웃 사용
```

**기타:** [1010] 임시저장 저장 처리와 연계. AppformMapper.getTempRequestKey / insertAppFormTempSave 참조.

---

## [7003-05]

> **출처:** MMSP-AN-01 기능목록 (2026-03-26 추가)
> **구분:** 서식지App / 공통 인증
> **요구사항ID:** REQ_010103, REQ_010201, REQ_010301, REQ_010401

**기능:** kt m모바일 휴대폰 번호 인증

**처리 로직 / 변환 처리:**

```
o 휴대폰번호 kt m모바일 휴대폰 번호 인증
  - 기기변경 / 서비스변경 / 명의변경 / 서비스해지 가능 휴대폰번호 인증
  - M전산 휴대폰 가입 정보 조회하여 체크
  - 연동: m플랫폼 (Y04)
  - 테이블: m전산DB - 조회
  - 개발방향: m모바일 m포탈 소스 참고 — BACK 단 소스 재사용
```

**기타:** [7002-01]/[7002-02] SMS 인증과 연계. JoinInfoSvc.joinInfo() → Y04 → X01 흐름 동일.

---

## [7003-06]

> **출처:** MMSP-AN-01 기능목록 (2026-03-26 추가)
> **구분:** 서식지App / 공통 조회
> **요구사항ID:** REQ_010103, REQ_010201, REQ_010401

**기능:** kt m모바일 가입정보 조회

**처리 로직 / 변환 처리:**

```
o 휴대폰번호 기 가입정보 조회
  - 연동: m플랫폼 (X01)
  - 테이블: m전산DB + m포탈DB - 조회
             스마트서식지DB - m포탈레이아웃 사용
  - 개발방향: m모바일 m포탈 소스 참고 — BACK 단 소스 재사용
```

**기타:** [7003-05] 휴대폰 번호 인증 후 X01 가입정보 조회 흐름. POST /api/v1/{domain}/join-info → JoinInfoSvc 처리.

---

## [7003-07]

> **출처:** MMSP-AN-01 기능목록 (2026-03-26 추가)
> **구분:** 서식지App / 서비스변경·서비스해지 신청서
> **요구사항ID:** REQ_010205, REQ_010401

**기능:** 사용요금 / 잔여요금 조회

**처리 로직 / 변환 처리:**

```
o 사용요금 조회 화면 구성
o 현 시점 기준 사용요금 조회
  - 연동: m플랫폼
  - 테이블: 스마트서식지DB - m포탈레이아웃 사용
  - 개발방향: m모바일 m포탈 소스 참고 — BACK 단 소스 재사용

o 잔여요금 조회
  - 실시간요금조회 (X18)
  - 연동: m플랫폼 (X18)
  - 테이블: 스마트서식지DB - m포탈레이아웃 사용
```

---

## [7003-08]

> **출처:** MMSP-AN-01 기능목록 (2026-03-26 추가)
> **구분:** 서식지App / 서비스해지 신청서
> **요구사항ID:** REQ_010401

**기능:** 위약금 조회

**처리 로직 / 변환 처리:**

```
o 위약금 조회
  - 스폰서 조회 (X54) 위약금
  - 심플할인 정보조회 (X62) 위약금
  - 연동: m플랫폼 (X54, X62)
  - 테이블: 스마트서식지DB - m포탈레이아웃 사용
  - 개발방향: m모바일 m포탈 소스 참고 — BACK 단 소스 재사용
```

**의사결정/확인필요:**

```
[완료(2026.03.23)]
[IT혁신팀 확인] (2026.03.10)
o MP 위약금 API 개발 예정 — 완료 후 연동
```

---

## [7003-09]

> **출처:** MMSP-AN-01 기능목록 (2026-03-26 추가)
> **구분:** 서식지App / 기타 / 로그인
> **요구사항ID:** REQ_010601

**기능:** ID/PW 로그인

**처리 로직 / 변환 처리:**

```
o ID/PW 로그인 화면 구성
o 로그인 유효성 체크
  - 아이디 존재 여부
  - 비밀번호 일치 여부
  - 로그인 3회 실패 시 계정 잠금 처리
  - 아이디에 해당하는 접근 가능 대리점 조회
  - 연동: m전산
  - 테이블: 스마트서식지DB - m포탈레이아웃 사용
  - 개발방향: kt 소스 참고
```

**의사결정/확인필요:**

```
[유통영업팀 확인] (2026.03.10)
o ID 하나에 여러 개의 패드를 사용하게 할 것인지 1:1로 제한할 것인지
=> 유통영업팀 확인 후 피드백 예정

[개발팀] (2026.03.08)
o 스마트서식지 App 사용 제한에 대해서 Mac Address 대신 단말 고유 식별자로 접근 제한할 방안 검토
```

---

## [7003-10]

> **출처:** MMSP-AN-01 기능목록 (2026-03-26 추가)
> **구분:** 서식지App / 부가기능 / 로그인 설정, 기타
> **요구사항ID:** REQ_011001, REQ_010603, REQ_010604

**기능:** 지문/Face ID 로그인 및 설정

**처리 로직 / 변환 처리:**

```
o 로그인 설정 화면 구성 — 로그인 설정 정보 조회
  - 테이블: 스마트서식지DB - m포탈레이아웃 사용

o 지문 설정/해제
  - 조회_지문 조회: 기존 설정 지문 정보 조회
  - 처리_지문 설정: 토큰 생성 및 단말기 현행화 처리
  - 처리_지문 설정 해제: 토큰 만료 처리 및 단말기 현행화 처리
  - 연동: FIDO (신규) 지문 설정 API

o Face ID 설정/해제
  - 조회_Face ID 조회: 기존 설정 Face ID 정보 조회
  - 처리_Face ID 설정: 토큰 생성 및 단말기 현행화 처리
  - 처리_Face ID 설정 해제: 토큰 만료 처리 및 단말기 현행화 처리
  - 연동: FIDO (신규) Face ID 설정 API

o 지문 로그인 / Face ID 로그인
  - 로그인 등록 여부 체크 (미등록 시 이용 불가)
  - 계정 유효성 체크는 ID/PW와 동일
  - 로그인 후 토큰 갱신 처리
  - 연동: FIDO

o App 사용 정보 조회 / App 업데이트 정보 조회
  - App 보안 제약 (루팅/탈옥폰 사용 불가), OS 제약 체크
  - App 버전 체크 및 업데이트 안내

  - 개발방향: m모바일 m포탈 소스 참고
```

**의사결정/확인필요:**

```
[유통영업팀 확인] (2026.03.10)
o 지문, 페이스 로그인 기능 개발 여부
=> 유통영업팀 확인 후 피드백 예정
```

---

## [7003-11]

> **출처:** MMSP-AN-01 기능목록 (2026-03-26 추가)
> **구분:** 서식지App / 기타 / 시스템 공통, 시스템 관리자
> **요구사항ID:** REQ_010701, REQ_021201

**기능:** WAS Cache 현행화

**처리 로직 / 변환 처리:**

```
o WAS 기동 시 Cache 정보 관리
  - 코드, 메뉴 등 캐시 현행화
  - 연동: 없음
  - 테이블: 스마트서식지DB - m포탈레이아웃 사용
             m전산DB + m포탈DB - 조회
  - 개발방향: m전산/m모바일 소스 참고
```

**의사결정/확인필요:**

```
[완료(2026.03.23)]
[개발팀 확인] (2026.03.10)
o Cache 현행화 개발 확인 완료
```

---

## [5001-02]

**JSP/JS:** /mobile/content/insrView.jsp, /resources/js/mobile/content/insrView.js  
**URL:** /m/content/insrView.do  
**Controller:** ComContentController.java - insrView  
**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  

---

## [5001-03]

**JSP/JS:** /mobile/content/insrView.jsp, /resources/js/mobile/content/insrView.js  
**URL:** /cs/reqFormDownloadCountUpdateAjax.do  
**Controller:** CsReqFormController.java - reqFormDownloadCountUpdateAjax  
**Service:** csReqFormSvc.reqFormDownloadCountUpdate  
**Service / Dao / SQL / API:**

```
csReqFormDao.reqFormDownloadCountUpdate
-> [SQL] CsReqFormMapper.reqFormDownloadCountUpdateStplt
```

**테이블/API:** NMCP_STPLT_DTL  
**처리 로직 / 변환 처리:**

```
o CsReqFormMapper.reqFormDownloadCountUpdateStplt
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
csReqFormDao.reqFormDownloadCountUpdate
-> [SQL] CsReqFormMapper.reqFormDownloadCountUpdate
```

**테이블/API:** NMCP_BOARD_ATT_DTL  
**처리 로직 / 변환 처리:**

```
o CsReqFormMapper.reqFormDownloadCountUpdate
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  

---

## [5001-04]

**JSP/JS:** /mobile/mypage/reqInsr.jsp, /resources/js/mobile/mypage/reqInsr.js  
**URL:** /m/mypage/reqInsr.do  
**Controller:** CustRequestController.java - reqInsr  
**처리 로직 / 변환 처리:**

```
o CustRequestController.java - reqInsr
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrList
  - 회선정보
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
custRequestService.getInsrInfo
  - 계약번호로 보험가입정보조회
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/getInsrInfo  
**처리 로직 / 변환 처리:**

```
o /mypage/getInsrInfo
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** getCustomerType  
**처리 로직 / 변환 처리:**

```
o getCustomerType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
maskingSvc.insertMaskingReleaseHist
  - 마스킹해제
```

**Service / Dao / SQL / API:**

```
maskingDao.insertMaskingReleaseHist
-> [SQL] MaskingMapper.insertMaskingReleaseHist
```

**테이블/API:** MCP_MASKING_RELEASE_HIST  
**처리 로직 / 변환 처리:**

```
o MaskingMapper.insertMaskingReleaseHist
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  

---

## [5001-05]

**JSP/JS:** /mobile/mypage/reqInsr.jsp, /resources/js/mobile/mypage/reqInsr.js  
**URL:** /mypage/custRequestAjax.do  
**Controller:** CustRequestController.java - custRequestAjax  
**처리 로직 / 변환 처리:**

```
o CustRequestController.java - custRequestAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** certService.getStepCnt  
**Service / Dao / SQL / API:**

```
certDao.getStepCnt
-> [SQL] CertMapper.getStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:** setCstmrInfo  
**처리 로직 / 변환 처리:**

```
o setCstmrInfo
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
setCstmrInfo
-> mypageService.selectCntrList
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
setCstmrInfo
-> getCustomerType
```

**처리 로직 / 변환 처리:**

```
o getCustomerType
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** custRequestService.getCustRequestSeq  
**Service / Dao / SQL / API:**

```
custRequestDao.getCustRequestSeq
-> [SQL] CustRequestMapper.getCustRequestSeq
```

**테이블/API:** NMCP_CUST_REQUEST_SEQ  
**처리 로직 / 변환 처리:**

```
o CustRequestMapper.getCustRequestSeq
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** custRequestService.insertCustRequestInsr  
**Service / Dao / SQL / API:**

```
custRequestDao.insertCustRequestInsr
-> [SQL] CustRequestMapper.insertCustRequestInsr
```

**테이블/API:** NMCP_CUST_REQUEST_INSR  
**처리 로직 / 변환 처리:**

```
o CustRequestMapper.insertCustRequestInsr
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
regSvcService.regSvcChg
  - 안심보험 부가서비스 가입 처리
```

**Service / Dao / SQL / API:**

```
mPlatFormService.regSvcChg
-> mplatFormServerAdapter.callService
-> [M플랫폼] X21 -> Y25 로 대체
```

**테이블/API:**

```
X21 -> Y25 로 대체
```

**처리 로직 / 변환 처리:**

```
o mPlatFormService.regSvcChg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

o X21 -> Y25 로 대체
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
custRequestScanService.prodSendScan
  - SCAN 서버에 서식지 데이터 생성 및 전송
```

**Service / Dao / SQL / API:** 5001-01  
**처리 로직 / 변환 처리:** o 5001-01 로직 전체  
**개발팀 전달일:** 03/24 전달  
---

**Service:** ipstatisticService.insertAdminAccessTrace  
**Service / Dao / SQL / API:**

```
fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 호출 로그 적재  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
fCommonSvc.getMspSmsTemplateMst
  - 안심보험가입(IS) 중 D(자급제30일이후), E(유심30일이내), F(유심30일이후) 고객에게는 문자를 전송
```

**Service / Dao / SQL / API:**

```
fCommonSvc.getMspSmsTemplateMst
-> fCommonDao.getMspSmsTemplateMst
-> [M포탈-M전산 API] /common/mspSmsTemplateMst
```

**처리 로직 / 변환 처리:**

```
o /common/mspSmsTemplateMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** custRequestService.getInsrInfoByCd  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/getInsrInfoByCd  
**처리 로직 / 변환 처리:**

```
o /mypage/getInsrInfoByCd
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** smsSvc.sendLms  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /sms/addNewSms  
**처리 로직 / 변환 처리:**

```
o /sms/addNewSms
-> 현행 ITO API 그대로 사용 : M전산/M포탈 테이블 INSERT/UPDATE/DELETE
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** ipstatisticService.insertAdminAccessTrace  
**Service / Dao / SQL / API:**

```
fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 공통모듈 - 호출 로그 적재  
**개발팀 전달일:** 03/24 전달  

---

## [5001-01]

**Service:**

```
custRequestScanService.prodSendScan
  - SCAN 서버에 서식지 데이터 생성 및 전송
  - "IS"
```

**처리 로직 / 변환 처리:**

```
o custRequestScanService.prodSendScan
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
custRequestDao.getReqInsrData
  - 안심보험신청
  - reqType = "IS"
-> [SQL] CustRequestMapper.getReqInsrData
```

**테이블/API:** NMCP_CUST_REQUEST_MST, NMCP_CUST_REQUEST_INSR  
**처리 로직 / 변환 처리:**

```
o CustRequestMapper.getReqInsrData
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
settingSsnData
  - 주민번호 분석 값 설정
```

**처리 로직 / 변환 처리:**

```
o settingSsnData
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
settingData
  - 각종 설정 값 설정
```

**처리 로직 / 변환 처리:**

```
o settingData
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
createXml
  - XML 파일 생성
```

**처리 로직 / 변환 처리:**

```
o createXml
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
createXml
-> getGroupCode
```

**처리 로직 / 변환 처리:**

```
o getGroupCode
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
createXml
-> custRequestDao.getAppFormPointList
-> [SQL] CustRequestMapper.getAppFormGroupPointList
```

**테이블/API:** NMCP_APP_GROUP_MST, NMCP_APP_GROUP_INFO, NMCP_APP_FORM_INFO  
**처리 로직 / 변환 처리:**

```
o CustRequestMapper.getAppFormGroupPointList
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경

ㅇ ITO AS-IS 소스에 로직 변경
 - 신규 포시에스 리포팅 시스템에 맞춰서 로직 변경
```

**기타:** 확인 필요 - 포시에스 솔루션에서 필요할지 확인  
**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
createXml
-> saveXML
```

**처리 로직 / 변환 처리:**

```
o saveXML
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
xmlFileSend
  - XML 파일 전송 및 삭제 처리
```

**처리 로직 / 변환 처리:**

```
o xmlFileSend
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
xmlFileSend
-> ipstatisticService.insertAdminAccessTrace
-> fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
custRequestDao.updateEmvScanMst
  - 안심보험신청
  - reqType = "IS"
-> [SQL] CustRequestMapper.updateEmvScanMst
```

**테이블/API:** EMV_SCAN_MST  
**처리 로직 / 변환 처리:**

```
o CustRequestMapper.updateEmvScanMst
-> SQL Copy 후 M포탈-M전산 API 에 SQL 추가하여 데이터 INSERT/UPDATE 처리
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
custRequestDao.updateEmvScanFile
  - 안심보험신청
  - reqType = "IS"
-> [SQL] CustRequestMapper.updateEmvScanFile
```

**테이블/API:** EMV_SCAN_FILE  
**처리 로직 / 변환 처리:**

```
o CustRequestMapper.updateEmvScanFile
-> SQL Copy 후 M포탈-M전산 API 에 SQL 추가하여 데이터 INSERT/UPDATE 처리
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
custRequestService.getOrgScanId
  - 안심보험신청
  - reqType = "IS"
-> [M포탈-M전산 API] /mypage/getOrgScanId
```

**처리 로직 / 변환 처리:**

```
o /mypage/getOrgScanId
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
custRequestDao.getMaxFileNum
  - 안심보험신청
  - reqType = "IS"
-> [SQL] CustRequestMapper.getMaxFileNum
```

**테이블/API:** EMV_SCAN_FILE  
**처리 로직 / 변환 처리:**

```
o CustRequestMapper.getMaxFileNum
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/23 전달  
---

**Service / Dao / SQL / API:**

```
custRequestDao.insertEmvScanFile
  - 안심보험신청
  - reqType = "IS"
-> [SQL] CustRequestMapper.insertEmvScanFile
```

**테이블/API:** EMV_SCAN_FILE  
**처리 로직 / 변환 처리:**

```
o CustRequestMapper.insertEmvScanFile
-> SQL Copy 후 M포탈-M전산 API 에 SQL 추가하여 데이터 INSERT/UPDATE 처리
```

**개발팀 전달일:** 03/23 전달  

---

## [5002-01]

**JSP/JS:** /mobile/content/combineSelf.jsp, /resources/js/mobile/content/combineSelf.js  
**URL:** /m/content/combineSelf.do  
**Controller:** CombineController.java - combineSelf  
**처리 로직 / 변환 처리:**

```
o CombineController.java - combineSelf
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** certService.delStepInfo  
**Service / Dao / SQL / API:**

```
certDao.delStepInfo
-> [SQL] CertMapper.delStepInfo
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.delStepInfo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
maskingSvc.insertMaskingReleaseHist
  - 마스킹해제
```

**Service / Dao / SQL / API:**

```
maskingDao.insertMaskingReleaseHist
-> [SQL] MaskingMapper.insertMaskingReleaseHist
```

**테이블/API:** MCP_MASKING_RELEASE_HIST  
**처리 로직 / 변환 처리:**

```
o MaskingMapper.insertMaskingReleaseHist
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  

---

## [5002-02]

**JSP/JS:** /mobile/content/combineSelf.jsp, /resources/js/mobile/content/combineSelf.js  
**URL:** /content/getNoLoginRateInfoAjax.do  
**처리 로직 / 변환 처리:**

```
o /content/getNoLoginRateInfoAjax.do
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectSocDesc
  - 요금제 조회
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/socDesc  
**처리 로직 / 변환 처리:**

```
o /mypage/socDesc
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  

---

## [5002-03]

**JSP/JS:** /mobile/content/combineSelf.jsp, /resources/js/mobile/content/combineSelf.js  
**URL:** /content/checkCombineSelfAjax.do  
**Controller:**

```
CombineController.java - checkCombineSelf
  - 마스터 결합 가능 여부 체크
```

**처리 로직 / 변환 처리:**

```
o CombineController.java - checkCombineSelf
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
NmcpServiceUtils.getCodeList("MasterCombineLineInfo")
  - 법인 회선 여부
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mPlatFormService.getAddSvcInfoDto
  - 이용중인 부가서비스 조회
```

**Service / Dao / SQL / API:**

```
mplatFormServerAdapter.callService
-> [M플랫폼] X20 -> X97 로 대체
```

**테이블/API:**

```
X20 -> X97 로 대체
```

**처리 로직 / 변환 처리:**

```
o mPlatFormService.getAddSvcInfoDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

o X20 -> X97 로 대체
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
myCombinationSvc.selectMspCombRateMapp
  - 대상 요금제 체크
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /msp/mspCombRateMapp  
**처리 로직 / 변환 처리:**

```
o /msp/mspCombRateMapp
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mplatFormService.moscCombSvcInfoList
  - 아무나결합/가족결합 여부
```

**Service / Dao / SQL / API:**

```
mplatFormServerAdapter.callService
-> [M플랫폼] X87
```

**테이블/API:** X87  
**처리 로직 / 변환 처리:**

```
o mplatFormService.moscCombSvcInfoList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  

---

## [5002-04]

**JSP/JS:** /mobile/content/combineSelf.jsp, /resources/js/mobile/content/combineSelf.js  
**URL:** /content/regCombineSelfAjax.do  
**Controller:**

```
CombineController.java - regCombineSelf
  - 마스터 결합 처리
```

**처리 로직 / 변환 처리:**

```
o CombineController.java - regCombineSelf
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
NmcpServiceUtils.getCodeList("MasterCombineLineInfo")
  - 법인 회선 여부
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
myCombinationSvc.moscSubMstCombChg
  - 서브회선 마스터 결합 신청
```

**처리 로직 / 변환 처리:**

```
o myCombinationSvc.moscSubMstCombChg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mplatFormService.moscSubMstCombChg
-> mplatFormServerAdapter.callService
-> [M플랫폼] Y44
```

**테이블/API:** Y44  
**처리 로직 / 변환 처리:**

```
o mplatFormService.moscSubMstCombChg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.insertServiceAlterTrace
-> mypageDao.insertServiceAlterTrace
-> [SQL] MypageMapper.insertServiceAlterTrace
```

**테이블/API:** MCP_SERVICE_ALTER_TRACE  
**처리 로직 / 변환 처리:**

```
o MypageMapper.insertServiceAlterTrace
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
myCombinationSvc.selectMspCombRateMapp
  - 대상 요금제 체크
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /msp/mspCombRateMapp  
**처리 로직 / 변환 처리:**

```
o /msp/mspCombRateMapp
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** myCombinationSvc.insertMcpReqCombine  
**Service / Dao / SQL / API:**

```
mypageService.insertServiceAlterTrace
-> myCombinationDao.insertMcpReqCombine
-> [SQL] MyCombinationMapper.insertMcpReqCombine
```

**테이블/API:** MCP_REQ_COMBINE  
**처리 로직 / 변환 처리:**

```
o MyCombinationMapper.insertMcpReqCombine
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**처리 로직 / 변환 처리:**

```
o ITO 배치 현행화 대상 테이블에 데이터 INSERT 처리
  - ITO 배치 : /combine/syncCombineSoloRequest

o 확인 필요 - M포탈에 데이터 현행화 처리해줘야할지 확인
```

**기타:** 확인 필요 - M포탈에 데이터 현행화 처리해줘야할지 확인  
**개발팀 전달일:** 03/24 전달  

---

## [아무나솔로]

**JSP/JS:** /mobile/mypage/myCombineList.jsp, /resources/js/mobile/mypage/myCombineList.js  
**URL:** /mypage/myCombineList.do  
**Controller:**

```
MypageController.java - myCombineList
  - 결합 내역 조회
```

**처리 로직 / 변환 처리:**

```
o MypageController.java - myCombineList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrList
  - 회선정보
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
maskingSvc.insertMaskingReleaseHist
  - 마스킹해제
```

**Service / Dao / SQL / API:**

```
maskingDao.insertMaskingReleaseHist
-> [SQL] MaskingMapper.insertMaskingReleaseHist
```

**테이블/API:** MCP_MASKING_RELEASE_HIST  
**처리 로직 / 변환 처리:**

```
o MaskingMapper.insertMaskingReleaseHist
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
NmcpServiceUtils.getCodeList("MasterCombineLineInfo")
  - 법인 회선 여부
```

**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  

---

## [아무나솔로]

**JSP/JS:** /mobile/mypage/myCombineList.jsp, /resources/js/mobile/mypage/myCombineList.js  
**URL:** /content/getCombiSvcListAjax.do  
**Controller:** CombineController.java - getCombiSvcList  
**처리 로직 / 변환 처리:**

```
o CombineController.java - getCombiSvcList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
myCombinationSvc.selectMspCombRateMapp
  - 대상 요금제 체크
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /msp/mspCombRateMapp  
**처리 로직 / 변환 처리:**

```
o /msp/mspCombRateMapp
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mPlatFormService.getAddSvcInfoDto
  - 이용중인 부가서비스 조회
```

**Service / Dao / SQL / API:**

```
mplatFormServerAdapter.callService
-> [M플랫폼] X20 -> X97 로 대체
```

**테이블/API:**

```
X20 -> X97 로 대체
```

**처리 로직 / 변환 처리:**

```
o mPlatFormService.getAddSvcInfoDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

o X20 -> X97 로 대체
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mplatFormService.moscCombSvcInfoList
  - 아무나결합/가족결합 여부
```

**Service / Dao / SQL / API:**

```
mplatFormServerAdapter.callService
-> [M플랫폼] X87
```

**테이블/API:** X87  
**처리 로직 / 변환 처리:**

```
o mplatFormService.moscCombSvcInfoList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** myCombinationSvc.selectMcpReqCombine  
**Service / Dao / SQL / API:**

```
myCombinationDao.selectMcpReqCombine
-> [SQL] MyCombinationMapper.selectMcpReqCombine
```

**테이블/API:** MCP_REQ_COMBINE  
**처리 로직 / 변환 처리:**

```
o MyCombinationMapper.selectMcpReqCombine
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  

---

## [아무나솔로]

**JSP/JS:** /mobile/mypage/myCombineList.jsp, /resources/js/mobile/mypage/myCombineList.js  
**URL:** /m/content/myShareDataList.do  
**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  

---

## [아무나솔로]

**JSP/JS:** /mobile/content/combineSelf.jsp, /resources/js/mobile/content/combineSelf.js  
**URL:** /content/getRateInfoAjax.do  
**Controller:**

```
CombineController.java - getRateInfo
  - 시간연장처리
```

**처리 로직 / 변환 처리:**

```
o CombineController.java - regCombineSelf
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** certService.delStepInfo  
**Service / Dao / SQL / API:**

```
certDao.delStepInfo
-> [SQL] CertMapper.delStepInfo
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.delStepInfo
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrList
  - 회선정보
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectSocDesc
  - 요금제 조회
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/socDesc  
**처리 로직 / 변환 처리:**

```
o /mypage/socDesc
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  

---

## [5002-05]

**URL:** /content/getCombineSoloTypeAjax.do  
**Controller:**

```
CombineController.java - combineSoloType
  - 아무나 SOLO 결합 가능 여부 체크 (신청서 조회)
```

**처리 로직 / 변환 처리:**

```
o CombineController.java - combineSoloType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
myCombinationSvc.selectMspCombRateMapp
  - 대상 요금제 체크
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /msp/mspCombRateMapp  
**처리 로직 / 변환 처리:**

```
o /msp/mspCombRateMapp
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  

---

## [5003-01]

**JSP/JS:** /portal/mypage/NumberView01.jsp, /resources/js/portal/mypage/numberView01.js  
**URL:** /mypage/numberView01.do  
**Controller:** MyOllehController.java - numberView  
**처리 로직 / 변환 처리:**

```
o MyOllehController.java - numberView
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
maskingSvc.insertMaskingReleaseHist
  - 마스킹해제
```

**Service / Dao / SQL / API:**

```
maskingDao.insertMaskingReleaseHist
-> [SQL] MaskingMapper.insertMaskingReleaseHist
```

**테이블/API:** MCP_MASKING_RELEASE_HIST  
**처리 로직 / 변환 처리:**

```
o MaskingMapper.insertMaskingReleaseHist
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageUserService.selectPrePayment
  - 선불 요금제 여부 조회
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/prePayment  
**처리 로직 / 변환 처리:**

```
o /mypage/prePayment
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  

---

## [5003-02]

**JSP/JS:** /portal/mypage/NumberView02.jsp, /resources/js/portal/mypage/numberView02.js  
**URL:** /mypage/numberView02.do  
**Controller:** MyOllehController.java - numberViewStep2  
**처리 로직 / 변환 처리:**

```
o MyOllehController.java - numberViewStep2
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.retvRstrtn  
**Service / Dao / SQL / API:**

```
mypageDao.retvRstrtn
-> [SQL] MypageMapper.retvRstrtn
```

**테이블/API:** NMCP_RETV_RSTRTN  
**처리 로직 / 변환 처리:**

```
o MypageMapper.retvRstrtn
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.retvRstrtnInsert  
**Service / Dao / SQL / API:**

```
mypageDao.retvRstrtnInsert
-> [SQL] MypageMapper.retvRstrtnInsert
```

**테이블/API:** NMCP_RETV_RSTRTN  
**처리 로직 / 변환 처리:**

```
o MypageMapper.retvRstrtnInsert
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.retvRstrtnUpSysDate  
**Service / Dao / SQL / API:**

```
mypageDao.retvRstrtnUpSysDate
-> [SQL] MypageMapper.retvRstrtnUpSysDate
```

**테이블/API:** NMCP_RETV_RSTRTN  
**처리 로직 / 변환 처리:**

```
o MypageMapper.retvRstrtnUpSysDate
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  

---

## [5003-03]

**JSP/JS:** /portal/mypage/NumberView02.jsp, /resources/js/portal/mypage/numberView02.js  
**URL:** /mypage/numChgeListAjax.do  
**Controller:** MyOllehController.java - numChgeListAjax  
**처리 로직 / 변환 처리:**

```
o MyOllehController.java - /mypage/numChgeListAjax.do
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.retvRstrtn  
**Service / Dao / SQL / API:**

```
mypageDao.retvRstrtn
-> [SQL] MypageMapper.retvRstrtn
```

**테이블/API:** NMCP_RETV_RSTRTN  
**처리 로직 / 변환 처리:**

```
o MypageMapper.retvRstrtn
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mPlatFormService.numChgeList  
**Service / Dao / SQL / API:**

```
mplatFormOsstServerAdapter.callService
-> [M플랫폼] X31
```

**테이블/API:** X31  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.numChgeList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.retvRstrtnInsert  
**Service / Dao / SQL / API:**

```
mypageDao.retvRstrtnInsert
-> [SQL] MypageMapper.retvRstrtnInsert
```

**테이블/API:** NMCP_RETV_RSTRTN  
**처리 로직 / 변환 처리:**

```
o MypageMapper.retvRstrtnInsert
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.retvRstrtnUpCnt  
**Service / Dao / SQL / API:**

```
mypageDao.retvRstrtnUpCnt
-> [SQL] MypageMapper.retvRstrtnUpCnt
```

**테이블/API:** NMCP_RETV_RSTRTN  
**처리 로직 / 변환 처리:**

```
o MypageMapper.retvRstrtnInsert
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.retvRstrtnUpSysDate  
**Service / Dao / SQL / API:**

```
mypageDao.retvRstrtnUpSysDate
-> [SQL] MypageMapper.retvRstrtnUpSysDate
```

**테이블/API:** NMCP_RETV_RSTRTN  
**처리 로직 / 변환 처리:**

```
o MypageMapper.retvRstrtnUpSysDate
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  

---

## [5003-04]

**JSP/JS:** /portal/mypage/NumberView02.jsp, /resources/js/portal/mypage/numberView02.js  
**URL:** /mypage/numChgeChgAjax.do  
**Controller:** MyOllehController.java - numChgeChgAjax  
**처리 로직 / 변환 처리:**

```
o MyOllehController.java - numChgeChgAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** certService.getStepCnt  
**Service / Dao / SQL / API:**

```
certDao.getStepCnt
-> [SQL] CertMapper.getStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageUserService.selectPrePayment
  - 선불 요금제 여부 조회
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/prePayment  
**처리 로직 / 변환 처리:**

```
o /mypage/prePayment
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mPlatFormService.numChgeChg  
**Service / Dao / SQL / API:**

```
mplatFormOsstServerAdapter.callService
-> [M플랫폼] X32
```

**테이블/API:** X32  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.numChgeChg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  

---

## [5004-01]

**JSP/JS:** /mobile/mypage/usimSelfChg.jsp, /resources/js/mobile/mypage/usimSelfChg.js  
**URL:** /m/mypage/usimSelfChg.do  
**Controller:** UsimSelfChgController.java - reSpnsrPlcyDc  
**처리 로직 / 변환 처리:**

```
o UsimSelfChgController.java - reSpnsrPlcyDc
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
maskingSvc.insertMaskingReleaseHist
  - 마스킹해제
```

**Service / Dao / SQL / API:**

```
maskingDao.insertMaskingReleaseHist
-> [SQL] MaskingMapper.insertMaskingReleaseHist
```

**테이블/API:** MCP_MASKING_RELEASE_HIST  
**처리 로직 / 변환 처리:**

```
o MaskingMapper.insertMaskingReleaseHist
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  

---

## [5004-02]

**JSP/JS:** /mobile/mypage/usimSelfChg.jsp, /resources/js/mobile/mypage/usimSelfChg.js  
**URL:** /mypage/usimSelfChgAjax.do  
**Controller:** UsimSelfChgController.java - usimSelfChgAjax  
**처리 로직 / 변환 처리:**

```
o UsimSelfChgController.java - usimSelfChgAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrListNoLogin
  - 비회원로그인
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNoLogin  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNoLogin
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
appformSvc.sendOsstUc0Service
  - EVENT_CODE_USIM_SELF_CHG
```

**처리 로직 / 변환 처리:**

```
o appformSvc.sendOsstUc0Service
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.insertMcpSelfUsimChg
-> [SQL] AppformMapper.insertMcpSelfUsimChg
```

**테이블/API:** MCP_SELF_USIM_CHG  
**처리 로직 / 변환 처리:**

```
o AppformMapper.insertMcpSelfUsimChg
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경

o 로직 추가
 - M포탈 테이블에 데이터 등록 처리
```

**기타:** 확인 필요 - M포탈 테이블로 데이터 넣어줘야할지 확인 필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mplatFormOsstServerAdapter.callService
-> [M플랫폼] UC0
```

**테이블/API:** UC0  
**처리 로직 / 변환 처리:**

```
o mplatFormOsstServerAdapter.callService
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
appformDao.updateMcpSelfUsimChgUC0
-> [SQL] AppformMapper.updateMcpSelfUsimChgUC0
```

**테이블/API:** MCP_SELF_USIM_CHG  
**처리 로직 / 변환 처리:**

```
o AppformMapper.updateMcpSelfUsimChgUC0
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경

o 로직 추가
 - M포탈 테이블에 데이터 현행화 처리
```

**기타:** 확인 필요 - M포탈 테이블로 데이터 넣어줘야할지 확인 필요  
**개발팀 전달일:** 03/24 전달  

---

## [5004-03]

**JSP/JS:** /mobile/mypage/usimSelfChg.jsp, /resources/js/mobile/mypage/usimSelfChg.js  
**URL:** /mypage/usimChgChkAjax.do  
**Controller:** UsimSelfChgController.java - usimChgChkAjax  
**Service:** appformSvc.usimChgChk  
**Service / Dao / SQL / API:**

```
appformDao.selectUsimChgResult
-> [SQL] AppformMapper.selectUsimChgResult
```

**테이블/API:** MCP_SELF_USIM_CHG  
**처리 로직 / 변환 처리:**

```
o AppformMapper.selectUsimChgResult
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  

---

## [5005-01]

**JSP/JS:** /mobile/mypage/pause/pauseCnl.jsp, /resources/js/mobile/mypage/pause/pauseCnl.js  
**URL:** /m/mypage/suspendCnl.do  
**Controller:** PauseController.java - suspendCnl  
**처리 로직 / 변환 처리:**

```
o PauseController.java - suspendCnl
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

o 일시정지, 분실정지 두개를 연속으로 체크해야함
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mPlatFormService.suspenCnlPosInfo  
**Service / Dao / SQL / API:**

```
mplatFormOsstServerAdapter.callService
-> [M플랫폼] X28
```

**테이블/API:** X28  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.suspenCnlPosInfo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  

---

## [5005-02]

**JSP/JS:** /mobile/mypage/pause/pauseCnl.jsp, /resources/js/mobile/mypage/pause/pauseCnl.js  
**URL:** /mypage/suspenCnlChgInAjax.do  
**Controller:** PauseController.java - suspenCnlChgInAjax  
**처리 로직 / 변환 처리:**

```
o PauseController.java - suspenCnlChgInAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

o 일시정지, 분실정지 두개를 연속으로 체크해야함
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** certService.getStepCnt  
**Service / Dao / SQL / API:**

```
certDao.getStepCnt
-> [SQL] CertMapper.getStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mPlatFormService.suspenCnlChgIn  
**Service / Dao / SQL / API:**

```
mplatFormOsstServerAdapter.callService
-> [M플랫폼] X30
```

**테이블/API:** X30  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.suspenCnlChgIn
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  

---

## [5005-03]

**JSP/JS:** /mobile/mypage/pause/lostCnlView.jsp, /resources/js/mobile/mypage/pause/lostCnlView.js  
**URL:** /m/mypage/lostCnlView.do  
**Controller:** PauseController.java - lostCnlView  
**처리 로직 / 변환 처리:**

```
o PauseController.java - lostCnlView
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

o 일시정지, 분실정지 두개를 연속으로 체크해야함
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mPlatFormService.pcsLostInfo  
**Service / Dao / SQL / API:**

```
mplatFormOsstServerAdapter.callService
-> [M플랫폼] X33
```

**테이블/API:** X33  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.pcsLostInfo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
maskingSvc.insertMaskingReleaseHist
  - 마스킹해제
```

**Service / Dao / SQL / API:**

```
maskingDao.insertMaskingReleaseHist
-> [SQL] MaskingMapper.insertMaskingReleaseHist
```

**테이블/API:** MCP_MASKING_RELEASE_HIST  
**처리 로직 / 변환 처리:**

```
o MaskingMapper.insertMaskingReleaseHist
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  

---

## [5005-04]

**JSP/JS:** /mobile/mypage/pause/lostCnlView.jsp, /resources/js/mobile/mypage/pause/lostCnlView.js  
**URL:** /mypage/pcsLostCnlChgAjax.do  
**Controller:** PauseController.java - pcsLostCnlChgAjax  
**처리 로직 / 변환 처리:**

```
o PauseController.java - pcsLostCnlChgAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

o 일시정지, 분실정지 두개를 연속으로 체크해야함
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mPlatFormService.checkMpCallEventCount  
**Service / Dao / SQL / API:**

```
mplatFormServerAdapter.checkMpCallEventCount
-> [M포탈-M전산 API] /mPlatform/checkMpCallCount
```

**처리 로직 / 변환 처리:**

```
o /mPlatform/checkMpCallCount
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mPlatFormService.pcsLostCnlChg  
**Service / Dao / SQL / API:**

```
mplatFormOsstServerAdapter.callService
-> [M플랫폼] X35
```

**테이블/API:** X35  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.pcsLostCnlChg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  

---

## [5006-01]

**JSP/JS:** /mobile/mypage/farPricePlanView.jsp, /resources/js/mobile/mypage/farPricePlanView.js  
**URL:** /m/mypage/farPricePlanView.do  
**Controller:** FarPricePlanController.java - doFarPricePlanView  
**처리 로직 / 변환 처리:**

```
o FarPricePlanController.java - /mobile/mypage/farPricePlanView
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectFarPricePlan  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/farPricePlan  
**처리 로직 / 변환 처리:**

```
o /mypage/farPricePlan
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mPlatFormService.getAddSvcInfoDto  
**Service / Dao / SQL / API:**

```
mplatFormOsstServerAdapter.callService
-> [M플랫폼] X20 -> X97 로 대체
```

**테이블/API:**

```
X20 -> X97 로 대체
```

**처리 로직 / 변환 처리:**

```
o mPlatFormService.getAddSvcInfoDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  - X20 -> X97 로 대체
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
maskingSvc.insertMaskingReleaseHist
  - 마스킹해제
```

**Service / Dao / SQL / API:**

```
maskingDao.insertMaskingReleaseHist
-> [SQL] MaskingMapper.insertMaskingReleaseHist
```

**테이블/API:** MCP_MASKING_RELEASE_HIST  
**처리 로직 / 변환 처리:**

```
o MaskingMapper.insertMaskingReleaseHist
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  

---

## [5006-02]

**JSP/JS:** /mobile/mypage/farPricePlanView.jsp, /resources/js/mobile/mypage/farPricePlanView.js  
**URL:** /m/mypage/myRateResvViewAjax.do  
**Controller:** FarPricePlanController.java - doMyRateResvViewAjax  
**처리 로직 / 변환 처리:**

```
o FarPricePlanController.java - doMyRateResvViewAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectFarPricePlan  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/farPricePlan  
**처리 로직 / 변환 처리:**

```
o /mypage/farPricePlan
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** farPricePlanService.getFarPricePlanWrapper  
**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** farPricePlanService.doFarPricePlanRsrvSearch  
**Service / Dao / SQL / API:**

```
mPlatFormService.doFarPricePlanRsrvSearch
-> mplatFormServerAdapter.callService
-> [M플랫폼] X90
```

**테이블/API:** X90  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.doFarPricePlanRsrvSearch
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.countFarPricePlanList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/countFarPricePlanList  
**처리 로직 / 변환 처리:**

```
o /mypage/countFarPricePlanList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  

---

## [5006-03]

**JSP/JS:** /mobile/mypage/farPricePlanView.jsp, /resources/js/mobile/mypage/farPricePlanView.js  
**URL:** /m/mypage/deleteMyRateResvAjax.do  
**Controller:** FarPricePlanController.java - deleteMyRateResvAjax  
**처리 로직 / 변환 처리:**

```
o FarPricePlanController.java - deleteMyRateResvAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** farPricePlanService.doFarPricePlanRsrvCancel  
**처리 로직 / 변환 처리:**

```
o farPricePlanService.doFarPricePlanRsrvCancel
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mPlatFormService.doFarPricePlanRsrvCancel
-> mplatFormServerAdapter.callServiceNe
-> [M플랫폼] X90
```

**테이블/API:** X90  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.doFarPricePlanRsrvCancel
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
ipStatisticService.selectRateResChgAccessTrace
-> fCommonDao.selectRateResChgAccessTrace
-> [SQL] CommonMapper.selectRateResChgAccessTrace
```

**테이블/API:** NMCP_RATE_RES_CHG_BAS  
**처리 로직 / 변환 처리:**

```
o CommonMapper.selectRateResChgAccessTrace
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용

o 로직 추가
 -> M포탈 NMCP_RATE_RES_CHG_BAS 테이블 조회 추가
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
ipStatisticService.deleteRateResChgAccessTrace
-> fCommonDao.deleteRateResChgAccessTrace
-> [SQL] CommonMapper.deleteRateResChgAccessTrace
```

**테이블/API:** NMCP_RATE_RES_CHG_BAS  
**처리 로직 / 변환 처리:**

```
o CommonMapper.deleteRateResChgAccessTrace
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경

o 로직 추가
 -> M포탈 NMCP_RATE_RES_CHG_BAS 테이블 등록 데이터 삭제 처리
```

**개발팀 전달일:** 03/24 전달  

---

## [5006-04]

**JSP/JS:** /mobile/mypage/farPricePlanView.jsp, /resources/js/mobile/mypage/farPricePlanView.js  
**URL:** /m/mypage/farPriceCtgTab1Ajax.do  
**Controller:** FarPricePlanController.java - farPriceCtgTab1Ajax  
**Service:** farPricePlanService.getCtgMapWrapper  
**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  

---

## [5006-05]

**JSP/JS:** /mobile/mypage/farPricePlanView.jsp, /resources/js/mobile/mypage/farPricePlanView.js  
**URL:** /m/mypage/farPriceCtgTab2Ajax.do  
**Controller:** FarPricePlanController.java - getFarPricePlanListAjax2  
**처리 로직 / 변환 처리:**

```
o FarPricePlanController.java - getFarPricePlanListAjax2
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** rateAdsvcGdncService.getProdCount  
**처리 로직 / 변환 처리:**

```
o rateAdsvcGdncService.getProdCount
  - 제외
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** rateAdsvcGdncService.getCtgXmlList  
**처리 로직 / 변환 처리:**

```
o rateAdsvcGdncService.getCtgXmlList
  - 제외
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  

---

## [5006-06]

**JSP/JS:** /mobile/mypage/farPricePlanView.jsp, /resources/js/mobile/mypage/farPricePlanView.js  
**URL:** /m/mypage/farPriceCtgTab3Ajax.do  
**Controller:** FarPricePlanController.java - getFarPricePlanListAjax3  
**처리 로직 / 변환 처리:**

```
o FarPricePlanController.java - getFarPricePlanListAjax3
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** rateAdsvcGdncService.getProdCount  
**처리 로직 / 변환 처리:**

```
o rateAdsvcGdncService.getProdCount
  - 제외
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** rateAdsvcGdncService.getCtgXmlList  
**처리 로직 / 변환 처리:**

```
o rateAdsvcGdncService.getCtgXmlList
  - 제외
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** farPricePlanService.getStrCtgXmlList  
**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.selectFarPricePlanList
-> [M포탈-M전산 API] /mypage/farPricePlanList
```

**처리 로직 / 변환 처리:**

```
o /mypage/farPricePlanList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용

o 가입 가능 요금제 조회 부분만 적용
```

**개발팀 전달일:** 03/24 전달  

---

## [5006-07]

**JSP/JS:** /mobile/mypage/farPricePlanView.jsp, /resources/js/mobile/mypage/farPricePlanView.js  
**URL:** /m/mypage/possibleStateCheckAjax.do  
**Controller:** FarPricePlanController.java - possibleStateCheck  
**처리 로직 / 변환 처리:**

```
o FarPricePlanController.java - possibleStateCheck
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mspService.checkKid  
**처리 로직 / 변환 처리:**

```
o mspService.checkKid
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mspDao.getCustomerSsn
-> [[M포탈-M전산 API]] /msp/customerSsn
```

**처리 로직 / 변환 처리:**

```
o [M포탈-M전산 API]
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** NmcpServiceUtils.getAge  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getAge
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.getEnggInfo  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/enggInfo1  
**처리 로직 / 변환 처리:**

```
o /mypage/enggInfo1
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** NmcpServiceUtils.getCodeList(GROUP_CODE_EXCEPTION_LIST_SOC_CODE)  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getCodeList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  --> 공통으로 사용
```

**기타:** 공통모듈 - 공통코드 조회 / 홍성민C  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mPlatFormService.getAddSvcInfoDto  
**Service / Dao / SQL / API:**

```
mplatFormServerAdapter.callService
-> [M플랫폼] X20 -> X97 로 대체
```

**테이블/API:**

```
X20 -> X97 로 대체
```

**처리 로직 / 변환 처리:**

```
o mPlatFormService.getAddSvcInfoDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  - X20 -> X97 로 대체
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** appformSvc.getMcpRequestByContractNum  
**Service / Dao / SQL / API:**

```
appformDao.getMcpRequest
-> [SQL] AppformMapper.getMcpRequest
```

**테이블/API:** MCP_REQUEST  
**처리 로직 / 변환 처리:**

```
o AppformMapper.getMcpRequest
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.getChannelInfo  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/channelInfo  
**처리 로직 / 변환 처리:**

```
o /mypage/channelInfo
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 유통 대리점 모집 경로 회선 체크 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectFarPricePlan  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/farPricePlan  
**처리 로직 / 변환 처리:**

```
o /mypage/farPricePlan
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectFarPriceAddInfo  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/farPriceAddInfo  
**처리 로직 / 변환 처리:**

```
o /mypage/farPriceAddInfo
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  

---

## [5006-08]

**JSP/JS:** /mobile/mypage/regServicePop.jsp, /resources/js/mobile/mypage/regServicePop.js  
**URL:** /m/mypage/regServicePop.do  
**Controller:** FarPricePlanController.java - doRegServicePop  
**처리 로직 / 변환 처리:**

```
o FarPricePlanController.java - doRegServicePop
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

o 요금제 안내 관련 로직은 불필요
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** fCommonSvc.getJehuProdInfo  
**처리 로직 / 변환 처리:**

```
o fCommonSvc.getJehuProdInfo
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** rateAdsvcGdncService.getRateAdsvcBnfitGdncDtlXmlList  
**처리 로직 / 변환 처리:**

```
o rateAdsvcGdncService.getRateAdsvcBnfitGdncDtlXmlList
  - 제외
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** rateAdsvcGdncService.getRateAdsvcBnfitGdncDtlXmlList  
**처리 로직 / 변환 처리:**

```
o rateAdsvcGdncService.getRateAdsvcBnfitGdncDtlXmlList
  - 제외
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  

---

## [5006-09]

**JSP/JS:** /mobile/mypage/regServicePop.jsp, /resources/js/mobile/mypage/regServicePop.js  
**URL:** /m/mypage/farPriceContent.do  
**Controller:** FarPricePlanController.java - farPriceContent  
**Service:** farPricePlanService.selectFarPricePlanList  
**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.selectFarPricePlanList
-> [M포탈-M전산 API] /mypage/farPricePlanList
```

**처리 로직 / 변환 처리:**

```
o /mypage/farPricePlanList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용

o 가입 가능 요금제 조회 부분만 적용
```

**개발팀 전달일:** 03/24 전달  

---

## [5006-10]

**JSP/JS:** /mobile/mypage/regServicePop.jsp, /resources/js/mobile/mypage/regServicePop.js  
**URL:** /mypage/isOverUsageChargeAjax.do  
**Controller:** CallRegController.java - isOverUsageChargeAjax  
**처리 로직 / 변환 처리:**

```
o CallRegController.java - isOverUsageChargeAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** callRegService.isOverUsageChargeAjax  
**Service / Dao / SQL / API:**

```
mPlatFormService.telTotalUseTimeDto
-> mplatFormServerAdapter.callServiceNe
-> [M플랫폼] X12
```

**테이블/API:** X12  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.telTotalUseTimeDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.selectSocDesc
-> [M포탈-M전산 API] /mypage/socDesc
```

**처리 로직 / 변환 처리:**

```
o /mypage/socDesc
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.selectRateMst
-> mypageDao.selectRateMst
-> [SQL] MypageMapper.selectRateMst
```

**테이블/API:** NMCP_RATE_MST  
**처리 로직 / 변환 처리:**

```
o MypageMapper.selectRateMst
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  

---

## [5006-11]

**JSP/JS:** /mobile/mypage/regServicePop.jsp, /resources/js/mobile/mypage/regServicePop.js  
**URL:** /mypage/selectRealTimeUseageAjax.do  
**Controller:** CallRegController.java - selectRealTimeUseageAjax  
**처리 로직 / 변환 처리:**

```
o CallRegController.java - isOverUsageChargeAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** callRegService.telTotalUseTimeDto  
**처리 로직 / 변환 처리:**

```
o callRegService.telTotalUseTimeDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** mypageService.selectCallSvcLimitCount  
**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mPlatFormService.telTotalUseTimeDto
-> mplatFormServerAdapter.callServiceNe
-> [M플랫폼] X12
```

**테이블/API:** X12  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.telTotalUseTimeDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** mypageService.insertMcpSelfcareStatistic  
**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  

---

## [5006-12]

**JSP/JS:** /mobile/mypage/regServicePop.jsp, /resources/js/mobile/mypage/regServicePop.js  
**URL:** /m/mypage/farPricePlanChgAjax.do  
**Controller:** FarPricePlanController.java - proPriceChg  
**처리 로직 / 변환 처리:**

```
o FarPricePlanController.java - proPriceChg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectFarPricePlan  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/farPricePlan  
**처리 로직 / 변환 처리:**

```
o /mypage/farPricePlan
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectFarPriceAddInfo  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/farPriceAddInfo  
**처리 로직 / 변환 처리:**

```
o /mypage/farPriceAddInfo
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** farPricePlanService.getAddSvcInfoDto  
**처리 로직 / 변환 처리:**

```
o farPricePlanService.getAddSvcInfoDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mPlatFormService.getAddSvcInfoDto
-> mplatFormServerAdapter.callService
-> [M플랫폼] X20 -> X97 로 대체
```

**테이블/API:**

```
X20 -> X97 로 대체
```

**처리 로직 / 변환 처리:**

```
o mPlatFormService.getAddSvcInfoDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  - X20 -> X97 로 대체
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.getCloseSubList
-> [M포탈-M전산 API] /mypage/closeSubList
```

**처리 로직 / 변환 처리:**

```
o /mypage/closeSubList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.checkAllreadPlanchgCount
-> mypageDao.checkAllreadPlanchgCount
-> [SQL] MypageMapper.checkAllreadPlanchgCount
```

**테이블/API:** MCP_SERVICE_ALTER_TRACE  
**처리 로직 / 변환 처리:**

```
o MypageMapper.checkAllreadPlanchgCount
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** mPlatFormService.moscRegSvcCanChgNeTrace  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.moscRegSvcCanChgNeTrace
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mPlatFormService.moscRegSvcCanChgNeTrace
-> fCommonSvc.getMspRateMst
-> fCommonDao.getMspRateMst
-> [M포탈-M전산 API] /common/mspRateMst
```

**처리 로직 / 변환 처리:**

```
o /common/mspRateMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mPlatFormService.moscRegSvcCanChgNeTrace
-> mplatFormServerAdapter.callServiceNe
-> [M플랫폼] X38
```

**테이블/API:** X38  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.moscRegSvcCanChgNeTrace
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.insertServiceAlterTrace
-> mypageDao.insertServiceAlterTrace
-> [SQL] MypageMapper.insertServiceAlterTrace
```

**테이블/API:** MCP_SERVICE_ALTER_TRACE  
**처리 로직 / 변환 처리:**

```
o MypageMapper.insertServiceAlterTrace
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** mPlatFormService.moscRegSvcCanChgNeTrace  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.moscRegSvcCanChgNeTrace
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mPlatFormService.moscRegSvcCanChgNeTrace
-> fCommonSvc.getMspRateMst
-> fCommonDao.getMspRateMst
-> [M포탈-M전산 API] /common/mspRateMst
```

**처리 로직 / 변환 처리:**

```
o /common/mspRateMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mPlatFormService.moscRegSvcCanChgNeTrace
-> mplatFormServerAdapter.callServiceNe
-> [M플랫폼] X38
```

**테이블/API:** X38  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.moscRegSvcCanChgNeTrace
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
fCommonSvc.getMspSmsTemplateMst
-> fCommonDao.getMspSmsTemplateMst
-> [M포탈-M전산 API] /common/mspSmsTemplateMst
```

**처리 로직 / 변환 처리:**

```
o /common/mspSmsTemplateMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
smsSvc.sendKakaoNoti
-> [M포탈-M전산 API] /sms/addKakaoNoti
```

**처리 로직 / 변환 처리:**

```
o /sms/addKakaoNoti
-> 현행 ITO API 그대로 사용 : M전산/M포탈 테이블 INSERT/UPDATE/DELETE
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.insertServiceAlterTrace
-> mypageDao.insertServiceAlterTrace
-> [SQL] MypageMapper.insertServiceAlterTrace
```

**테이블/API:** MCP_SERVICE_ALTER_TRACE  
**처리 로직 / 변환 처리:**

```
o MypageMapper.insertServiceAlterTrace
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.insertSocfailProcMst
-> [M포탈-M전산 API] /mypage/insertSocfailProcMst
```

**처리 로직 / 변환 처리:**

```
o /mypage/insertSocfailProcMst
-> SQL Copy 후 M포탈-M전산 API 에 SQL 추가하여 데이터 INSERT/UPDATE 처리
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mPlatFormService.farPricePlanChgNeTrace
-> mplatFormServerAdapter.callServiceNe
-> [M플랫폼] X19 -> Y25 대체 적용
```

**테이블/API:**

```
X19 -> Y25 대체
```

**처리 로직 / 변환 처리:**

```
o mPlatFormService.farPricePlanChgNeTrace
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  - X19 -> Y25 대체 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.insertServiceAlterTrace
-> mypageDao.insertServiceAlterTrace
-> [SQL] MypageMapper.insertServiceAlterTrace
```

**테이블/API:** MCP_SERVICE_ALTER_TRACE  
**처리 로직 / 변환 처리:**

```
o MypageMapper.insertServiceAlterTrace
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mPlatFormService.farPricePlanChgNeTrace
-> mplatFormServerAdapter.callServiceNe
-> [M플랫폼] X19 -> Y25 대체 적용
```

**테이블/API:**

```
X19 -> Y25 대체
```

**처리 로직 / 변환 처리:**

```
o mPlatFormService.farPricePlanChgNeTrace
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  - X19 -> Y25 대체 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.insertServiceAlterTrace
-> mypageDao.insertServiceAlterTrace
-> [SQL] MypageMapper.insertServiceAlterTrace
```

**테이블/API:** MCP_SERVICE_ALTER_TRACE  
**처리 로직 / 변환 처리:**

```
o MypageMapper.insertServiceAlterTrace
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.getChrgPrmtIdSocChg
-> [M포탈-M전산 API] /mypage/getChrgPrmtIdSocChg
```

**처리 로직 / 변환 처리:**

```
o /mypage/getChrgPrmtIdSocChg
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.insertDisApd
-> [M포탈-M전산 API] /mypage/insertDisApd
```

**처리 로직 / 변환 처리:**

```
o /mypage/insertDisApd
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
fCommonSvc.getMspSmsTemplateMst
-> fCommonDao.getMspSmsTemplateMst
-> [M포탈-M전산 API] /common/mspSmsTemplateMst
```

**처리 로직 / 변환 처리:**

```
o /common/mspSmsTemplateMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
smsSvc.sendKakaoNoti
-> [M포탈-M전산 API] /sms/addKakaoNoti
```

**처리 로직 / 변환 처리:**

```
o /sms/addKakaoNoti
-> 현행 ITO API 그대로 사용 : M전산/M포탈 테이블 INSERT/UPDATE/DELETE
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.insertServiceAlterTrace
-> mypageDao.insertServiceAlterTrace
-> [SQL] MypageMapper.insertServiceAlterTrace
```

**테이블/API:** MCP_SERVICE_ALTER_TRACE  
**처리 로직 / 변환 처리:**

```
o MypageMapper.insertServiceAlterTrace
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.insertSocfailProcMst
-> [M포탈-M전산 API] /mypage/insertSocfailProcMst
```

**처리 로직 / 변환 처리:**

```
o /mypage/insertSocfailProcMst
-> SQL Copy 후 M포탈-M전산 API 에 SQL 추가하여 데이터 INSERT/UPDATE 처리
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.getromotionDcList
-> [M포탈-M전산 API] /mypage/romotionDcList
```

**처리 로직 / 변환 처리:**

```
o /mypage/romotionDcList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mPlatFormService.regSvcChgNeTrace
-> mplatFormServerAdapter.callServiceNe
-> [M플랫폼] X21 -> Y25 로 대체
```

**테이블/API:**

```
X21 -> Y25 로 대체
```

**처리 로직 / 변환 처리:**

```
o mPlatFormService.regSvcChgNeTrace
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

o X21 -> Y25 로 대체
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.insertServiceAlterTrace
-> mypageDao.insertServiceAlterTrace
-> [SQL] MypageMapper.insertServiceAlterTrace
```

**테이블/API:** MCP_SERVICE_ALTER_TRACE  
**처리 로직 / 변환 처리:**

```
o MypageMapper.insertServiceAlterTrace
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.insertSocfailProcMst
-> [M포탈-M전산 API] /mypage/insertSocfailProcMst
```

**처리 로직 / 변환 처리:**

```
o /mypage/insertSocfailProcMst
-> SQL Copy 후 M포탈-M전산 API 에 SQL 추가하여 데이터 INSERT/UPDATE 처리
```

**개발팀 전달일:** 03/24 전달  

---

## [5006-13]

**JSP/JS:** /mobile/mypage/regServicePop.jsp, /resources/js/mobile/mypage/regServicePop.js  
**URL:** /m/mypage/farPricePlanResChgAjax.do  
**Controller:** FarPricePlanController.java - farPricePlanResChgAjax  
**처리 로직 / 변환 처리:**

```
o FarPricePlanController.java - farPricePlanResChgAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** farPricePlanService.doFarPricePlanRsrvChg  
**처리 로직 / 변환 처리:**

```
o farPricePlanService.doFarPricePlanRsrvChg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mPlatFormService.doFarPricePlanRsrvChg
-> mplatFormServerAdapter.callService
-> [M플랫폼] X88
```

**테이블/API:** X88  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.doFarPricePlanRsrvChg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
ipStatisticService.insertRateResChgAccessTrace
-> fCommonDao.insertRateResChgAccessTrace
-> [SQL] CommonMapper.insertRateResChgAccessTrace
```

**테이블/API:** NMCP_RATE_RES_CHG_BAS  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertRateResChgAccessTrace
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경

o 로직 추가
 -> M포탈 NMCP_RATE_RES_CHG_BAS 테이블 등록처리
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
ipStatisticService.insertAdminAccessTrace
-> fCommonDao.insertIpStatAdmin
-> [SQL] CommonMapper.insertIpStatAdmin
```

**테이블/API:** MCP_ADMIN_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStatAdmin
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  
---

**처리 로직 / 변환 처리:**

```
o 로직 추가
 - M포탈 요금제 변경 예약 테이블에 등록 처리
```

**개발팀 전달일:** 03/24 전달  

---

## [요금제변경]

**JSP/JS:** /mobile/rate/rateLayer.jsp,  
**URL:** /m/rate/rateLayer.do  
**Controller:**

```
RateAdsvcGdncController.java - rateLayer
  - 요금제 상세화면 팝업
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  

---

## [요금제변경]

**URL:** /m/mypage/chgRateInfoViewAjax.do  
**Controller:** FarPricePlanController.java - chgRateInfoViewAjax  
**Service:** mypageService.selectFarPricePlanList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/farPricePlanList  
**처리 로직 / 변환 처리:**

```
o /mypage/farPricePlanList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 미사용  
**개발팀 전달일:** 03/24 전달  

---

## [요금제변경]

**URL:** /mypage/checkOtpAndReg.do  
**Controller:**

```
FarPricePlanController.java - checkOtpAndRegAjax
  - OTP 번호 확인 및 부가 서비스 가입 처리
```

**처리 로직 / 변환 처리:**

```
o FarPricePlanController.java - checkOtpAndRegAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 미사용  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 미사용  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 미사용  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
mypageService.selectCntrListNoLogin
  - 비회원로그인
```

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNoLogin  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNoLogin
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 미사용  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
farPricePlanService.regSvcChgNe
  - 자회선 부가서비스 가입
```

**Service / Dao / SQL / API:**

```
mPlatFormService.regSvcChgNe
-> mplatFormServerAdapter.callServiceNe
-> [M플랫폼] X21
```

**테이블/API:** X21  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.regSvcChgNe
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 미사용  
**개발팀 전달일:** 03/24 전달  
---

**Service:** ipstatisticService.insertAccessTrace  
**처리 로직 / 변환 처리:**

```
o ipstatisticService.insertAccessTrace
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 미사용  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
fCommonDao.insertIpStat
-> [SQL] CommonMapper.insertIpStat
```

**테이블/API:** MCP_ACCESS_TRACE  
**처리 로직 / 변환 처리:**

```
o CommonMapper.insertIpStat
-> SQL Copy 후 M포탈-M전산 API 에 SQL 추가하여 데이터 INSERT/UPDATE 처리
```

**기타:** 제외 - 미사용  
**개발팀 전달일:** 03/24 전달  

---

## [요금제변경]

**URL:** /batch/resChangeAddPrdAjax.do  
**Controller:**

```
FarPricePlanController.java - resChangeAddPrd
  - 요금제 예약 변경 부가서비스 일괄 처리
```

**처리 로직 / 변환 처리:**

```
o FarPricePlanController.java - resChangeAddPrd
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 요금제 예약 변경 배치 사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** fCommonSvc.getRateResChgList  
**Service / Dao / SQL / API:**

```
fCommonDao.getRateResChgList
-> [SQL] CommonMapper.getRateResChgList
```

**테이블/API:** NMCP_RATE_RES_CHG_BAS  
**처리 로직 / 변환 처리:**

```
o CommonMapper.getRateResChgList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** farPricePlanService.getRateInfo  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/getRateInfo  
**처리 로직 / 변환 처리:**

```
o /mypage/getRateInfo
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 요금제 예약 변경 배치 사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectFarPricePlan  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/farPricePlan  
**처리 로직 / 변환 처리:**

```
o /mypage/farPricePlan
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** fCommonSvc.updateNmcpRateResChgBas  
**Service / Dao / SQL / API:**

```
fCommonDao.updateNmcpRateResChgBas
-> [SQL] CommonMapper.updateNmcpRateResChgBas
```

**테이블/API:** NMCP_RATE_RES_CHG_BAS  
**처리 로직 / 변환 처리:**

```
o CommonMapper.updateNmcpRateResChgBas
-> SQL Copy 후 M포탈-M전산 API 에 SQL 추가하여 데이터 INSERT/UPDATE 처리
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.getChrgPrmtIdSocChg  
**처리 로직 / 변환 처리:**

```
o mypageService.getChrgPrmtIdSocChg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** farPricePlanService.batchResChangeAddPrdCall  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNoLogin  
**처리 로직 / 변환 처리:**

```
o farPricePlanService.batchResChangeAddPrdCall
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNoLogin  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNoLogin
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mPlatFormService.getAddSvcInfoDto
-> mplatFormServerAdapter.callService
-> [M플랫폼] X20
```

**테이블/API:** X20  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.getAddSvcInfoDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.getCloseSubList
-> [M포탈-M전산 API] /mypage/closeSubList
```

**처리 로직 / 변환 처리:**

```
o /mypage/closeSubList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** mPlatFormService.moscRegSvcCanChgNeTrace  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.moscRegSvcCanChgNeTrace
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mPlatFormService.moscRegSvcCanChgNeTrace
-> fCommonSvc.getMspRateMst
-> fCommonDao.getMspRateMst
-> [M포탈-M전산 API] /common/mspRateMst
```

**처리 로직 / 변환 처리:**

```
o /common/mspRateMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mPlatFormService.moscRegSvcCanChgNeTrace
-> mplatFormServerAdapter.callServiceNe
-> [M플랫폼] X38
```

**테이블/API:** X38  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.moscRegSvcCanChgNeTrace
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.insertServiceAlterTrace
-> mypageDao.insertServiceAlterTrace
-> [SQL] MypageMapper.insertServiceAlterTrace
```

**테이블/API:** MCP_SERVICE_ALTER_TRACE  
**처리 로직 / 변환 처리:**

```
o MypageMapper.insertServiceAlterTrace
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.insertServiceAlterTrace
-> mypageDao.insertServiceAlterTrace
-> [SQL] MypageMapper.insertServiceAlterTrace
```

**테이블/API:** MCP_SERVICE_ALTER_TRACE  
**처리 로직 / 변환 처리:**

```
o MypageMapper.insertServiceAlterTrace
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.insertSocfailProcMst
-> [M포탈-M전산 API] /mypage/insertSocfailProcMst
```

**처리 로직 / 변환 처리:**

```
o /mypage/insertSocfailProcMst
-> SQL Copy 후 M포탈-M전산 API 에 SQL 추가하여 데이터 INSERT/UPDATE 처리
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.insertServiceAlterTrace
-> mypageDao.insertServiceAlterTrace
-> [SQL] MypageMapper.insertServiceAlterTrace
```

**테이블/API:** MCP_SERVICE_ALTER_TRACE  
**처리 로직 / 변환 처리:**

```
o MypageMapper.insertServiceAlterTrace
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.getromotionDcList
-> [M포탈-M전산 API] /mypage/romotionDcList
```

**처리 로직 / 변환 처리:**

```
o /mypage/romotionDcList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mPlatFormService.regSvcChgNeTrace
-> mplatFormServerAdapter.callServiceNe
-> [M플랫폼] X21
```

**테이블/API:** X21  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.regSvcChgNeTrace
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.insertServiceAlterTrace
-> mypageDao.insertServiceAlterTrace
-> [SQL] MypageMapper.insertServiceAlterTrace
```

**테이블/API:** MCP_SERVICE_ALTER_TRACE  
**처리 로직 / 변환 처리:**

```
o MypageMapper.insertServiceAlterTrace
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mPlatFormService.regSvcChgNeTrace
-> mplatFormServerAdapter.callServiceNe
-> [M플랫폼] X21
```

**테이블/API:** X21  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.regSvcChgNeTrace
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.insertServiceAlterTrace
-> mypageDao.insertServiceAlterTrace
-> [SQL] MypageMapper.insertServiceAlterTrace
```

**테이블/API:** MCP_SERVICE_ALTER_TRACE  
**처리 로직 / 변환 처리:**

```
o MypageMapper.insertServiceAlterTrace
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.insertServiceAlterTrace
-> mypageDao.insertServiceAlterTrace
-> [SQL] MypageMapper.insertServiceAlterTrace
```

**테이블/API:** MCP_SERVICE_ALTER_TRACE  
**처리 로직 / 변환 처리:**

```
o MypageMapper.insertServiceAlterTrace
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.insertSocfailProcMst
-> [M포탈-M전산 API] /mypage/insertSocfailProcMst
```

**처리 로직 / 변환 처리:**

```
o /mypage/insertSocfailProcMst
-> SQL Copy 후 M포탈-M전산 API 에 SQL 추가하여 데이터 INSERT/UPDATE 처리
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.insertServiceAlterTrace
-> mypageDao.insertServiceAlterTrace
-> [SQL] MypageMapper.insertServiceAlterTrace
```

**테이블/API:** MCP_SERVICE_ALTER_TRACE  
**처리 로직 / 변환 처리:**

```
o MypageMapper.insertServiceAlterTrace
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mypageService.insertSocfailProcMst
-> [M포탈-M전산 API] /mypage/insertSocfailProcMst
```

**처리 로직 / 변환 처리:**

```
o /mypage/insertSocfailProcMst
-> SQL Copy 후 M포탈-M전산 API 에 SQL 추가하여 데이터 INSERT/UPDATE 처리
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** fCommonSvc.updateNmcpRateResChgBas  
**Service / Dao / SQL / API:**

```
fCommonDao.updateNmcpRateResChgBas
-> [SQL] CommonMapper.updateNmcpRateResChgBas
```

**테이블/API:** NMCP_RATE_RES_CHG_BAS  
**처리 로직 / 변환 처리:**

```
o CommonMapper.updateNmcpRateResChgBas
-> SQL Copy 후 M포탈-M전산 API 에 SQL 추가하여 데이터 INSERT/UPDATE 처리
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.insertServiceAlterTrace  
**Service / Dao / SQL / API:**

```
mypageDao.insertServiceAlterTrace
-> [SQL] MypageMapper.insertServiceAlterTrace
```

**테이블/API:** MCP_SERVICE_ALTER_TRACE  
**처리 로직 / 변환 처리:**

```
o MypageMapper.insertServiceAlterTrace
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.insertSocfailProcMst  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/insertSocfailProcMst  
**처리 로직 / 변환 처리:**

```
o /mypage/insertSocfailProcMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 요금제 예약 변경 배치 ITO에 통합사용하므로 불필요  
**개발팀 전달일:** 03/24 전달  

---

## [5007-01]

**JSP/JS:** /mobile/mypage/regServiceView.jsp, /resources/js/mobile/mypage/regServiceView.js  
**URL:** /m/mypage/regServiceView.do  
**Controller:** RegSvcController.java - regServiceView  
**처리 로직 / 변환 처리:**

```
o RegSvcController.java - regServiceView
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** NmcpServiceUtils.getBirthDateToAmericanAge  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getBirthDateToAmericanAge
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
maskingSvc.insertMaskingReleaseHist
  - 마스킹해제
```

**Service / Dao / SQL / API:**

```
maskingDao.insertMaskingReleaseHist
-> [SQL] MaskingMapper.insertMaskingReleaseHist
```

**테이블/API:** MCP_MASKING_RELEASE_HIST  
**처리 로직 / 변환 처리:**

```
o MaskingMapper.insertMaskingReleaseHist
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**개발팀 전달일:** 03/24 전달  

---

## [5007-02]

**JSP/JS:** /mobile/mypage/regServiceView.jsp, /resources/js/mobile/mypage/regServiceView.js  
**URL:** /m/mypage/addSvcListAjax.do  
**Controller:** RegSvcController.java - addSvcListAjax  
**처리 로직 / 변환 처리:**

```
o RegSvcController.java - addSvcListAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** regSvcService.selectAddSvcInfoDto  
**Service / Dao / SQL / API:**

```
mPlatFormService.getAddSvcInfoDto
-> mplatFormServerAdapter.callService
-> [M플랫폼] X20 -> X97 로 대체
```

**테이블/API:**

```
X20 -> X97 로 대체
```

**처리 로직 / 변환 처리:**

```
o mPlatFormService.getAddSvcInfoDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
  - X20 -> X97 로 대체
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectRegService  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/regService  
**처리 로직 / 변환 처리:**

```
o /mypage/regService
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  

---

## [5007-03]

**JSP/JS:** /mobile/mypage/regServiceView.jsp, /resources/js/mobile/mypage/regServiceView.js  
**URL:** /m/mypage/myAddSvcListAjax.do  
**Controller:** RegSvcController.java - myAddSvcListAjax  
**처리 로직 / 변환 처리:**

```
o RegSvcController.java - myAddSvcListAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** regSvcService.selectmyAddSvcList  
**처리 로직 / 변환 처리:**

```
o regSvcService.selectmyAddSvcList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mPlatFormService.getAddSvcInfoParamDto
-> mplatFormServerAdapter.callServiceNe
-> [M플랫폼] X97
```

**테이블/API:** X97  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.getAddSvcInfoParamDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
fCommonSvc.getMspRateMst
-> fCommonDao.getMspRateMst
-> [M포탈-M전산 API] /common/mspRateMst
```

**처리 로직 / 변환 처리:**

```
o /common/mspRateMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** regSvcService.getMpSocListByDiv  
**처리 로직 / 변환 처리:**

```
o regSvcService.getMpSocListByDiv
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
regSvcDao.getRoamCdList
-> [SQL] RegSvcMapper.getRoamCdList
```

**테이블/API:** NMCP_RATE_ADSVC_GDNC_PROD_REL  
**처리 로직 / 변환 처리:**

```
o RegSvcMapper.getRoamCdList
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경

o 로밍 부가서비스 목록 코드만 참고
   -> 스마트서식지 공통코드에 추가로 관리하는 방향
```

**개발팀 전달일:** 03/24 전달  

---

## [5007-05]

**JSP/JS:** /mobile/mypage/regServiceView.jsp, /resources/js/mobile/mypage/regServiceView.js  
**URL:** /m/mypage/moscRegSvcCanChgAjax.do  
**Controller:**

```
RegSvcController.java - moscRegSvcCanChgAjax
  - 부가서비스 해지
```

**처리 로직 / 변환 처리:**

```
o RegSvcController.java - moscRegSvcCanChgAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** regSvcService.moscRegSvcCanChg  
**처리 로직 / 변환 처리:**

```
o regSvcService.moscRegSvcCanChg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
fCommonSvc.getMspRateMst
-> fCommonDao.getMspRateMst
-> [M포탈-M전산 API] /common/mspRateMst
```

**처리 로직 / 변환 처리:**

```
o /common/mspRateMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mPlatFormService.moscRegSvcCanChg
-> mplatFormServerAdapter.callService
-> [M플랫폼] X38
```

**테이블/API:** X38  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.moscRegSvcCanChg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**JSP/JS:** /mobile/mypage/regServiceView.jsp, /resources/js/mobile/mypage/regServiceView.js  
**URL:** /m/rate/adsvcContent.do  
**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**JSP/JS:** /mobile/mypage/regServiceView.jsp, /resources/js/mobile/mypage/regServiceView.js  
**URL:** /m/mypage/getContRateAdditionAjax.do  
**Controller:** RegSvcController.java - getContRateAdditionAjax  
**Service:** regSvcService.selectAddSvcDtl  
**처리 로직 / 변환 처리:**

```
o regSvcService.selectAddSvcDtl
 - 제외
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**URL:** /xmlInfo/getRateAdsvcDtlAjax.do  
**Controller:** RegSvcController.java - getRateAdsvcGdncDtlXml  
**Service:** rateAdsvcGdncService.getRateAdsvcGdncDtlXml  
**처리 로직 / 변환 처리:**

```
o rateAdsvcGdncService.getRateAdsvcGdncDtlXml
  - 제외
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  

---

## [5007-04]

**JSP/JS:** /mobile/mypage/addSvcViewPop.jsp, /resources/js/mobile/mypage/addSvcViewPop.js  
**URL:** /m/mypage/addSvcViewPop.do  
**Controller:** RegSvcController.java - addSvcViewPop  
**처리 로직 / 변환 처리:**

```
o RegSvcController.java - addSvcViewPop
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** myBenefitService.selectCustPoint  
**처리 로직 / 변환 처리:**

```
o myBenefitService.selectCustPoint
 - 제외
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  

---

## [5007-06]

**JSP/JS:** /mobile/mypage/addSvcViewPop.jsp, /resources/js/mobile/mypage/addSvcViewPop.js  
**URL:** /m/mypage/regSvcChgAjax.do  
**Controller:** RegSvcController.java - regSvcChgAjax  
**처리 로직 / 변환 처리:**

```
o RegSvcController.java - regSvcChgAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** certService.getStepCnt  
**Service / Dao / SQL / API:**

```
certDao.getStepCnt
-> [SQL] CertMapper.getStepCnt
```

**테이블/API:** NMCP_CRT_VLD_DTL  
**처리 로직 / 변환 처리:**

```
o CertMapper.getStepCnt
  - 본인인증은 불필요하므로 본인인증 로그 적재 로직을 스마트신청서 인증마다 로그 적재로 개발
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
certService.vdlCertInfo
  - cert 비교 공통 함수
  - 소스 : CertServiceImpl-vdlCertInfo.java
```

**처리 로직 / 변환 처리:**

```
o certService.vdlCertInfo
  - 인증로그 적재 로직 공통화하여 사용
```

**기타:** 공통모듈 - 인증로그  
**개발팀 전달일:** 03/24 전달  
---

**Service:** regSvcService.moscRegSvcCanChg  
**처리 로직 / 변환 처리:**

```
o regSvcService.moscRegSvcCanChg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
fCommonSvc.getMspRateMst
-> fCommonDao.getMspRateMst
-> [M포탈-M전산 API] /common/mspRateMst
```

**처리 로직 / 변환 처리:**

```
o /common/mspRateMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mPlatFormService.moscRegSvcCanChgSeq
-> mplatFormServerAdapter.callService
-> [M플랫폼] X38
```

**테이블/API:** X38  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.moscRegSvcCanChgSeq
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** regSvcService.regSvcChg  
**Service / Dao / SQL / API:**

```
mPlatFormService.regSvcChg
-> mplatFormServerAdapter.callService
-> [M플랫폼] X21 -> Y25 로 대체
```

**테이블/API:**

```
X21 -> Y25 로 대체
```

**처리 로직 / 변환 처리:**

```
o mPlatFormService.regSvcChg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용

o X21 -> Y25 로 대체
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** myBenefitService.selectCustPoint  
**처리 로직 / 변환 처리:**

```
o myBenefitService.selectCustPoint
 - 제외
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** pointService.editPoint  
**처리 로직 / 변환 처리:**

```
o pointService.editPoint
 - 제외
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  

---

## [5007-07]

**JSP/JS:** /mobile/mypage/addSvcTmViewPop.jsp, /resources/js/mobile/mypage/addSvcViewPop.js  
**URL:** /m/mypage/addSvcViewPop.do  
**Controller:**

```
RegSvcController.java - addSvcViewPop
  - 불법 TM수신차단
```

**처리 로직 / 변환 처리:**

```
o RegSvcController.java - addSvcViewPop
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  

---

## [5007-08]

**JSP/JS:** /mobile/mypage/addSvcStealNumberPop.jsp, /resources/js/mobile/mypage/addSvcViewPop.js  
**URL:** /m/mypage/addSvcViewPop.do  
**Controller:**

```
RegSvcController.java - addSvcViewPop
  - 번호도용차단서비스
```

**처리 로직 / 변환 처리:**

```
o RegSvcController.java - addSvcViewPop
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  

---

## [5007-08]

**JSP/JS:** /mobile/mypage/addSvcRoamingPop.jsp, /resources/js/mobile/mypage/addSvcViewPop.js  
**URL:** /m/mypage/addSvcViewPop.do  
**Controller:**

```
RegSvcController.java - addSvcViewPop
  - 로밍 하루종일 ON 부가서비스
```

**처리 로직 / 변환 처리:**

```
o RegSvcController.java - addSvcViewPop
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**개발팀 전달일:** 03/24 전달  

---

## [부가서비스신청/해제]

**JSP/JS:** /mobile/mypage/roamingView.jsp, /resources/js/mobile/mypage/roamingView.js  
**URL:** /m/mypage/roamingView.do  
**Controller:** RegSvcController.java - roamingView  
**처리 로직 / 변환 처리:**

```
o RegSvcController.java - roamingView
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service:** NmcpServiceUtils.getBirthDateToAmericanAge  
**처리 로직 / 변환 처리:**

```
o NmcpServiceUtils.getBirthDateToAmericanAge
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service:**

```
maskingSvc.insertMaskingReleaseHist
  - 마스킹해제
```

**Service / Dao / SQL / API:**

```
maskingDao.insertMaskingReleaseHist
-> [SQL] MaskingMapper.insertMaskingReleaseHist
```

**테이블/API:** MCP_MASKING_RELEASE_HIST  
**처리 로직 / 변환 처리:**

```
o MaskingMapper.insertMaskingReleaseHist
  - SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  

---

## [부가서비스신청/해제]

**JSP/JS:** /mobile/mypage/roamingView.jsp, /resources/js/mobile/mypage/roamingView.js  
**URL:** /m/mypage/roamingCanChgAjax.do  
**Controller:** RegSvcController.java - roamingCanChgAjax  
**처리 로직 / 변환 처리:**

```
o RegSvcController.java - roamingCanChgAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service:** regSvcService.moscRegSvcCanChg  
**처리 로직 / 변환 처리:**

```
o regSvcService.moscRegSvcCanChg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
fCommonSvc.getMspRateMst
-> fCommonDao.getMspRateMst
-> [M포탈-M전산 API] /common/mspRateMst
```

**처리 로직 / 변환 처리:**

```
o /common/mspRateMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mPlatFormService.moscRegSvcCanChg
-> mplatFormServerAdapter.callService
-> [M플랫폼] X38
```

**테이블/API:** X38  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.moscRegSvcCanChg
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service:** regSvcService.moscRegSvcCanChgSeq  
**처리 로직 / 변환 처리:**

```
o regSvcService.moscRegSvcCanChgSeq
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
fCommonSvc.getMspRateMst
-> fCommonDao.getMspRateMst
-> [M포탈-M전산 API] /common/mspRateMst
```

**처리 로직 / 변환 처리:**

```
o /common/mspRateMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mPlatFormService.moscRegSvcCanChgSeq
-> mplatFormServerAdapter.callService
-> [M플랫폼] X38
```

**테이블/API:** X38  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.moscRegSvcCanChgSeq
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  

---

## [부가서비스신청/해제]

**URL:** /m/mypage/regRoamListAjax.do  
**Controller:** RegSvcController.java - regRoamListAjax  
**처리 로직 / 변환 처리:**

```
o RegSvcController.java - regRoamListAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service:** regSvcService.getRoamList  
**처리 로직 / 변환 처리:**

```
o regSvcService.getRoamList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** rateAdsvcGdncService.getMapWrapper  
**처리 로직 / 변환 처리:**

```
o rateAdsvcGdncService.getMapWrapper
  - 제외
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:** rateAdsvcGdncService.getRateAdsvcGdncProdRel  
**처리 로직 / 변환 처리:**

```
o rateAdsvcGdncService.getRateAdsvcGdncProdRel
  - 제외
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** rateAdsvcGdncService.deduplicateProdList  
**처리 로직 / 변환 처리:**

```
o rateAdsvcGdncService.deduplicateProdList
  - 제외
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  

---

## [부가서비스신청/해제]

**URL:** /m/mypage/myRoamJoinListAjax.do  
**Controller:** RegSvcController.java - myRoamJoinListAjax  
**처리 로직 / 변환 처리:**

```
o RegSvcController.java - myRoamJoinListAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service:** regSvcService.selectmyAddSvcList  
**처리 로직 / 변환 처리:**

```
o regSvcService.selectmyAddSvcList
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
mPlatFormService.getAddSvcInfoParamDto
-> mplatFormServerAdapter.callServiceNe
-> [M플랫폼] X97
```

**테이블/API:** X97  
**처리 로직 / 변환 처리:**

```
o mPlatFormService.getAddSvcInfoParamDto
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service / Dao / SQL / API:**

```
fCommonSvc.getMspRateMst
-> fCommonDao.getMspRateMst
-> [M포탈-M전산 API] /common/mspRateMst
```

**처리 로직 / 변환 처리:**

```
o /common/mspRateMst
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service:** regSvcService.getMpSocListByDiv  
**Service / Dao / SQL / API:**

```
regSvcDao.getRoamCdList
-> [SQL] RegSvcMapper.getRoamCdList
```

**테이블/API:** NMCP_RATE_ADSVC_GDNC_PROD_REL  
**처리 로직 / 변환 처리:**

```
o RegSvcMapper.getRoamCdList
-> SQL Copy 후 스마트신청서App 테이블에 맞춰서 변경

o 로밍 부가서비스 목록 코드만 참고
   -> 스마트서식지 공통코드에 추가로 관리하는 방향
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service:** rateAdsvcGdncService.getRateAdsvcProdRelBySoc  
**처리 로직 / 변환 처리:**

```
o rateAdsvcGdncService.getRateAdsvcProdRelBySoc
  - 제외
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** rateAdsvcGdncService.getProdBySeq  
**처리 로직 / 변환 처리:**

```
o rateAdsvcGdncService.getProdBySeq
  - 제외
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** regSvcService.getUpdateYn  
**처리 로직 / 변환 처리:**

```
o regSvcService.getUpdateYn
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** regSvcService.getEndDttmUsePrd  
**처리 로직 / 변환 처리:**

```
o regSvcService.getEndDttmUsePrd
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** regSvcService.getCtnByNcn  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrListNoLogin  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrListNoLogin
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  

---

## [부가서비스신청/해제]

**JSP/JS:** /mobile/mypage/roamingViewPop.jsp, /resources/js/mobile/mypage/roamingViewPop.js  
**URL:** /m/mypage/roamingViewPop.do  
**Controller:** RegSvcController.java - roamingViewPop  
**처리 로직 / 변환 처리:**

```
o RegSvcController.java - roamingCanChgAjax
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.selectCntrList  
**Service / Dao / SQL / API:** [M포탈-M전산 API] /mypage/cntrList  
**처리 로직 / 변환 처리:**

```
o /mypage/cntrList
-> SQL Copy 후 스마트신청서App 에 동일하게 사용 : M포탈/전산 테이블링크하여 사용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service:** mypageService.checkUserType  
**처리 로직 / 변환 처리:**

```
o mypageService.checkUserType
  - M포탈/M전산 소스 로직 스마트신청서App 에 적용
```

**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  
---

**Service:** rateAdsvcGdncService.getProdBySeq  
**처리 로직 / 변환 처리:**

```
o rateAdsvcGdncService.getProdBySeq
  - 제외
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  
---

**Service:** rateAdsvcGdncService.getRateAdsvcGdncProdRel  
**처리 로직 / 변환 처리:**

```
o rateAdsvcGdncService.getRateAdsvcGdncProdRel
  - 제외
```

**기타:** 제외 - 불필요  
**개발팀 전달일:** 03/24 전달  

---

## [부가서비스신청/해제]

**JSP/JS:** /mobile/mypage/roamingViewPop.jsp, /resources/js/mobile/mypage/roamingViewPop.js  
**URL:** /m/rate/roamingJoinAjax.do  
**Controller:** RateAdsvcGdncController.java - roamingJoinAjax  
**기타:** 제외 - 부가서비스 로직으로 처리  
**개발팀 전달일:** 03/24 전달  

---
