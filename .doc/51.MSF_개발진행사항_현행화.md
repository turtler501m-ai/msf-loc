# 51. MSF 개발진행사항 현행화

> 작성 시작: 2026-04-03 | 최종 업데이트: 2026-04-06
> 목적: 세션별 주요 변경사항 누적 기록 (커밋 단위)

---

## 2026-04-03

### [1] 신규 프로젝트 시작 — 패키지 전면 재구성 (eba1814)

- 기존 MCP 레거시 구조에서 MSF 신규 패키지 구조로 전면 재구성
- 루트 패키지: `com.ktmmobile.msf`
---

### [2] 빌드 성공 버전 초기 구현 (92f4ec6, 7dd730f, f0814eb)

- Spring Boot 2.7 + Java 11 기반 신규 백엔드 골격 구성
- pom.xml: 의존성 추가 (jdom, commons-lang3, commons-lang, commons-collections4, jaxb-api)
- `crypto-1.0.8-RELEASE.jar` 로컬 시스템 의존성 추가 (`lib/` 폴더)
- `import ktds.crypto` → `import com.ktds.crypto` import 경로 수정
- `jakarta.xml.bind` → `javax.xml.bind` (Spring Boot 2.7 호환)
- `CollectionUtils.isNotEmpty()` → `!CollectionUtils.isEmpty()` 수정
- `SessionUtils.java` — `ParseException` import 누락 추가
- `SfMypageSvcImpl.selectExistingConsent()` 미구현 메서드 복원
- pom.xml compiler exclude: `servicechange/**` 일괄 제외 → 개별 파일 제외로 변경

---

### [3] 부가서비스 TOBE 변환 (ad17792)

**변환 방식**: 신규 파일 생성 없이 기존 `MsfRegSvc*` 파일 직접 변환

#### 신규 DTO (5개)

| 파일 | 설명 |
|------|------|
| `AdditionReqDto.java` | 공통 요청 (ncn, ctn, custId) |
| `AdditionApplyReqDto.java` | 신청/해지 요청 (+ soc, ftrNewParam, prodHstSeq, flag) |
| `AdditionMyListResVO.java` | 이용중 목록 응답 |
| `AdditionAvailableResVO.java` | 가입가능 목록 응답 (list/listA/listC) |
| `AdditionApplyResVO.java` | 신청/해지 결과 응답 (success, message) |

#### MplatFormService.java

| 변경 | 내용 |
|------|------|
| Y25 메서드 추가 | `regSvcChgY25()` — X21(부가서비스신청) 대체 |

#### MsfRegSvcController.java

| 변경 전 (ASIS) | 변경 후 (TOBE) |
|---------------|--------------|
| `@Controller` | `@RestController @RequestMapping("/api/v1/addition")` |
| `myAddSvcListAjax.do` | `POST /api/v1/addition/my-list` |
| `addSvcListAjax.do` | `POST /api/v1/addition/available-list` |
| `moscRegSvcCanChgAjax.do` | `POST /api/v1/addition/cancel` |
| `regSvcChgAjax.do` | `POST /api/v1/addition/reg` |
| `regServiceView.do` | 제외 — join-info 기구현 대체 |
| `addSvcViewPop.do` | 제외 — 프론트 팝업 처리 |
| ASIS 메서드 전체 | `// [ASIS] ... — ...` 주석 처리 (삭제 금지) |

#### MsfRegSvcService.java (인터페이스)

- TOBE 메서드 4개 추가: `getMyAddSvcList`, `getAvailableAddSvcList`, `cancelAddSvc`, `regAddSvc`
- ASIS 메서드 전체 주석 처리

#### MsfRegSvcServiceImpl.java

| ASIS | TOBE |
|------|------|
| X20(`getAddSvcInfoDto`) | X97(`getAddSvcInfoParamDto`) |
| X21(`regSvcChg`) | Y25(`regSvcChgY25`) |
| `MyPageSearchDto` 세션 의존 | `AdditionReqDto` 요청 바디 수신 |
| `Map<String,Object>` 반환 | VO 반환 |
| ASIS 메서드 전체 | `// [ASIS] ...` 주석 처리 |

- `MsfMypageUserService` autowire 제거 (TOBE 메서드 미사용)
- private 유틸 메서드 (`getMpSocListByDiv`, `getMcpRegServiceListByDiv`, `chkRemove`) 인터페이스에서 내부로 이동

#### 스킬 업데이트 (`.claude/skills/asis-tobe-convert/SKILL.md`)

- 접두사 `Sf` → `Msf` 로 전면 변경
- ASIS 로직 처리 원칙 섹션 추가:
  - TOBE 무관 로직은 **삭제 금지**, 반드시 주석 처리
  - 주석 형식: `// [ASIS] {기능 설명} — {제외 이유}`

---

## 미구현 보류 항목 (부가서비스)

| 항목 | 이유 |
|------|------|
| `POST /api/v1/comm/msp-rate-mst` | M포탈/M전산 연동 방향 미확정 |
| 로밍 부가서비스 코드 목록 | MSF 공통코드 관리 방향 결정 필요 |
| 인증 로그 적재 | 공통 미구현 |
| 마스킹 해제 로그 | 공통 미구현 |

---

## 2026-04-03 (계속)

### [4] 부가서비스 TOBE 전환 파일 주석 보강 (3bb9b32)

**대상 파일 9개** — 코드 변경 없이 주석 추가만 진행

| 파일 | 주석 보강 내용 |
|------|--------------|
| `MsfRegSvcController.java` | TOBE 변환 이력, 각 API 처리 흐름, 제거된 의존성 설명 |
| `MsfRegSvcService.java` | M플랫폼 코드 변경(X20→X97, X21→Y25) 이력, 파라미터/반환값 변경 이력 |
| `MsfRegSvcServiceImpl.java` | ASIS→TOBE 변환 근거, 각 처리 단계 인라인 주석, 미이관 항목 주석 |
| `AdditionReqDto.java` | 각 필드 의미, ASIS Map 키 대응 설명 |
| `AdditionApplyReqDto.java` | 각 필드 의미, 분류 기준(유료/무료) 설명 |
| `AdditionMyListResVO.java` | 필드별 ASIS 원본 키 대응 |
| `AdditionAvailableResVO.java` | listA/listC 분류 기준(SOC 코드 분류) 설명 |
| `AdditionApplyResVO.java` | 응답 필드 설명 |
| `MplatFormService.java` | `regSvcChgY25()`: X21 vs Y25 차이점, LOCAL 목업 처리 방식 설명 |

---

### [5] 부가서비스 TOBE 함수명 ASIS 일치화 및 스킬 네이밍 규칙 등록 (5869c70)

**배경**: TOBE 메서드명이 임의로 작성되어 있어 ASIS 원본과 일치화 필요

#### MsfRegSvcService / MsfRegSvcServiceImpl / MsfRegSvcController 함수명 변경

| 변경 전 | 변경 후 | 적용 위치 |
|--------|--------|---------|
| `getMyAddSvcList` | `selectMyAddSvcList` | Service + Impl + Controller |
| `getAvailableAddSvcList` | `selectAddSvcInfoDto` | Service + Impl + Controller |
| `cancelAddSvc` | `moscRegSvcCanChg` | Service + Impl + Controller (내부 자기참조 포함) |
| `regAddSvc` | `regSvcChg` | Service + Impl + Controller |

#### 스킬 네이밍 규칙 등록 (`.claude/skills/asis-tobe-convert/SKILL.md` §2-2)

- **ASIS↔TOBE 함수명 일치 원칙** 추가
  - Service 메서드 = ASIS 함수명 그대로 (시그니처만 교체)
  - Controller 메서드 = ASIS 함수명에서 `"Ajax"` 접미사만 제거

---

### [6] 문서 파일명 정리 및 줄바꿈 정규화 (a641467)

#### .doc/ 파일 리네임 (파일럿개발 참고용 접미사 추가)

| 이전 | 이후 |
|------|------|
| `15.개발기능목록_ASIS_TOBE_기능분석명세서.md` | `15.개발기능목록_ASIS_TOBE_기능분석명세서_파일럿개발_참고.md` |
| `18.요구사항ID별_ASIS_TOBE_개발진행명세.md` | `18.요구사항ID별_ASIS_TOBE_개발진행명세_파일럿개발_참고.md` |
| `30.REQ_010201-010204_서비스변경_개발분석.md` | `30.REQ_010201-010204_서비스변경_개발분석_파일럿개발_참고.md` |
| `40.테스트_진행현황.md` | `40.테스트_진행현황_파일럿개발_참고.md` |

- `.doc/img.png` 삭제 (미사용 이미지)
- Java 소스 다수 줄바꿈 `LF → CRLF` 정규화 (내용 변경 없음)

---

### [7] MplatFormService → MsfMplatFormService 리네임 및 관련 파일 정리 (2b92246)

**배경**: `Msf` 접두사 네이밍 규칙을 M플랫폼 어댑터 클래스에 일괄 적용

#### 리네임 대상

| 변경 전 | 변경 후 |
|--------|--------|
| `MplatFormService.java` | `MsfMplatFormService.java` |
| `MplatFormServerAdapter.java` | `MsfMplatFormServerAdapter.java` |
| `MplatFormOsstServerAdapter.java` | `MsfMplatFormOsstServerAdapter.java` |
| `MplatFormOsstWebServerAdapter.java` | `MsfMplatFormOsstWebServerAdapter.java` |
| `MplatFormOsstWebService.java` | `MsfMplatFormOsstWebService.java` |

#### 의존 파일 일괄 수정

- import 경로 및 `@Autowired` 타입명 전체 수정
- 대상: `AppformController`, `AppformSvcImpl`, `EsimSvcImpl`, `MsfCombineController`, `MsfCustRequestController`, `MsfFarPricePlanController`, `MsfMyOllehController`, `MsfMyinfoController`, `MsfPauseController`, `MsfUsimSelfChgController`, `MsfChargeServiceImpl`, `MsfCustRequestServiceImpl`, `MsfFarPricePlanServiceImpl`, `MsfMyCombinationSvclmpl`, `MsfMyShareDataSvcImpl`, `MsfMyinfoServiceImpl`, `MsfRegSvcServiceImpl`, `FCommonController`, `FCommonSvcImpl`, `SmsSvcImpl`, `UsimServiceImpl`, `MsfPayInfoServiceImpl`, `MsfPaywayServiceImpl`, `MsfRealTimePayServiceImpl`, `MsfPayInfoService`, `MsfPaywayService`

#### 신규 추가 — NiceCertify 서비스/컨트롤러

| 파일 | 설명 |
|------|------|
| `NiceCertifyController.java` | NICE 본인인증 처리 컨트롤러 (기존 MCP 이관) |
| `NiceCertifySvc.java` | 인터페이스 |
| `NiceCertifySvcImpl.java` | 구현체 — NICE 인증 요청/결과 처리 |
| `NiceLogSvc.java` | 인증 로그 인터페이스 |
| `NiceLogSvcImpl.java` | 인증 로그 저장 구현체 |

- pom.xml: 미사용 의존성 제거
