<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ktis.msp.batch.manager.common.auth.AuthChkMapper">
	
	<!-- 권한 여부 체크 -->
	<select id="chkUsrGrpAuth" parameterType="java.util.HashMap" resultType="int">
		SELECT COUNT(1)
		  FROM (SELECT DISTINCT A.USR_ID
		               , C.AUTH_ID
		          FROM CMN_USR_MST A
		               , CMN_USG_GRP_ASGN_MST B
		               , CMN_GRP_AUTH_ASGN_MST C
		         WHERE 1 =1
		           AND A.USR_ID = #{USER_ID , jdbcType=VARCHAR}
		           AND A.USR_ID = B.USR_ID
		           AND B.GRP_ID = C.GRP_ID
		           AND C.AUTH_ID = #{AUTH_ID , jdbcType=VARCHAR}) T
	</select>
	
	
</mapper>