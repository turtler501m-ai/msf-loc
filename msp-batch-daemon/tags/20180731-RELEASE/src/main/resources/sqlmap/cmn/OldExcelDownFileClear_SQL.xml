<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ktis.msp.batch.job.cmn.smsmgmt.mapper.OldExcelDownFileMapper">
	
	<!-- 배치가 동작하는 일 이전의 모든 데이터를 추출한다. -->
	<select id="getOldExcelDownFileList" resultType="String">
		/* OldExcelDownFileMapper.getOldExcelDownFileList */
		SELECT FILE_PATH
		  FROM MSP_BOND_EXCEL_MGMT
		 WHERE 1=1
		   AND TO_CHAR(REGST_DTTM, 'YYYYMMDD') <![CDATA[ < ]]> TO_CHAR(SYSDATE, 'YYYYMMDD')
	</select>
	
</mapper>