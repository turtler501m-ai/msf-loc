<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ktis.msp.batch.manager.mapper.VacCommonMapper">
	
	<!-- 가상계좌등록,삭제 호출 -->
	<select id="callVacOnceInfo" statementType="CALLABLE" 
		parameterType="com.ktis.msp.batch.manager.vo.VacCommonVO"
		resultType="com.ktis.msp.batch.manager.vo.VacCommonVO">
		/** VacCommonMapper.callVacOnceInfo */
		{ CALL P_VAC_ONCE
				(	  #{opCode, mode=IN, jdbcType=VARCHAR}
					, #{gubun, mode=IN, jdbcType=VARCHAR}
					, #{orderKey, mode=IN, jdbcType=VARCHAR}
					, #{vacBankCd, mode=IN, jdbcType=VARCHAR}
					, #{amount, mode=IN, jdbcType=VARCHAR}
					, #{expireDt, mode=IN, jdbcType=VARCHAR}
					, #{remark, mode=IN, jdbcType=VARCHAR}
					, #{adminId, mode=IN, jdbcType=VARCHAR}
					, #{retCd, mode=OUT, jdbcType=VARCHAR}
					, #{retMsg, mode=OUT, jdbcType=VARCHAR}
					, #{vacBankCd, mode=OUT, jdbcType=VARCHAR}
					, #{vacId, mode=OUT, jdbcType=VARCHAR}
				) 
		}
	</select>
	
	<select id="selectVacBankNm" parameterType="String" resultType="String">
		SELECT	CD_NM
		FROM   	PPS_KT_COMMON_CD
		WHERE  	TABLE_NM = 'PPS_VAC'
		AND    	COL = 'BANK_CODE'
		AND    	CD = #{bankCd}
	</select>
	
</mapper>