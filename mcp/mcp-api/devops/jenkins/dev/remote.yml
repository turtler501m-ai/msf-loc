config:
  credentials_id: MSC-API-DEV
#  retry_with_prompt: true
#  retry_and_return: true
#  retry_count: 3

remote_groups:
  r_group_1:
    - name: jboss
      host: MSC-API-DEV

command_groups:
  c_group_1:
    - puts:
        - from: '/data/release/dev/mcp-api/mcp-api.war'
          into: '/jboss/applications/mcp-api/was/'
          override: true
        - from: '/data/release/dev/mcp-api/ctrl.sh'
          into: '/jboss/domains/'
          override: true
    - commands:
        - 'chmod 755 /jboss/applications/mcp-api/was/mcp-api.war'
        - 'ls -lrt /jboss/applications/mcp-api/was/mcp-api.war'
        - 'chmod 755 /jboss/domains/ctrl.sh'
        - 'ls -lrt /jboss/domains/ctrl.sh'
        - 'cd /jboss/domains; sh ctrl.sh'

steps:
  deploy:
    - remote_groups:
        - r_group_1
      command_groups:
        - c_group_1