<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="RegSvcMapper">
    <select id="getRoamCdList" resultType="String">
    /* RegSvcMapper.getRoamCdList 로밍 부가서비스 요금제코드 조회 */
		SELECT RATE_ADSVC_CD
		  FROM NMCP_RATE_ADSVC_GDNC_PROD_REL
		 WHERE RATE_ADSVC_GDNC_SEQ IN ( SELECT RATE_ADSVC_GDNC_SEQ
		                                  FROM NMCP_RATE_ADSVC_GDNC_BAS
		                                 WHERE ADD_DIV_CD = 'R' )
    </select>
</mapper>