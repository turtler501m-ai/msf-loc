
/** AI 요금제 추천 팝업 오픈 */
function aiRecommendPopOpen(){

  // AILandingUrl > 각 레이아웃에 존재
  const landing = $("#AILandingUrl").val();
  if(!landing) return;

  const popup = window.open(landing, "_blank");
  popup.focus();
}
