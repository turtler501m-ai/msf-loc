<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="
                http://www.springframework.org/schema/beans
                http://www.springframework.org/schema/beans/spring-beans.xsd
                http://www.springframework.org/schema/security
                http://www.springframework.org/schema/security/spring-security.xsd">

    <authentication-manager/>

    <!-- 정적 리소스는 시큐리티 완전 제외 -->
    <http pattern="/resources/**" security="none"/>

    <!-- 인증은 네가 기존 세션으로 처리하므로, 모두 허용(permitAll)로 두고 CSRF만 켠다 -->
    <http auto-config="false" use-expressions="true">
        <csrf request-matcher-ref="csrfMatcher"/>
        <http-basic/>        <!-- ← 추가 -->
        <anonymous/>         <!-- ← 익명 인증도 켜두면 안전 -->
        <!-- 권한은 모두 통과(permitAll) — 기존 세션 로직으로 처리 -->
        <intercept-url pattern="/**" access="permitAll"/>
    </http>

    <beans:bean id="csrfMatcher" class="com.ktmmobile.mcp.security.AntPathCsrfRequestMatcher">
        <!-- CSRF 보호할 경로 -->
        <beans:property name="protectedPatterns">
            <beans:list>
                <beans:value>/mypage/billWayReSendAjax.do</beans:value>
                <beans:value>/mypage/billChgAjax.do</beans:value>
            </beans:list>
        </beans:property>
        <!-- CSRF 제외할 경로(웹훅/외부콜백 등) -->
        <beans:property name="excludePatterns">
            <beans:list>
                <beans:value>/resources/**</beans:value>
            </beans:list>
        </beans:property>
    </beans:bean>

</beans:beans>
