<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="EventPromotionMapper">

    <delete id="deleteExpiredJoinList">
        DELETE FROM NMCP_EVENT_PRMT_JOIN_HIST
        WHERE PROMOTION_END_DATE <![CDATA[<]]> TO_CHAR(SYSDATE - 180, 'YYYYMMDD')
    </delete>

    <update id="expireEventPromotion">
        UPDATE NMCP_EVENT_PRMT
        SET USE_YN = 'N'
          , RVISN_ID = 'BATCH'
          , RVISN_DTTM = SYSDATE
        WHERE PROMOTION_END_DATE <![CDATA[<]]> TO_CHAR(SYSDATE - 180, 'YYYYMMDD')
          AND USE_YN = 'Y'
    </update>
</mapper>